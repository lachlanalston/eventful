const t=[{id:4624,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Fundamental",title:"Successful Logon",short_desc:"A user or process successfully authenticated and was granted access.",description:"Event 4624 is generated every time an account successfully logs on to the computer. The event captures who logged on, how they logged on (logon type), from where, and under what process. It is one of the highest-volume events in the Security log and forms the backbone of authentication auditing. Logon Type is the most important field — Type 2 is interactive (keyboard at the machine), Type 3 is network (file share, mapped drive), Type 7 is unlock, Type 10 is Remote Interactive (RDP), and Type 11 is cached credentials.",why_it_happens:"Windows generates this event as part of Logon/Logoff auditing whenever the Security Account Manager (SAM) or Kerberos authentication packages validate credentials successfully. Every resource access — opening a file share, unlocking a workstation, an RDP session — produces a logon event. Services also generate Type 5 (service logon) events on startup. The volume makes this event noisy on domain controllers, which see every network logon from every machine authenticating to AD.",what_good_looks_like:"Expected: Type 3 logons from your file server to member computers during business hours, Type 2 logons from known user accounts, Type 5 logons from known service accounts. Investigate: logons outside business hours for sensitive accounts, Type 10 (RDP) logons from unexpected source IPs, logons from accounts that should not be accessing a particular machine, Type 9 (NewCredentials/RunAs) logons.",common_mistakes:["Treating all 4624 events as significant — on a domain controller you will see thousands per hour; filter by logon type first","Forgetting that Type 3 logons are normal for every mapped drive, printer access, or file share access",'Overlooking the "Account for which logon was performed" vs the "Subject" field — the Subject is often SYSTEM, the target account is the important one',"Not checking the Workstation Name and Source IP fields to see where the logon originated","Assuming a logon means a human sat at the keyboard — services, scheduled tasks, and scripts all generate logon events"],causes:["User interactively logging in at the console (Type 2)","User accessing a network share or mapped drive (Type 3)","A service starting under a service account (Type 5)","A user unlocking their workstation (Type 7)","An RDP session being established (Type 10)","Credentials being cached used offline (Type 11)","A process using RunAs or alternate credentials (Type 9)"],steps:["Open Event Viewer → Windows Logs → Security, filter for Event ID 4624",'Identify the "Logon Type" field — this tells you how the logon occurred','Check "Account Name" under "New Logon" — this is who logged on','Check "Workstation Name" and "Source Network Address" for remote logons',"For Type 10 logons, cross-reference with RDS events 21, 25, 4778","If investigating suspicious activity, export and pivot on Account Name and Source IP","Use PowerShell snippet below to filter by logon type or account name"],symptoms:["user logged in","who logged into this computer","when did someone log on","check login history","see who accessed this pc","authentication successful","rdp login","remote login","account logged on"],tags:["authentication","logon","audit","rdp","kerberos","ntlm","security","baseline"],powershell:`# Successful Logon Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

# Get all successful logons and decode logon type
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4624
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated      = $_.TimeCreated
        TargetAccount    = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        LogonType        = ($data | Where-Object Name -eq 'LogonType').'#text'
        SourceIP         = ($data | Where-Object Name -eq 'IpAddress').'#text'
        WorkstationName  = ($data | Where-Object Name -eq 'WorkstationName').'#text'
        AuthPackage      = ($data | Where-Object Name -eq 'AuthenticationPackageName').'#text'
    }
} | Where-Object { $_.TargetAccount -notlike '*$' } |
    Sort-Object TimeCreated -Descending |
    Format-Table -AutoSize`,related_ids:[4625,4634,4647,4648,4672,4771,4776,4778],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4624"},{id:4625,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Fundamental",title:"Failed Logon",short_desc:"An account failed to log on — wrong password, bad account, or locked out.",description:'Event 4625 is generated whenever a logon attempt fails. It is the primary event for investigating account lockouts, brute-force attacks, and misconfigured service accounts. The most critical field is "Failure Reason" / "Sub Status" code — 0xC000006A means wrong password, 0xC0000234 means account already locked, 0xC000006D means bad username, 0xC000006F means logon outside permitted hours. A burst of these events from a single source IP strongly suggests a brute-force or password spray attack.',why_it_happens:`Windows logs failed logons as part of Logon/Logoff auditing. Failures happen for many reasons: typos, cached credentials after a password change, stale service account credentials, or deliberate attack. The Sub Status code is generated by the authentication package (Kerberos or NTLM) and distinguishes between "wrong password" and "account doesn't exist", which is important — attackers enumerating accounts generate mostly 0xC000006D errors.`,what_good_looks_like:"One or two failed logons followed by a successful 4624 is normal (typo, then correct password). Investigate: repeated failures for the same account from multiple sources (password spray), repeated failures for many accounts from one source (credential stuffing), failures for accounts that shouldn't exist (deleted user still configured in a service), failures outside business hours for interactive accounts.",common_mistakes:["Looking at 4625 without also looking at 4740 — the lockout event tells you which computer triggered it","Ignoring the Sub Status code — it distinguishes wrong password from bad account, which matters for attack detection","Only looking at the DC — failed logons also appear on the member computer where the attempt occurred","Not checking the Caller Computer Name or Source Network Address when investigating remote failures","Assuming every failed logon is an attack — users mistype passwords constantly"],causes:["User typed wrong password","Cached credentials not updated after password change","Service account configured with old password","Account locked out from previous failures","Account disabled or deleted","Logon attempt outside permitted hours or from non-allowed workstation","Brute-force or password spray attack","VPN or remote access client using stale credentials"],steps:["Filter Security log for 4625 on the target machine or DC",'Note the "Account Name" — is this a known user, service account, or unknown name?',"Check Sub Status code: 0xC000006A = wrong password, 0xC0000234 = locked, 0xC000006D = bad username","If Sub Status 0xC000006A: find what device is sending the bad password (Workstation Name / Source IP)","If Sub Status 0xC0000234: find 4740 lockout event to see triggering computer","Correlate Source IP with your asset inventory — is it a known machine?","If many accounts failed from one IP, escalate — likely spray attack","Check for corresponding 4740 to confirm lockout occurred"],symptoms:["user cant log in","account locked out","wrong password","login failed","authentication failed","bad password","user keeps getting locked out","account lockout investigation","brute force","failed authentication"],tags:["authentication","lockout","failed-logon","brute-force","password","security","audit"],powershell:`# Failed Logon Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

# Decode Sub Status codes for failure reason
$subStatusMap = @{
    '0xC000006A' = 'Wrong password'
    '0xC0000234' = 'Account locked out'
    '0xC000006D' = 'Bad username or authentication failure'
    '0xC000006F' = 'Logon outside permitted hours'
    '0xC0000070' = 'Logon from unauthorised workstation'
    '0xC000015B' = 'Logon type not granted'
    '0xC0000064' = 'Account does not exist'
    '0xC0000072' = 'Account disabled'
    '0xC000006E' = 'Account restriction'
}

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4625
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    $sub  = ($data | Where-Object Name -eq 'SubStatus').'#text'
    [PSCustomObject]@{
        TimeCreated    = $_.TimeCreated
        TargetAccount  = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        FailureReason  = $subStatusMap[$sub] ?? $sub
        SourceIP       = ($data | Where-Object Name -eq 'IpAddress').'#text'
        LogonType      = ($data | Where-Object Name -eq 'LogonType').'#text'
        Workstation    = ($data | Where-Object Name -eq 'WorkstationName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4624,4740,4767,4771,4776],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4625"},{id:4627,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Intermediate",title:"Group Membership Enumeration During Logon",short_desc:"Windows enumerated group memberships for an account at logon time.",description:"Event 4627 records the full list of security groups a user is a member of at the time of logon. It is generated alongside 4624 when group membership auditing is enabled. The event includes every group SID — domain groups, local groups, special identities (Everyone, Authenticated Users), and privilege groups (Administrators, Domain Admins). It is primarily useful for confirming what access a user had at a specific point in time, which is valuable in insider threat investigations and compliance audits.",why_it_happens:"When a user logs on, the Local Security Authority (LSA) builds an access token containing all the user's group memberships and privileges. Event 4627 is the audit record of this token-building process. It captures the group state at logon time, not the current state — if group membership changes after logon, the existing session's token is not updated until the user logs off and back on.",what_good_looks_like:"Normal: user is a member of expected groups (Domain Users, department groups, maybe a few resource groups). Investigate: a standard user appearing in Domain Admins, Schema Admins, or Backup Operators; group memberships that change between logon events for the same user; a service account with unexpectedly broad group membership.",common_mistakes:["Confusing this event with group membership change events (4728, 4732) — 4627 shows membership at logon, not changes","Not realising the token is built at logon — a group change won't show up until next logon","Ignoring the special identity SIDs (S-1-1-0 Everyone, S-1-5-11 Authenticated Users) in the membership list — these are normal","This event can be very large — some users have 50+ group memberships and the XML is lengthy"],causes:["Any successful interactive or network logon with group membership auditing enabled","Elevated privilege use via RunAs generating a separate logon token","Service account logon building its access token"],steps:["Filter Security log for 4627 alongside a specific 4624 using the LogonID correlation field",'Look at the "Group Membership" field for any unexpected privileged groups',"Cross-reference with HR records if investigating insider threat or access review","Compare group memberships across multiple logon events for the same user to spot changes","Use the LogonID from 4624 to find the corresponding 4627 for the same session"],symptoms:["what groups is this user in","check user group membership","audit user access","privileged group membership","when did user get admin rights"],tags:["group-membership","access-token","privileged-access","audit","compliance"],powershell:`# Group Membership Enumeration Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4627
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List`,related_ids:[4624,4728,4732,4756,4672],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4627"},{id:4648,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Intermediate",title:"Logon with Explicit Credentials (RunAs)",short_desc:"A process used different credentials than the logged-on user to access a resource.",description:'Event 4648 is generated when a process uses alternate credentials — via RunAs, a "net use" command with credentials, or a script that embeds credentials. It records both the account that initiated the logon (the "Subject") and the account whose credentials were used (the "Account Whose Credentials Were Used"). This event is important because it can reveal credential exposure: if an attacker has compromised a machine, they may use 4648 to pivot using harvested credentials. It also appears during legitimate admin activities.',why_it_happens:"Windows generates 4648 when the LogonUser() API is called with the LOGON32_LOGON_NEW_CREDENTIALS flag, or when CreateProcessWithLogonW is used. These APIs are called by RunAs, scheduled tasks using alternate accounts, scripts with embedded credentials, and tools like PsExec. The event is generated on the machine where the credential use originates, not on the target machine.",what_good_looks_like:`Expected: an IT admin using RunAs to launch a management console with admin credentials, a script explicitly connecting to a remote resource. Investigate: a standard user account generating 4648 events (they shouldn't be using alternate credentials), the "Target Server" being a sensitive system, 4648 events at unusual times, credentials for accounts that shouldn't be used interactively.`,common_mistakes:["Confusing 4648 with 4624 Type 9 (NewCredentials logon) — they are related but 4648 is on the source machine, 4624 Type 9 on the target","Not noticing this event because it is often buried in high-volume logs — set an alert for 4648 from standard user accounts","Ignoring scheduled tasks — they generate 4648 on every run if configured with alternate credentials",'Missing that "mapped drives" configured with credentials generate 4648 repeatedly'],causes:["Admin using RunAs to elevate","Script or batch file embedding credentials","Scheduled task configured with alternate account credentials","net use command with explicit credentials","PsExec or similar tools using alternate credentials","Malware using harvested credentials to pivot laterally"],steps:["Filter Security log for 4648 on the suspect machine",'Identify "Account Whose Credentials Were Used" — is this a privileged account?','Check "Target Server Name" — what resource was being accessed?',"Check the initiating process name — is it cmd.exe, powershell.exe, or an unknown executable?","Correlate with 4624 Type 9 on the target machine to confirm the credential use succeeded","If the initiating process is unexpected, investigate the process tree","Check if the Subject account should normally be using alternate credentials at all"],symptoms:["runas command","explicit credentials","alternate credentials","user running as admin","credentials used by different account","pass the hash","lateral movement","privilege escalation"],tags:["runas","credentials","lateral-movement","privilege-escalation","security","audit"],powershell:`# Explicit Credential Use (RunAs) Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4648
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated      = $_.TimeCreated
        SubjectAccount   = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        CredentialUsed   = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        TargetServer     = ($data | Where-Object Name -eq 'TargetServerName').'#text'
        ProcessName      = ($data | Where-Object Name -eq 'ProcessName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4624,4672,4688],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4648"},{id:4663,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Advanced",title:"File or Object Access Attempt",short_desc:"An account attempted to access a file, registry key, or Active Directory object.",description:"Event 4663 records every access attempt to an audited object — a file, folder, registry key, or Active Directory object. It is generated only when object access auditing is enabled AND the specific object has a System Access Control List (SACL) configured to audit that type of access. The event records who accessed the object, what type of access was requested (Read, Write, Delete, etc.), and whether it succeeded. This event is extremely noisy without careful SACL scoping and should only be enabled on sensitive objects.",why_it_happens:"Windows generates 4663 as part of Object Access auditing. The audit policy must be enabled (either File System or Registry subcategory), and the specific object must have a SACL that defines which users and access types to audit. Without a SACL, no events are generated regardless of audit policy. SACLs are configured in the Security tab → Advanced → Auditing of a file or folder. Group Policy can deploy SACLs at scale.",what_good_looks_like:"Normal: known users accessing expected files during business hours. Investigate: access to files outside normal working hours, DELETE or WRITE access to sensitive files like scripts or configs, access from unexpected accounts (e.g., service accounts accessing user documents), a burst of access events suggesting bulk file enumeration or exfiltration.",common_mistakes:["Enabling Object Access auditing globally without configuring SACLs — this generates zero events (policy without SACLs = nothing)","Enabling SACLs on high-volume locations like C:\\ — the event volume will overwhelm the security log instantly",'Not filtering for the "Accesses" field — Read Data, Write Data, Delete are very different risk levels',"Confusing 4663 (access attempt) with 4656 (handle request) — 4663 means the access actually happened","Not setting the security log size large enough to retain 4663 events before they are overwritten"],causes:["Normal file access by authorised users","Application accessing files it needs to function","Backup agent reading files","Malware reading, writing, or deleting sensitive files","Insider threat accessing data they should not","Ransomware encrypting files (massive burst of write events)"],steps:['Confirm Object Access auditing is enabled via auditpol /get /subcategory:"File System"',"Confirm the target file/folder has a SACL configured (Security → Advanced → Auditing)","Filter Security log for 4663, scoped to the object name in question",'Check "Accesses" field — is it Read, Write, Delete, or others?','Check "Account Name" — is the accessing account expected to touch this file?',"For ransomware investigation, look for a burst of Write events across many files from one process","Correlate with 4688 to find the process that made the access"],symptoms:["who accessed this file","file was deleted","file was modified","ransomware file access","audit file access","sensitive file accessed","who read this document","data exfiltration investigation"],tags:["file-access","object-access","sacl","audit","ransomware","data-exfiltration","dlp"],powershell:`# File/Object Access Investigation
# Eventful

$computer   = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime  = (Get-Date).AddHours(-24)  # Adjust time range as needed
$objectName = 'C:\\Sensitive\\folder'   # Replace with target path

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4663
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated  = $_.TimeCreated
        Account      = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        ObjectName   = ($data | Where-Object Name -eq 'ObjectName').'#text'
        Accesses     = ($data | Where-Object Name -eq 'Accesses').'#text'
        ProcessName  = ($data | Where-Object Name -eq 'ProcessName').'#text'
    }
} | Where-Object { $_.ObjectName -like "*$objectName*" } |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4656,4670,4688,4624],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4663"},{id:4670,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Advanced",title:"Permissions on Object Changed",short_desc:"The access control list (ACL) on a file, registry key, or AD object was modified.",description:'Event 4670 is generated when the permissions (DACL) on a file, folder, registry key, or Active Directory object are changed. It records the original permissions and the new permissions in SDDL format, along with who made the change and what process performed it. This event requires Object Access auditing and an appropriate SACL on the object. ACL changes on sensitive resources — like adding "Everyone Full Control" to a sensitive folder — are a strong indicator of privilege escalation or lateral movement.',why_it_happens:"When an application or user calls SetSecurityInfo() or SetNamedSecurityInfo() to modify an object's DACL, Windows logs 4670. This happens during legitimate admin tasks (granting a user access to a folder), but also during attacks where adversaries try to weaken permissions on sensitive files or registry keys to gain persistent access.",what_good_looks_like:"Expected: IT admin changing folder permissions through a documented change process. Investigate: permissions changed outside of change windows, a non-admin account changing permissions, permissions changed on system binaries or registry run keys, ACL changes that add broad access (Everyone, Authenticated Users) to sensitive paths.",common_mistakes:["Not having SACL audit entries on sensitive paths — 4670 will not fire without both audit policy and SACL",'Ignoring the SDDL strings because they look complex — focus on the "New SD" field, specifically any A;;FA;;;WD (Full Access for Everyone) or similar broad grants',"Not correlating with 4688 to find the process that made the change"],causes:["IT admin explicitly modifying folder permissions","Software installer setting permissions on program files","Malware weakening permissions on files for persistence","Ransomware modifying ACLs to ensure write access before encryption","GPO applying new file system permissions"],steps:["Filter Security log for 4670",'Check "Object Name" — what file/folder was changed?','Decode the "New SD" SDDL string — look for WD (World/Everyone) with FA (Full Access)','Check "Subject Account Name" — was this a known admin doing authorized work?','Check "Process Name" — was it Explorer.exe (manual), or something unexpected?',"Review change management records to see if this was planned","If unauthorized, restore original permissions from backup and investigate further"],symptoms:["permissions changed on file","acl modified","access control changed","who changed permissions","folder permissions modified","security descriptor changed"],tags:["permissions","acl","dacl","sacl","file-system","privilege-escalation","audit"],powershell:`# Permissions Change Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4670
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List`,related_ids:[4663,4688,4698],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4670"},{id:4672,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Intermediate",title:"Special Privileges Assigned to New Logon",short_desc:"A user logged on with administrative or sensitive privileges in their access token.",description:"Event 4672 is generated immediately after 4624 whenever the account that logged on holds one or more sensitive privileges. These include SeDebugPrivilege (debug any process), SeBackupPrivilege (read any file for backup), SeImpersonatePrivilege (impersonate any user), SeTakeOwnershipPrivilege, and others that grant powerful OS capabilities. This event effectively marks every admin logon. It appears extremely frequently for SYSTEM and built-in admin accounts — the key is filtering for unexpected accounts or unexpected machines.",why_it_happens:"When the LSA builds an access token for a logon, it checks whether any of the user's group memberships or direct assignments include sensitive privileges. If they do, 4672 is generated. Local Administrators always hold many of these privileges. Domain Admins inherit them. The event fires even if the account never actually uses those privileges — it is based on token construction, not privilege use.",what_good_looks_like:"Expected: Domain Admin accounts generating 4672 on DCs and servers they manage, local admin accounts generating it on workstations. Investigate: 4672 for standard user accounts (they should not hold sensitive privileges), 4672 on machines the admin account shouldn't access, an account gaining SeDebugPrivilege that wasn't previously an admin.",common_mistakes:["Alerting on every 4672 — SYSTEM generates one on every boot, and every admin logon generates one","Not filtering for the specific privileges in the event — SeDebugPrivilege and SeTcbPrivilege are more dangerous than SeChangeNotifyPrivilege","Ignoring 4672 on domain controllers — these are critical machines and unexpected admin logons matter more there","Not correlating with 4624 using the LogonID — they should always appear together"],causes:["Admin user logging on interactively or via RDP","Service running under a privileged service account starting","Scheduled task running under admin credentials","Malware that has elevated to SYSTEM or admin generating logon events"],steps:["Filter Security log for 4672",'Check "Account Name" — is this account expected to be an admin?','Check "Privileges" — focus on SeDebugPrivilege, SeTcbPrivilege, SeBackupPrivilege',"Correlate with 4624 using the LogonID to see where the logon came from","On DCs, any unexpected 4672 should be immediately investigated","Cross-reference with AD group membership to see if the privilege assignment is expected"],symptoms:["admin logged on","privileged logon","who has admin access","administrative logon","sensitive privileges","debug privilege","domain admin logon"],tags:["privileges","admin","access-token","audit","security-baseline"],powershell:`# Special Privileges Assigned Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4672
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated  = $_.TimeCreated
        AccountName  = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        Domain       = ($data | Where-Object Name -eq 'SubjectDomainName').'#text'
        Privileges   = ($data | Where-Object Name -eq 'PrivilegeList').'#text'
    }
} | Where-Object { $_.AccountName -notlike '*$' -and $_.AccountName -ne 'SYSTEM' -and $_.AccountName -ne 'LOCAL SERVICE' -and $_.AccountName -ne 'NETWORK SERVICE' } |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4624,4627,4648],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4672"},{id:4688,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Advanced",title:"New Process Created",short_desc:"A new process was created — records executable path, parent process, and user context.",description:"Event 4688 records every new process creation when Process Creation auditing is enabled. It captures the full executable path, the account running it, the parent process, and — when command line auditing is also enabled — the exact command line arguments. This is one of the most valuable events for threat hunting and incident response. It shows exactly what programs were run, by whom, and under what process lineage. Attackers frequently try to live off the land using built-in tools (cmd.exe, powershell.exe, wmic.exe, certutil.exe) — 4688 reveals this.",why_it_happens:'Windows generates 4688 through the Process Tracking audit subcategory. It fires every time CreateProcess() is called and a new process kernel object is instantiated. On a busy workstation this can be hundreds per hour. The command line is only included if "Include command line in process creation events" is enabled in Group Policy (Computer Configuration → Admin Templates → System → Audit Process Creation). This setting is off by default.',what_good_looks_like:"Normal: known applications launching from standard paths (C:\\Program Files\\, C:\\Windows\\System32\\), with expected parent processes (explorer.exe for user-launched apps, services.exe for services). Investigate: executables running from unusual paths (temp folders, AppData, recycle bin), PowerShell encoded commands, processes launched by unusual parents (Word spawning cmd.exe), known LOLBins (certutil, mshta, regsvr32) used unexpectedly.",common_mistakes:["Not enabling command line auditing — the executable path alone is much less useful than the full command line","Not enabling this audit policy at all — it is off by default and must be explicitly enabled","Being overwhelmed by volume and not using specific parent process or path filters to narrow down","Missing that Windows Defender and AV engines create many processes when scanning — these are expected noise"],causes:["User launching an application","A service or scheduled task spawning a child process","Script interpreter (cmd.exe, powershell.exe, wscript.exe) executing code","An exploit using a trusted application to run attacker-controlled code","Malware executing a payload","Lateral movement tools like PsExec creating remote processes"],steps:['Confirm audit policy: auditpol /get /subcategory:"Process Creation"',"Confirm command line logging is enabled via GPO or local security policy","Filter 4688 for a specific user, time window, or parent process","Look for processes running from non-standard paths (AppData, Temp, Recycle Bin)","Look for cmd.exe or powershell.exe spawned by Office applications (Word, Excel)","Check for encoded PowerShell commands (-enc or -EncodedCommand)","Build a process tree using ParentProcessId and ProcessId fields","Cross-reference unusual processes with VirusTotal by hash if available"],symptoms:["what programs were run","process executed","command was run","malware running process","suspicious executable","powershell command ran","process tracking","who ran this program","application launched"],tags:["process-creation","threat-hunting","lolbins","powershell","malware","incident-response","audit"],powershell:`# Process Creation Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

# Note: Command line captured only if "Include command line in process creation events" GPO is enabled
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4688
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated    = $_.TimeCreated
        Account        = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        NewProcess     = ($data | Where-Object Name -eq 'NewProcessName').'#text'
        CommandLine    = ($data | Where-Object Name -eq 'CommandLine').'#text'
        ParentProcess  = ($data | Where-Object Name -eq 'ParentProcessName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4624,4648,4698,4663],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4688"},{id:4698,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Intermediate",title:"Scheduled Task Created",short_desc:"A new scheduled task was registered on the system.",description:"Event 4698 is generated when a new scheduled task is created through Task Scheduler. It records the task name, the account that created it, and the full task XML including triggers, actions, and the account it runs under. Scheduled tasks are a favourite persistence mechanism for attackers — they survive reboots, run silently, and blend in with legitimate administrative tasks. Every new task creation should be reviewed, especially outside of known change windows.",why_it_happens:"Windows records task creation through the Object Access auditing subsystem. The Task Scheduler service calls the security audit API when a task is registered via the COM interface, the schtasks command, or PowerShell. The task XML embedded in the event contains everything about the task — when it runs, what it runs, and under what credentials.",what_good_looks_like:"Expected: software installers creating update tasks (Google, Adobe, Microsoft), IT management tools (RMM agents, monitoring), GPO-deployed tasks. Investigate: tasks created outside business hours, tasks running from AppData or Temp directories, tasks using encoded commands, tasks running under SYSTEM that weren't there before, task names mimicking system tasks but in slightly wrong locations.",common_mistakes:["Assuming tasks in unusual folders like C:\\Windows\\System32\\Tasks\\ are legitimate just because of the path","Not reading the full task XML in the event — the Action element reveals the actual command being run",'Overlooking tasks that use "schtasks /create" from command line — they also generate 4698',"Not setting a baseline of legitimate scheduled tasks to compare against"],causes:["Software installation creating an update or maintenance task","IT admin scheduling a maintenance script","RMM or monitoring agent deploying a task","Malware creating a persistence mechanism","Attacker using schtasks for lateral execution or persistence"],steps:["Filter Security log for 4698",'Read the "Task Name" — does it match a known application?','Read the "Task Content" XML — examine the Action element for the command being executed','Check the "Subject Account Name" — was this created by a known admin or SYSTEM?',"Verify the task exists and matches what the event recorded: Get-ScheduledTask | Where-Object TaskName -eq '<name>'","Check the trigger — when does it run? At logon? Every minute?","If suspicious, delete with Remove-ScheduledTask and investigate further"],symptoms:["scheduled task created","new task in task scheduler","malware persistence","task created overnight","suspicious scheduled task","who created this task","task running as system"],tags:["scheduled-task","persistence","malware","audit","security"],powershell:`# Scheduled Task Creation Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4698
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated   = $_.TimeCreated
        CreatedBy     = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        TaskName      = ($data | Where-Object Name -eq 'TaskName').'#text'
        TaskContent   = ($data | Where-Object Name -eq 'TaskContent').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-List`,related_ids:[4700,4702,4688,4624],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4698"},{id:4700,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Intermediate",title:"Scheduled Task Enabled",short_desc:"A previously disabled scheduled task was enabled.",description:"Event 4700 records when a scheduled task transitions from disabled to enabled. Attackers sometimes create tasks in a disabled state to avoid immediate execution, then enable them later when they are ready to use them. It is also generated when an admin re-enables a task they previously disabled. The event includes the task name, the account that enabled it, and a timestamp.",why_it_happens:"Task Scheduler generates a security audit event when a task's enabled status is changed. This happens when a task is enabled via the Task Scheduler GUI, the schtasks /change /enable command, or the PowerShell Enable-ScheduledTask cmdlet. The event is logged on the machine where the task lives.",what_good_looks_like:"Expected: an IT admin enabling a task during a maintenance window, software update enabling its own task. Investigate: a disabled task being re-enabled at an unusual time, a task being enabled immediately after being created (suggesting automated persistence setup), tasks being enabled that you don't recognise.",common_mistakes:["Treating this event in isolation — always check 4698 to find the original task creation","Not realising a task can be created disabled (to avoid detection), then enabled later"],causes:["IT admin enabling a maintenance task","Software enabling its own update task","Malware enabling a previously created persistence task","GPO enabling a centrally managed task"],steps:["Filter Security log for 4700","Note the Task Name and cross-reference with 4698 to see when it was created","Check who enabled it and whether they should have access to that task","Verify the task still exists and inspect it with Get-ScheduledTask","If suspicious, disable and investigate: Disable-ScheduledTask -TaskName '<name>'"],symptoms:["scheduled task enabled","task enabled","task turned on","task was activated"],tags:["scheduled-task","persistence","audit","security"],powershell:`# Scheduled Task Enabled Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4700
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List`,related_ids:[4698,4702,4688],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4700"},{id:4702,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Intermediate",title:"Scheduled Task Updated",short_desc:"An existing scheduled task's configuration was modified.",description:"Event 4702 records modifications to existing scheduled tasks — changes to the action (what it runs), triggers (when it runs), or run-as account. Like task creation (4698), this event includes the full task XML after the change, making it possible to see exactly what was modified. Attackers who have established a foothold may modify existing legitimate tasks to add malicious actions, making this event critical for detecting that type of persistence modification.",why_it_happens:"Task Scheduler generates 4702 when an existing task's properties are changed through any interface — the GUI, schtasks /change, or Set-ScheduledTask in PowerShell. The full updated task XML is embedded in the event.",what_good_looks_like:"Expected: software updates changing their own task schedules, IT admins modifying maintenance tasks. Investigate: changes to tasks you did not initiate, actions being added or changed to run from suspicious paths, trigger intervals being shortened (an attacker making a task run more frequently), run-as account being changed to a more privileged account.",common_mistakes:["Only looking at task creation (4698) for persistence — attackers often modify existing tasks to avoid creating obvious new ones","Not comparing the new XML against the previous known-good task definition"],causes:["Software update modifying its own task","IT admin changing task schedule or action","Malware modifying a legitimate task to add malicious commands","Attacker changing the run-as account of a task to escalate privileges"],steps:["Filter Security log for 4702","Read the full task XML to see what changed","Cross-reference with 4698 to see the original task definition","Check who made the change and when","If the action changed, examine the new command being run","Compare with current task definition: (Get-ScheduledTask -TaskName '<name>').Actions"],symptoms:["scheduled task changed","task modified","task action changed","task schedule changed","who modified this task"],tags:["scheduled-task","persistence","audit","security"],powershell:`# Scheduled Task Update Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4702
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List`,related_ids:[4698,4700,4688],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4702"},{id:4719,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Critical",skill_level:"Advanced",title:"System Audit Policy Changed",short_desc:"The local audit policy was modified — someone changed what security events are logged.",description:'Event 4719 is one of the most significant security events — it records that the audit policy itself was changed. This matters enormously because an attacker who changes the audit policy can blind the security log before carrying out subsequent malicious activity. The event shows which audit subcategory was changed, who changed it, and what it was changed to. "No Auditing" on any category should immediately raise concern.',why_it_happens:'Windows generates 4719 whenever auditpol.exe modifies a local audit policy, Group Policy applies an audit configuration, or an application calls the audit policy APIs. Importantly, this event is generated even when the change is to disable auditing of something — the event itself is always logged because "Audit Policy Change" auditing cannot be disabled.',what_good_looks_like:'Expected: GPO enforcing audit policy and generating 4719 on policy refresh, planned changes during a security hardening exercise. Investigate: audit subcategories being set to "No Auditing" (especially Logon, Process Creation, Account Management), changes made outside of GPO refresh times, changes by accounts other than SYSTEM or known admin accounts.',common_mistakes:["Not alerting on 4719 at all — this is one of the most important events to have high-priority alerts for","Assuming SYSTEM making this change is always GPO — check if GPO is actually the source or if something else changed it","Not knowing what your baseline audit policy is, making it impossible to notice changes"],causes:["Group Policy applying or refreshing audit settings","IT admin using auditpol.exe to change settings","Security tool modifying audit configuration","Attacker disabling auditing before malicious activity","Malware evading detection by disabling process tracking or logon auditing"],steps:["Filter Security log for 4719 — any hit should be investigated",'Check "Account Name" — was this SYSTEM (likely GPO) or a human account?',"Check the subcategory and what it was changed to (Enabled/Disabled/No Auditing)","Run auditpol /get /category:* to see current policy","If unauthorized change: restore policy via GPO or auditpol /set",'If policy was set to "No Auditing" on critical categories, check what else happened while auditing was disabled',"Alert SOC and escalate if this was not a planned change"],symptoms:["audit policy changed","logging disabled","audit disabled","who changed audit policy","event logging turned off","audit policy modified","someone turned off logging"],tags:["audit-policy","evasion","critical","security","compliance"],powershell:`# Audit Policy Change Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4719
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List

# Also check current audit policy state
Write-Host "
--- Current Audit Policy ---" -ForegroundColor Cyan
auditpol /get /category:*`,related_ids:[4688,4624,4698],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4719"},{id:4720,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Fundamental",title:"User Account Created",short_desc:"A new user account was created.",description:"Event 4720 records the creation of a new local or domain user account. It includes the name of the new account, the account that created it, and attributes like the account's full name and description. Unauthorized account creation is a major red flag — attackers create accounts for persistence, and insiders may create shadow accounts. In MSP environments, alert on any account creation that was not preceded by a service desk ticket or change request.",why_it_happens:"Windows generates 4720 through Account Management auditing whenever Net User, the local Users and Groups MMC snap-in, or Active Directory Users and Computers creates an account. The event is generated on the machine where the account was created — for domain accounts, this is the domain controller that processed the request.",what_good_looks_like:"Expected: IT admin creating accounts during onboarding following a documented process. Investigate: accounts created outside business hours, accounts created by non-admin users, accounts with names that mimic system accounts (svc_backup2, administrator1), accounts created on endpoints rather than the domain controller.",common_mistakes:["Not having an alert for 4720 — any account creation should be a low-friction alert","Forgetting that local account creation (on a workstation) also generates 4720 — and local admin accounts are often more dangerous than domain ones","Not correlating with HR onboarding records to verify the account was expected"],causes:["IT admin onboarding a new employee","Software installing a service account","RMM or automation tool creating an account","Attacker creating a backdoor account for persistence","Insider threat creating a secondary account"],steps:["Filter Security log for 4720",'Note "New Account Name" and "Account Domain"','Check "Subject Account Name" — who created this account?',"Verify the creation aligns with an onboarding ticket or change request","If domain account: check the DC event log for the original creation event","Check if the account was immediately added to privileged groups (look for 4728, 4732 nearby)","If unauthorized: disable immediately with Disable-ADAccount, then investigate"],symptoms:["new account created","new user was created","account creation","unknown user account appeared","backdoor account","who created this account","new local account"],tags:["account-management","user-creation","persistence","audit","security"],powershell:`# User Account Creation Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4720
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated    = $_.TimeCreated
        NewAccount     = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        CreatedBy      = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        Domain         = ($data | Where-Object Name -eq 'TargetDomainName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4722,4728,4732,4726],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4720"},{id:4722,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Fundamental",title:"User Account Enabled",short_desc:"A user account was enabled after being disabled.",description:"Event 4722 records that a previously disabled user account was re-enabled. This is important because it can indicate that a dormant account (perhaps a former employee's account that was disabled but not deleted) has been reactivated. Dormant accounts are attractive targets for attackers because they may still have valid group memberships, permissions, and access, but are less monitored than active accounts.",why_it_happens:`Windows generates 4722 as part of Account Management auditing when an account's "Account is disabled" flag is cleared. This happens via Active Directory Users and Computers, Net User, or PowerShell Enable-ADAccount. The event is generated on the machine (or DC) where the change was made.`,what_good_looks_like:"Expected: IT admin re-enabling a user account after a period of leave, following a documented process. Investigate: former employee accounts being re-enabled, accounts that should remain permanently disabled (terminated contractors), accounts re-enabled outside business hours.",common_mistakes:["Not correlating with 4625 — if a disabled account is generating failed logon attempts, someone may be trying to use it","Not checking when the account was last active before being disabled"],causes:["HR process for returning employee","IT admin re-enabling an account following a support ticket","Attacker re-enabling a dormant account for lateral access","Automated process incorrectly re-enabling accounts"],steps:["Filter Security log for 4722","Note the account being enabled and who enabled it","Check when the account was last active before being disabled","Verify this matches an HR or helpdesk ticket","Check for subsequent logon events (4624) for the re-enabled account","If unauthorized: re-disable immediately and investigate"],symptoms:["account enabled","account reactivated","disabled account turned on","old account re-enabled","user account unlocked by admin"],tags:["account-management","dormant-accounts","audit","security"],powershell:`# User Account Enabled Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4722
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List`,related_ids:[4720,4725,4624],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4722"},{id:4723,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Fundamental",title:"Password Change Attempt",short_desc:"A user attempted to change their own password.",description:"Event 4723 records when a user attempts to change their own password (as opposed to 4724, which is an admin resetting someone else's password). The event records whether the attempt succeeded or failed. Failed attempts may indicate the user forgot their current password (they must know it to change it), or that a password complexity policy blocked the new password. This event is primarily used to track voluntary password changes for compliance auditing.",why_it_happens:"Windows generates 4723 as part of Account Management auditing. A user changing their own password calls a different API than an admin reset — the user must provide their current password for verification. Failures generate a 4723 event with a failure reason, while successes generate 4723 followed by audit trail entries.",what_good_looks_like:"Expected: users changing passwords after expiry reminders or when prompted by policy. Investigate: repeated failures (user can't remember current password — may need admin reset), password changes for service accounts (these should be managed, not user-initiated), password changes outside business hours.",common_mistakes:["Confusing 4723 (self-service change) with 4724 (admin reset) — different privileges and workflows","Ignoring failures — a user who failed to change their own password may then call the helpdesk for a reset"],causes:["User changing password proactively","Password expiry prompt accepted","User response to security training recommendation","Suspected compromise prompting password change","Compliance policy requiring periodic change"],steps:["Filter Security log for 4723","Note success or failure","If failure: check failure reason — may need to assist user with a 4724 admin reset","Correlate with any recent 4625 events for the same account","For service accounts: if a user is changing a service account password, escalate immediately"],symptoms:["user changed password","password change attempt","user updating their password","password expired","password change failed"],tags:["password","account-management","audit","compliance"],powershell:`# Password Change Attempt Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4723
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List`,related_ids:[4724,4625,4740],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4723"},{id:4724,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Fundamental",title:"Password Reset Attempt",short_desc:"An administrator attempted to reset another account's password.",description:"Event 4724 records when a privileged account attempts to reset another user's password — without needing to know the current password. This requires Reset Password rights in Active Directory or local administrator privileges for local accounts. It is a critical event for account security auditing. If an attacker has compromised an admin account, resetting a target user's password is an early step in account takeover.",why_it_happens:"Windows generates 4724 as part of Account Management auditing when a user with sufficient privilege calls the password reset API. Unlike 4723 (self-service change), 4724 does not require the current password. The event is generated on the DC that processed the request for domain accounts.",what_good_looks_like:"Expected: helpdesk staff resetting passwords via a documented ticket. Investigate: password resets by non-helpdesk accounts, resets for accounts not in a current ticket, a compromised admin account resetting passwords (especially for other admin accounts), password resets for service accounts.",common_mistakes:["Not correlating with a helpdesk ticket system — every 4724 should have a corresponding ticket","Missing that this event is generated on the DC, not the helpdesk workstation","Not alerting when admin accounts reset other admin account passwords — this is very sensitive"],causes:["Helpdesk agent following a verified password reset request","Admin resetting their own account through an admin tool","Automated password management system rotating credentials","Attacker using a compromised admin account for account takeover","Insider threat resetting a target account's password"],steps:["Filter Security log for 4724 on DC or target machine",'Note the "Target Account Name" and "Subject Account Name" (who reset whom)',"Verify against helpdesk ticket records","If no ticket: contact the admin who made the change to verify","If unauthorized: immediately re-rotate the affected account's password and investigate the admin account","Check for 4624 logons using the affected account after the reset"],symptoms:["password reset","admin reset password","who reset this users password","password was changed by helpdesk","account password changed","forced password reset"],tags:["password","account-management","helpdesk","audit","security"],powershell:`# Password Reset Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4724
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated    = $_.TimeCreated
        ResetBy        = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        TargetAccount  = ($data | Where-Object Name -eq 'TargetUserName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4723,4720,4625],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4724"},{id:4725,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Fundamental",title:"User Account Disabled",short_desc:"A user account was disabled.",description:"Event 4725 records the disabling of a user account. This should correspond to offboarding processes, security responses, or account lifecycle management. Unexpected account disabling — especially for admin accounts or shared service accounts — can indicate sabotage or an attacker trying to lock out defenders during an incident. Equally, if you see an account disabled followed by being re-enabled (4722), investigate who did both.",why_it_happens:`Windows generates 4725 as part of Account Management auditing when an account's "Account is disabled" flag is set. This happens through AD Users and Computers, Net User, or Disable-ADAccount in PowerShell.`,what_good_looks_like:"Expected: IT admin disabling accounts as part of offboarding (aligned with HR termination list). Investigate: accounts disabled outside of standard offboarding process, admin accounts being disabled unexpectedly, service accounts being disabled causing service outages.",common_mistakes:["Not linking account disable events to an HR or ITSM ticket for the offboarding","Not checking for service dependencies before disabling service accounts","Ignoring who performed the disable — was it the expected admin or an unexpected account?"],causes:["Employee offboarding","Security incident response (compromised account)","Account lockout mitigation (disabling before investigation)","Policy enforcement","Accidental disable","Insider sabotage"],steps:["Filter Security log for 4725",'Identify "Target Account" and "Subject Account" (who disabled whom)',"Verify against offboarding records or incident tickets","If a service account: check for 7000/7023 service failure events","If unauthorized: re-enable the account and investigate immediately","If part of a security response: confirm the account is appropriately contained"],symptoms:["account disabled","user account disabled","who disabled this account","account turned off","user locked out by admin"],tags:["account-management","offboarding","audit","security"],powershell:`# User Account Disabled Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4725
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List`,related_ids:[4722,4720,4726],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4725"},{id:4726,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Fundamental",title:"User Account Deleted",short_desc:"A user account was permanently deleted.",description:"Event 4726 records the deletion of a user account. This is more severe than disabling (4725) because the action is harder to reverse — especially for local accounts on a workstation where no AD recycle bin exists. Unauthorised account deletion can be a sign of sabotage or an attacker covering their tracks by removing the account they used. The event includes who deleted the account and which account was deleted.",why_it_happens:"Windows generates 4726 as part of Account Management auditing when a user account is deleted via Active Directory Users and Computers, Net User /delete, or Remove-ADUser. For AD accounts with the Recycle Bin feature enabled, the account can be restored. For local accounts, deletion is permanent.",what_good_looks_like:"Expected: IT admin deleting accounts following the retention period after offboarding (accounts usually disabled first, then deleted 30-90 days later). Investigate: accounts deleted immediately after disabling (skipping the retention period), deletion of admin or service accounts, accounts deleted outside of a change window.",common_mistakes:["Not having AD Recycle Bin enabled — makes recovery much harder","Deleting accounts without first auditing what resources they own (files, mailboxes, groups)","Not keeping records — once deleted without recycle bin, the SID is gone"],causes:["End of account retention period after offboarding","Cleanup of test or temporary accounts","Admin error","Attacker covering tracks by deleting accounts they created (4720)","Insider sabotage targeting specific accounts"],steps:["Filter Security log for 4726",'Note "Target Account" (deleted) and "Subject Account" (who deleted)',"Check if there was a preceding 4725 (disable before delete) — expected for offboarding","If no prior 4725: account was deleted directly, which is unusual","For AD accounts: attempt restore from Recycle Bin if available","For local accounts: deletion is permanent — document the incident"],symptoms:["account deleted","user account removed","who deleted this account","account missing from AD","user account gone"],tags:["account-management","offboarding","audit","security"],powershell:`# User Account Deletion Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4726
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List`,related_ids:[4725,4720,4722],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4726"},{id:4728,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Fundamental",title:"Member Added to Global Security Group",short_desc:"A user or computer was added to an Active Directory global security group.",description:"Event 4728 records membership additions to global security groups in Active Directory. Global groups apply domain-wide and are typically used to assign permissions to users of the same domain. Additions to high-privilege groups — Domain Admins, Enterprise Admins, Schema Admins — should immediately generate an alert. Even additions to lower-privilege groups can matter if the group controls access to sensitive resources.",why_it_happens:"Windows generates 4728 as part of Account Management auditing on domain controllers when a member is added to a global group. Global groups have domain-wide scope and are replicated across all DCs. The event captures who was added (Target Account) and who made the change (Subject).",what_good_looks_like:"Expected: IT admin adding a new employee to department groups during onboarding, following a ticket. Investigate: anyone added to Domain Admins, Schema Admins, or Group Policy Creator Owners, additions made outside business hours, additions not linked to a ticket, a standard user account being used to make group changes.",common_mistakes:["Not having real-time alerts for additions to Domain Admins — this should page someone immediately","Only monitoring Domain Admins and missing other dangerous groups like Backup Operators or Account Operators","Not tracking what groups service accounts are members of"],causes:["IT admin following onboarding process","Permissions change for a project or role","Attacker adding compromised account to privileged group","Software deployment requiring group membership","GPO-managed group membership update"],steps:["Filter Security log for 4728 on a DC",'Note the "Member Account Name" (who was added) and "Group Name"','Check "Subject Account Name" — who made the change?',"Verify against a helpdesk ticket or change request","If the group is privileged (Domain Admins, Backup Operators): immediately escalate","If unauthorized: remove the account from the group immediately","Check for subsequent 4624 logon events from the newly added account"],symptoms:["user added to group","member added to domain admins","privilege escalation group","who added this user to the group","group membership change","unexpected admin group member"],tags:["group-membership","account-management","privilege-escalation","audit","security"],powershell:`# Global Group Member Added Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4728
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated   = $_.TimeCreated
        MemberAdded   = ($data | Where-Object Name -eq 'MemberName').'#text'
        GroupName     = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        ChangedBy     = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4732,4756,4720,4672],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4728"},{id:4732,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Fundamental",title:"Member Added to Local Security Group",short_desc:"A user or computer was added to a local security group (e.g., local Administrators).",description:"Event 4732 records membership additions to local security groups — most importantly the local Administrators group. Unlike domain groups (4728, 4756), local groups apply only to the specific machine. Adding an account to the local Administrators group gives that account full control over the machine, including the ability to read all files, install software, and bypass many security controls. This is a very high-value alert for endpoint security.",why_it_happens:"Windows generates 4732 on the local machine as part of Account Management auditing when a user is added to a local group via Computer Management, Net Localgroup, or Add-LocalGroupMember. On domain-joined machines, this may also be triggered by GPO Restricted Groups or Local Users and Groups preferences.",what_good_looks_like:"Expected: IT policy intentionally adding specific accounts to local Admins (e.g., helpdesk group added via GPO). Investigate: standard user accounts being added to local Admins manually, domain users being added to local Admins on machines they shouldn't administer, additions made by unexpected accounts or at unexpected times.",common_mistakes:["Not monitoring local group changes on endpoints — most SIEM configurations only monitor DCs","Not realising that local admin rights on a workstation can be used to dump credentials with tools like Mimikatz","Assuming the change was made by GPO just because the machine is domain-joined"],causes:["IT admin adding a user to local admins for a specific task","GPO Restricted Groups policy applying","Software installer adding its service account to local admins","Attacker adding a compromised account to local admins for persistence","User escalating their own privileges after an exploit"],steps:["Filter Security log for 4732 on the specific machine",'Check "Group Name" — is it Administrators or another sensitive group?','Check "Member Account Name" — who was added?','Check "Subject Account Name" — who made the change?',"Verify this was an authorized change (ticket, GPO)","If unauthorized: remove immediately with Remove-LocalGroupMember","Investigate how the person making the change had rights to do so"],symptoms:["added to local admins","user became local administrator","local admin group changed","who added this user to administrators","unexpected local admin","user got local admin rights"],tags:["group-membership","local-admin","privilege-escalation","endpoint","audit","security"],powershell:`# Local Group Member Added Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4732
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated  = $_.TimeCreated
        MemberAdded  = ($data | Where-Object Name -eq 'MemberName').'#text'
        GroupName    = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        ChangedBy    = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4728,4756,4720,4672],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4732"},{id:4740,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Fundamental",title:"User Account Locked Out",short_desc:"An account reached the failed logon threshold and was locked out.",description:'Event 4740 is generated on the domain controller (or local machine for local accounts) when an account is locked out due to too many failed password attempts. Critically, this event identifies the "Caller Computer Name" — the machine that sent the bad passwords. This is the starting point for every account lockout investigation. Note that 4740 is generated on the DC that processed the lockout, not on the machine the bad password came from.',why_it_happens:'Windows locks accounts when the failed logon count reaches the configured bad password threshold (typically 5-10 attempts). The lockout counter is maintained by the DC holding the PDC Emulator role. When the threshold is crossed, the PDC Emulator logs 4740 and the account is marked as locked. The "Caller Computer Name" field reveals which machine sent the authentication attempts that caused the lockout.',what_good_looks_like:"Expected: occasional lockouts from users mistyping passwords, quickly resolved. Investigate: repeated lockouts for the same account throughout the day (stale credentials on a device), lockouts coming from unexpected machines (a server, not a workstation), lockouts at 3am, multiple accounts locking out simultaneously (password spray attack).",common_mistakes:["Looking for 4740 on the workstation rather than the domain controller — it's on the DC","Looking at the wrong DC — check the PDC Emulator specifically for 4740 events, as lockouts are processed there","Stopping after finding the Caller Computer Name without investigating what application on that machine is sending bad passwords","Not looking at 4625 events on the Caller Computer to find the specific process sending bad credentials","Resetting the password without finding the source — the lockout will just happen again immediately"],causes:["User locked out after forgetting new password","Stale credentials cached on a mobile device after password change","Service configured with old password still attempting authentication","Mapped drive or Outlook profile with old credentials","Brute-force attack from an external source","VPN client using expired cached credentials"],steps:["Find 4740 on the PDC Emulator: Get-ADDomain | Select-Object PDCEmulator",'Note the "Caller Computer Name" from the event — go to that machine next',"On the Caller Computer, look for 4625 events matching the locked account",'Identify the "Caller Process Name" in 4625 — this reveals what application is sending bad passwords',"Common sources: Outlook (stale profile), mapped drives, scheduled tasks, mobile email apps, Chrome/Firefox saved passwords","Fix the credential source (update password in the application), then unlock the account with 4767","If no obvious source: check for RunAS, PST files, old VPN configs"],symptoms:["account locked out","user cant log in account locked","account keeps locking","lockout keeps happening","why does this account keep locking out","AD account locked","user account locked all the time","lockout every morning"],tags:["lockout","authentication","password","security","helpdesk","fundamental"],powershell:`# Account Lockout Investigation
# Eventful
# Run this on the PDC Emulator DC

$computer  = (Get-ADDomain).PDCEmulator   # Targets the PDC Emulator
$startTime = (Get-Date).AddHours(-24)
$lockedUser = 'username'                   # Replace with the locked-out username

# Find lockout events and the triggering machine
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4740
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated   = $_.TimeCreated
        LockedAccount = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        CallerMachine = ($data | Where-Object Name -eq 'CallerComputerName').'#text'
    }
} | Where-Object { $_.LockedAccount -eq $lockedUser -or $lockedUser -eq 'username' } |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4625,4767,4771,4776],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4740"},{id:4756,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Fundamental",title:"Member Added to Universal Security Group",short_desc:"A user or computer was added to a universal security group in Active Directory.",description:"Event 4756 records membership additions to universal security groups. Universal groups have forest-wide scope and can contain members from any domain in the forest. They are often used for enterprise-wide access control and appear in the Global Catalog. Like 4728 (global group changes), additions to privileged universal groups should be immediately alerted on.",why_it_happens:"Windows generates 4756 as part of Account Management auditing on DCs when a member is added to a universal group. Universal groups are replicated to the Global Catalog, meaning they are visible across the entire forest. Changes to these groups affect forest-wide access control.",what_good_looks_like:"Expected: IT admin updating enterprise access groups as part of onboarding. Investigate: additions to groups with forest-wide administrative rights, changes made by non-admin accounts, changes outside business hours.",common_mistakes:["Treating universal groups as less important than global groups — high-privilege universal groups can have forest-wide impact","Not monitoring the Global Catalog for group changes if the forest spans multiple domains"],causes:["IT admin updating enterprise access control groups","New user requiring forest-wide resource access","Software deployment using universal groups for licensing","Attacker adding accounts to forest-wide privileged groups"],steps:["Filter Security log for 4756 on a DC",'Note "Member Account Name" and "Group Name"','Check "Subject Account Name" — who made the change?',"Verify against ticket/change record","If the group is privileged, escalate immediately and remove if unauthorized"],symptoms:["universal group membership changed","user added to enterprise group","forest wide group change","universal security group member added"],tags:["group-membership","universal-group","active-directory","audit","security"],powershell:`# Universal Group Member Added Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with DC hostname
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4756
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List`,related_ids:[4728,4732,4720,4672],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4756"},{id:4767,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Fundamental",title:"User Account Unlocked",short_desc:"A locked user account was unlocked by an administrator.",description:"Event 4767 records when a locked-out account is manually unlocked by an admin. It includes who unlocked the account and which account was unlocked. On its own this is routine, but when combined with 4740 (lockout) data, it lets you see the full lockout/unlock cycle and measure how long users are locked out. If an account is being unlocked repeatedly, it means the root cause of the lockout was not fixed — the account will lock again.",why_it_happens:'Windows generates 4767 when an admin clears the "Account is locked out" flag via AD Users and Computers, Unlock-ADAccount, or Net User /active:yes. The event is generated on the DC that processes the unlock.',what_good_looks_like:"Expected: helpdesk unlocking accounts as part of a verified support call. Investigate: the same account being unlocked multiple times in a day (lockout root cause not fixed), unlocks happening at 3am (automated, or unauthorized), accounts being unlocked without a corresponding helpdesk ticket.",common_mistakes:["Unlocking the account without finding and fixing the source of bad credentials — it will lock again","Not verifying user identity before unlocking — social engineering can trick helpdesk into unlocking attacker-controlled accounts"],causes:["Helpdesk response to user support request","Automated unlock script","Admin directly managing account lifecycle","Scheduled task running unlock process"],steps:["Filter Security log for 4767","Match with preceding 4740 events for the same account","If the account locks out again after unlock: the root cause was not fixed","Investigate the Caller Computer Name from 4740 and fix the credential source","Track unlock frequency — repeated unlocks signal an unresolved problem"],symptoms:["account unlocked","account unlock","helpdesk unlocked account","who unlocked this account","account was unlocked"],tags:["lockout","account-management","helpdesk","audit"],powershell:`# Account Unlock Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with PDC Emulator DC hostname
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4767
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated    = $_.TimeCreated
        UnlockedBy     = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        TargetAccount  = ($data | Where-Object Name -eq 'TargetUserName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4740,4625,4624],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4767"},{id:4771,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Intermediate",title:"Kerberos Pre-Authentication Failed",short_desc:"A Kerberos authentication attempt failed at the pre-authentication stage.",description:"Event 4771 is generated on domain controllers when a Kerberos pre-authentication request fails. The failure code indicates the reason: 0x12 means account disabled, 0x18 means wrong password (pre-auth failed), 0x17 means password expired, 0x25 means the client clock is out of sync. This event is the Kerberos equivalent of 4625 for NTLM — it appears on the DC, not the workstation. A burst of 0x18 codes from many accounts is a strong indicator of a Kerberos password spray.",why_it_happens:"Kerberos pre-authentication is a security feature where the client must prove it knows the user's password before the KDC issues a Ticket Granting Ticket (TGT). The client encrypts a timestamp with the user's password hash. If the decryption fails (wrong password), 4771 is logged on the DC. If an account does not require pre-authentication (a security misconfiguration), AS-REP Roasting attacks become possible.",what_good_looks_like:"Expected: occasional 4771 events with 0x18 code from users mistyping passwords. Investigate: many 0x18 events for many accounts from one source IP (password spray), accounts without pre-authentication required (AS-REP Roast target), clock skew errors (0x25) that may indicate a time manipulation attack, failures for service accounts.",common_mistakes:["Not monitoring the DC for 4771 — junior admins only look at the workstation and miss Kerberos failures","Not knowing which accounts have pre-auth disabled — run: Get-ADUser -Filter {DoesNotRequirePreAuth -eq $true}","Confusing 4771 with 4768 (Kerberos TGT request) — 4771 is specifically a failure"],causes:["User typed wrong password","Account locked out or disabled","Password expired","Client clock out of sync with DC (>5 minute skew)","Kerberos password spray attack","Service account credentials not updated after password change"],steps:["Filter Security log for 4771 on the domain controller","Check failure code: 0x18 = wrong password, 0x12 = disabled, 0x17 = expired, 0x25 = clock skew","If 0x25: check time synchronization on the client machine","If many 0x18 for many accounts from one IP: password spray in progress — block the source","If 0x12 or account not found: correlate with 4740 to find lockout cause","Check if the affected account has pre-auth disabled: Get-ADUser <name> -Properties DoesNotRequirePreAuth"],symptoms:["kerberos authentication failed","kerberos error","kdc error","cannot authenticate to domain","domain login failure","kerberos pre auth failed","time skew kerberos","krb5 error"],tags:["kerberos","authentication","domain-controller","password-spray","audit","security"],powershell:`# Kerberos Pre-Auth Failure Investigation
# Eventful
# Run on Domain Controller

$computer  = $env:COMPUTERNAME  # Run on a DC
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

$failureCodeMap = @{
    '0x6'  = 'Client not found in Kerberos database'
    '0x12' = 'Account disabled, expired, or locked'
    '0x17' = 'Password has expired'
    '0x18' = 'Wrong password (pre-auth failed)'
    '0x19' = 'Pre-authentication required'
    '0x25' = 'Clock skew too large (>5 min)'
    '0x32' = 'Service not available'
}

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4771
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    $code = ($data | Where-Object Name -eq 'Status').'#text'
    [PSCustomObject]@{
        TimeCreated   = $_.TimeCreated
        AccountName   = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        FailureReason = $failureCodeMap[$code] ?? $code
        ClientAddress = ($data | Where-Object Name -eq 'IpAddress').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4625,4740,4776,4624],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4771"},{id:4776,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Intermediate",title:"NTLM Authentication Attempt",short_desc:"The domain controller attempted to validate NTLM credentials.",description:"Event 4776 is generated on the domain controller that processed an NTLM authentication request. It records the account name, the workstation, and whether authentication succeeded or failed. NTLM is the legacy authentication protocol — modern environments should primarily use Kerberos. High volumes of 4776 events may indicate NTLM relay attacks, pass-the-hash attempts, or clients that cannot reach a DC for Kerberos. Failed 4776 events with error code 0xC000006A indicate wrong credentials.",why_it_happens:"NTLM authentication occurs when Kerberos is unavailable or not supported — accessing resources by IP address instead of hostname, accessing resources in non-domain environments, older clients and servers. The DC validates the NTLM challenge/response by re-computing the expected response using the stored credential hash. If it matches, authentication succeeds.",what_good_looks_like:"Expected: some NTLM authentication for legacy applications, connecting to resources by IP. Investigate: large volumes of failed 4776 for one account (brute-force or pass-the-hash), NTLM from unexpected sources, NTLM where Kerberos should be working, errors indicating pass-the-hash tools were used (error code 0xC000006D with blank workstation name).",common_mistakes:["Treating all NTLM as malicious — it is still common in many environments and not all of it is suspicious",'Not knowing what "NTLM relay attack" means — an attacker intercepts NTLM challenges and relays them to authenticate elsewhere',"Not checking if NTLM is being used for internal authentications that should be Kerberos (accessing shares by IP instead of name)"],causes:["Client accessing resource by IP instead of DNS name (forces NTLM)","Legacy application not supporting Kerberos","Domain trust using NTLM","NTLM relay attack in progress","Pass-the-hash attack using captured NTLM hash","Service account cached credentials using NTLM"],steps:["Filter Security log for 4776 on the DC","Check Error Code: 0xC0000064 = bad username, 0xC000006A = wrong password, 0x0 = success","Check Workstation Name — is it a known machine?","High volume from one machine with failures = possible pass-the-hash or brute-force","Blank Workstation Name = possible network-level attack","If investigating NTLM relay: look for successful 4776 where the source is not the real client"],symptoms:["ntlm authentication","ntlm failed","pass the hash","ntlm relay","legacy authentication","ntlm error","authentication ntlm"],tags:["ntlm","authentication","pass-the-hash","relay","domain-controller","audit","security"],powershell:`# NTLM Authentication Investigation
# Eventful
# Run on Domain Controller

$computer  = $env:COMPUTERNAME  # Run on a DC
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4776
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    $err  = ($data | Where-Object Name -eq 'Status').'#text'
    [PSCustomObject]@{
        TimeCreated  = $_.TimeCreated
        AccountName  = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        Workstation  = ($data | Where-Object Name -eq 'Workstation').'#text'
        ErrorCode    = $err
        Outcome      = if ($err -eq '0x0') { 'Success' } else { 'Failure' }
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4625,4740,4771,4624],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4776"},{id:4778,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Intermediate",title:"RDP Session Reconnected",short_desc:"A previously disconnected Remote Desktop session was reconnected.",description:"Event 4778 is generated when a Remote Desktop or Remote Assistance session is reconnected after being in a disconnected state. Unlike 4624 (which is logged when a new session starts), 4778 specifically indicates a reconnection to an existing disconnected session. It includes the account name, the client machine name, and the client address. Paired with 4779 (disconnection), it gives a complete picture of RDP session lifecycle.",why_it_happens:'When an RDP session is disconnected (rather than logged off), the session remains alive on the server in a suspended state. When the user — or any user with appropriate credentials — reconnects, Windows logs 4778 with the "Logon Type 10" designation. A new session logs 4624 Type 10; a reconnection to existing disconnected session logs 4778.',what_good_looks_like:"Expected: a user reconnecting to their own session after a network drop or laptop close/open. Investigate: reconnections from different IP addresses than the original session (session hijacking risk), reconnections to sessions owned by other users, reconnections at unusual times to sessions that have been disconnected for extended periods.",common_mistakes:["Treating 4778 identically to 4624 — reconnections are a distinct event type with different security implications","Not noticing when a user reconnects to another user's session (on RDS servers, this is possible for admins)","Not correlating Client Address in 4778 vs 4779 to detect session hijacking"],causes:["User reconnecting after network interruption","User reconnecting after closing laptop lid","Admin reconnecting to manage a disconnected session","Automated reconnection by RDP client","Session hijacking (admin connecting to another user's session)"],steps:["Filter Security log for 4778",'Note "Account Name" and "Client Address"',"Compare Client Address with the previous 4779 (disconnect) for the same session","If Client Address changed: possible session handoff or compromise — investigate","On RDS servers: check for admins reconnecting to other users' sessions","Correlate with RDS events 25 (session reconnected) in the TerminalServices-LocalSessionManager log"],symptoms:["rdp reconnected","remote desktop reconnect","session reconnected","rdp session resumed","remote session resumed"],tags:["rdp","session-reconnect","remote-desktop","audit","security"],powershell:`# RDP Session Reconnect Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4778
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated    = $_.TimeCreated
        AccountName    = ($data | Where-Object Name -eq 'AccountName').'#text'
        ClientName     = ($data | Where-Object Name -eq 'ClientName').'#text'
        ClientAddress  = ($data | Where-Object Name -eq 'ClientAddress').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4779,4624,4634,21,25],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4778"},{id:4779,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Intermediate",title:"RDP Session Disconnected",short_desc:"A Remote Desktop session was disconnected (not logged off — session stays alive).",description:"Event 4779 records when an RDP or Remote Assistance session transitions to disconnected state — meaning the user closed the RDP window or lost connectivity, but did not formally log off. The session remains alive on the server and can be reconnected (4778). This event includes the account, client machine, and client address. Understanding the difference between a disconnect (4779) and a logoff (4634) is fundamental to RDP session management.",why_it_happens:"When the RDP client window is closed without logging off, or when a network interruption occurs, the RDP session state transitions from active to disconnected. Windows keeps the session alive for the configured session idle/disconnect timeout period. If the session times out without reconnection, it is logged off. 4779 is the audit record of the disconnect transition.",what_good_looks_like:"Expected: users disconnecting and reconnecting throughout the day, especially on RDS servers where multiple users work. Investigate: sessions that stay disconnected for very long periods (potential abandoned sessions with data exposed), many sessions disconnecting simultaneously (network event), disconnect followed by reconnection from a different IP.",common_mistakes:["Assuming a 4779 means the user logged off — they did not, the session is still alive","Not checking for idle disconnected sessions on RDS servers — these waste resources and are a security risk"],causes:["User closed RDP window without logging off","Network interruption broke the connection","RDP client timeout","Admin forced disconnect of a session","Session policy disconnected idle session"],steps:["Filter Security log for 4779",'Note the "Client Address" — where was the session connecting from?',"Check if the session was reconnected (4778) after disconnect","If no 4778 follows: session is still disconnected — check if it should be terminated","On RDS: enumerate disconnected sessions with: qwinsta /server:<server>","Terminate stale sessions with: logoff <sessionid> /server:<server>"],symptoms:["rdp disconnected","remote desktop disconnected","session disconnected","rdp session ended","user disconnected from rdp"],tags:["rdp","session-disconnect","remote-desktop","audit"],powershell:`# RDP Session Disconnect Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4779
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated   = $_.TimeCreated
        AccountName   = ($data | Where-Object Name -eq 'AccountName').'#text'
        ClientName    = ($data | Where-Object Name -eq 'ClientName').'#text'
        ClientAddress = ($data | Where-Object Name -eq 'ClientAddress').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4778,4624,4634,24,40],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4779"},{id:4798,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Advanced",title:"User's Local Group Membership Enumerated",short_desc:"A process queried the local group membership of a specific user account.",description:"Event 4798 is generated when a process enumerates the local group memberships of a user account. This is frequently triggered by Windows components during logon as part of building the access token, but it can also be triggered by reconnaissance scripts enumerating which local groups a user belongs to. When generated by unexpected processes (not winlogon.exe, lsass.exe, or explorer.exe) at scale, it may indicate an attacker performing internal reconnaissance.",why_it_happens:"Applications and Windows components enumerate group membership using the NetUserGetLocalGroups() API or its equivalent. During normal logon, lsass.exe calls this to build the access token. Security assessment tools, and unfortunately attackers, call the same APIs to understand what access a user or set of users has across local machines.",what_good_looks_like:"Expected: winlogon.exe, lsass.exe, and explorer.exe generating these events during logon. Investigate: enumeration by unexpected processes, high-volume enumeration of many accounts in a short time from one process (automated reconnaissance), enumeration of admin accounts specifically.",common_mistakes:["Alerting on every 4798 — most are legitimate logon infrastructure","Not filtering by Process Name — the process doing the enumeration is the critical field"],causes:["Normal Windows logon process building access token","Security compliance tools performing audits","Attacker tool enumerating accounts for privilege escalation targeting","IT management scripts checking group membership"],steps:["Filter Security log for 4798",'Check "CallerProcessName" — is it a known Windows process or something unexpected?',"If unexpected process: investigate with 4688 to find where that process came from","Check if multiple accounts were enumerated in quick succession from the same process"],symptoms:["local group enumeration","who is in local admins","group membership query","account reconnaissance","listing local group members"],tags:["reconnaissance","group-membership","enumeration","audit","security"],powershell:`# Local Group Membership Enumeration Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4798
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated   = $_.TimeCreated
        TargetAccount = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        CallerProcess = ($data | Where-Object Name -eq 'CallerProcessName').'#text'
        SubjectUser   = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
    }
} | Where-Object { $_.CallerProcess -notlike '*winlogon*' -and $_.CallerProcess -notlike '*lsass*' } |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4799,4627,4688],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4798"},{id:4799,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Advanced",title:"Local Group Membership Enumerated",short_desc:"A process queried the membership of a local security group.",description:"Event 4799 is generated when a process enumerates the members of a local security group (as opposed to 4798, which enumerates the groups a user belongs to). This event is particularly interesting when the target group is Administrators — it reveals what processes are checking who has local admin rights on the machine. Malware and attacker tools frequently query local Administrators group membership to understand the privilege landscape before escalating.",why_it_happens:"Enumeration via the NetLocalGroupGetMembers() API generates 4799. This is called by many legitimate tools (LAPS, Windows Admin Center, compliance tools), but also by attacker tools during post-exploitation reconnaissance.",what_good_looks_like:"Expected: management tools and Windows components enumerating Administrators membership. Investigate: unknown processes enumerating Administrators specifically, batch enumeration of many groups in a short period, enumeration from processes launched by unusual parents.",common_mistakes:["Not filtering by the Target Group Name — enumeration of Administrators is much more interesting than Other groups","Treating every 4799 as suspicious when most are legitimate management tool activity"],causes:["Windows management tools checking group membership","LAPS (Local Administrator Password Solution) checking admin accounts","Attacker tool enumerating local admin members for targeting","Compliance scanning tools"],steps:["Filter Security log for 4799",'Check "Target Group Name" — is it Administrators?','Check "Calling Process Name" — is it a known tool or unexpected?',"Correlate with 4688 if the calling process is unusual","High frequency from one process: possible automated reconnaissance"],symptoms:["local admins enumerated","who is in administrators group","listing administrator group members","local group query","local group enumeration"],tags:["reconnaissance","group-membership","enumeration","local-admin","audit","security"],powershell:`# Local Group Enumeration Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4799
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated   = $_.TimeCreated
        TargetGroup   = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        CallerProcess = ($data | Where-Object Name -eq 'CallerProcessName').'#text'
        SubjectUser   = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4798,4627,4688,4732],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4799"},{id:1102,source:"Microsoft-Windows-Eventlog",channel:"Security",severity:"Warning",skill_level:"Intermediate",title:"Security Audit Log Cleared",short_desc:"The Security event log was cleared — records who cleared it and when.",description:"Event ID 1102 from Microsoft-Windows-Eventlog is written to the Security log immediately before it is cleared. It records the account that performed the clear. For IT support, this event explains why a Security log starts from a recent date with no earlier history — someone cleared it. In an IT support context this is usually an admin clearing logs to free space or a management script. The key is identifying who cleared it and whether that was expected. All evidence from before the clear is permanently gone.",why_it_happens:"Written by the Windows event logging service immediately before the Security log is wiped. Clearing the Security log requires SeSecurityPrivilege — only administrators hold this right. Event 1102 is always the last event before the gap in the log.",what_good_looks_like:"Absent in most IT support logs. When present: check the SubjectUserName field — was this a known admin at an expected time? If yes, low priority. Unknown account or unexpected timing warrants follow-up.",causes:["Admin clearing the log manually to free disk space","Log management or SIEM tool rotating the log","Diagnostic or setup script clearing logs","Automated policy clearing logs on schedule"],steps:["Check SubjectUserName — who cleared the log","Confirm with that user or their manager whether the action was intentional","Check Event 104 in System log — if System log was also cleared at the same time, investigate further","Note: all log data before the clear is permanently gone"],symptoms:["security log empty","no security events before a date","security log was cleared","event log history missing","logs start from today"],tags:["log-clear","security","audit","admin"],powershell:`# Security Log Clear History
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName = 'Security'
    Id      = 1102
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[104,4624,4672],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-1102"},{id:4946,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Intermediate",title:"Windows Firewall Rule Added",short_desc:"A rule was added to the Windows Firewall exception list — records who added it and what was allowed.",description:"Event 4946 is generated whenever a new rule is added to the Windows Firewall. It records the profile (Domain, Private, Public), the rule name, and the account that added it. This event is invaluable for auditing unauthorized firewall changes — if a user or malware adds an inbound allow rule to bypass security controls, this event captures it. On managed endpoints, firewall rules should only be changed by Group Policy or by authorized IT personnel; any unexpected 4946 is worth investigating.",why_it_happens:"Windows logs this event when any process or user with sufficient privilege adds a firewall rule via Windows Defender Firewall with Advanced Security, netsh advfirewall, PowerShell New-NetFirewallRule, or the Windows Firewall API. Malware and remote access tools often add inbound rules to maintain access.",what_good_looks_like:"Firewall rule additions should match known maintenance windows, software installations, or GPO pushes. Any rule added outside these windows or from an unexpected account needs investigation.",common_mistakes:['Not auditing Windows Firewall changes at all — these events only appear if "Audit Policy Change" auditing is enabled',"Not correlating 4946 with the process that created the rule — a rule added by cmd.exe or powershell.exe under a user account is suspicious","Missing that software installers routinely add firewall exceptions — checking the rule name often clarifies whether it is legitimate"],causes:["Software installation adding a firewall exception for its service or port","Administrator manually adding an exception for a new application","Group Policy pushing a new firewall rule","Malware or unauthorized remote access tool adding an inbound allow rule","RMM tool or monitoring agent adding its required ports"],steps:["Check the rule name in Event 4946 — legitimate software usually names rules clearly","Check the Subject Account Name — was it a service, an admin, or a user account?","Cross-reference with Event 4688 (process creation) at the same time to see which process added the rule",'View the current rule: Get-NetFirewallRule -DisplayName "<rule name>" | Get-NetFirewallPortFilter','If the rule is unauthorized: Remove-NetFirewallRule -DisplayName "<rule name>"',"Check for Event 4948 (rule deleted) to see if rules are being toggled on and off"],symptoms:["firewall rule added","new firewall exception created","who added firewall rule","firewall change audit","firewall rule modification","unauthorized firewall rule","firewall exception added by software","malware added firewall rule","firewall rules changed","audit firewall changes"],tags:["firewall","audit","policy-change","security","network","rule"],powershell:`# Firewall rule change audit
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName = 'Security'
    Id      = @(4946, 4947, 4948, 4950)
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        EventId     = $_.Id
        RuleName    = ($data | Where-Object Name -eq 'RuleName').'#text'
        Account     = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4948,4950,4954,4688],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4946"},{id:4948,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Intermediate",title:"Windows Firewall Rule Deleted",short_desc:"A firewall rule was deleted — if it was a protective rule, this may open a security gap.",description:"Event 4948 is generated when a Windows Firewall rule is deleted. While adding rules (4946) is the typical concern for malware, deleting rules is equally important — an attacker may delete inbound block rules or outbound restriction rules to open communication channels. It is also common for misconfigured software uninstallers to delete firewall rules that were not theirs. The event records the rule name, profile, and the account that performed the deletion.",why_it_happens:"Rule deletion occurs via the Windows Firewall console, netsh advfirewall delete rule, Remove-NetFirewallRule in PowerShell, or the Windows Firewall API. Software uninstallers routinely delete their own rules, but deletion of rules named after security tools or baseline policies is suspicious.",what_good_looks_like:"Rule deletions matching software uninstallation events or known maintenance. Any deletion of a rule you do not recognize, or a deletion that correlates with suspicious activity, requires investigation.",common_mistakes:["Not correlating with Event 4688 (process creation) to see which process deleted the rule","Not checking what the deleted rule was protecting against — some rules block known attack vectors","Assuming rule deletion is always benign because uninstallers delete rules routinely"],causes:["Software uninstaller removing its own firewall exception","Administrator cleaning up stale firewall rules","Malware or attacker removing a blocking rule to open a port","Group Policy overwriting and removing locally-created rules"],steps:["Check the rule name in Event 4948 — does the name correspond to known software?","Check the Subject Account Name — admin, service, or user account?","Cross-reference with software uninstall events (Event 1033 in Application log) at the same time","If the deleted rule protected an important service: recreate it: New-NetFirewallRule with appropriate parameters","Check if Event 4946 (rule added) follows 4948 closely — rules being replaced may indicate legitimate reconfiguration"],symptoms:["firewall rule deleted","firewall rule removed","who deleted firewall rule","firewall rule missing","firewall exception removed","security rule deleted","firewall change","audit firewall deletion"],tags:["firewall","audit","policy-change","security","network","rule","deletion"],powershell:`# Firewall rule deletion audit — last 7 days
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4948
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        RuleName    = ($data | Where-Object Name -eq 'RuleName').'#text'
        Profile     = ($data | Where-Object Name -eq 'ProfileChanged').'#text'
        Account     = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4946,4950,4688],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4948"},{id:4950,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Warning",skill_level:"Intermediate",title:"Windows Firewall Setting Changed",short_desc:"A Windows Firewall setting was changed — could indicate the firewall was disabled, a profile was changed, or default action was modified.",description:"Event 4950 is generated when a Windows Firewall setting (not a rule, but a global setting) is changed. This includes changes to whether the firewall is enabled/disabled per profile, the default inbound/outbound action, notifications settings, or unicast responses to multicast. Disabling the Windows Firewall via this mechanism generates Event 4950. This is a higher-severity event than 4946/4948 because it affects the overall firewall posture rather than a single rule.",why_it_happens:"Firewall setting changes occur via the Windows Defender Firewall control panel, Group Policy, netsh advfirewall set, or the Windows Security Center API. Attackers and malware commonly disable the firewall entirely to simplify outbound communication.",what_good_looks_like:"No unexpected 4950 events. Firewall settings should only change during controlled maintenance or Group Policy updates. Any 4950 outside a maintenance window — especially if it shows the firewall being disabled — is a priority investigation.",common_mistakes:["Not checking which setting changed — disabling the firewall vs enabling logging are very different severity levels","Not correlating with the account that made the change — user accounts should not be disabling the firewall"],causes:["Administrator disabling the firewall for troubleshooting (and forgetting to re-enable it)","Group Policy changing firewall profile settings","Malware disabling the firewall to reduce detection or enable inbound connections","Software installation modifying firewall defaults"],steps:["Read Event 4950 carefully to identify which setting changed","If the firewall was disabled: re-enable it immediately via Group Policy or Set-NetFirewallProfile -Enabled True","Check the Subject Account Name — who or what made the change?","Cross-reference with Event 4688 to identify the process that triggered the change","If firewall was disabled by non-admin: investigate for malware — check Event 4688 for suspicious process activity around the same time"],symptoms:["windows firewall disabled","firewall turned off","firewall settings changed","who disabled the firewall","firewall profile changed","firewall configuration changed","windows firewall setting modified","firewall default action changed"],tags:["firewall","audit","security","policy-change","disabled","network"],powershell:`# Firewall settings change audit and current state
# Eventful

# Current firewall state per profile
Get-NetFirewallProfile | Select-Object Name, Enabled, DefaultInboundAction, DefaultOutboundAction

# Firewall setting change events — last 7 days
Get-WinEvent -FilterHashtable @{
    LogName   = 'Security'
    Id        = @(4950, 4954)
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[4946,4948,4954],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4950"},{id:5140,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Intermediate",title:"Network Share Accessed",short_desc:"A network share was accessed — records who connected, from where, and which share.",description:"Event 5140 is generated when a user successfully connects to a network share. It records the account name, the source IP address, the share name, and the access type. This is a key event for auditing who is accessing shared folders on a file server or domain controller. On a DC, share access to ADMIN$, C$, or IPC$ from unexpected accounts or IPs is a significant security concern — these are common targets for lateral movement and credential theft tools like Mimikatz. Note: Object Access auditing must be enabled for this event to appear.",why_it_happens:"Windows logs this event when the SMB server service accepts a connection to a share. It fires on the server side, meaning the event appears on the file server being accessed, not on the client initiating the connection.",what_good_looks_like:"Share access matching expected file server activity. Investigate: access to ADMIN$ or C$ from workstations (administrative shares should only be accessed by IT), access outside business hours, access from unexpected source IPs, bulk access events from a single account.",common_mistakes:['Not enabling Object Access auditing — Event 5140 requires "Audit File Share" auditing to be enabled under Advanced Audit Policy',"Not checking the Share Name field — access to IPC$, ADMIN$, or C$ is much more significant than access to a named share","Not correlating source IP with a known device — source IP identifies the accessing machine"],causes:["User accessing a file server share (normal operation)","IT admin connecting to ADMIN$ or C$ for remote management","Backup software accessing shares to back up data","Lateral movement — attacker using stolen credentials to access shares on other machines","Worm or ransomware scanning and accessing network shares"],steps:["Confirm Audit File Share is enabled: secpol.msc → Advanced Audit Policy → Object Access → Audit File Share","Filter Security log for Event 5140","Note the Share Name — ADMIN$, C$, and IPC$ are high-value targets","Note the Source Address — which machine is connecting?","Correlate unexpected share access with Event 4624 (logon) at the same time — verify the logon type and source IP match","For suspicious lateral movement: check if the same source IP is accessing shares on multiple machines"],symptoms:["who accessed shared folder","network share access audit","file share access log","who connected to file server","audit share access","who accessed admin share","network share audit trail","lateral movement file share","unauthorized share access","ransomware accessing shares"],tags:["share","audit","smb","file-server","lateral-movement","security","network"],powershell:`# Network share access audit
# Eventful
# Note: Requires "Audit File Share" enabled in Advanced Audit Policy

$startTime = (Get-Date).AddHours(-24)

Get-WinEvent -FilterHashtable @{
    LogName = 'Security'
    Id      = 5140
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated  = $_.TimeCreated
        Account      = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        ShareName    = ($data | Where-Object Name -eq 'ShareName').'#text'
        SourceIP     = ($data | Where-Object Name -eq 'IpAddress').'#text'
        AccessType   = ($data | Where-Object Name -eq 'AccessMask').'#text'
    }
} | Where-Object { $_.ShareName -notlike '*IPC*' } |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[5145,4624,4625,4688],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-5140"},{id:5145,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Advanced",title:"Network Share File Access Check",short_desc:"A check was made to see if a client can access specific files or folders within a network share — high-volume but granular audit trail.",description:"Event 5145 is generated when Windows checks whether a client can access a specific file or folder within a network share. Where Event 5140 fires once per share connection, Event 5145 fires for each individual file or folder access check within that share. This means 5145 is extremely high-volume on active file servers — a user browsing a share generates hundreds of these events. Its value is forensic: when investigating a specific incident, 5145 tells you exactly which files were accessed or attempted, not just that the share was connected.",why_it_happens:'Every file or folder access within a network share is preceded by an access check. Windows Audit generates Event 5145 to record these checks for both successful and failed access (the failure case — "Access Denied" on a file within an accessible share — is particularly valuable for investigating unauthorized access attempts).',what_good_looks_like:'High volume is normal. Look for 5145 with "Access Denied" result (denied checks) for files the user should not be accessing, or bulk file access (hundreds of unique files in a short time window — ransomware indicator).',common_mistakes:["Enabling 5145 on a busy file server without filtering — it will generate millions of events per day and overwhelm the Security log","Confusing 5145 volume (access checks) with 5140 (share connections) — they are different levels of granularity","Not filtering for failed access checks — success events are mostly noise, but Denied events pinpoint unauthorized access attempts"],causes:["Normal file server access (high volume, expected)","Ransomware enumerating and accessing all files on a share (burst of access events across many files in seconds)","Data exfiltration — bulk file access by a single account","Unauthorized access attempt — access denied results on restricted folders"],steps:["Enable Audit Detailed File Share under Advanced Audit Policy if 5145 events are not appearing","When investigating: filter 5145 for the specific account, IP, and time window",'Look for "Access Denied" (failure) events — these reveal which files a user tried but failed to access',"For ransomware investigation: count unique file paths accessed per minute — ransomware generates hundreds per second","Use Event 5140 first (share-level) to narrow down which share to investigate, then drill into 5145"],symptoms:["which files were accessed on file server","file access audit","who accessed specific file","file server detailed audit","ransomware file access log","unauthorized file access","file access denied log","bulk file access audit","data exfiltration investigation","file access forensics"],tags:["share","audit","smb","file-access","forensics","security","ransomware"],powershell:`# Detailed file share access — filter for denied checks or specific account
# Eventful
# Note: Very high volume on active servers — always filter by time and account

$targetAccount = 'username'  # Replace with account to investigate
$startTime     = (Get-Date).AddHours(-2)

Get-WinEvent -FilterHashtable @{
    LogName   = 'Security'
    Id        = 5145
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    $acct = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
    if ($acct -like "*$targetAccount*") {
        [PSCustomObject]@{
            TimeCreated  = $_.TimeCreated
            Account      = $acct
            Share        = ($data | Where-Object Name -eq 'ShareName').'#text'
            RelativePath = ($data | Where-Object Name -eq 'RelativeTargetName').'#text'
            AccessMask   = ($data | Where-Object Name -eq 'AccessMask').'#text'
        }
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[5140,4624,4625],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-5145"},{id:4103,source:"Microsoft-Windows-PowerShell",channel:"Microsoft-Windows-PowerShell/Operational",severity:"Info",skill_level:"Advanced",title:"PowerShell: Module Logging — Command Executed",short_desc:"Records every PowerShell command and pipeline output when module logging is enabled — key for security investigations.",description:"Event 4103 is generated when PowerShell module logging is enabled and a PowerShell command or pipeline executes. It captures the full command input, the module name, and the output. Module logging provides visibility into PowerShell activity at the command level — including commands run by scripts, remote sessions, and constrained language mode bypasses. For security investigations, 4103 events reveal what commands an attacker or malware ran via PowerShell, even if the script attempted to evade detection. This log must be explicitly enabled via Group Policy or the registry.",why_it_happens:"Module logging is configured via Group Policy (Computer Configuration → Administrative Templates → Windows Components → Windows PowerShell → Turn on Module Logging) or the registry. When enabled, the PowerShell engine writes each command and its output to the Operational log.",what_good_looks_like:"In a security-conscious environment, 4103 provides an audit trail of all PowerShell activity. Focus investigation on: commands run outside business hours, commands run by unexpected accounts, commands accessing sensitive paths or registry locations, commands using encoded or obfuscated parameters.",common_mistakes:["Enabling module logging without also enabling script block logging (4104) — both together give the most complete picture","Forgetting to enable module logging via GPO first — no 4103 events appear without it","Not filtering by account or time window — 4103 is very high-volume on busy systems"],causes:["Any PowerShell command execution when module logging is enabled","PowerShell remoting commands (Enter-PSSession, Invoke-Command)","Scheduled tasks running PowerShell scripts","Malware using PowerShell as its execution engine"],steps:["Enable module logging: HKLM:\\SOFTWARE\\Policies\\Microsoft\\Windows\\PowerShell\\ModuleLogging → EnableModuleLogging = 1","Open Event Viewer → Applications and Services → Microsoft → Windows → PowerShell → Operational","Filter for Event ID 4103","When investigating: filter by account name and time window","Look for encoded commands: -EncodedCommand or [System.Convert]::FromBase64String — these are obfuscation red flags","Correlate with Event 4104 (script block logging) for the full script content"],symptoms:["what powershell commands were run","powershell audit log","powershell command history audit","who ran powershell","powershell activity log","powershell security audit","powershell logging","investigate powershell commands","malware used powershell","powershell module logging"],tags:["powershell","audit","security","logging","module-logging","forensics"],powershell:`# PowerShell module logging — recent commands
# Eventful
# Note: Requires module logging enabled via GPO or registry

Get-WinEvent -FilterHashtable @{
    LogName      = 'Microsoft-Windows-PowerShell/Operational'
    Id           = 4103
    StartTime    = (Get-Date).AddHours(-24)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, UserId, Message |
    Sort-Object TimeCreated -Descending |
    Select-Object -First 50 | Format-List`,related_ids:[4104,4688,4624],ms_docs:"https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_logging_windows"},{id:4104,source:"Microsoft-Windows-PowerShell",channel:"Microsoft-Windows-PowerShell/Operational",severity:"Warning",skill_level:"Advanced",title:"PowerShell: Script Block Logging — Script Content Captured",short_desc:"The full text of a PowerShell script block was logged — critical for investigating what malicious or suspicious scripts actually contained.",description:"Event 4104 captures the complete text of PowerShell script blocks as they are compiled — including dynamically constructed scripts, obfuscated payloads that have been decoded, and commands passed via -EncodedCommand (PowerShell automatically decodes and logs the plaintext). This makes 4104 the most powerful PowerShell security event: even heavily obfuscated malware reveals its true content in the 4104 log. Windows automatically logs 4104 for script blocks containing suspicious keywords (like Invoke-Mimikatz, Invoke-WebRequest to unusual URLs, etc.) regardless of whether full script block logging is enabled. Full logging captures everything.",why_it_happens:'Script block logging is enabled via Group Policy or the registry. Windows also has automatic "suspicious script detection" that generates 4104 warnings for script content matching known attack patterns — these appear even without full logging being enabled, which is why 4104 sometimes appears without 4103.',what_good_looks_like:"In a well-defended environment, 4104 is enabled for all PowerShell execution. Severity Warning events (Windows automatically flagged the content as suspicious) need immediate attention. Look for: encoded command decodes, calls to Invoke-Expression or iex, downloads from the internet (Invoke-WebRequest, System.Net.WebClient), reflection and memory injection patterns.",common_mistakes:["Not enabling script block logging, relying only on module logging (4103) — 4103 shows commands but not full script content","Ignoring Warning-level 4104 events that Windows automatically generated — these are pre-filtered for suspicious content","Not checking that the decoded content of -EncodedCommand is visible in the 4104 body — PowerShell decodes before logging"],causes:["Any PowerShell script execution when script block logging is enabled","Automatically generated by Windows when script content matches suspicious patterns (even without full logging)","PowerShell-based malware, fileless malware, or post-exploitation frameworks (Cobalt Strike, Metasploit)","Legitimate administrative scripts (high volume)"],steps:["Enable full script block logging: HKLM:\\SOFTWARE\\Policies\\Microsoft\\Windows\\PowerShell\\ScriptBlockLogging → EnableScriptBlockLogging = 1","Filter Event Viewer for 4104 with Level = Warning — Windows pre-flagged these as suspicious","Read the script block content in the event body — look for encoded strings, web downloads, and injection patterns","Cross-reference the User SID and timestamp with Event 4624 (logon) to establish who ran the script","If malicious content found: run a full EDR/AV scan immediately and investigate the account for compromise","Combine with 4103 events to reconstruct the full execution context"],symptoms:["what did powershell script do","powershell script content","malicious powershell script","powershell script block logging","investigate powershell script","powershell obfuscated command","encoded powershell command","powershell malware investigation","fileless malware powershell","powershell script captured","invoke-expression powershell audit","cobalt strike powershell"],tags:["powershell","security","logging","script-block","malware","forensics","advanced"],powershell:`# PowerShell script block logging — recent and suspicious entries
# Eventful
# Note: Requires script block logging enabled. Warning-level events appear automatically for suspicious content.

# Warning-level 4104 events (auto-flagged as suspicious by Windows)
Get-WinEvent -FilterHashtable @{
    LogName      = 'Microsoft-Windows-PowerShell/Operational'
    Id           = 4104
    Level        = 3   # 3 = Warning
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, UserId, Message |
    Sort-Object TimeCreated -Descending | Format-List

# All 4104 events (requires full script block logging enabled)
# Get-WinEvent -FilterHashtable @{ LogName = 'Microsoft-Windows-PowerShell/Operational'; Id = 4104; StartTime = (Get-Date).AddDays(-1) } -ErrorAction SilentlyContinue | Select-Object TimeCreated, UserId, Message | Sort-Object TimeCreated -Descending | Select-Object -First 20 | Format-List`,related_ids:[4103,4688,4624,4625],ms_docs:"https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_logging_windows"},{id:4634,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Fundamental",title:"Account Logoff",short_desc:"A user or service account logged off — session ended.",description:"Event 4634 is generated when a logon session ends. It is paired with Event 4624 (logon) to form a complete session record. For interactive sessions you can calculate session duration by matching 4634 to 4624 using the Logon ID. For network logons (Type 3), 4634 events are very frequent and generally not individually significant. Note: for interactive logons, Windows may instead log Event 4647 (user-initiated logoff) and omit 4634.",why_it_happens:"Windows generates this event when the Security Account Manager (SAM) or Kerberos package terminates a logon session — either because the user explicitly logged off, because the session timed out, or because the system is shutting down. High-volume environments see thousands of Type 3 logoff events per hour.",what_good_looks_like:"Each 4634 paired with a corresponding 4624 using matching Logon ID. Session durations reasonable for the account type and logon type.",common_mistakes:["Treating 4634 as meaningful for every Type 3 network logon — these are extremely noisy on file servers","Expecting 4634 for all interactive sessions — Windows often logs 4647 instead for user-initiated logoffs","Not matching by Logon ID when calculating session duration"],causes:["User clicked Start → Sign Out or switched user","Session timeout (screensaver lock + idle policy)","System shutdown or restart","Network session expired or connection dropped","Service or batch job logon session terminated"],steps:["Match to corresponding 4624 using Logon ID to determine session duration","Check Logon Type — Type 10 logoffs are RDP sessions ending","Filter by Account Name for a specific user's logoff history","Cross-reference with 4647 for interactive logoffs"],symptoms:["user logged off","when did user log off","session ended","logoff history","account sign out audit","user session duration"],tags:["authentication","logoff","audit","session","security"],powershell:`# Logon/Logoff Session History
# Eventful

$computer  = $env:COMPUTERNAME
$startTime = (Get-Date).AddDays(-1)
$targetUser = ''  # Optional: filter by username

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Security'
    Id        = @(4624, 4634, 4647)
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        EventId     = $_.Id
        Account     = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        LogonType   = ($data | Where-Object Name -eq 'LogonType').'#text'
        LogonId     = ($data | Where-Object Name -eq 'TargetLogonId').'#text'
    }
} | Where-Object { -not $targetUser -or $_.Account -eq $targetUser } |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4624,4647,4648],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4634"},{id:4647,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Fundamental",title:"User-Initiated Logoff",short_desc:"A user explicitly initiated a logoff from an interactive session.",description:"Event 4647 is generated when a user explicitly initiates logoff from an interactive session (Types 2, 10, 11). Unlike Event 4634 which covers all session terminations, 4647 specifically indicates a deliberate user action. For sensitive accounts, seeing a 4647 outside of business hours may indicate unauthorized interactive access. 4647 is often the last event logged before a workstation is locked or user profile is unloaded.",why_it_happens:'Generated by Windows when the user selects "Sign out", "Switch User", or initiates a logoff via keyboard shortcut. For RDP sessions, it is generated when the user closes the RDP client and disconnects from the session.',what_good_looks_like:"Logoffs for interactive sessions during expected hours. Admin accounts logging off from servers after completing work.",common_mistakes:["Confusing 4647 with 4634 — 4647 is user-initiated only; 4634 covers all session ends including timeouts and shutdowns"],causes:["User clicked Start → Sign Out","User disconnected an RDP session","Logoff initiated by IT via Group Policy or remote management","Automated logoff script ran at end of day"],steps:["Check Account Name and time — was this during expected hours?","Check Logon ID and correlate with 4624 to determine total session time","For RDP: cross-reference with 4778 (reconnect) and 24 (TS disconnect) events"],symptoms:["user logged off","who signed out","interactive logoff","user session ended","user sign out audit"],tags:["authentication","logoff","interactive","audit","security"],powershell:`# User-Initiated Logoff History
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4647
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        Account     = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        Domain      = ($data | Where-Object Name -eq 'TargetDomainName').'#text'
        LogonId     = ($data | Where-Object Name -eq 'TargetLogonId').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4624,4634,4778],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4647"},{id:4657,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Advanced",title:"Registry Value Modified",short_desc:"A registry value was created, modified, or deleted — requires object access auditing on the key.",description:'Event 4657 is generated when a registry value is created, modified, or deleted on a key that has been configured for auditing via SACL (System Access Control List). This event captures the old and new value data, the process that made the change, and the user account responsible. It is essential for tracking unauthorized configuration changes, malware persistence mechanisms (run keys), and software modifications. Requires "Audit Registry" subcategory enabled AND a SACL on the specific key.',why_it_happens:"Not generated for all registry writes — only for keys where a SACL audit entry has been configured. Security tools, Group Policy, and HIPS solutions often configure SACLs on sensitive keys like HKLM\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Run, HKLM\\SYSTEM, and HKLM\\SAM. Malware commonly modifies run keys for persistence, which this event captures.",what_good_looks_like:"Authorized software installs, Windows Updates, and GPO changes appearing in 4657. Unknown processes or user accounts modifying sensitive keys should be investigated.",common_mistakes:["Expecting 4657 for all registry writes — it only fires when the specific key has a SACL configured",'Not enabling "Audit Registry" under Advanced Audit Policy — 4657 will not generate without it','Missing the "Old Value" and "New Value" data in the event, which shows exactly what changed'],causes:["Software installation or uninstallation modifying registry","Group Policy applying registry settings","User or admin manually editing registry","Malware adding persistence via run keys or service keys","Antivirus or security software modifying configuration keys"],steps:['Enable "Audit Registry" policy: Advanced Audit Policy → Object Access → Audit Registry',"Add SACL to sensitive keys via regedit → key Properties → Security → Auditing","Filter Security log for Event 4657",'Check "Process Name" — unexpected processes (cmd.exe, powershell.exe, wscript.exe) modifying run keys are suspicious','Check "Old Value Data" vs "New Value Data" to see exactly what changed',"Cross-reference with Event 4688 (process created) for the process that made the change"],symptoms:["registry modified","who changed registry","registry key changed","registry audit","track registry changes","startup registry key modified","run key changed","malware persistence registry"],tags:["registry","audit","object-access","security","persistence","malware-detection"],powershell:`# Registry Modification Audit
# Eventful
# Note: Requires "Audit Registry" policy AND SACL on the target key

Get-WinEvent -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4657
    StartTime = (Get-Date).AddDays(-1)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated  = $_.TimeCreated
        SubjectUser  = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        ProcessName  = ($data | Where-Object Name -eq 'ProcessName').'#text'
        ObjectName   = ($data | Where-Object Name -eq 'ObjectName').'#text'
        ValueName    = ($data | Where-Object Name -eq 'ObjectValueName').'#text'
        OldValue     = ($data | Where-Object Name -eq 'OldValue').'#text'
        NewValue     = ($data | Where-Object Name -eq 'NewValue').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-List`,related_ids:[4688,4624,4663],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4657"},{id:4768,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Advanced",title:"Kerberos Authentication Ticket (TGT) Requested",short_desc:"A Kerberos TGT was requested from a domain controller — initial domain authentication.",description:"Event 4768 is generated on a domain controller every time a Kerberos Ticket-Granting Ticket (TGT) is requested. This is the first step of Kerberos authentication — before a user can access any resource, they must obtain a TGT from the DC. The event captures the account name, client IP address, encryption type used, and result code. Failed TGT requests (non-0x0 result codes) indicate authentication problems — 0x18 = bad password, 0x6 = account not found, 0x12 = account disabled.",why_it_happens:"Kerberos TGT requests are generated by domain-joined computers at login, by services at startup, and whenever a Kerberos ticket expires and must be renewed. Monitoring 4768 on DCs allows detection of password spray attacks (same account, many IPs), credential stuffing, and unusual off-hours authentication.",what_good_looks_like:"TGT requests from known workstation IPs for known accounts during business hours. Encryption type should be AES256 (0x12) for modern environments — RC4 (0x17) is legacy and may indicate older clients or pass-the-hash tooling.",common_mistakes:["Not filtering by result code — successful (0x0) TGTs are normal, failures (0x18, 0x6, 0x12) are what to watch","Ignoring the encryption type field — 0x17 (RC4) requests from modern clients can indicate pass-the-hash or downgrade attacks","Not correlating IP addresses with known workstation inventory"],causes:["User logging in to domain-joined computer","Service account authenticating at service start","Failed login attempt — bad password or disabled account","Password spray or brute force attack on domain accounts","Kerberos ticket renewal at natural expiry"],steps:["Filter for 4768 on domain controllers with non-zero result codes for failure analysis","Result code 0x18 = wrong password — repeated occurrences may indicate a spray attack","Result code 0x6 = account not found — check for typos or enumeration attempts","Result code 0x12 = disabled account being targeted","Check Encryption Type: 0x17 (RC4) from modern OS clients is suspicious","Correlate Client Address with workstation inventory for unknown source IPs"],symptoms:["Kerberos authentication failing","domain login failing","Kerberos TGT error","account not found Kerberos","Kerberos bad password","password spray attack","Kerberos ticket request failed","domain authentication error","RC4 encryption Kerberos"],tags:["kerberos","authentication","tgt","domain-controller","security","brute-force"],powershell:`# Kerberos TGT Request Analysis (run on DC)
# Eventful

$startTime = (Get-Date).AddHours(-4)

# Failed TGT requests (non-zero result codes)
Get-WinEvent -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4768
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated    = $_.TimeCreated
        Account        = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        ClientIP       = ($data | Where-Object Name -eq 'IpAddress').'#text'
        Status         = ($data | Where-Object Name -eq 'Status').'#text'
        EncryptionType = ($data | Where-Object Name -eq 'TicketEncryptionType').'#text'
    }
} | Where-Object { $_.Status -ne '0x0' } |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[4769,4771,4625,4624],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4768"},{id:4769,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Advanced",title:"Kerberos Service Ticket Requested",short_desc:"A Kerberos service ticket (TGS) was requested to access a specific resource.",description:"Event 4769 is generated on a domain controller when a client requests a Kerberos service ticket (TGS — Ticket-Granting Service ticket) to access a specific resource (file share, SQL server, web app, etc.). Unlike 4768 (TGT), this event identifies which specific service the user is trying to access. Failed 4769 requests indicate access problems, and suspicious patterns (Kerberoasting — requesting tickets for many service accounts with RC4 encryption) are detectable via this event.",why_it_happens:"Every time a Kerberos-authenticated user accesses a service, a TGS ticket is requested. This includes accessing file shares, logging into SQL, connecting to Exchange, and more. Kerberoasting attacks request TGS tickets for service accounts offline to crack their passwords — these show as many 4769 events with encryption type 0x17 (RC4) for service account SPNs.",what_good_looks_like:"TGS requests with AES encryption (0x12/0x11). Service tickets for expected SPNs from expected client IPs. No bulk requests for service account SPNs with RC4 encryption.",common_mistakes:["Not alerting on bulk 4769 events with EncryptionType 0x17 for service accounts — this is a Kerberoasting indicator","Ignoring the Service Name field — it tells you exactly what resource was being accessed","Not correlating failed 4769 with access denied errors on the target resource"],causes:["User accessing a network resource (normal operation)","Kerberoasting attack — attacker requesting service tickets to crack offline","Service account with weak password being targeted","Access denied to resource requiring specific SPN","SPN misconfiguration causing Kerberos fallback to NTLM"],steps:["Monitor for 4769 with EncryptionType 0x17 (RC4) for service account SPNs — Kerberoasting indicator","Check Service Name for unusual SPNs being requested","Bulk 4769 requests from a single IP in a short window is suspicious","Ensure service accounts use AES encryption: Set-ADUser <svc> -KerberosEncryptionType AES256","Use Group Managed Service Accounts (gMSAs) for services to eliminate password cracking risk"],symptoms:["Kerberoasting attack","Kerberos service ticket failed","kerberos access denied","service ticket request error","Kerberos TGS error","RC4 Kerberos service ticket","service account kerberos attack","kerberos service ticket audit"],tags:["kerberos","service-ticket","tgs","kerberoasting","domain-controller","security"],powershell:`# Kerberos Service Ticket Analysis (run on DC)
# Eventful
# Alert: bulk RC4 tickets for service accounts = Kerberoasting

$startTime = (Get-Date).AddHours(-1)

Get-WinEvent -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4769
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated    = $_.TimeCreated
        Account        = ($data | Where-Object Name -eq 'TargetUserName').'#text'
        ServiceName    = ($data | Where-Object Name -eq 'ServiceName').'#text'
        ClientIP       = ($data | Where-Object Name -eq 'IpAddress').'#text'
        EncryptionType = ($data | Where-Object Name -eq 'TicketEncryptionType').'#text'
        Status         = ($data | Where-Object Name -eq 'Status').'#text'
    }
} | Where-Object { $_.EncryptionType -eq '0x17' -and $_.ServiceName -notlike '*$' } |
    Group-Object ServiceName | Sort-Object Count -Descending | Format-Table -AutoSize`,related_ids:[4768,4771,4624],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4769"},{id:4697,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Intermediate",title:"Service Installed on System",short_desc:"A new Windows service was installed — captures service name, binary path, and installing account.",description:"Event 4697 is generated when a new service is installed via the Service Control Manager. It captures the service name, the binary path (ImagePath), the service type, start type, and the account under which it runs. This event is critical for security monitoring — malware frequently installs services for persistence, and attackers use service creation as a lateral movement and persistence technique.",why_it_happens:"Any process with SeLoadDriverPrivilege or equivalent can install a service. Normal sources include software installers, Windows Updates, and administrative scripts. Malicious sources include malware dropping a service binary, lateral movement tools (e.g., PsExec, Cobalt Strike), and ransomware establishing persistence.",what_good_looks_like:"Services installed from expected paths (C:\\Program Files, C:\\Windows) by expected accounts (SYSTEM, admin accounts) during software deployment windows. No services with random names, paths in temp directories, or UNC paths.",common_mistakes:["Not alerting on services with binary paths in user-writeable locations (Temp, AppData, Downloads)","Not checking the ImagePath for encoded commands or suspicious parameters","Missing services installed from network paths (UNC) — these are common in lateral movement"],causes:["Software installer registering a new service","Windows Update installing a component","IT admin deploying a service via script","Malware or attack tool installing a persistence service","Remote management tool (PsExec, SC.exe) installing a service for lateral movement"],steps:["Filter Security log for Event 4697",'Check "Service File Name" (ImagePath) — paths in Temp, AppData, or UNC are suspicious','Check "Subject" — who installed the service? Unexpected user accounts are a red flag','Verify the service binary is signed: Get-AuthenticodeSignature "<path>"','Check when the binary was dropped: (Get-Item "<path>").CreationTime',"Cross-reference with Event 4688 (process create) for the process that installed the service"],symptoms:["new service installed","unknown service appeared","malware service installed","service installed audit","suspicious service created","service persistence","who installed this service","lateral movement service"],tags:["service","persistence","malware-detection","security","audit","lateral-movement"],powershell:`# Service Installation Audit
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName   = 'Security'
    Id        = 4697
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated  = $_.TimeCreated
        InstalledBy  = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        ServiceName  = ($data | Where-Object Name -eq 'ServiceName').'#text'
        ImagePath    = ($data | Where-Object Name -eq 'ServiceFileName').'#text'
        StartType    = ($data | Where-Object Name -eq 'ServiceStartType').'#text'
        Account      = ($data | Where-Object Name -eq 'ServiceAccount').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[7045,4688,4624],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4697"},{id:5157,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Advanced",title:"Windows Firewall Blocked a Connection",short_desc:"The Windows Filtering Platform blocked a network connection — captures process, port, direction, and IP.",description:"Event 5157 is generated when the Windows Filtering Platform (the kernel-level firewall engine) blocks a connection attempt. It captures the process that initiated the connection, source and destination IP/port, and the protocol. Unlike firewall rule events (4946/4948), this event is about actual traffic being blocked in real-time. When enabled, this is the key event for detecting outbound connection attempts by malware, lateral movement, or unauthorized services trying to communicate.",why_it_happens:'Generated under "Audit Filtering Platform Connection" policy for every dropped packet that matches the WFP filters. This creates very high volume on busy systems — typically only enabled for specific investigations or critical servers. Malware attempting C2 connections, unauthorized port scanners, and services misconfigured to use blocked ports all generate 5157 events.',what_good_looks_like:"Known applications blocked on known ports (e.g., browsers blocked from unusual ports). No unusual processes (svchost.exe using non-standard ports, cmd.exe initiating outbound connections).",common_mistakes:["Enabling this policy on high-traffic servers without log size limits — can generate millions of events per day","Not filtering by Process Name when investigating — the process is the most important field","Confusing 5157 (blocked) with 5156 (allowed) — both can be informative but serve different purposes"],causes:["Windows Firewall rule blocking outbound/inbound connection","Malware attempting command-and-control connection on blocked port","Service misconfigured to use a blocked port","Application attempting to bind to a privileged port without permission","Lateral movement tool attempting to connect to blocked internal ports"],steps:['Enable Audit Filtering Platform Connection: auditpol /set /subcategory:"Filtering Platform Connection" /success:enable /failure:enable',"Filter Security log for 5157",'Focus on "Application Name" (process path) — cmd.exe, powershell.exe, or unknown processes making outbound connections',"Note Destination Address and Port — C2 traffic often goes to unusual ports or external IPs","Correlate with 4688 (process create) to see what spawned the process"],symptoms:["firewall blocking connection","application cannot connect","connection blocked by firewall","outbound connection blocked","malware C2 connection blocked","Windows firewall dropped connection","service cannot reach server","firewall audit blocked traffic"],tags:["firewall","wfp","network","blocked","security","audit","malware-detection"],powershell:`# Windows Firewall Blocked Connection Analysis
# Eventful
# Note: Requires "Audit Filtering Platform Connection" → Failure enabled

$startTime = (Get-Date).AddHours(-1)

Get-WinEvent -FilterHashtable @{
    LogName   = 'Security'
    Id        = 5157
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated  = $_.TimeCreated
        ProcessName  = ($data | Where-Object Name -eq 'Application').'#text'
        SrcPort      = ($data | Where-Object Name -eq 'SourcePort').'#text'
        DstAddress   = ($data | Where-Object Name -eq 'DestAddress').'#text'
        DstPort      = ($data | Where-Object Name -eq 'DestPort').'#text'
        Protocol     = ($data | Where-Object Name -eq 'Protocol').'#text'
    }
} | Group-Object DstAddress | Sort-Object Count -Descending |
    Select-Object -First 20 | Format-Table -AutoSize`,related_ids:[5156,4946,4948,4688],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-5157"},{id:5156,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Advanced",title:"Windows Firewall Allowed a Connection",short_desc:"The Windows Filtering Platform permitted a network connection — process, IP, and port captured.",description:"Event 5156 is the allowed-connection counterpart to 5157. It is generated when Windows Filtering Platform permits a connection. Enabled for threat hunting to identify what processes are making network connections. Very high volume on servers — typically only enabled during specific investigations. Useful for detecting unexpected processes (cmd.exe, PowerShell) making network connections, even when those connections succeed.",why_it_happens:'Generated under "Audit Filtering Platform Connection" policy (Success enabled). Every permitted network connection generates this event. On a busy server this can be hundreds of thousands per hour.',what_good_looks_like:"Expected application processes connecting to known IPs and ports. No shells, scripting engines, or system utilities making unusual outbound connections.",common_mistakes:["Enabling on production servers without capping the log size — the volume can cause other events to be overwritten","Not filtering by Process Name — looking at raw 5156 events without filtering is overwhelming"],causes:["Normal application network activity","Malware successfully connecting to C2 (permitted by firewall)","Unauthorized lateral movement that the firewall allows","Data exfiltration on allowed ports (80, 443)"],steps:['Enable: auditpol /set /subcategory:"Filtering Platform Connection" /success:enable',"Filter by suspicious process names: cmd.exe, powershell.exe, wscript.exe, mshta.exe","Look for connections from unexpected processes to external IPs on ports 80/443","Correlate process with 4688 (process create) to see the full launch chain"],symptoms:["what is connecting to internet","unknown process making network connection","application network activity audit","firewall allowed connection audit","detect malware C2 traffic","process making outbound connection","network connection logging"],tags:["firewall","wfp","network","allowed","security","audit","threat-hunting"],powershell:`# Windows Firewall Allowed Connection Analysis
# Eventful
# Note: High volume — filter by process name

Get-WinEvent -FilterHashtable @{
    LogName   = 'Security'
    Id        = 5156
    StartTime = (Get-Date).AddMinutes(-30)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        ProcessName = ($data | Where-Object Name -eq 'Application').'#text'
        DstAddress  = ($data | Where-Object Name -eq 'DestAddress').'#text'
        DstPort     = ($data | Where-Object Name -eq 'DestPort').'#text'
    }
} | Where-Object { $_.ProcessName -match 'cmd|powershell|wscript|mshta|rundll32' } |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[5157,4946,4688],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-5156"},{id:6273,source:"Microsoft-Windows-Security-Auditing",channel:"Security",severity:"Info",skill_level:"Intermediate",title:"Network Policy Server Denied Access",short_desc:"NPS (RADIUS) denied a VPN or 802.1X authentication request — includes reason code.",description:"Event 6273 from Network Policy Server (NPS/RADIUS) is generated when NPS denies a connection request from a VPN, 802.1X wireless, or 802.1X wired client. The event includes the username, client IP (NAS IP), reason code, and network policy name. Common reason codes: 16 = account locked, 23 = user not permitted for dial-in, 65 = certificate not trusted, 66 = client certificate expired. This is the primary event for diagnosing VPN and wireless authentication failures.",why_it_happens:"NPS evaluates each authentication request against configured network policies. If the policies reject the request (wrong credentials, unauthorized account, policy condition not met, certificate issue), 6273 is generated. Common scenarios: user account does not have VPN access granted in AD, certificate expired, or NPS connection request policy is misconfigured.",what_good_looks_like:"All legitimate VPN and 802.1X users receive event 6272 (access granted) rather than 6273. Rare 6273 events from expired credentials or misconfigured clients.",common_mistakes:["Not reading the Reason-Text field — it contains a plain-English explanation of why access was denied",'Forgetting that user accounts need the "Allow" dial-in permission in AD or NPS must override via policy',"Not checking the network policy name — it tells you which policy matched and rejected the request","Ignoring the NAS-IP-Address — this identifies which VPN gateway or access point is denying access"],causes:["User account does not have VPN/dial-in permission in AD","User password expired or account disabled","Network policy condition not met (group membership, time-of-day restriction)","Client certificate expired or not trusted by NPS (802.1X/PEAP)","Wrong password or certificate presented","NPS network policy misconfigured — wrong conditions"],steps:["Open Event Viewer on NPS server → Security log, filter for Event 6273",'Read "Reason-Text" field for a plain-English explanation','Check "Fully Qualified Account Name" — verify the account exists and is not locked','Check AD dial-in permission: ADUC → user Properties → Dial-in tab → "Allow access" or "Control access through NPS Network Policy"',"Check NPS network policy for conditions that may be blocking the user","For certificate errors: verify the client certificate is valid and the NPS server trusts the issuing CA"],symptoms:["VPN access denied","VPN login failing","user cannot connect to VPN","802.1x authentication failed","wireless authentication denied","NPS denied access","RADIUS authentication failed","remote access denied","network policy denied","VPN certificate error"],tags:["vpn","nps","radius","802.1x","wireless","authentication","security"],powershell:`# NPS Authentication Failure Analysis
# Eventful

$startTime = (Get-Date).AddHours(-24)

# NPS denials (6273) and successes (6272) — run on NPS server
Get-WinEvent -FilterHashtable @{
    LogName   = 'Security'
    Id        = @(6272, 6273)
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated  = $_.TimeCreated
        EventId      = $_.Id
        Result       = if ($_.Id -eq 6272) { 'GRANTED' } else { 'DENIED' }
        Account      = ($data | Where-Object Name -eq 'SubjectUserName').'#text'
        NasIP        = ($data | Where-Object Name -eq 'CallingStationID').'#text'
        ReasonCode   = ($data | Where-Object Name -eq 'ReasonCode').'#text'
        ReasonText   = ($data | Where-Object Name -eq 'Reason').'#text'
        PolicyName   = ($data | Where-Object Name -eq 'NetworkPolicyName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-List`,related_ids:[6272,4625,4768],ms_docs:"https://learn.microsoft.com/en-us/windows-server/networking/technologies/nps/nps-top"}],i=[{id:41,source:"Microsoft-Windows-Kernel-Power",channel:"System",severity:"Critical",skill_level:"Intermediate",title:"Kernel Power: Unexpected Reboot",short_desc:"The system rebooted without a clean shutdown — power loss, crash, or hard reset.",description:"Event ID 41 from the Kernel-Power source is generated on the NEXT boot after an unexpected shutdown. It indicates the system did not go through a normal shutdown sequence — the most likely causes are a power cut, someone hitting the power button, a kernel panic (BSOD), or overheating causing emergency shutdown. The BugcheckCode field is critical: if it's 0, there was no BSOD (power loss or hard reset). A non-zero BugcheckCode means there was a crash — look at 1001 for the full minidump analysis.",why_it_happens:'Windows maintains a "clean shutdown flag" in the registry (HKLM\\SYSTEM\\CurrentControlSet\\Control\\Session Manager\\Memory Management\\PagingFiles and the boot-status driver). If the system loses power, crashes, or is hard-reset before the OS can clear this flag during a normal shutdown, the next boot detects the unexpected condition and logs Event 41. BugcheckCode 0 = no crash occurred (hardware power event). Non-zero = the kernel itself crashed.',what_good_looks_like:"Occasional Event 41 after a power outage is normal. Investigate: repeated Event 41 without an obvious power explanation, BugcheckCode that keeps repeating (same hardware fault), Event 41 with a non-zero BugcheckCode (actual kernel crash), clusters of Event 41 at similar times of day (thermal throttling at peak CPU load).",common_mistakes:["Ignoring the BugcheckCode field — 0 means no crash, non-zero means BSOD and you need minidump analysis","Not checking Event 6008 in the same log — it provides the timestamp of the unexpected shutdown","Not checking the system for hardware issues: check SMART on disk, RAM with memtest86, PSU voltage, temperatures","Assuming the OS caused the crash without ruling out hardware first — Event 41 with code 0 is almost always hardware or power"],causes:["Power outage or UPS failure","Manual hard reset (power button hold)","Kernel panic (BSOD) — check BugcheckCode","Overheating causing emergency shutdown","Faulty RAM causing memory corruption","Failing power supply delivering inconsistent voltage","Hardware driver crash"],steps:["Find Event 41 and note BugcheckCode — if 0, no BSOD occurred","Check Event 6008 nearby to confirm unexpected shutdown timestamp","If BugcheckCode non-zero: check Event 1001 for full minidump details","Check Windows Reliability Monitor for pattern over time (search: perfmon /rel)","Check system temperatures: Get-WmiObject MSAcpi_ThermalZoneTemperature -Namespace root/wmi","Run hardware diagnostics: memory test, disk SMART, PSU check","If BugcheckCode 0x9F: driver power state failure — check device manager for driver issues","If recurring: consider UPS installation or hardware replacement"],symptoms:["computer randomly reboots","pc turns off by itself","unexpected reboot","computer keeps restarting","blue screen then restart","machine shut off randomly","server restarted overnight","sudden shutdown"],tags:["reboot","crash","kernel-power","bsod","hardware","uptime","critical"],powershell:`# Unexpected Reboot Investigation (Kernel Power 41)
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)   # Look back further for reboot patterns

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Kernel-Power'
    Id           = 41
    StartTime    = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated    = $_.TimeCreated
        BugcheckCode   = ($data | Where-Object Name -eq 'BugcheckCode').'#text'
        PowerButtonOn  = ($data | Where-Object Name -eq 'PowerButtonTimestamp').'#text'
        SleepInProgress= ($data | Where-Object Name -eq 'SleepInProgress').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[1001,6008,1074,6006],ms_docs:"https://learn.microsoft.com/en-us/troubleshoot/windows-client/performance/windows-kernel-event-id-41-error"},{id:55,source:"Ntfs",channel:"System",severity:"Error",skill_level:"Intermediate",title:"NTFS File System Corruption",short_desc:"The NTFS driver detected corruption on a volume — could lead to data loss.",description:"Event ID 55 from the Ntfs source indicates that the NTFS file system driver detected on-disk corruption. This can be a corrupt Master File Table (MFT), invalid file records, damaged metadata structures, or bad sectors affecting file system data. This event is serious — it can lead to data loss, application failures, and in severe cases an unbootable system. Corruption can appear without obvious symptoms until a file is accessed.",why_it_happens:"NTFS corruption occurs when writes are interrupted mid-operation (power loss during write), when the storage medium has bad sectors, when the disk controller reports errors that corrupt in-flight data, when storage drivers have bugs, or when RAM errors cause corrupt data to be written to disk. NTFS is transactional and has a journal ($LogFile), but this only protects against metadata corruption from clean failures — bad sectors and RAM corruption can still cause Event 55.",what_good_looks_like:"No Event 55 at all is ideal. Any Event 55 should be investigated. Repeated Event 55 on the same volume is urgent — the drive may be failing. Correlate with disk errors in the Disk event log (Event 7, 11, 15, 51) to see if bad sectors are triggering the corruption.",common_mistakes:["Running chkdsk on a running system rather than scheduling it for next boot — chkdsk /f cannot fully repair a mounted volume","Only running chkdsk and not investigating the underlying cause (failing disk, PSU issues, RAM)",'Ignoring Event 55 because the user "seems fine" — corruption can be silently expanding',"Not checking the disk SMART data alongside Event 55"],causes:["Power loss during a write operation","Failing hard disk with bad sectors","Faulty storage controller or cable","RAM errors causing corrupt data to be written","USB drive safely removed while mounted with pending writes","Virtual machine storage issues (snapshot corruption, thin provisioning exhaustion)","RAID array degradation affecting write integrity"],steps:["Note which volume is affected from the event message","Schedule chkdsk: chkdsk C: /f /r /b (requires reboot for system volume)","Check disk SMART health: Get-Disk | Get-StorageReliabilityCounter | Select-Object *","Check for disk errors: Get-WinEvent -FilterHashtable @{LogName='System'; ProviderName='disk'} | Select -First 20","Check Event 7, 11, 51 in System log for I/O errors on the same disk","If a VM: check datastore health, free space, snapshot state","If recurring: consider disk replacement and restore from backup","Verify backup is working before running chkdsk /r"],symptoms:["file system corruption","ntfs corruption","disk corruption","chkdsk finding errors","files corrupted","cant open files","disk errors","file system errors"],tags:["ntfs","corruption","disk","filesystem","data-integrity","chkdsk"],powershell:`# NTFS Corruption Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)

# Find NTFS corruption events
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Ntfs'
    Id           = 55
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List

# Also check for disk I/O errors
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'disk'
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Select-Object -First 10 | Format-List`,related_ids:[41,1001,7023],ms_docs:"https://learn.microsoft.com/en-us/windows-server/storage/file-server/troubleshoot/troubleshoot-nfs-file-server"},{id:1001,source:"Microsoft-Windows-WER-SystemErrorReporting",channel:"System",severity:"Critical",skill_level:"Intermediate",title:"Windows Error Reporting: BugCheck (BSOD)",short_desc:"A kernel-mode crash (Blue Screen of Death) was recorded with stop code and parameters.",description:"Event ID 1001 from WER-SystemErrorReporting records the details of a kernel crash — the stop code (BugcheckCode), parameters, and a reference to the minidump file. This event is generated on the next boot after a BSOD. The BugcheckCode is the most important field: common codes include 0x0000007E (driver error), 0x00000050 (PAGE_FAULT_IN_NONPAGED_AREA, often bad RAM or driver), 0x0000009F (DRIVER_POWER_STATE_FAILURE), 0x00000124 (WHEA_UNCORRECTABLE_ERROR, hardware fault).",why_it_happens:"When the Windows kernel encounters an unrecoverable error — typically a driver accessing invalid memory, a hardware fault, or kernel data structure corruption — it halts execution and writes a memory dump. The dump file (usually C:\\Windows\\Minidump\\*.dmp) contains the full call stack at the time of the crash. Event 1001 is the event log representation of this crash, generated after the system reboots.",what_good_looks_like:"A system that never BSODs. If it does crash once, investigate the BugcheckCode. Repeated BSODs with the same code strongly suggest a specific hardware fault or driver bug. Multiple different codes on the same machine suggest bad RAM (random memory corruption causes many different stop codes).",common_mistakes:["Reading the bugcheck code without looking up what it means — each code has specific causes","Not analysing the minidump file — the code alone isn't enough; you need WinDbg or WhoCrashed to identify the responsible driver","Blaming Windows when the driver shown in WinDbg analysis is a third-party driver (AV, NIC, storage)","Running driver verifier without understanding it can cause additional crashes (use in isolated test environment)","Not checking RAM with memtest86 for at least 8 passes when stop code varies"],causes:["Faulty or incompatible device driver (most common)","RAM hardware fault","CPU overheating or overclocking instability","Power supply delivering inconsistent voltage","Failing or corrupt SSD/HDD","Kernel-mode malware or rootkit","Windows system file corruption","Hardware fault (GPU, NIC, storage controller)"],steps:["Find BugcheckCode in Event 1001 and look it up: https://learn.microsoft.com/en-us/windows-hardware/drivers/debugger/bug-check-code-reference2","Open minidump: C:\\Windows\\Minidump\\ — open with WinDbg or use WhoCrashed (free tool)","WinDbg: !analyze -v will identify the crashing driver","Update or roll back the identified driver first","If code 0x124 (WHEA): run hardware diagnostics — this is a hardware fault","If variable codes: run memtest86 overnight — likely bad RAM","Check system temperatures and clean dust from fans/heatsinks","If driver-related: check Windows Update and vendor site for driver updates"],symptoms:["blue screen","bsod","blue screen of death","stop error","computer crashes with blue screen","windows crash","pc restarts with blue screen","kernel crash","stop code","memory dump"],tags:["bsod","crash","kernel","minidump","driver","hardware","critical"],powershell:`# BSOD / BugCheck Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-30)  # Look back 30 days for crash patterns

# Find all BSOD records
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-WER-SystemErrorReporting'
    Id           = 1001
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List

# List minidump files
Write-Host "
--- Minidump Files ---" -ForegroundColor Cyan
Get-ChildItem -Path "\\\\$computer\\c$\\Windows\\Minidump\\" -Filter "*.dmp" -ErrorAction SilentlyContinue |
    Sort-Object LastWriteTime -Descending | Select-Object Name, LastWriteTime, Length | Format-Table -AutoSize`,related_ids:[41,6008,1074],ms_docs:"https://learn.microsoft.com/en-us/windows-hardware/drivers/debugger/bug-check-code-reference2"},{id:1074,source:"USER32",channel:"System",severity:"Info",skill_level:"Fundamental",title:"System Shutdown or Restart Initiated by Process",short_desc:"A process or user initiated a shutdown or restart, with reason and initiating process recorded.",description:"Event ID 1074 records when a shutdown or restart is initiated programmatically or via the Windows shutdown dialog. It captures who initiated the shutdown, what process called it, the reason code, and whether it was a shutdown or restart. This is the primary event for determining why a machine was shut down or restarted intentionally. It contrasts with Event 6008 (unexpected shutdown) — if you see 6008 without a preceding 1074, the shutdown was unplanned.",why_it_happens:'Windows generates 1074 whenever ExitWindowsEx() or InitiateSystemShutdown() is called with the shutdown or restart flag. This happens when a user clicks "Restart" or "Shut down", when Windows Update installs patches and requires a reboot, when an administrator runs "shutdown /r", or when management software (RMM, SCCM) triggers a reboot. The Reason Code provides structured information about why the restart occurred (e.g., 0x0 = other, 0x80020003 = OS/reconfiguration/planned).',what_good_looks_like:"Expected: 1074 events from Windows Update (reason code OS: Planned), from users shutting down at end of day, from RMM tools patching. Investigate: 1074 events at unexpected times, restarts initiated by unfamiliar processes, restarts on servers without a corresponding change ticket.",common_mistakes:["Not checking 1074 before concluding a reboot was unexpected — always look for 1074 first",'Ignoring the "Process" field — knowing whether it was shutdown.exe, wusa.exe, or an unknown binary matters',"Not checking for 1074 on servers when investigating unplanned downtime"],causes:["User-initiated shutdown or restart","Windows Update requiring reboot after patch installation","RMM tool triggering a managed reboot","Administrator running shutdown command","Application requesting system restart (installer)","Group Policy forcing restart after changes"],steps:["Filter System log for Event 1074",'Check "Process Name" — was it Windows Update, shutdown.exe, or an RMM tool?','Check "Reason" — does it match expected maintenance?','Check "User" — who was logged on when the restart was triggered?',"If on a server: correlate with change records","If Process Name is unexpected: investigate with Event 4688 to trace the process"],symptoms:["computer was restarted","who restarted this machine","server rebooted","why did it restart","shutdown reason","machine shut down","reboot reason","windows update reboot"],tags:["shutdown","restart","reboot","maintenance","audit","uptime"],powershell:`# Shutdown/Restart Reason Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-30)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'USER32'
    Id           = 1074
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List`,related_ids:[41,6006,6008,1076],ms_docs:"https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-server-2008-R2-and-2008/cc940437(v=ws.10)"},{id:1076,source:"USER32",channel:"System",severity:"Info",skill_level:"Intermediate",title:"Unexpected Shutdown Reason Recorded",short_desc:"After an unexpected shutdown (6008), a user provided the reason via the shutdown tracker.",description:"Event ID 1076 is generated when the Windows Shutdown Event Tracker is enabled and a user provides a reason for the previous unexpected shutdown. This event appears after a 6008 (unexpected shutdown was detected) and a user logs in and fills in the reason dialog. Common in server environments where administrators are required to document the cause of unexpected outages. The event captures the user who provided the reason and the reason text they entered.",why_it_happens:"Windows Server has the Shutdown Event Tracker enabled by default. When the system detects that the previous shutdown was unexpected (via 6008), it prompts the next user to log in to provide a reason. This is a governance control to ensure unexpected downtime is documented. The feature can be configured via Group Policy.",what_good_looks_like:"Expected: admins documenting reasons for unexpected server reboots. Investigate: the reason entered does not match known events, the same admin keeps logging reasons suggesting systematic unplanned outages, reasons indicating hardware problems that should be addressed.",common_mistakes:["Relying on 1076 alone for root cause — the reason entered is user-supplied and may be generic or incorrect","Not having the Shutdown Event Tracker enabled on servers — it helps with accountability"],causes:["Administrator documenting the previous unexpected shutdown","Automatic population by management software","Policy requiring documentation of server downtime"],steps:["Correlate 1076 with the preceding 6008 event","Read the reason text — does it provide useful diagnosis information?","Cross-reference with Event 41 or 1001 to find technical root cause","If a pattern of unexpected shutdowns: escalate to hardware investigation"],symptoms:["unexpected shutdown explanation","shutdown reason recorded","shutdown tracker","documented reboot reason","server restart explanation"],tags:["shutdown","restart","accountability","audit","server"],powershell:`# Unexpected Shutdown Reason Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-30)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'USER32'
    Id           = 1076
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Format-List`,related_ids:[6008,41,1074,6006],ms_docs:null},{id:6005,source:"EventLog",channel:"System",severity:"Info",skill_level:"Fundamental",title:"Event Log Service Started (System Boot)",short_desc:"The Windows Event Log service started — marks the beginning of a new boot.",description:"Event ID 6005 is generated by the Event Log service itself when it starts during system boot. It reliably marks the beginning of a new Windows session in the event log. When combined with 6006 (event log stopped, clean shutdown) and 6008 (unexpected shutdown), you can build a complete timeline of system uptime and shutdown history. It is one of the simplest but most useful events for determining when a machine was last booted.",why_it_happens:"The Event Log service is one of the earliest services to start during Windows boot. When it initialises and opens the event log files, it writes Event 6005 to record its own startup. This event has been present since Windows NT.",what_good_looks_like:"Every 6005 should be paired with either a preceding 6006 (clean shutdown) or a preceding 6008 (unexpected shutdown). Missing 6006 before a 6005 means the previous session ended unexpectedly — this is your signal to look for 41, 1001, or other crash indicators.",common_mistakes:["Not checking what precedes 6005 — without a 6006, the previous boot ended badly","Confusing 6005 with 6006 — 6005 = start (boot), 6006 = stop (shutdown)"],causes:["Normal system boot after clean shutdown","System boot after power restoration","System boot after BSOD or hard reset"],steps:["Look for 6005 events to find all boot times","Check what precedes each 6005 — is there a 6006 (clean shutdown) or 6008 (unexpected)?","If no 6006 before 6005: previous session ended unexpectedly, look for 41 and 1001","Calculate uptime between 6005 pairs for availability reporting"],symptoms:["when did this computer last start","boot time","system started","when was it turned on","last reboot time","computer started up"],tags:["boot","startup","uptime","availability","fundamental"],powershell:`# System Boot History Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-30)  # Adjust time range as needed

# Get boot and shutdown timeline
$boots = Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'EventLog'
    Id           = @(6005, 6006, 6008)
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id,
        @{N='Event'; E={
            switch ($_.Id) {
                6005 { 'BOOT (Event Log Started)' }
                6006 { 'SHUTDOWN (Clean)' }
                6008 { 'SHUTDOWN (Unexpected)' }
            }
        }} | Sort-Object TimeCreated

$boots | Format-Table -AutoSize`,related_ids:[6006,6008,41,1074],ms_docs:null},{id:6006,source:"EventLog",channel:"System",severity:"Info",skill_level:"Fundamental",title:"Event Log Service Stopped (Clean Shutdown)",short_desc:"The Event Log service stopped — marks the end of a clean shutdown sequence.",description:"Event ID 6006 is generated when the Windows Event Log service stops as part of a clean shutdown. It is the last event written before the system shuts down. If you are investigating why a machine rebooted and you see a 6006 followed by a 6005 (next boot), the shutdown was intentional and clean. If 6005 appears without a preceding 6006, the previous session ended unexpectedly.",why_it_happens:"During a clean shutdown, the Event Log service is one of the last services to stop. Before stopping, it writes 6006 to record its own clean termination. This provides a reliable audit trail of clean shutdowns.",what_good_looks_like:"Every 6006 should be followed by a 6005. If you are investigating an unexpected reboot or crash, look for the absence of 6006 before a 6005.",common_mistakes:["Confusing 6006 with 6005 — 6006 = clean stop, 6005 = start","Not checking for 6006 when investigating whether a shutdown was clean or unexpected"],causes:["User-initiated shutdown","Windows Update restart","Administrator shutdown","RMM-triggered reboot"],steps:["Look for 6006 events to confirm clean shutdowns","If missing 6006 before a 6005: unexpected shutdown — investigate Event 41, 6008","Pair with 1074 to find who initiated the clean shutdown"],symptoms:["clean shutdown","planned shutdown","machine was shut down cleanly","system shutdown normally"],tags:["shutdown","clean-shutdown","uptime","audit"],powershell:`# Clean Shutdown Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-30)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'EventLog'
    Id           = 6006
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List`,related_ids:[6005,6008,1074,41],ms_docs:null},{id:6008,source:"EventLog",channel:"System",severity:"Error",skill_level:"Fundamental",title:"Unexpected Shutdown Recorded",short_desc:"The previous system shutdown was unexpected — generated on the next boot.",description:"Event ID 6008 is generated at boot time when Windows detects that the previous shutdown was not clean. The event message includes the date and time the unexpected shutdown occurred (the time the system last had a clean record). This event is your first indicator of an unplanned outage — the starting point for investigating BSODs, power losses, and hard resets. It does not tell you why the shutdown happened — for that, look at Event 41 and Event 1001.",why_it_happens:"Windows maintains a boot-status file that is updated throughout normal operation and properly closed during a clean shutdown. If the system loses power, crashes, or is hard-reset, this file is not properly closed. On the next boot, Windows checks the file, detects the improper closure, and logs Event 6008 to record the unexpected termination.",what_good_looks_like:"No 6008 events on a healthy, stable machine. A 6008 followed by investigation and resolution is acceptable. Repeated 6008 events — especially on servers — indicate an ongoing problem that must be resolved.",common_mistakes:["Reading 6008 and not following up with Event 41 and Event 1001","Assuming 6008 always means BSOD — it also occurs for power loss and hard reset","Not correlating 6008 timestamp with UPS or power monitoring logs"],causes:["Power failure (no UPS)","Hard reset (power button)","Kernel crash (BSOD)","Hypervisor force-shutdown of VM","Hardware failure causing abrupt halt"],steps:["Find Event 6008 and note the timestamp of the unexpected shutdown","Look for Event 41 (Kernel Power) near the same time","Look for Event 1001 (BugCheck) — if present, a BSOD occurred","If no 41 or 1001: likely hardware power event (check UPS logs)","Check Reliability Monitor for a visual timeline: perfmon /rel"],symptoms:["unexpected shutdown","computer shut off unexpectedly","unclean shutdown","power outage reboot","machine crashed","previous session ended unexpectedly","system did not shut down properly"],tags:["shutdown","unexpected","crash","power","uptime","fundamental"],powershell:`# Unexpected Shutdown Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-30)  # Adjust time range as needed

# Get unexpected shutdown events
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'EventLog'
    Id           = 6008
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List`,related_ids:[41,1001,6005,6006,1074],ms_docs:null},{id:6013,source:"EventLog",channel:"System",severity:"Info",skill_level:"Fundamental",title:"System Uptime",short_desc:"Daily event recording total system uptime in seconds — useful for availability reporting.",description:"Event ID 6013 is generated once per day (usually around midnight or on event log startup) and records the current system uptime in seconds. While simple, it provides a built-in uptime audit trail. If you want to know how long a machine has been up without running a command, you can look for the most recent 6013 event and calculate from its timestamp. It is also useful for confirming SLAs — a server with 6013 showing many days of uptime has been stable.",why_it_happens:"The Event Log service generates 6013 once every 24 hours as a housekeeping record. The uptime value in seconds comes from the system kernel's KeQueryTimeIncrement counter, which starts at boot.",what_good_looks_like:"Any 6013 is normal. Use the uptime value to calculate last boot time: (Get-Date).AddSeconds(-[int]$uptimeSeconds). Uptime longer than your patching cycle means the machine has not been rebooted for updates.",common_mistakes:["Not realising 6013 fires at log startup too, not just midnight — so a reboot generates a 6013 with low uptime","Using 6013 instead of checking LastBootUpTime directly — Get-CimInstance Win32_OperatingSystem is more reliable"],causes:["Automatic daily system heartbeat from Event Log service"],steps:["Find the most recent 6013 event","Note the uptime in seconds from the message","Calculate last boot: (Get-Date).AddSeconds(-<uptime_seconds>)","For real-time uptime: (Get-CimInstance Win32_OperatingSystem).LastBootUpTime"],symptoms:["check system uptime","how long has this been on","when was last reboot","system runtime","availability check"],tags:["uptime","availability","sla","baseline","monitoring"],powershell:`# System Uptime Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed

# Direct uptime query (most accurate)
$os = Get-CimInstance -ComputerName $computer -ClassName Win32_OperatingSystem
Write-Host "Last Boot Time: $($os.LastBootUpTime)"
Write-Host "Uptime: $((Get-Date) - $os.LastBootUpTime)"

# Also check recent 6013 events for historical uptime
$startTime = (Get-Date).AddDays(-7)
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'EventLog'
    Id           = 6013
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Format-List`,related_ids:[6005,6006,6008],ms_docs:null},{id:7e3,source:"Service Control Manager",channel:"System",severity:"Error",skill_level:"Fundamental",title:"Service Failed to Start",short_desc:"The Service Control Manager could not start a service at boot or on demand.",description:'Event ID 7000 is generated by the Service Control Manager (SCM) when a service fails to start. The event includes the service name and an error code that identifies why it failed. This is the primary event for service startup failures. Common error codes: 2 = "The system cannot find the file" (service binary missing), 5 = "Access is denied" (service account lacks permissions), 1053 = "The service did not respond to the start or control request" (service timeout).',why_it_happens:"The SCM attempts to start services during boot or when a dependent service requires them. Startup failures can occur because the service binary was deleted or moved, the service account credentials are wrong, a required dependency service failed first (see 7001), the service binary threw an exception immediately on start, or the service account was locked out or disabled.",what_good_looks_like:"No 7000 events on a healthy system. Any 7000 should be investigated — even for non-critical services, failed services can indicate a broken application. On a server, a failed critical service may mean the server is partially or fully non-functional.",common_mistakes:["Trying to start the service without reading the error code — the code tells you exactly what went wrong","Not checking Event 7001 for dependency failures — if a dependency failed, fix that first","Resetting the service account password without also updating it in the service configuration","Not checking that the service binary executable actually exists at the path configured"],causes:["Service binary file missing or corrupt","Service account credentials invalid or account locked/disabled","Dependency service not running","Insufficient permissions for service account","Service crashed immediately on start","Antivirus blocking service binary","Registry configuration corrupt for the service"],steps:["Filter System log for Event 7000","Note the service name and error code","Error 2 (file not found): check if binary exists at configured path","Error 5 (access denied): check service account permissions","Error 1053 (timeout): check Event 7009 and investigate why service is not responding","Check Event 7001 for dependency failures first","Check service configuration: Get-Service <name> | Select-Object *","Check service account status in AD if it uses a domain account"],symptoms:["service wont start","service failed to start","service not starting","service startup failure","service is stopped","application service failed","windows service error","service does not start on boot"],tags:["service","startup","scm","fundamental","reliability"],powershell:`# Service Startup Failure Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed

# Find service startup failures
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = @(7000, 7001, 7009, 7023)
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Sort-Object TimeCreated -Descending |
    Format-List`,related_ids:[7001,7009,7023,7031,7045],ms_docs:"https://learn.microsoft.com/en-us/windows/client-management/troubleshoot-service-startup-errors"},{id:7001,source:"Service Control Manager",channel:"System",severity:"Error",skill_level:"Fundamental",title:"Service Dependency Failed",short_desc:"A service could not start because a service it depends on failed or did not start.",description:"Event ID 7001 records that a service failed to start because one of its dependencies failed. Windows services can declare dependencies on other services — the SCM tries to start dependencies first. If a dependency fails (7000), all dependent services will also fail with 7001. This creates a cascade: one failed core service can trigger dozens of 7001 events. Always trace back to find the root cause failure (the 7000 event) rather than trying to fix each 7001 individually.",why_it_happens:"Service dependencies are configured in the service registry key under DependOnService or DependOnGroup. When the SCM starts the system, it topologically sorts services by dependency and starts them in order. A failure in a low-level service propagates to all services above it in the dependency tree.",what_good_looks_like:"A 7001 that follows a 7000 for the dependency is expected cascade behavior. Find and fix the 7000 first. If you see 7001 without a corresponding 7000 for the dependency service, the dependency may have started but then crashed (check 7031).",common_mistakes:["Fixing each 7001 service individually without finding the root 7000 cause","Not understanding that a single dependency failure can cascade to many 7001 events","Not checking the dependency chain: Get-Service -ComputerName <host> <servicename> -RequiredServices"],causes:["Root dependency service failed to start (Event 7000)","Root dependency service crashed after starting (Event 7031)","Dependency service disabled","Circular dependency (unusual)"],steps:["Find Event 7001 and note which dependency failed","Find the 7000 event for the failing dependency service","Fix the root cause dependency first","Once dependency is fixed, restart dependent services: Start-Service <name>","Check dependency chain: (Get-Service <name>).RequiredServices"],symptoms:["service dependency failed","dependent service failed","service cant start because of dependency","multiple services not starting","service dependency error"],tags:["service","dependency","startup","scm","reliability"],powershell:`# Service Dependency Failure Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-3)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = 7001
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List`,related_ids:[7e3,7009,7023,7031],ms_docs:null},{id:7009,source:"Service Control Manager",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Service Start Timeout",short_desc:"A service did not respond to a start request within the timeout period (typically 30s).",description:'Event ID 7009 records that a service was started but failed to report "running" status within the SCM timeout period (default 30 seconds, configurable via HKLM\\SYSTEM\\CurrentControlSet\\Control\\ServicesPipeTimeout). This typically means the service binary is hung during initialization — perhaps waiting for a network resource, deadlocked in its startup code, or taking longer than expected to load. The service may eventually start or may time out and fail with a 7000.',why_it_happens:'The SCM sends a "start" control code to the service and waits for the service to call SetServiceStatus() with SERVICE_RUNNING within the timeout window. If the service is doing heavy initialization work (database connections, loading large files, network calls) it may exceed the timeout. Timeout errors during boot are common after power failure when disks or networks are slow.',what_good_looks_like:"No 7009 events. If 7009 appears: check if the service eventually started, investigate what is blocking it during initialization.",common_mistakes:["Increasing ServicesPipeTimeout as the fix without investigating why the service is slow to start","Not checking whether the service is doing network calls during startup that may be failing","Treating 7009 and 7000 as the same — 7009 is timeout specifically, 7000 is general failure"],causes:["Service doing heavy initialization work exceeding timeout","Network-dependent service waiting for unavailable network resource","Deadlock in service startup code","System overloaded during boot","Service binary issue causing slow startup"],steps:["Filter System log for 7009","Note the service name","Check if a 7000 follows (timeout → failure) or if the service eventually started","Check the Application log for service-specific error messages","Check if the timeout occurs consistently or only after cold boot","If network-dependent: check network availability during boot"],symptoms:["service timeout","service timed out starting","service took too long to start","service start hung","service not responding on startup"],tags:["service","timeout","startup","scm","performance"],powershell:`# Service Timeout Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = 7009
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List`,related_ids:[7e3,7001,7011,7023],ms_docs:null},{id:7011,source:"Service Control Manager",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Service Transaction Timeout",short_desc:"A service did not respond to a control request (stop, pause, continue) within the timeout.",description:"Event ID 7011 is similar to 7009 but occurs when an already-running service fails to respond to a control request — typically a stop, pause, or continue command — within the timeout period. This usually means the service is hung or deadlocked in its running state. A service that cannot be stopped normally may need to be terminated via Task Manager or by stopping the process directly.",why_it_happens:"The SCM sends control codes to services via the service's service control handler. If the service's handler is blocked (thread deadlock, hung I/O, or simply not processing the control pipe), it does not acknowledge the control request, and the SCM times out.",what_good_looks_like:"No 7011 events. Any 7011 indicates a service is not properly handling control signals, which is a reliability concern.",common_mistakes:["Not finding the service's process ID and killing it if the service won't stop normally","Trying to restart a hung service without ending its process first"],causes:["Service deadlocked in its running code","Service I/O operation blocking the control thread","Service bug in control handler","High system load preventing the service thread from running"],steps:["Identify the service and find its process: Get-WmiObject Win32_Service | Where-Object Name -eq '<name>' | Select ProcessId","Check what the process is doing: Invoke-Command -ComputerName $computer { Get-Process -Id <pid> | Select-Object * }","If the service is hung: Stop-Process -Id <pid> -Force, then Start-Service <name>","Review application event log for service-specific error messages","If recurring: investigate the service for deadlock conditions"],symptoms:["service hung","service not responding","cant stop service","service wont stop","service transaction timeout","service hung after stopping"],tags:["service","timeout","hung","scm","reliability"],powershell:`# Service Transaction Timeout Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = 7011
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List`,related_ids:[7e3,7009,7023,7031],ms_docs:null},{id:7023,source:"Service Control Manager",channel:"System",severity:"Error",skill_level:"Fundamental",title:"Service Terminated with Error",short_desc:"A service stopped and reported a non-zero error code.",description:"Event ID 7023 is generated when a service terminates and returns a non-zero error code to the SCM. Unlike 7031 (unexpected crash), 7023 means the service exited intentionally with an error — it detected a problem and called ExitProcess with an error code. The event includes the error code, which helps diagnose the issue. This is commonly seen with services that fail due to configuration problems, missing dependencies, or license issues.",why_it_happens:"A service calls ExitProcess() or returns a non-zero exit code from its service main function, signalling to the SCM that it failed. The SCM records this as 7023. The exit code is typically a Win32 error code (e.g., 5 = Access Denied, 1060 = Service not found) or an application-specific error code.",what_good_looks_like:"No 7023 events. Any 7023 warrants investigation. Check the Application event log for additional context from the service itself.",common_mistakes:["Trying to fix 7023 without also checking the Application event log — the service may log more detail about why it exited","Treating 7023 the same as 7031 — 7023 is a graceful exit with error, 7031 is an unexpected crash"],causes:["Service detected missing configuration file","Service could not connect to required database","Service license validation failed","Service detected permission problem","Service explicitly exiting after detecting unrecoverable error"],steps:["Filter System log for 7023 and note service name and error code","Check Application log for service-specific messages around the same time","Look up the error code (Win32 error or application-specific)","Check service configuration files and dependencies","Restart service after fixing underlying cause: Start-Service <name>"],symptoms:["service terminated with error","service exited with error code","service crashed with error","service stopped unexpectedly with code","service failure error"],tags:["service","error","failure","scm","reliability"],powershell:`# Service Termination Error Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = 7023
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List`,related_ids:[7e3,7001,7031,7034],ms_docs:null},{id:7031,source:"Service Control Manager",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Service Crashed Unexpectedly",short_desc:"A service terminated unexpectedly — the SCM may attempt automatic recovery actions.",description:"Event ID 7031 records that a service terminated unexpectedly — meaning it did not exit gracefully and the SCM did not expect the termination. This is typically caused by the service process crashing (access violation, unhandled exception) rather than exiting cleanly. The event includes how many times the service has crashed (crash count) and what recovery action was taken. Recovery actions are configured per-service (restart, run a program, reboot) and can be viewed in the service properties.",why_it_happens:"When a service process terminates without deregistering from the SCM, the SCM detects the orphaned service process and logs 7031. This happens when the service binary crashes due to an unhandled exception, memory corruption, or external process termination. The SCM then checks the configured failure actions (First/Second/Subsequent failure) and executes the appropriate recovery.",what_good_looks_like:"No 7031 events. First crash: investigate. Repeated crashes: urgent — the service has a serious problem or the application has a bug. Check the Application event log for Application Error (ID 1000) events that will have the faulting module and exception code.",common_mistakes:["Not checking Event 1000 in the Application log alongside 7031 — the application crash detail is there",'Enabling "restart service automatically" without finding root cause — the service will keep crashing',"Not checking if an update or config change preceded the first crash"],causes:["Service binary bug causing access violation","Memory corruption","Unhandled exception in service code","Third-party DLL injected into service process crashing it","Out of memory condition","Antivirus terminating the service process (false positive)"],steps:["Find Event 7031 and note the service name and crash count","Check Application log for Event 1000 (Application Error) matching the service around the same time","Note faulting module in 1000 — is it the service's own DLL or a third-party one?","Check if there were recent updates or configuration changes","Review service recovery actions: (Get-WmiObject Win32_Service | Where Name -eq '<name>').FailureActions","If recurring: contact software vendor or check for patches"],symptoms:["service crashed","service keeps crashing","service keeps stopping","service restarting repeatedly","service died","service unexpectedly terminated","service auto restarted"],tags:["service","crash","failure","scm","reliability","application-crash"],powershell:`# Service Crash Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed

# Get service crashes
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = @(7031, 7034)
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List

# Also check Application log for crash details
Write-Host "
--- Application Crash Events (1000) ---" -ForegroundColor Cyan
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Application'
    Id        = 1000
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Select-Object -First 5 | Format-List`,related_ids:[7034,7e3,7023,1e3],ms_docs:null},{id:7034,source:"Service Control Manager",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Service Crashed Unexpectedly (Repeated)",short_desc:"A service crashed unexpectedly — this event marks subsequent crashes after the first.",description:"Event ID 7034 is very similar to 7031 but represents the second and subsequent unexpected terminations of a service. The SCM distinguishes between the first crash (7031) and repeat crashes (7034) because the recovery actions are typically different. If the configured recovery actions (restart, run program, reboot) are not resolving the crash, 7034 events indicate an ongoing problem that the automatic recovery is not fixing.",why_it_happens:'Generated by the SCM each time after the first crash when a service exits unexpectedly. Unlike 7031 (which may have a "restart service" recovery action), by the third crash the configured recovery action may be "take no action" — leaving the service permanently stopped.',what_good_looks_like:"No 7034 events. Any 7034 with high crash count is urgent — the service is in a crash loop.",common_mistakes:["Treating 7034 and 7031 as identical — 7034 indicates the problem is persistent and recovery actions are not working","Not counting how many times the service has crashed — the crash count indicates severity"],causes:["Same as 7031 — persistent service bug, hardware fault, or external cause","Recovery action set to restart, which keeps restarting into the same crash","Application configuration issue that is not fixed between restarts"],steps:["Filter System log for 7034 and count occurrences for the same service","Follow 7031 investigation steps","Check configured recovery actions in service properties","If crash count is high: disable automatic recovery temporarily and investigate root cause","Contact software vendor with crash details from Event 1000"],symptoms:["service keeps crashing over and over","service crash loop","service restarting every few minutes","service crashing repeatedly","service in crash loop"],tags:["service","crash","loop","reliability","scm"],powershell:`# Repeated Service Crash Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = 7034
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List`,related_ids:[7031,7e3,7023,1e3],ms_docs:null},{id:7036,source:"Service Control Manager",channel:"System",severity:"Info",skill_level:"Fundamental",title:"Service State Changed",short_desc:"A service entered a running or stopped state — normal operational event.",description:'Event ID 7036 records every time a service transitions to "running" or "stopped" state. It is one of the highest-volume SCM events and by itself is not suspicious — services start and stop constantly. However, 7036 becomes very useful when you want to know exactly when a specific service started or stopped, and whether a service that should always be running was temporarily stopped.',why_it_happens:'The SCM logs 7036 every time a service changes to a "running" or "stopped" state. This includes boot-time service starts, user-initiated stops and starts, and stops due to crashes (where it will appear alongside 7031/7034).',what_good_looks_like:"Normal: Windows Defender, Print Spooler, and other services stopping and starting as expected. Investigate: a critical service (SQL, IIS, AD) stopping without a corresponding planned action, a security service (antivirus, firewall) stopping unexpectedly, many services stopping simultaneously (suggests shutdown or crash).",common_mistakes:["Being overwhelmed by 7036 volume — filter specifically for the service and state you care about","Not using 7036 to confirm when a service was available during a troubleshooting window"],causes:["Normal service lifecycle","Scheduled maintenance","Service crash recovery","Intentional admin action","Malware stopping security services"],steps:["Filter System log for 7036 scoped to a specific service name",'Look for "stopped" transitions on critical or security services',"Correlate timestamps with 7031/7034 crash events","Check if antivirus or security services stopped unexpectedly"],symptoms:["service stopped","service started","service state changed","when did this service start","when did this service stop"],tags:["service","state","lifecycle","scm","monitoring"],powershell:`# Service State Change Investigation
# Eventful

$computer     = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime    = (Get-Date).AddDays(-3)  # Adjust time range as needed
$serviceName  = 'WinDefend'   # Replace with service to track

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = 7036
    StartTime    = $startTime
} -ErrorAction SilentlyContinue | Where-Object {
    $_.Message -like "*$serviceName*"
} | Select-Object TimeCreated, Message | Format-Table -AutoSize`,related_ids:[7e3,7031,7034,7040],ms_docs:null},{id:7040,source:"Service Control Manager",channel:"System",severity:"Info",skill_level:"Intermediate",title:"Service Start Type Changed",short_desc:"A service's startup type was changed (e.g., Automatic to Disabled).",description:"Event ID 7040 records changes to a service's start type — Automatic, Manual, Disabled. This is important from a security perspective because disabling a security service (antivirus, firewall, audit service) is a technique attackers use to weaken defenses before carrying out malicious activity. It also helps troubleshoot cases where a service unexpectedly stops starting on boot — someone may have changed it to Manual or Disabled.",why_it_happens:"The SCM logs 7040 when the start type of a service is modified. This happens via the Services MMC snap-in, sc.exe config command, the Set-Service cmdlet, or registry modifications. Group Policy can also force start type changes, which will appear as 7040 events.",what_good_looks_like:"Expected: planned changes to service start types during configuration management. Investigate: security-critical services (Windows Defender, Windows Firewall, Event Log) being changed to Disabled or Manual, changes made at unusual times, changes not reflected in change management records.",common_mistakes:["Not alerting on changes to security services' start type — this is a common attacker technique","Changing a service to Automatic without understanding why it was Manual"],causes:["IT admin changing service configuration","Software installer modifying service startup","Group Policy applying service configuration","Attacker disabling security services","Malware modifying startup type to survive reboots or disable detection"],steps:["Filter System log for 7040","Note service name and new start type",'If "Disabled" or "Manual" for a security service: immediate investigation',"Restore correct start type: Set-Service <name> -StartupType Automatic","Correlate with 4688 to find what process made the change"],symptoms:["service start type changed","service disabled","service changed to manual","antivirus service disabled","windows defender disabled","firewall service disabled"],tags:["service","configuration","security","defense-evasion","scm"],powershell:`# Service Start Type Change Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = 7040
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List`,related_ids:[7036,7e3,7031,4688],ms_docs:null},{id:7045,source:"Service Control Manager",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"New Service Installed",short_desc:"A new service was installed and registered with the Service Control Manager.",description:"Event ID 7045 records when a new service is installed on the system. Like scheduled task creation (4698), new service installation is a favourite persistence mechanism for attackers — services run under SYSTEM or service accounts, start automatically, and are often overlooked by defenders focused on user-space activity. The event includes the service name, binary path, account it runs under, and the start type. Service names that don't match known software should be immediately investigated.",why_it_happens:"When a service is created via the SCM APIs (CreateService), the SCM logs 7045. This happens during software installation, when admin tools deploy services, and when malware installs a service for persistence or remote access. The binary path field is particularly important — malicious services often run from unusual paths (AppData, Temp, random hex-named directories).",what_good_looks_like:"Expected: known software installing services during installation (SQL Server, antivirus, monitoring agents). Investigate: services installed outside of known software installation events, binary paths in AppData, Temp, or with random names, services running as LocalSystem (SYSTEM) installed by non-admin processes, service names that try to blend in with system services.",common_mistakes:["Not having a baseline list of expected services to compare against","Missing that even services installed by an admin account can be malicious if the admin account was compromised","Not checking the binary path — a service that looks legitimate by name but runs from AppData is suspicious"],causes:["Software installation creating a service","RMM or monitoring agent deploying a service","IT admin installing a management service","Malware installing a persistent service","Attacker using PsExec or similar (which installs a temporary service)"],steps:["Filter System log for 7045","Note service name, binary path, account, and start type","Check if binary path is in a standard location (Program Files, Windows, etc.)","Verify the service matches a known installation event (Event 4688, installer logs)","Check the service binary hash against VirusTotal if suspicious","Investigate unknown services: Get-Service <name> | Select-Object *","Remove malicious services: sc.exe delete <name>"],symptoms:["new service installed","unknown service appeared","suspicious service","new service on system","service installed by malware","unauthorized service"],tags:["service","persistence","installation","malware","scm","security"],powershell:`# New Service Installation Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = 7045
    StartTime    = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated   = $_.TimeCreated
        ServiceName   = ($data | Where-Object Name -eq 'ServiceName').'#text'
        BinaryPath    = ($data | Where-Object Name -eq 'ImagePath').'#text'
        AccountName   = ($data | Where-Object Name -eq 'AccountName').'#text'
        StartType     = ($data | Where-Object Name -eq 'StartType').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[7036,7e3,4688,4698],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4697"},{id:12,source:"Microsoft-Windows-Kernel-General",channel:"System",severity:"Information",skill_level:"Beginner",title:"Operating System Started",short_desc:"The OS initialized successfully and logged the exact start time for this boot.",description:"Event ID 12 from Kernel-General is written early in every normal boot. It records the precise system time the OS kernel started running. On its own it is informational — no action needed. Its primary value in incident analysis is as a timeline anchor: you can see exactly when the machine booted, correlate it against Event 13 (shutdown) and Event 41 (unexpected reboot), and determine uptime at the time of an incident.",why_it_happens:"Logged by the Windows kernel during initialization on every boot, before user-mode processes start. The timestamp comes from the hardware clock (RTC) before time sync occurs, so it may be slightly off by a few seconds from NTP-corrected time.",what_good_looks_like:"One Event 12 per boot cycle. Frequent Event 12 entries without corresponding Event 13 entries before them indicates repeated unexpected reboots — correlate with Event 41.",causes:["Normal system boot","Reboot after update","Reboot following a crash (Event 41)","Reboot after manual shutdown"],steps:["Use Event 12 timestamps to map the full boot history of the machine","Check if Event 13 appears before each Event 12 — missing Event 13 means the previous shutdown was unexpected","Correlate with Event 41 to confirm crash vs. clean reboot","Measure uptime between Event 12 and the incident timestamp"],symptoms:["when did the computer start","boot time","last reboot time","system start time","os startup"],tags:["boot","startup","kernel","timeline","uptime"],powershell:`# Boot History (last 30 days)
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Kernel-General'
    Id           = @(12, 13)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id,
        @{N='Event'; E={ if ($_.Id -eq 12) {'STARTED'} else {'SHUTDOWN'} }} |
    Sort-Object TimeCreated | Format-Table -AutoSize`,related_ids:[13,41,6008,6005],ms_docs:null},{id:13,source:"Microsoft-Windows-Kernel-General",channel:"System",severity:"Information",skill_level:"Beginner",title:"Operating System Shutdown",short_desc:"The OS began a clean shutdown and logged the exact time.",description:"Event ID 13 from Kernel-General is written at the beginning of every clean, intentional shutdown or restart. It is the counterpart to Event 12. In incident analysis its absence is the key signal — if you see Event 12 (boot) without a preceding Event 13 (clean shutdown), the previous session ended unexpectedly. That gap, combined with Event 41 or 6008, confirms a crash, power loss, or hard reset.",why_it_happens:"The Windows kernel writes Event 13 during the shutdown phase after user and service shutdown has completed. It is one of the last events written before the OS halts. If the machine crashes or loses power before reaching this phase, Event 13 is never written.",what_good_looks_like:"Every Event 12 (boot) should be preceded by an Event 13 (clean shutdown) from the previous boot. Exception: the very first boot after OS installation.",causes:["Normal user-initiated shutdown or restart","Shutdown via update installation","Remote shutdown command","System entering hibernation (S4 sleep)"],steps:["Find Event 12 entries and check for a preceding Event 13 — gap = unexpected shutdown","If no Event 13 before a boot: check Event 41 (crash/power loss) or Event 6008 (unexpected shutdown)","Correlate shutdown time with Event 1074 to see what initiated the shutdown","Repeated missing Event 13 entries = recurring stability problem requiring investigation"],symptoms:["when did the computer shut down","last shutdown time","clean shutdown","unexpected reboot history","os shutdown time"],tags:["shutdown","kernel","timeline","uptime","clean-shutdown"],powershell:`# Shutdown and Boot History (last 30 days)
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Kernel-General'
    Id           = @(12, 13)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id,
        @{N='Event'; E={ if ($_.Id -eq 12) {'STARTED'} else {'SHUTDOWN'} }} |
    Sort-Object TimeCreated | Format-Table -AutoSize`,related_ids:[12,41,1074,6008],ms_docs:null},{id:18,source:"Microsoft-Windows-Kernel-General",channel:"System",severity:"Information",skill_level:"Beginner",title:"System Time Changed",short_desc:"The system clock was adjusted — either by NTP sync, a user, or a domain time policy.",description:"Event ID 18 from Kernel-General logs whenever the system clock is changed. The event records the old time, new time, and the process that made the change. In normal environments this happens regularly via W32tm (Windows Time Service) syncing to a domain controller or NTP server. It becomes significant in incident analysis if: the time change is large (hours or days), the process making the change is not W32tm or a trusted service, or it occurs immediately before suspicious activity (attackers sometimes shift clocks to corrupt log correlation).",why_it_happens:"The Windows Time Service (W32tm) adjusts the clock periodically to stay in sync with its configured time source. Domain-joined machines sync to a domain controller; standalone machines use time.windows.com. Large jumps happen when the machine was offline for a long time, the CMOS battery died, or someone manually changed the time.",what_good_looks_like:"Small adjustments (milliseconds to seconds) by the SYSTEM process or W32tm are normal. Investigate: large adjustments (minutes or more), adjustments by a non-system process, repeated adjustments in a short period, or time changes that correlate with other suspicious events.",causes:["NTP or domain time sync (normal)","Manual clock change by a user or admin","CMOS/RTC battery failure causing clock drift","VM host adjusting guest clock","Time zone change","System recovering from long offline period"],steps:["Check the ProcessName field — W32tm.exe or SYSTEM is expected","Check the magnitude of the change — small ms adjustments are normal, large jumps are not","If large jump: check CMOS battery health and NTP sync status (w32tm /query /status)","If made by unexpected process: investigate that process — potential indicator of tampering","Run: w32tm /query /status to check current sync health"],symptoms:["clock changed","system time wrong","time jumped","timestamps are off","ntp sync problem","clock drift"],tags:["time","clock","ntp","sync","kernel","timeline"],powershell:`# System Time Change History
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Kernel-General'
    Id           = 18
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        OldTime     = ($data | Where-Object Name -eq 'OldTime').'#text'
        NewTime     = ($data | Where-Object Name -eq 'NewTime').'#text'
        Process     = ($data | Where-Object Name -eq 'ProcessName').'#text'
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[12,13],ms_docs:null},{id:20,source:"Microsoft-Windows-WindowsUpdateClient",channel:"System",severity:"Error",skill_level:"Beginner",title:"Windows Update Installation Failure",short_desc:"A Windows Update failed to install — the update and error code are recorded.",description:"Event ID 20 from WindowsUpdateClient is logged when an update download or installation fails. It records the update title, KB number, and the error code. This is the primary event for diagnosing Windows Update failures. The error code is the critical field — it maps to a specific failure reason (network, disk space, component corruption, conflicting software, etc.). Repeated failures of the same KB usually indicate an underlying system health problem rather than a transient network issue.",why_it_happens:"Windows Update failures occur for many reasons: network interruption during download, insufficient disk space (Windows needs 10–20 GB free), corruption in the Windows Update component store (DISM /ScanHealth), driver conflicts, third-party security software blocking the update, or the update being genuinely incompatible with installed hardware/software.",what_good_looks_like:"Occasional single failures followed by a successful install on retry are normal. Investigate: the same KB failing repeatedly across multiple attempts, multiple different KBs all failing, failures with component store corruption errors (0x800F0***), failures in the last 30 days with no successful updates.",common_mistakes:['Not checking the error code — "update failed" tells you nothing, the hex code tells you everything',"Running Windows Update troubleshooter first without checking disk space (most common root cause)","Not checking that Windows Update services are running (wuauserv, bits, cryptsvc, msiserver)","Forgetting that some updates require a reboot before the next update can install"],causes:["Insufficient free disk space (need 10–20 GB)","Windows Update component store corruption","Windows Update services stopped or disabled","Network connectivity issue during download","Third-party security software blocking installation","Conflicting software or driver","Pending reboot blocking further updates"],steps:["Note the error code from Event 20 — search it with the KB number for specific guidance","Check disk space: Get-PSDrive C | Select-Object Used, Free","Verify Update services running: Get-Service wuauserv, bits, cryptsvc | Select-Object Name, Status","Run DISM to check component health: DISM /Online /Cleanup-Image /CheckHealth","If corruption found: DISM /Online /Cleanup-Image /RestoreHealth","Then run: sfc /scannow","Check Event 19 (successful download) — if missing, download itself is failing (network/BITS)","Check free space in C:\\Windows\\SoftwareDistribution — clear with: net stop wuauserv && rd /s /q C:\\Windows\\SoftwareDistribution && net start wuauserv"],symptoms:["windows update failed","update wont install","update error","kb failed to install","windows update stuck","updates keep failing","cumulative update failed","feature update failed"],tags:["windows-update","patch","installation","error","maintenance"],powershell:`# Windows Update Failure History
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-WindowsUpdateClient'
    Id           = @(19, 20, 21, 43)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $type = switch ($_.Id) {
        19 { 'DOWNLOAD_OK'  }
        20 { 'INSTALL_FAIL' }
        21 { 'INSTALL_FAIL' }
        43 { 'DOWNLOAD_FAIL'}
    }
    [PSCustomObject]@{
        Time    = $_.TimeCreated
        Type    = $type
        Message = $_.Message.Substring(0, [Math]::Min(120, $_.Message.Length))
    }
} | Sort-Object Time -Descending | Format-Table -AutoSize`,related_ids:[19,1074],ms_docs:"https://learn.microsoft.com/en-us/windows/deployment/update/windows-update-error-reference"},{id:5,source:"Microsoft-Windows-Kernel-Boot",channel:"System",severity:"Information",skill_level:"Intermediate",title:"Boot Configuration Data Loaded",short_desc:"The Boot Configuration Data (BCD) store was read and applied during startup.",description:"Event ID 5 from Kernel-Boot is logged during the early boot phase when Windows reads its Boot Configuration Data (BCD) store. The event records which boot entry was selected and the boot type (normal, safe mode, WinRE, etc.). It is informational in a normal boot, but useful for diagnosing repeated failures to boot into normal mode, unexpected safe mode boots, or BCD corruption. Check the BootType field: 1 = normal boot, 2 = safe mode, 3 = safe mode with networking.",why_it_happens:"The Windows Boot Manager reads the BCD store on every boot to determine which OS to load and in what mode. Event 5 is written once per boot as confirmation that this step completed. If the BCD is corrupt or missing, the boot process fails before this event is written.",what_good_looks_like:"BootType 1 (normal) on every boot. Investigate: repeated BootType 2/3 (safe mode) without admin action, multiple Event 5 entries for a single boot cycle (can indicate boot repair attempts), or absence of Event 5 before a successful Event 12 (rare, possible if BCD events are filtered).",causes:["Normal system boot (BootType 1)","Safe mode boot (BootType 2/3) — user or automatic recovery","Windows Recovery Environment (BootType 4)","Automatic Repair triggered by repeated boot failures","Admin booting into diagnostic mode"],steps:["Check BootType field — normal is 1, safe mode is 2 or 3","If repeated safe mode boots: check what triggered them (user, automatic repair, or policy)","If BCD issues suspected: bcdedit /enum all (run as admin)","To repair BCD: boot from Windows media → Repair your computer → Startup Repair","Check Event 12 and 41 for correlation with unexpected boot modes"],symptoms:["booted into safe mode","unexpected safe mode","boot configuration","bcd","startup mode","computer keeps booting to recovery"],tags:["boot","bcd","safe-mode","startup","kernel-boot"],powershell:`# Recent Boot Type History
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Kernel-Boot'
    Id           = 5
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    $type = ($data | Where-Object Name -eq 'BootType').'#text'
    $desc = switch ($type) {
        '1' { 'Normal' }
        '2' { 'Safe Mode' }
        '3' { 'Safe Mode with Networking' }
        '4' { 'WinRE / Recovery' }
        default { "Type $type" }
    }
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        BootType    = $type
        Description = $desc
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[12,41,6008],ms_docs:null},{id:27,source:"Microsoft-Windows-Kernel-Boot",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"Boot Environment Error",short_desc:"The boot environment encountered a problem setting or reading a boot configuration value.",description:'Event ID 27 from Kernel-Boot is generated when the boot environment fails to properly apply a configuration value from the BCD store, or when a requested boot option cannot be set. This often appears after failed Windows Update installs, interrupted in-place upgrades, or BCD corruption. It can also appear from the disk driver context when a device was removed without a proper dismount ("surprise removal"). Check the Provider field in the raw event — Kernel-Boot indicates a boot configuration issue; the disk driver indicates a storage device removal problem.',why_it_happens:"Boot configuration errors arise when the BCD store has an inconsistency, a pending boot operation could not complete (e.g., an update that required a specific boot mode failed), or the boot hardware abstraction layer encountered unexpected firmware behaviour. Disk-context Event 27 occurs when Windows detects that a device was physically removed while still mounted — common with hot-swap bays, USB drives with running I/O, or iSCSI targets that dropped.",what_good_looks_like:"Absence is normal. Any occurrence warrants investigation. A single isolated Event 27 after a known upgrade attempt is low priority. Repeated occurrences or Event 27 combined with disk I/O errors (51, 129) is higher priority.",causes:["Failed or interrupted Windows Update requiring a boot-time operation","BCD store corruption or inconsistency","Storage device removed without safe ejection (Kernel-Boot or disk context)","Firmware/UEFI reporting an unexpected boot configuration state","iSCSI or network storage target disconnect"],steps:["Check the Provider/Source field — determines if this is a boot config or disk removal issue","For boot config: run bcdedit /enum all and look for inconsistencies","Run startup repair if the machine had boot problems: boot from Windows media","For disk removal: identify which device was removed and check Event 51 or 129 around the same time","Check Windows Update history for failed installs immediately before Event 27"],symptoms:["boot error","boot configuration problem","drive surprise removal","device disconnected unexpectedly","usb drive error on removal","iscsi disconnected"],tags:["boot","bcd","disk","removal","kernel-boot","storage"],powershell:`# Boot Environment Errors
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName   = 'System'
    Id        = 27
    StartTime = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, ProviderName, LevelDisplayName, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[5,12,41,51,129],ms_docs:null},{id:238,source:"Microsoft-Windows-Kernel-Processor-Power",channel:"System",severity:"Information",skill_level:"Advanced",title:"Processor Power Capability Change",short_desc:"A processor reported a change in its available power or performance states.",description:"Event ID 238 from Kernel-Processor-Power is logged when a processor reports a change in its power management capabilities or performance state enumeration. This typically occurs during boot, after a driver update, or when firmware/UEFI adjusts CPU power policy. On its own it is informational. It becomes relevant in incident investigations involving unexpected CPU throttling, performance degradation, or thermal events — particularly when combined with Event 37 (CPU speed limited by firmware) or high-temperature readings.",why_it_happens:"Modern processors expose their available performance and power states (P-states, C-states) to the OS via ACPI. When the set of available states changes — due to thermal limits, firmware intervention, driver update, or hardware capability reporting — Windows logs Event 238 to record the new state of affairs. In some cases, the CPU reports fewer performance states than expected because the firmware is limiting it due to a thermal condition.",what_good_looks_like:"Appearing once at boot for each logical processor is normal. Investigate: Event 238 occurring outside of boot, in conjunction with performance complaints, or combined with thermal events and Event 37.",causes:["Normal processor power state enumeration at boot","CPU driver or firmware update changing available P-states","Thermal throttling causing firmware to restrict performance states","Virtualisation host changing CPU capability exposure","BIOS/UEFI setting change affecting power management"],steps:["Check if Event 238 appears only at boot — if so, likely informational","If mid-operation: check for thermal events and CPU temperature (Get-WmiObject MSAcpi_ThermalZoneTemperature -Namespace root/wmi)","Check Event 37 (Kernel-Processor-Power) for CPU speed limiting",'Check BIOS/UEFI for power management settings — "Performance" mode vs "Power Saver"',"Ensure CPU drivers and chipset drivers are current"],symptoms:["cpu throttling","processor performance changed","cpu running slow","processor power state","cpu frequency reduced"],tags:["cpu","power","performance","throttling","processor","kernel"],powershell:`# CPU Power Events
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Kernel-Processor-Power'
    Id           = @(37, 238, 247)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[37,247,41],ms_docs:null},{id:247,source:"Microsoft-Windows-Kernel-Processor-Power",channel:"System",severity:"Information",skill_level:"Advanced",title:"Processor Performance State Transition",short_desc:"A processor transitioned to a different performance (P-state) level.",description:"Event ID 247 from Kernel-Processor-Power records a significant processor performance state (P-state) transition — typically a switch to a lower performance tier due to power policy, thermal management, or battery/power plan constraints. Like Event 238, it is informational in isolation but important when investigating CPU performance degradation. The event records the processor index, the target performance percentage, and the reason for the transition. Repeated 247 events showing reductions to low percentages indicate sustained throttling.",why_it_happens:"Windows power management continuously adjusts CPU performance states to balance power consumption against workload demand. Event 247 is written when this adjustment is significant enough to log — particularly when performance is constrained rather than just scaled up. The trigger can be a temperature threshold being reached, a power plan switch (Balanced → Power Saver), a UPS switching to battery, or firmware-level limits being applied.",what_good_looks_like:"Occasional 247 entries with performance levels that return to 100% are normal under balanced power plans. Investigate: consistent 247 entries showing low performance percentages during business hours, 247 paired with thermal events, or 247 following a UPS or power supply event.",causes:["Active power plan throttling CPU (Balanced or Power Saver mode)","Thermal throttling — CPU too hot","UPS switched to battery — system reducing power draw","Firmware-level power limits (TDP limits)","Virtualisation host restricting CPU performance"],steps:["Check the performance percentage in the event — sustained below 50% is a problem","Check CPU temperatures during the throttling period","Review active power plan: powercfg /getactivescheme","Change to High Performance if throttling is unwanted: powercfg /setactive SCHEME_MIN","Check Event 37 for firmware-level speed limits","If on a laptop/UPS: check power source at time of events"],symptoms:["cpu running slow","processor throttled","performance degraded","computer feels sluggish","cpu percentage low","performance state change"],tags:["cpu","power","throttling","performance","p-state","thermal"],powershell:`# Processor Performance State Events
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Kernel-Processor-Power'
    Id           = @(37, 238, 247)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        Time        = $_.TimeCreated
        EventId     = $_.Id
        Processor   = ($data | Where-Object Name -eq 'ProcessorNumber').'#text'
        PerfPercent = ($data | Where-Object Name -eq 'TargetProcessorThrottle').'#text'
        Message     = $_.Message.Substring(0, [Math]::Min(100, $_.Message.Length))
    }
} | Sort-Object Time -Descending | Format-Table -AutoSize`,related_ids:[238,37,41],ms_docs:null},{id:51,source:"disk",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"Disk I/O Error During Paging Operation",short_desc:"Windows detected an error reading or writing to the disk during a paging (virtual memory) operation.",description:'Event ID 51 from the disk driver is generated when a read or write error occurs on the disk during a paging operation — meaning Windows was trying to swap data between RAM and the page file (or read a mapped file) and the disk returned an error. This is a significant warning. A single Event 51 can be a transient glitch; repeated Event 51 entries almost always indicate a failing disk, a loose SATA/NVMe cable, or a failing disk controller. The machine may still appear to function normally while accumulating these errors, then fail suddenly. Event 51 is one of the most common events found in logs from computers that are "randomly slow" or "randomly freeze."',why_it_happens:"Paging operations are constant on a busy system — Windows uses virtual memory to extend RAM by writing data to disk. When the disk returns an error on one of these operations, Event 51 is written. The OS retries the operation, so the user often sees only a brief freeze or slowdown. The underlying cause is almost always hardware: bad disk sectors, a failing drive, a loose cable, an overheating disk, or a failing disk controller.",what_good_looks_like:"Absence is normal. Even a single Event 51 warrants checking disk SMART data. Multiple Event 51 entries in a short window means the disk is likely failing and data is at risk.",common_mistakes:["Dismissing Event 51 as a one-off without checking SMART data","Not checking the physical cable — a loose SATA cable is a very common cause and a 10-second fix","Waiting for the disk to fail completely before acting — backups should start now","Forgetting that Event 51 causes user-visible symptoms: freezing, slowness, application crashes"],causes:["Failing hard disk (bad sectors, mechanical failure)","Loose or failing SATA/NVMe data cable","Failing disk controller or motherboard storage port","Overheating disk (check drive temperature)","Failing SSD (NAND wear, controller issues)","External USB drive with a poor connection"],steps:["Count Event 51 occurrences — frequency and pattern matter","Identify which disk: check the device path in the event (e.g., \\Device\\Harddisk0)","Check disk SMART data immediately: Get-PhysicalDisk | Get-StorageReliabilityCounter | Select-Object DeviceId, ReadErrorsTotal, WriteErrorsTotal, Wear","Use CrystalDiskInfo or manufacturer tool for full SMART attribute read","Physically check cables — reseat SATA data cable at both ends (drive and motherboard)","Check Event 7 (disk) and Event 11 (disk) nearby — hardware malfunction markers","Check Event 129 (StorPort) — disk reset events alongside 51 = imminent failure","Backup immediately if SMART shows reallocated sectors or pending sectors > 0"],symptoms:["computer randomly freezes","computer randomly slow","random hangs","blue screen of death","disk error","hard drive failing","applications crashing randomly","file system corruption","computer lags then recovers","disk making clicking noise"],tags:["disk","storage","hardware","paging","failure","critical","sata","nvme"],powershell:`# Disk I/O Error Investigation
# Eventful

# Count recent disk errors
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'disk'
    Id           = @(51, 7, 11)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Group-Object Id |
    Select-Object Name, Count |
    Format-Table -AutoSize

# SMART reliability data
Get-PhysicalDisk | ForEach-Object {
    $rel = $_ | Get-StorageReliabilityCounter
    [PSCustomObject]@{
        Disk              = $_.FriendlyName
        MediaType         = $_.MediaType
        HealthStatus      = $_.HealthStatus
        ReadErrors        = $rel.ReadErrorsTotal
        WriteErrors       = $rel.WriteErrorsTotal
        Wear              = "$($rel.Wear)%"
        Temperature       = "$($rel.Temperature) C"
        PowerOnHours      = $rel.PowerOnHours
    }
} | Format-Table -AutoSize`,related_ids:[129,153,7,11,55,41],ms_docs:null},{id:129,source:"storahci",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"StorPort: Reset to Device Initiated",short_desc:"The storage controller timed out waiting for a disk response and issued a hardware reset.",description:'Event ID 129 from storahci (or StorPort) means the storage controller sent a command to the disk and the disk did not respond within the timeout window, forcing the controller to reset the device to recover. This is a serious hardware warning. Unlike Event 51 which happens during paging I/O, Event 129 indicates the disk stopped responding entirely — even briefly. The user typically experiences a multi-second freeze followed by recovery, a BSOD, or a "delayed write failed" error. On an SSD this almost always means the drive is failing or has a firmware bug. On a spinning disk it usually means imminent mechanical failure.',why_it_happens:"The AHCI/NVMe controller expects the disk to respond to commands within a set timeout (usually 30 seconds for AHCI). If the disk stalls — due to bad sectors forcing repeated read retries, a mechanical head stall, thermal shutdown, or firmware hang — the controller times out and issues a bus reset. The OS recovers the I/O but logs the reset. On healthy hardware this never happens.",what_good_looks_like:"Absence is normal. Any occurrence of Event 129 is abnormal and requires investigation. A single Event 129 on a spinning disk after years of service may be a one-off; any recurrence means replace the disk.",common_mistakes:["Treating Event 129 as low priority — it is not, the drive is telling you it is struggling","Not checking whether the machine uses AHCI vs NVMe — source will be storahci or stornvme respectively","Replacing the cable but not checking SMART — the drive itself may be the problem","Missing that Event 129 during a backup job means the backup may be corrupt"],causes:["Failing hard disk (mechanical failure, bad sectors exhausting retry budget)","Failing or poorly firmware-updated SSD","Overheating drive entering thermal protection","Failing SATA/NVMe cable or connector","Failing disk controller or motherboard storage chip","Firmware bug in the drive (check manufacturer for firmware update)"],steps:["Check the device path in the event to identify which disk","Run SMART immediately — Event 129 is a high-priority disk failure indicator","Check Event 51 nearby — combination of 51 + 129 = near-certain disk failure","Check drive temperature: Get-PhysicalDisk | Get-StorageReliabilityCounter | Select Temperature","Check for pending/reallocated sectors in SMART (any non-zero = replace soon)","Update disk firmware from manufacturer — some 129 events are firmware bugs","Back up immediately and plan disk replacement"],symptoms:["computer freezes for several seconds","system hangs then recovers","blue screen inaccessible boot device","delayed write failed error","drive not responding","ssd freezing","hard drive hang","disk reset","storage controller error"],tags:["disk","storage","hardware","reset","storahci","nvme","failure","critical"],powershell:`# StorPort Reset Investigation
# Eventful

# Check for disk reset events
Get-WinEvent -FilterHashtable @{
    LogName   = 'System'
    Id        = @(129, 51, 153)
    StartTime = (Get-Date).AddDays(-14)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, ProviderName, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List

# SMART health check
Get-PhysicalDisk | ForEach-Object {
    $rel = $_ | Get-StorageReliabilityCounter
    [PSCustomObject]@{
        Disk          = $_.FriendlyName
        Health        = $_.HealthStatus
        Temperature   = "$($rel.Temperature) C"
        ReadErrors    = $rel.ReadErrorsTotal
        WriteErrors   = $rel.WriteErrorsTotal
        Wear          = "$($rel.Wear)%"
    }
} | Format-Table -AutoSize`,related_ids:[51,153,7,11,55,41],ms_docs:null},{id:153,source:"disk",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"Disk Retriable I/O Error",short_desc:"A disk I/O operation failed but was retried successfully — an early warning of disk problems.",description:"Event ID 153 from the disk driver indicates a disk I/O operation failed on the first attempt but succeeded on retry. The OS handles this transparently so the user typically notices nothing — this is what makes Event 153 particularly dangerous. It is an early warning sign that appears weeks or months before a disk starts producing Event 51 (paging errors) and Event 129 (disk resets). Seeing Event 153 in a log is the best opportunity to catch a failing disk before it causes data loss or a system crash. Treat it as a yellow flag: investigate, check SMART, increase backup frequency.",why_it_happens:"Magnetic hard disks can fail to read a sector on first pass due to a weak magnetic signal, a vibration, or early-stage surface degradation. The drive retries internally (up to several times) and then the OS driver also retries. If a later retry succeeds, Event 153 is written rather than Event 51. SSDs can produce 153 during early NAND cell degradation. The key insight: a disk that needs retries to succeed is a disk that is getting worse.",what_good_looks_like:"Absence is normal for a healthy drive. Even one or two Event 153 entries justifies checking SMART. A cluster of 153 entries or 153 appearing alongside 51 or 129 means the disk is in an active failure mode.",causes:["Early-stage disk surface degradation (HDD)","Weak magnetic sectors starting to fail (HDD)","SSD NAND cell wear approaching end of life","Vibration or physical shock causing temporary read failure","Marginal power delivery to the disk","Loose data or power cable causing intermittent contact"],steps:["Note how many Event 153 entries appear and over what time period","Check SMART data: reallocated sectors, pending sectors, uncorrectable sectors","Compare 153 frequency over time — increasing rate = accelerating failure","Check Event 51 and 129 — if those appear alongside 153, disk failure is active not just early","Increase backup frequency immediately","Plan disk replacement even if SMART looks clean — 153 can precede SMART-reported failures"],symptoms:["disk errors in event log","hard drive warnings","early disk failure","smart warning","drive health warning","occasional disk errors","disk slowly failing"],tags:["disk","storage","hardware","warning","early-warning","smart","failure"],powershell:`# Disk Health - Early Warning Check
# Eventful

# Disk error event frequency (last 30 days)
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'disk'
    Id           = @(153, 51, 7, 11)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Group-Object Id |
    Select-Object @{N='EventId'; E={$_.Name}},
                  @{N='Count';   E={$_.Count}} |
    Format-Table -AutoSize

# SMART data
Get-PhysicalDisk | ForEach-Object {
    $rel = $_ | Get-StorageReliabilityCounter
    [PSCustomObject]@{
        Disk              = $_.FriendlyName
        Health            = $_.HealthStatus
        ReadErrors        = $rel.ReadErrorsTotal
        WriteErrors       = $rel.WriteErrorsTotal
        Wear              = "$($rel.Wear)%"
        PowerOnHours      = $rel.PowerOnHours
    }
} | Format-Table -AutoSize`,related_ids:[51,129,7,11],ms_docs:null},{id:37,source:"Microsoft-Windows-Kernel-Processor-Power",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"CPU Speed Limited by Firmware",short_desc:"The processor is running slower than its rated speed due to a firmware-imposed limit — most commonly overheating.",description:'Event ID 37 from Kernel-Processor-Power is the single most important event for diagnosing "my computer got suddenly slow" tickets. It means the CPU firmware (BIOS/UEFI) has capped the processor speed below its rated maximum. The event records the processor number, the current performance percentage (100% = full speed, lower values = throttled), and optionally the reason. The most common cause by far is overheating — when a CPU hits its thermal limit, the firmware reduces its clock speed to protect it, causing an immediate and dramatic performance drop that the user experiences as the computer becoming unusably slow.',why_it_happens:'Modern CPUs have built-in thermal protection: when die temperature hits the TjMax threshold (typically 90–105°C depending on CPU model), the firmware reduces the clock multiplier to cut heat output. This "thermal throttling" keeps the CPU alive but makes it run at a fraction of its rated speed. Other causes: the active power plan is set to Power Saver or Balanced (which caps CPU performance), a laptop is running on battery, BIOS power settings are misconfigured, or a virtualisation host is constraining the guest.',what_good_looks_like:"Absence is normal on a healthy desktop. On laptops under power plans, occasional 37 entries with modest throttling are normal. Investigate: Event 37 showing performance at 30% or lower, Event 37 appearing repeatedly during normal workloads, or a sudden onset of Event 37 after months of none (thermal paste dried out, heatsink clogged with dust).",common_mistakes:["Not checking the CPU temperature — the event alone does not tell you the temperature","Fixing the power plan without checking if overheating is the actual cause — if it is, changing the power plan just masks the problem","Forgetting laptops throttle on battery — always test on mains power before diagnosing hardware","Not checking whether the heatsink fan is spinning — a failed fan causes immediate sustained throttling"],causes:["CPU overheating — dried thermal paste, dust-blocked heatsink, failed fan","Power plan set to Balanced or Power Saver","Laptop running on battery (power-saving throttle)","BIOS power management settings restricting TDP","Virtualisation host CPU resource limit","High ambient temperature in the room or enclosure"],steps:["Check the performance percentage in the event — below 50% sustained is a serious problem","Check CPU temperature with HWMonitor, Core Temp, or PowerShell (ACPI thermal zones)","If temp > 85°C under light load: clean heatsink fins and replace thermal paste","Check the active power plan: powercfg /getactivescheme — switch to High Performance for testing","Check fan operation: physically listen and use BIOS fan monitor","On a laptop: test on mains power — if throttling stops, it was a battery power policy","Check BIOS for power/performance settings — some have a Power Limit that is set too low"],symptoms:["computer suddenly slow","computer became slow overnight","cpu running slow","processor throttled","computer sluggish","everything is slow","computer slow after a while","laptop slow on battery","performance dropped","computer slow when hot"],tags:["cpu","performance","throttling","thermal","overheating","slowness","power"],powershell:`# CPU Throttling Investigation
# Eventful

# Check for throttling events
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Kernel-Processor-Power'
    Id           = 37
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        Time        = $_.TimeCreated
        Processor   = ($data | Where-Object Name -eq 'ProcessorNumber').'#text'
        PerfPercent = ($data | Where-Object Name -eq 'TargetProcessorThrottle').'#text'
    }
} | Sort-Object Time -Descending | Format-Table -AutoSize

# Current power plan
powercfg /getactivescheme

# CPU temperature via ACPI (not all hardware exposes this)
Get-WmiObject MSAcpi_ThermalZoneTemperature -Namespace root/wmi -ErrorAction SilentlyContinue |
    Select-Object InstanceName,
        @{N='TempC'; E={ [math]::Round(($_.CurrentTemperature - 2732) / 10, 1) }} |
    Format-Table -AutoSize`,related_ids:[238,247,41],ms_docs:"https://learn.microsoft.com/en-us/troubleshoot/windows-client/performance/cpu-frequency-limited-firmware"},{id:7026,source:"Service Control Manager",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Boot-Start or System-Start Driver Failed to Load",short_desc:"A driver that is supposed to load at boot failed — can cause BSODs, missing hardware, or system instability.",description:"Event ID 7026 from the Service Control Manager is written when a driver configured to load at boot time (boot-start or system-start type) fails to initialise. These are some of the earliest drivers loaded — storage controllers, file system drivers, hardware abstraction drivers. A failed boot-start driver can cause BSODs during or after boot, missing hardware devices, system instability, or degraded performance. The event names the driver that failed. Critical drivers (disk controller, NTFS) failing will usually result in a BSOD before Windows fully loads; less critical drivers result in Event 7026 with Windows still functional but a hardware component non-functional.",why_it_happens:"Boot-start drivers fail for several reasons: the driver binary is missing or corrupt (Windows Update gone wrong, malware damage), the hardware the driver supports is no longer present (USB device was unplugged), a driver update introduced a bug, or the driver is incompatible with the current OS version. After a Windows upgrade, old third-party drivers for hardware that was not migrated cleanly are a common source.",what_good_looks_like:"No Event 7026 in a healthy system. A single occurrence after a driver update or hardware change is worth investigating but may resolve on reboot. Repeated Event 7026 for the same driver = persistent problem requiring remediation.",common_mistakes:["Ignoring 7026 because Windows boots fine — the failed driver may control a device that looks functional but is running degraded","Not checking Device Manager after seeing 7026 — the failed device will usually show a yellow warning","Reinstalling drivers before checking if the underlying hardware is present and recognised in BIOS"],causes:["Driver binary missing or corrupt","Incompatible or outdated driver after Windows Update","Hardware removed but driver still registered","Third-party driver conflict","Malware corrupting driver files","Failed Windows in-place upgrade leaving stale drivers"],steps:["Note the driver name from the event","Open Device Manager — look for yellow warning triangles (devmgmt.msc)","Right-click the affected device → Update Driver or Roll Back Driver","If driver is for removed hardware: uninstall the device in Device Manager","Check Windows Update — a pending driver update may fix the issue","Run: sfc /scannow to check for and repair corrupt system files","If after a Windows upgrade: use compatibility mode or download the latest driver from the manufacturer"],symptoms:["driver failed to load","device not working after reboot","blue screen on boot","hardware not detected","driver error on startup","device missing after update","system instability after driver update"],tags:["driver","boot","hardware","service","bsod","scm","stability"],powershell:`# Boot Driver Failure Investigation
# Eventful

# Failed boot drivers (last 30 days)
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = 7026
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending | Format-List

# Devices with errors in Device Manager
Get-PnpDevice | Where-Object { $_.Status -ne 'OK' } |
    Select-Object Status, Class, FriendlyName, InstanceId |
    Format-Table -AutoSize`,related_ids:[7e3,7001,7034,41,1001],ms_docs:null},{id:10016,source:"Microsoft-Windows-DistributedCOM",channel:"System",severity:"Warning",skill_level:"Beginner",title:"DCOM Permission Error (Usually Harmless)",short_desc:"A process tried to start a DCOM server without the required permissions. Appears constantly in most Windows logs — almost always harmless noise.",description:"Event ID 10016 from DistributedCOM is one of the most common events in Windows System logs and is responsible for an enormous amount of wasted diagnostic time. It means a process attempted to activate or call a DCOM (Distributed Component Object Model) server and was denied due to missing launch or activation permissions. Despite appearing as a Warning and sometimes an Error, this event is almost never the cause of user-reported problems. Microsoft itself ships Windows with several built-in components that generate 10016 continuously — the permissions gap is intentional or a long-standing unfixed bug in many cases. In practice: if a user reports crashes, slowness, or application failure, Event 10016 is almost certainly not the cause. Look elsewhere.",why_it_happens:"Windows is built extensively on COM/DCOM — nearly every system component uses it for inter-process communication. Many COM servers have fine-grained security descriptors that restrict which accounts can launch or activate them. When an app or service (including built-in Windows processes like Explorer, Taskbar, or Update Orchestrator) tries to activate a COM server and lacks explicit permission, Event 10016 is written. Microsoft has never fixed many of these permission mismatches because the underlying operations succeed through fallback paths.",what_good_looks_like:"Present in virtually every Windows system log — this is normal. Only investigate 10016 if: the event is from a third-party application that is actually broken, the CLSID/AppID matches an application you are actively troubleshooting, or it correlates precisely with user-reported errors from that same application.",common_mistakes:["Assuming Event 10016 is causing the problem the user reported — it almost never is",'Spending time "fixing" 10016 by editing DCOM permissions in Component Services — this is risky and rarely necessary',"Not looking past 10016 to find the actual cause (disk errors, application crashes, driver failures)"],causes:["Built-in Windows components with unfixed permission mismatches (expected, ignore)","Third-party software with misconfigured DCOM registration (investigate if the app is broken)","Application running under a restricted account trying to access DCOM server (check if the app is misbehaving)"],steps:["Identify the application or service generating the event from the AppID/CLSID field","If it is a Windows built-in component (Taskbar, Explorer, Update, BrokerInfrastructure): ignore it","If it is a third-party app that the user says is broken: check the vendor's known issues","Look past 10016 for other events that correlate with the actual reported problem","Do NOT edit DCOM security in Component Services unless specifically directed by a vendor KB"],symptoms:["dcom error","event 10016","distributed com error","lots of warnings in event log","event log full of warnings"],tags:["dcom","com","permissions","noise","warning","harmless","common"],powershell:`# DCOM Error Summary (to assess volume and source)
# Eventful — These are almost always harmless. Check the source AppID.

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-DistributedCOM'
    Id           = 10016
    StartTime    = (Get-Date).AddDays(-1)
} -ErrorAction SilentlyContinue |
    Group-Object { ($_ | Select-Object -ExpandProperty Message).Substring(0, 80) } |
    Select-Object Count, Name |
    Sort-Object Count -Descending |
    Format-Table -AutoSize`,related_ids:[],ms_docs:null},{id:1,source:"Microsoft-Windows-Power-Troubleshooter",channel:"System",severity:"Information",skill_level:"Beginner",title:"System Resumed from Sleep",short_desc:"The system woke from sleep (S3) and logged the sleep duration and wake source.",description:"Event ID 1 from Power-Troubleshooter is written every time the system resumes from sleep (S3 suspend-to-RAM). It records the time the system entered sleep, the time it woke, and the wake source (what triggered the wakeup — a keyboard press, mouse movement, network packet, scheduled task, or Wake-on-LAN). In a healthy system this is informational. It becomes diagnostic when investigating sleep-related crashes (system does not resume, resumes to BSOD, or resumes with corrupted state), unexpected wakeups keeping a machine awake all night, or correlating crash events against sleep/wake cycles.",why_it_happens:"Written by the Power Troubleshooter component on every S3 resume. The SleepTime and WakeTime fields give exact duration. The WakeSourceType and WakeSourceText fields identify what triggered the resume — this is the key data for diagnosing unwanted wakeups.",what_good_looks_like:"Present on any machine using sleep mode — normal. Investigate: Event 1 followed immediately by Event 41 or 1001 (crash on resume), Event 1 entries at unexpected hours (machine waking overnight), missing Event 1 when user says the machine would not wake (possibly hung in sleep state).",causes:["Normal user wakeup (keyboard, mouse, power button)","Network adapter Wake-on-LAN packet","Scheduled task configured to wake the system","Windows Update waking machine to install updates","USB device activity triggering resume","Automatic Maintenance task waking the machine"],steps:["Check WakeSourceText field — identifies exactly what woke the machine","For overnight wakeups: check scheduled tasks and Windows Update settings","To list all wake timers: powercfg /waketimers","To check last wake source: powercfg /lastwake","If crashes on resume: check Event 41 and 1001 immediately after Event 1 timestamps",'To disable Wake-on-LAN: Device Manager → Network Adapter → Power Management → uncheck "Allow this device to wake the computer"',"For a machine that will not resume: check Event 42 (entered sleep) then look for a missing Event 1"],symptoms:["computer wakes up by itself","pc turns on overnight","computer woke unexpectedly","crash after sleep","blue screen after waking","computer wont wake from sleep","machine on when i arrive","sleep not working"],tags:["sleep","power","resume","wake","wakeup","s3"],powershell:`# Sleep/Wake History and Wake Sources
# Eventful

# Recent wake events
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Power-Troubleshooter'
    Id           = 1
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        WakeTime   = $_.TimeCreated
        SleepTime  = ($data | Where-Object Name -eq 'SleepTime').'#text'
        WakeSource = ($data | Where-Object Name -eq 'WakeSourceText').'#text'
    }
} | Sort-Object WakeTime -Descending | Format-Table -AutoSize

# Current wake timers
powercfg /waketimers

# Last wake source
powercfg /lastwake`,related_ids:[42,107,41,6008],ms_docs:null},{id:42,source:"Microsoft-Windows-Kernel-Power",channel:"System",severity:"Information",skill_level:"Beginner",title:"System Entering Sleep",short_desc:"The system is transitioning into a sleep state (S3 or S4 hibernate).",description:'Event ID 42 from Kernel-Power is logged when the system begins a sleep transition. The TargetSleepState field indicates the target state: 3 = sleep (S3, suspend-to-RAM), 4 = hibernate (S4, suspend-to-disk). This event pairs with Event 1 (resume from sleep) and Event 107 (resume from hibernate) to build a complete sleep/wake timeline. Its diagnostic value is in detecting unexpected sleep entries (machine going to sleep unexpectedly during use) and as the "last checkpoint" before a machine that failed to resume — if Event 42 exists but no Event 1 follows, the machine may have crashed during sleep or failed to wake.',why_it_happens:"Written by the Kernel-Power component when the OS commits to entering a sleep state, after all pre-sleep notifications have been sent to drivers and applications. The actual system state change happens immediately after this event is written.",what_good_looks_like:"Appears on every sleep entry — normal. Investigate: Event 42 (sleep) with no following Event 1 (wake) — machine may have hard-crashed during sleep. Event 42 occurring unexpectedly during active use — could be an aggressive power plan timeout or a driver triggering sleep.",causes:["User-initiated sleep (Start → Sleep, closing laptop lid)","Power plan idle timeout","Windows Automatic Maintenance triggering sleep after completion","Remote management or policy forcing sleep","Low battery threshold on laptop triggering hibernation"],steps:["Check TargetSleepState: 3 = sleep, 4 = hibernate","If Event 42 exists but no Event 1 follows: machine likely crashed in sleep — check Event 41","If machine sleeps unexpectedly: check power plan idle timeout settings (powercfg /query)","Check for wake after Event 42: look for Event 1 or 107 with matching timestamp","For laptops sleeping unexpectedly: check battery threshold settings in power plan"],symptoms:["computer goes to sleep by itself","pc keeps going to sleep","computer slept and wont wake","machine powered off during sleep","laptop sleeping unexpectedly","computer sleeps too quickly"],tags:["sleep","hibernate","power","s3","s4","kernel-power"],powershell:`# Sleep Transition History
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Kernel-Power'
    Id           = @(42, 107)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml   = [xml]$_.ToXml()
    $data  = $xml.Event.EventData.Data
    $state = ($data | Where-Object Name -eq 'TargetSleepState').'#text'
    $desc  = switch ($_.Id) {
        42  { if ($state -eq '4') {'ENTERING HIBERNATE'} else {'ENTERING SLEEP'} }
        107 { 'RESUMED FROM HIBERNATE' }
    }
    [PSCustomObject]@{
        Time      = $_.TimeCreated
        EventId   = $_.Id
        Transition = $desc
    }
} | Sort-Object Time -Descending | Format-Table -AutoSize`,related_ids:[1,107,41,6008],ms_docs:null},{id:107,source:"Microsoft-Windows-Kernel-Power",channel:"System",severity:"Information",skill_level:"Beginner",title:"System Resumed from Hibernation",short_desc:"The system woke from hibernation (S4) — the OS was restored from the hibernation file on disk.",description:"Event ID 107 from Kernel-Power is written when the system resumes from hibernation (S4 suspend-to-disk), where system state was saved to hiberfil.sys and full power was cut. Unlike sleep (S3) which only cuts power to non-essential components, hibernation cuts all power — so resume requires reading the entire system state back from disk. This makes hibernate resume slower than sleep resume and more dependent on disk health. If Event 107 is absent after an Event 42 with TargetSleepState=4, the machine failed to resume from hibernation — check for disk errors (Event 51, 129) and Event 41.",why_it_happens:'Hibernate is triggered by a power plan low-battery threshold, an explicit hibernate command, or "Fast Startup" on Windows 10/11 (which hibernates the kernel session on shutdown). Fast Startup means that on most Windows 10/11 machines, every normal shutdown is followed by a hibernate-style resume on next boot — Event 107 will appear on machines using Fast Startup even without user-initiated hibernation.',what_good_looks_like:"Present on machines with hibernate or Fast Startup enabled — normal. On Windows 10/11 with Fast Startup, expect Event 107 on most boots instead of Event 12 (clean OS start). Investigate: Event 107 absent when expected (failed hibernate resume), Event 107 followed by application instability (state corruption during restore), or Event 107 taking unusually long (slow disk causing slow resume).",causes:["Laptop reaching critical battery threshold","User explicitly choosing Hibernate from Start menu","Fast Startup on Windows 10/11 (normal shutdown uses hibernate)","Hybrid sleep resuming from disk after power loss"],steps:["If machine fails to resume from hibernate: check Event 41 (crash) and disk health events (51, 129)","If applications are unstable after resume: Fast Startup may be restoring a corrupt session — disable it and do a full shutdown",'To disable Fast Startup: Control Panel → Power Options → Choose what the power buttons do → uncheck "Turn on fast startup"',"If hibernate file corrupt: run powercfg /h off then powercfg /h on to rebuild it","Check disk read speed — slow hibernate resume is almost always a disk health or interface speed issue"],symptoms:["computer wont come back from hibernate","resume from hibernate failed","slow to wake from hibernate","fast startup issue","shutdown and restart slow","hibernate not working","applications broken after resume"],tags:["hibernate","power","resume","s4","fast-startup","kernel-power"],powershell:`# Hibernate Resume History
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Kernel-Power'
    Id           = 107
    StartTime    = (Get-Date).AddDays(-14)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize

# Check if Fast Startup is enabled
$fss = (Get-ItemProperty 'HKLM:SYSTEMCurrentControlSetControlSession ManagerPower' -Name HiberbootEnabled -ErrorAction SilentlyContinue).HiberbootEnabled
"Fast Startup enabled: $($fss -eq 1)"

# Hibernate file status
powercfg /h`,related_ids:[42,1,41,51,129],ms_docs:null},{id:7,source:"disk",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"Disk Bad Block Detected",short_desc:"The disk driver confirmed a bad block — a sector that cannot be reliably read. Immediate action required.",description:"Event ID 7 from the disk driver means the drive has a confirmed bad sector — a physical location on the disk that cannot be read even after exhausting all internal retries. This is more severe than Event 51 (paging I/O error, which may recover) and more definitive than Event 153 (retriable error). A bad block is unrecoverable at the current sector. The OS will attempt to remap the sector using the drive's spare sector pool, but the underlying cause — media degradation, mechanical damage, or NAND cell failure — is progressive. Any appearance of Event 7 means the disk is failing. Data backup should begin immediately and disk replacement should be planned.",why_it_happens:"Hard disks and SSDs maintain a pool of spare sectors to remap bad blocks. When a sector fails all read retries (in-drive and OS-level), the driver logs Event 7 and requests remapping. On an HDD this indicates physical media degradation — the magnetic coating has failed at that location. On an SSD it indicates NAND cell wear or a controller fault. The drive may continue to function for days or months after the first Event 7, but the failure is confirmed and progressive.",what_good_looks_like:"Absence is normal for a healthy drive — even one occurrence is significant. Any Event 7 means the disk has confirmed unrecoverable media damage.",common_mistakes:["Running chkdsk and thinking it fixed the problem — chkdsk marks bad sectors but the drive is still failing","Not starting a backup immediately upon seeing Event 7","Waiting for more symptoms before acting — bad blocks multiply, not stay singular","Assuming the data on the bad block was unimportant"],causes:["Physical media degradation (HDD platter surface damage)","NAND cell wear-out (SSD)","Mechanical shock or vibration damage","Overheating causing write errors that corrupt sectors permanently","Drive age — HDD sectors degrade over time under normal use"],steps:["Start a backup immediately — this drive is failing","Check SMART: reallocated sector count (attribute 5) should now be non-zero","Run chkdsk /r to mark bad sectors and attempt data recovery from affected areas: chkdsk C: /r /x","Check Event 51 and 129 nearby — if all three present, drive failure is active and accelerating","Order a replacement drive — do not wait","After data backup, consider running manufacturer diagnostic tool for a full surface scan"],symptoms:["bad sector","hard drive bad block","disk failing","chkdsk found errors","hard drive error","file system errors","disk read error","drive dying","data corruption","files corrupted"],tags:["disk","storage","bad-block","bad-sector","hardware","critical","failure","data-loss"],powershell:`# Bad Block and Disk Error Investigation
# Eventful

# Disk error events (last 30 days)
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'disk'
    Id           = @(7, 11, 51, 153)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List

# SMART — check reallocated sectors
Get-PhysicalDisk | ForEach-Object {
    $rel = $_ | Get-StorageReliabilityCounter
    [PSCustomObject]@{
        Disk         = $_.FriendlyName
        Health       = $_.HealthStatus
        ReadErrors   = $rel.ReadErrorsTotal
        WriteErrors  = $rel.WriteErrorsTotal
        Wear         = "$($rel.Wear)%"
    }
} | Format-Table -AutoSize`,related_ids:[11,51,129,153,55],ms_docs:null},{id:11,source:"disk",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Disk Controller Error",short_desc:"The disk driver detected a controller-level error on a storage device — hardware fault in the drive, cable, or controller.",description:"Event ID 11 from the disk driver indicates a controller error — the disk or the controller responsible for communicating with it returned an error that the driver could not handle through normal I/O retry. The event identifies the affected device path (e.g., \\Device\\Harddisk0\\DR0). This is distinct from Event 51 (paging I/O error) and Event 7 (bad block) — Event 11 points more specifically to a hardware communication failure rather than a media read failure. Common sources: a failing disk, a bad SATA cable, a failing SATA port on the motherboard, or an overloaded/failing disk controller. On a machine reporting random crashes or freezes, Event 11 paired with Event 51 is a strong indicator of imminent drive failure.",why_it_happens:"The disk driver communicates with drives via AHCI/NVMe commands. When the drive reports an internal error condition (not just a read retry failure, but an actual hardware error status), the driver logs Event 11. Causes include: the drive reporting an unrecoverable command error, the SATA/NVMe interface experiencing signal integrity issues (bad cable, bent pin, marginal power), or the drive controller itself failing.",what_good_looks_like:"Absence is normal. Any occurrence warrants investigation. Event 11 on an HDD with multiple occurrences over days or weeks means replace the drive. Event 11 on an SSD may indicate a firmware or controller issue — check for firmware updates before replacing.",common_mistakes:["Assuming it is always the drive — a bad SATA cable is a very common cause and takes 30 seconds to replace","Not checking which disk the error is on (the device path in the event)","Replacing the drive without testing the cable first — replacing the drive then getting the same error from the cable is frustrating and expensive"],causes:["Failing hard disk or SSD (most common)","Loose or failing SATA data cable","Failing SATA port on motherboard","Insufficient or noisy power to the drive","Failing disk controller chip","Drive overheating"],steps:["Identify the affected disk from the device path in the event","Reseat SATA cable at both ends — replace if possible (cheapest fix first)","Check SMART data for the identified disk","Move the drive to a different SATA port on the motherboard to rule out a bad port","Check drive temperature — Get-PhysicalDisk | Get-StorageReliabilityCounter | Select Temperature","Check Event 7 and 51 nearby — triple combination means replace immediately","Back up data before doing further testing"],symptoms:["disk error","hard drive error","controller error","drive not responding","random crashes","computer freezing","disk read write error","storage device error","sata error"],tags:["disk","storage","controller","hardware","cable","error","failure"],powershell:`# Disk Controller Error Investigation
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'disk'
    Id           = @(11, 7, 51)
    StartTime    = (Get-Date).AddDays(-14)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List

# Physical disk health
Get-PhysicalDisk | ForEach-Object {
    $rel = $_ | Get-StorageReliabilityCounter
    [PSCustomObject]@{
        Disk        = $_.FriendlyName
        Health      = $_.HealthStatus
        Temperature = "$($rel.Temperature) C"
        ReadErrors  = $rel.ReadErrorsTotal
        WriteErrors = $rel.WriteErrorsTotal
        Wear        = "$($rel.Wear)%"
    }
} | Format-Table -AutoSize`,related_ids:[7,51,129,153,55],ms_docs:null},{id:17,source:"Microsoft-Windows-WHEA-Logger",channel:"System",severity:"Error",skill_level:"Advanced",title:"Corrected Hardware Error (WHEA)",short_desc:"The CPU or memory hardware detected and silently corrected a hardware error — the system survived but a fault was logged.",description:"Event ID 17 from WHEA-Logger (Windows Hardware Error Architecture) means the CPU, RAM, or another hardware component detected an error internally and corrected it without crashing the system. The machine kept running, but the hardware had to intervene. Corrected hardware errors (also called corrected machine check errors or CMCEs) are significant because they indicate real hardware faults — faulty RAM, a degrading CPU, a PCI-E device with signal integrity issues, or a motherboard trace problem. A single Event 17 may be a transient glitch. Repeated Event 17 entries almost always precede Event 18 (fatal hardware error) and a BSOD. They are an early warning that hardware is degrading.",why_it_happens:"Modern CPUs include error-correcting circuits (ECC logic in caches, memory controllers with ECC if using ECC RAM, and Machine Check Architecture). When these circuits detect and correct a fault, they increment error counters. When the counter crosses a threshold, Windows logs Event 17. ECC RAM corrects single-bit memory errors — Event 17 is how Windows surfaces those corrections. Non-ECC RAM cannot correct errors, so the first indication of RAM failure on consumer hardware is often a crash rather than Event 17.",what_good_looks_like:"Absent on healthy hardware. Occasional Event 17 on a heavily loaded server with ECC RAM may be normal. Investigate on desktop/laptop hardware: any occurrence. Investigate on servers: increasing frequency over days or weeks.",common_mistakes:["Dismissing Event 17 because the machine is still running fine — it will not stay fine","Not running memory diagnostics after seeing Event 17 involving memory banks","Not checking CPU temperatures — thermal stress causes correctable errors before causing crashes"],causes:["Faulty or failing RAM (most common)","CPU degradation or damage (overheating, overvoltage)","Failing PCI-E device with signal integrity errors","Motherboard trace or slot fault","Overclocked system running out of stability headroom","RAM running at incorrect voltages or timings (XMP profile issues)"],steps:["Note how many Event 17 entries appear and over what timeframe — increasing frequency is urgent","Check Event 18 nearby — if both are present, hardware failure is active","Check CPU temperatures under load (HWMonitor, Core Temp)","Run Windows Memory Diagnostic: mdsched.exe — or better, Memtest86 overnight","If memory errors found: try RAM sticks one at a time to isolate the faulty module","Check BIOS — disable XMP/DOCP profiles and run RAM at stock speeds as a test","If CPU-related: check thermal paste, check for physical damage"],symptoms:["random crashes","random blue screens","hardware error","memory error","cpu error","system instability","bsod different errors each time","random bsod codes"],tags:["hardware","whea","ram","cpu","memory","error-correction","crash","instability"],powershell:`# WHEA Hardware Error Investigation
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-WHEA-Logger'
    Id           = @(17, 18)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id,
        @{N='Severity'; E={ if ($_.Id -eq 18) {'FATAL'} else {'CORRECTED'} }},
        LevelDisplayName |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize

# Check event frequency (rising count = accelerating failure)
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-WHEA-Logger'
    Id           = @(17, 18)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue | Measure-Object | Select-Object Count`,related_ids:[18,41,1001],ms_docs:"https://learn.microsoft.com/en-us/windows-hardware/drivers/whea/windows-hardware-error-architecture-overview"},{id:18,source:"Microsoft-Windows-WHEA-Logger",channel:"System",severity:"Critical",skill_level:"Advanced",title:"Fatal Hardware Error (WHEA)",short_desc:"An unrecoverable hardware error caused the system to crash — RAM, CPU, PCI-E, or firmware fault.",description:"Event ID 18 from WHEA-Logger is a Critical-level event written when a hardware component reports a fatal, unrecoverable error that forces the system to crash (BSOD). Unlike Event 41 (unexpected reboot, which covers any crash type including software) or Event 1001 (minidump, which is written after the crash), Event 18 specifically points to a hardware-level failure — the CPU, RAM, chipset, or a PCI-E device triggered a Machine Check Exception (MCE) that could not be corrected. If you see Event 18 in a log, the cause of the crash is hardware, not software. This fundamentally changes the investigation: no amount of driver updates or OS reinstalls will fix it.",why_it_happens:"The Machine Check Architecture (MCA) built into modern CPUs monitors for hardware errors. When a fatal, uncorrectable error occurs — a multi-bit memory error (uncorrectable by ECC), a CPU cache error, a PCI-E bus error, or a firmware-detected fault — the CPU triggers a Machine Check Exception. Windows cannot continue and crashes. WHEA logs Event 18 with detailed error records identifying which hardware component failed and what type of error occurred.",what_good_looks_like:"Absence is normal. Any occurrence of Event 18 means hardware failed and caused the crash — investigation is required. This is not a software problem.",common_mistakes:["Reinstalling Windows after a WHEA 18 crash — OS reinstall will not fix a hardware fault","Blaming a driver when Event 18 is present — the driver may surface the error but hardware is the root cause","Not running memory diagnostics — RAM is the most common cause of WHEA 18 on consumer hardware","Missing that overclocking causes WHEA 18 — first step on an overclocked system is reset BIOS to defaults"],causes:["Failing or failed RAM module (most common on consumer hardware)","CPU fault — physical damage, overheating, overvoltage","Failing PCI-E device (GPU, NVMe drive, network card)","Motherboard fault — trace damage, failing VRMs","Overclocking instability","Firmware/UEFI bug triggering a false hardware error report"],steps:["Confirm Event 18 exists — this changes the investigation from software to hardware","Check if system is overclocked — reset BIOS to defaults as first step","Run Memtest86 (bootable USB, run overnight) — most thorough RAM test","If Event 17 also present: hardware was degrading before the fatal event","Check CPU temperature logs — WHEA 18 from thermal damage shows sustained high temps before crash","Check Event 1001 for the bugcheck code — the WHEA-specific bugchecks are 0x124 (WHEA_UNCORRECTABLE_ERROR) and 0x19C","If RAM tests pass: test with one component at a time (swap GPU, test NVMe on another system)","Consider professional hardware diagnostics if component swap testing is not possible"],symptoms:["bsod whea uncorrectable error","blue screen 0x124","hardware crash","random blue screen","computer crashes under load","gaming causes crash","crash during heavy use","memory hardware error","cpu error crash"],tags:["hardware","whea","fatal","crash","bsod","ram","cpu","mce","critical"],powershell:`# Fatal Hardware Error Investigation
# Eventful

# WHEA fatal and corrected errors
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-WHEA-Logger'
    Id           = @(17, 18)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Sort-Object TimeCreated | Format-List

# Corresponding crash dumps
Get-WinEvent -FilterHashtable @{
    LogName = 'System'
    Id      = @(41, 1001)
    StartTime = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated | Format-List`,related_ids:[17,41,1001],ms_docs:"https://learn.microsoft.com/en-us/windows-hardware/drivers/whea/windows-hardware-error-architecture-overview"},{id:46,source:"volmgr",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Crash Dump Initialization Failed",short_desc:"Windows could not set up crash dump — no minidumps will be saved when the machine crashes.",description:"Event ID 46 from volmgr means Windows tried to configure its crash dump facility during boot and failed. The consequence: when the machine crashes (BSOD), no minidump file is written to disk. This creates a diagnostic dead end — Event 41 shows the machine crashed, but there is no Event 1001, no minidump in C:\\Windows\\Minidump, and no way to do post-crash analysis. The root cause is almost always a missing or undersized page file on the system drive. Windows requires a page file at least as large as physical RAM to capture a complete memory dump, or at least 1 MB to capture a minidump.",why_it_happens:'Windows crash dump is written to the page file on the system drive (C:) at the moment of a crash, then extracted to a .dmp file on next boot. If there is no page file, or the page file is on a different drive than the OS, or the page file is too small, the crash dump system cannot initialise and logs Event 46. Common causes: administrators disabling the page file to "improve performance" (this is a myth and also breaks crash dumps), drive running out of space causing the page file to be removed, or the OS installed to a drive without a page file configured.',what_good_looks_like:"Absence is normal. Any occurrence means you will get no crash data when the machine BSODs — fix before the next crash.",common_mistakes:["Disabling the page file thinking it improves performance — it does not, and it breaks crash dump and some applications","Putting the page file on a non-system drive — crash dump requires it on the C: drive","Not checking disk space — Windows will reduce or remove the page file if C: fills up"],causes:["Page file disabled or set to zero on system drive","Page file located on non-system drive only","Page file too small (less than 1 MB prevents even minidumps)","C: drive full — Windows removed page file automatically","Corrupt page file configuration in registry"],steps:["Check current page file: System Properties → Advanced → Performance → Settings → Advanced → Virtual Memory",'Ensure page file exists on C: drive — set to "System managed" if unsure',"Check free space on C: — page file needs room to grow: Get-PSDrive C | Select-Object Used, Free","After fixing page file: reboot and verify Event 46 is gone on next boot","To confirm crash dumps are now working: check C:\\Windows\\Minidump after next crash","Minimum for minidumps: 1 MB page file on C:. Minimum for complete memory dump: RAM size + 1 MB"],symptoms:["no minidump after crash","no crash dump","minidump folder empty","bsod no dump file","crash analysis impossible","event 41 no event 1001","page file missing"],tags:["crash-dump","minidump","page-file","bsod","diagnostic","volmgr"],powershell:`# Crash Dump Configuration Check
# Eventful

# Check for dump init failures
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'volmgr'
    Id           = 46
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Format-List

# Current page file configuration
Get-WmiObject Win32_PageFileSetting | Select-Object Name, InitialSize, MaximumSize
Get-WmiObject Win32_PageFileUsage  | Select-Object Name, CurrentUsage, PeakUsage, AllocatedBaseSize

# Free space on system drive
Get-PSDrive C | Select-Object Used, Free`,related_ids:[41,1001],ms_docs:null},{id:157,source:"disk",channel:"System",severity:"Warning",skill_level:"Beginner",title:"Disk Surprise Removed",short_desc:"A disk was physically disconnected without being safely ejected — potential data loss or corruption.",description:'Event ID 157 from the disk driver is written when a storage device is physically removed while Windows still had it mounted — a "surprise removal." This is most commonly a USB drive, external hard drive, or SD card pulled out without using "Safely Remove Hardware." It can also occur with internal drives if a SATA cable fails or a drive loses power mid-operation. The risk is data loss and file system corruption: any files with open write handles at the time of removal may be partially written, and the file system may be in an inconsistent state requiring chkdsk on next connection. On laptops with external drives used for backup, this event appearing outside of intentional removal is a sign of a failing cable or enclosure.',why_it_happens:"Windows keeps storage devices mounted as long as they are connected and used. Surprise removal bypasses the normal dismount sequence — write buffers are not flushed, open file handles are not closed cleanly, and the file system journal is not committed. The disk driver detects the device has vanished from the bus and logs Event 157.",what_good_looks_like:"Absent for internal drives under all circumstances — any Event 157 on an internal disk is a hardware fault. For external drives, occasional occurrences during intentional removal without using Safely Remove are low priority. Recurring Event 157 on an external backup drive at unexpected times indicates a failing cable, enclosure, or USB port.",causes:["USB/external drive physically removed without safe eject","Failing SATA cable on internal drive losing contact","Failing drive enclosure or USB adapter","Power interruption to internal or external drive","Loose USB connection on laptop (bumped cable)","USB hub losing power under load"],steps:["Identify the affected device from the event","For external drives: run chkdsk on next connection to check file system integrity","For internal drives: immediate hardware investigation — check SATA cable, power connector","If recurring on external backup drive at unexpected times: replace the cable and enclosure","Check Event 7 and 11 alongside 157 — combined with disk read errors suggests failing drive not just cable","Enable write caching safely: only disable write caching on USB drives in Device Manager if you consistently safe-eject"],symptoms:["drive disconnected unexpectedly","usb drive removed without ejecting","external drive disappeared","backup drive disconnected","drive letter disappeared","files corrupted on usb drive","internal drive disconnected"],tags:["disk","storage","usb","removal","external","data-loss","corruption"],powershell:`# Disk Surprise Removal History
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'disk'
    Id           = @(157, 7, 11)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[7,11,51,27],ms_docs:null},{id:104,source:"Microsoft-Windows-Eventlog",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"System Event Log Cleared",short_desc:"The System event log was cleared — explains missing log history and records who did it.",description:"Event ID 104 from Microsoft-Windows-Eventlog is written to the System log immediately before it is cleared. Like Event 1102 (Security log cleared), it records the account that performed the action. For IT support this event is practically important: when a technician uploads a System log that only goes back a few days on a machine that has been running for years, Event 104 is why. It also means any disk errors, crash events, service failures, and other diagnostic data from before the clear are gone. Clearing the System log only requires local administrator rights — it is easier to do accidentally in Event Viewer than clearing the Security log.",why_it_happens:"Written immediately before the System log is wiped. Any local administrator can clear the System log through Event Viewer (right-click → Clear Log) or via wevtutil. This happens when admins clean up machines, run setup scripts, or when log management tools rotate logs.",what_good_looks_like:"Absent normally. When present: check who cleared it and confirm it was intentional. If System and Security logs were both cleared at the same time, correlate with Event 1102.",causes:["Admin clearing log in Event Viewer to free space or start fresh","Setup or imaging script clearing logs before deploying a machine","Log management tool rotating logs",'User accidentally clicking "Clear Log" in Event Viewer'],steps:["Check the user account in the event message","Note the timestamp — all diagnostic history before this time is gone","If System and Security logs both cleared at same time: also check Event 1102","Confirm with the admin whether this was intentional"],symptoms:["system log only goes back a few days","no event history","event log was cleared","missing logs","log starts from recent date","no crash history in logs"],tags:["log-clear","system","admin","history","diagnostic"],powershell:`# System Log Clear History
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Eventlog'
    Id           = 104
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[1102],ms_docs:null},{id:19,source:"Microsoft-Windows-WindowsUpdateClient",channel:"System",severity:"Info",skill_level:"Fundamental",title:"Windows Update Installed Successfully",short_desc:"A Windows Update or patch installed without error — records the KB number and update title.",description:'Event 19 from WindowsUpdateClient is logged when a Windows Update installs successfully. It records the KB article number and the full update title. This is the "all clear" counterpart to Event 20 (installation failure). Its value is audit and timeline: confirming when a specific patch was installed, verifying that a KB required by a vendor was actually applied, or determining what changed on a system before a problem started. Checking whether a system problem started immediately after a specific Event 19 is a core troubleshooting technique.',why_it_happens:"Windows Update client writes Event 19 to the System log when the installation phase of an update completes successfully. Note that some updates require a reboot to finalize — Event 19 fires when the installation is queued successfully, not necessarily when the reboot-required finalization occurs.",what_good_looks_like:"Regular Event 19 entries reflecting monthly Patch Tuesday updates. Correlate with Event 20 (failures) — if updates frequently fail and occasionally succeed, the underlying failure cause is still present.",common_mistakes:["Assuming Event 19 means the update is fully applied — some updates need a reboot to complete (check for pending reboot indicators)","Not checking whether a problem started immediately after a specific KB installed — Event 19 timestamps are the key","Looking in the Application log for Windows Update events — they appear in the System log"],causes:["Monthly Patch Tuesday cumulative update installed","Out-of-band security hotfix installed","Driver update delivered via Windows Update","Feature update installed"],steps:["Filter System log for Event 19 to see update installation history","To check if a specific KB is installed: Get-HotFix -Id KB<number>","If troubleshooting a regression: find Event 19 entries before the problem started to identify candidate KBs","To uninstall a suspect KB: wusa /uninstall /kb:<number> — only as a last resort with vendor guidance","For a complete update history: Get-WinEvent with provider WindowsUpdateClient IDs 19, 20, 43"],symptoms:["which updates installed","windows update history","when was patch installed","kb installed successfully","update installed log","patch tuesday history","what updates were applied","check windows update log","confirm patch installed","windows update audit"],tags:["windows-update","patch","installation","success","kb","maintenance"],powershell:`# Windows Update installation history
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-WindowsUpdateClient'
    Id           = @(19, 20, 43)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $type = switch ($_.Id) {
        19 { 'SUCCESS' }
        20 { 'FAILURE' }
        43 { 'STARTED' }
    }
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        Result      = $type
        Update      = $_.Message -replace '^.*?update: ', '' -replace '\r?
.*', ''
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[20,43],ms_docs:null},{id:43,source:"Microsoft-Windows-WindowsUpdateClient",channel:"System",severity:"Info",skill_level:"Fundamental",title:"Windows Update Installation Started",short_desc:"Windows Update began installing an update — useful for correlating a system change or performance impact with a specific update.",description:"Event 43 from WindowsUpdateClient records when Windows Update begins installing a specific update. It captures the KB number and update title at the moment installation starts. This event is valuable for two investigations: first, if a system experienced a performance issue, reboot, or behavioral change, correlating that time with nearby Event 43 entries often reveals which update triggered it. Second, if an update appears stuck, Event 43 without a following Event 19 (success) indicates the installation started but never completed.",why_it_happens:"Windows Update client writes Event 43 to the System log when it begins the installation phase of an update, after the download phase has completed. This is the first marker in the installation timeline; Event 19 (success) or Event 20 (failure) follow when installation completes.",what_good_looks_like:"Every Event 43 should be followed by Event 19 (success) or Event 20 (failure) within a reasonable timeframe. A lone Event 43 with no following event may indicate the update installation hung or was interrupted by a shutdown.",common_mistakes:["Not checking whether Event 43 is followed by Event 19 or 20 — a lone 43 means the installation did not complete","Not correlating system instability or reboots with Event 43 timestamps"],causes:["Windows Update service beginning scheduled patch installation","Manually triggered update installation from Settings","WSUS or SCCM pushing an update to the machine"],steps:["Filter System log for Event 43 to see when specific updates started installing","Check if a following Event 19 or 20 appears — if no follow-up event, the installation was interrupted","Correlate Event 43 timestamps with any incident or performance change reported by users","For a hung installation: check if there is a pending reboot preventing updates from completing"],symptoms:["update started installing","when did update start","patch install started","windows update started","update installation started","kb started installing","patch installation log"],tags:["windows-update","patch","installation","started","kb","maintenance"],powershell:`# Windows Update install started and result history
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-WindowsUpdateClient'
    Id           = @(19, 20, 43)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id,
        @{N='Result'; E={ switch($_.Id){ 19{'SUCCESS'} 20{'FAILURE'} 43{'STARTED'} }}},
        @{N='Update'; E={ $_.Message -replace '^.*?update: ','' -replace '\r?
.*','' }} |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[19,20],ms_docs:null},{id:36871,source:"Schannel",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Schannel — Fatal TLS/SSL Error",short_desc:"Schannel encountered a fatal error in the TLS/SSL handshake — connection failed.",description:"Event 36871 from Schannel (the Windows TLS implementation) indicates a fatal error occurred during a TLS handshake. Unlike 36882 (chain building failure), 36871 covers a broad range of handshake failures including protocol mismatch, cipher suite negotiation failure, and unexpected alert messages. The event message includes an error code that identifies the specific failure. Any HTTPS, LDAPS, SMTPS, or other TLS-protected connection that fails handshake generates this event on the receiving end.",why_it_happens:"TLS handshakes fail when the client and server cannot agree on a protocol version or cipher suite, when certificates are invalid or untrusted, when required extensions are missing, or when network disruption causes incomplete handshakes. Event 36871 is the catch-all for Schannel fatal errors.",what_good_looks_like:"No Schannel errors in System log. TLS connections negotiate TLS 1.2 or 1.3. Cipher suites are modern (AES-GCM, ChaCha20). Certificates are valid, trusted, and unexpired.",common_mistakes:["Not checking the error code in the event — different codes point to different root causes","Fixing only the server side when the issue may be a client-side protocol restriction","Not verifying both client and server support the same TLS version and cipher suites"],causes:["Protocol version mismatch — client requires TLS 1.2+ but server only offers 1.0/1.1","No common cipher suite between client and server","Certificate validation failure (expired, untrusted, wrong CN)","Network disruption causing TCP reset mid-handshake","Application sending unexpected data on a TLS socket"],steps:["Note the error code from the event message","Test TLS connectivity: Test-NetConnection -ComputerName <server> -Port 443","Check TLS versions enabled on server: Get-TLSCipherSuite","Use TestSSL or openssl s_client to diagnose protocol/cipher negotiation","Check certificate validity on the service endpoint","Review IIS/application SSL binding if relevant"],symptoms:["TLS handshake failed","SSL error connecting","HTTPS connection error","Schannel error","TLS connection failed","SSL handshake failed","cannot connect HTTPS","TLS fatal alert","secure connection failed"],tags:["tls","ssl","schannel","https","certificate","network"],powershell:`# Schannel TLS Error Investigation
# Eventful

# All Schannel errors (36871, 36874, 36887, 36888)
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Schannel'
    Level        = @(1, 2)
    StartTime    = (Get-Date).AddHours(-24)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List

# Check enabled TLS cipher suites
Get-TlsCipherSuite | Select-Object Name, Exchange, Cipher, Hash | Format-Table -AutoSize`,related_ids:[36874,36882,36887,36888],ms_docs:"https://learn.microsoft.com/en-us/windows-server/security/tls/schannel-security-support-provider-technical-reference"},{id:36874,source:"Schannel",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Schannel — TLS Connection Request Rejected (Protocol Not Supported)",short_desc:"TLS connection rejected because the client requested a protocol version the server does not support.",description:"Event 36874 is generated when Schannel rejects an incoming TLS connection because the client is using a TLS/SSL version that has been disabled on the server. This is common after hardening SSL/TLS settings — for example, disabling TLS 1.0/1.1 on Windows Server 2022 breaks legacy clients still using those versions. The event message identifies the rejected protocol version.",why_it_happens:"When Windows Server TLS hardening is applied (via Group Policy or IIS Crypto), older TLS versions are disabled. Clients — particularly legacy applications, old network printers, older Windows versions, and embedded devices — may only support the disabled versions. When they attempt to connect, Schannel rejects the handshake and logs 36874.",what_good_looks_like:"No 36874 events after TLS hardening. All clients support TLS 1.2+. Use TLS 1.2 for all connections.",common_mistakes:["Disabling TLS 1.0/1.1 without first auditing what clients are using those protocols","Not checking the client-side — the client also needs TLS 1.2 enabled and .NET may need updating","Forgetting that LDAP over TLS (LDAPS port 636) can also be affected"],causes:["Server disabled TLS 1.0 or TLS 1.1 but client only supports those versions","Old application hard-coded to use a specific SSL/TLS version","Legacy printer, scanner, or IoT device using old TLS","Client .NET Framework version does not support TLS 1.2 without registry fix","Client LDAPS connection using SSL 3.0 or TLS 1.0"],steps:["Check if TLS hardening was recently applied on the server","Identify the client from Event 36874 message or network capture","Test which TLS versions the client supports using openssl or browser devtools","For Windows clients: enable TLS 1.2 in .NET and WinHTTP if not enabled","For legacy devices: evaluate if they can be updated or replaced","For applications: check vendor support for TLS 1.2 and update if available"],symptoms:["TLS protocol not supported","SSL protocol mismatch","connection rejected after TLS hardening","legacy app cannot connect after update","printer not connecting after security update","TLS 1.0 disabled connection error","old application SSL error","LDAPS connection failing"],tags:["tls","ssl","schannel","protocol-mismatch","hardening","legacy"],powershell:`# Check TLS versions enabled on Windows Server
# Eventful

# Check registry for enabled/disabled TLS protocol versions
$protocols = @('SSL 2.0','SSL 3.0','TLS 1.0','TLS 1.1','TLS 1.2','TLS 1.3')
foreach ($proto in $protocols) {
    $path = "HKLM:SYSTEMCurrentControlSetControlSecurityProvidersSCHANNELProtocols$protoServer"
    $enabled = if (Test-Path $path) {
        (Get-ItemProperty $path -ErrorAction SilentlyContinue).Enabled
    } else { 'not set (default)' }
    [PSCustomObject]@{ Protocol = $proto; ServerEnabled = $enabled }
} | Format-Table -AutoSize`,related_ids:[36871,36887,36888],ms_docs:"https://learn.microsoft.com/en-us/windows-server/security/tls/tls-registry-settings"},{id:36887,source:"Schannel",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Schannel — Fatal Alert Received from Remote",short_desc:"The remote party sent a TLS fatal alert, terminating the connection.",description:"Event 36887 is logged when the remote server or client sent a TLS fatal alert code, which causes the TLS connection to terminate immediately. The event includes the alert code — common codes include 40 (handshake failure), 42 (bad certificate), 44 (certificate revoked), 45 (certificate expired), 46 (certificate unknown), 48 (unknown CA). This indicates the remote side rejected the local certificate or handshake parameters.",why_it_happens:"The remote party rejects the TLS handshake when the presented certificate fails validation, the CA is not trusted, the certificate is revoked, or the cipher suites do not match. Alert code 40 (handshake_failure) is the most common and can mean many things; alert code 45 (certificate_expired) specifically means the local certificate presented to the remote is expired.",what_good_looks_like:"No 36887 events. Local certificates are valid, trusted by remote parties, and renewed before expiry.",common_mistakes:["Looking only at the local server for the problem when 36887 means the REMOTE party rejected this server","Not reading the alert code — it narrows down the failure category significantly","Not checking if the service's bound certificate has expired (alert 45 = certificate_expired)"],causes:["Local service certificate expired — remote client rejects it","Local certificate CA not trusted by the remote party","Certificate CN/SAN does not match the hostname being connected to","Certificate revoked and remote performs CRL check","Cipher suite mismatch (alert 40 = handshake_failure)","Mutual TLS required but local certificate is wrong type"],steps:["Note the alert code from the event message","Alert 45: local certificate expired — check certlm.msc for the service binding","Alert 42/44/46: certificate issue — verify local cert is valid, trusted, and correctly bound","Alert 40: cipher mismatch — compare cipher suites with the remote party","Check IIS/service certificate binding if this is a web server","Run: certutil -verify -urlfetch <certfile> on the local certificate"],symptoms:["TLS fatal alert received","certificate rejected by remote","TLS handshake failure alert","remote rejected certificate","TLS connection dropped by remote","certificate error connecting to remote","SSL alert code 40","SSL alert code 45 certificate expired"],tags:["tls","ssl","schannel","certificate","alert","handshake"],powershell:`# Schannel Fatal Alert Investigation
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Schannel'
    Id           = 36887
    StartTime    = (Get-Date).AddHours(-24)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Sort-Object TimeCreated -Descending | Format-List

# Check local machine certificate expiry (service binding)
Get-ChildItem Cert:LocalMachineMy |
    Select-Object Subject, NotAfter, Thumbprint,
        @{N='DaysLeft'; E={ [int](($_.NotAfter - (Get-Date)).TotalDays) }} |
    Sort-Object DaysLeft | Format-Table -AutoSize`,related_ids:[36871,36874,36888,36882],ms_docs:"https://learn.microsoft.com/en-us/windows-server/security/tls/schannel-security-support-provider-technical-reference"},{id:36888,source:"Schannel",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Schannel — Fatal Alert Generated (Sent to Remote)",short_desc:"Schannel generated and sent a TLS fatal alert to the remote — this system rejected the connection.",description:"Event 36888 is the mirror of 36887 — it is logged when this system (not the remote party) sends a TLS fatal alert and terminates the handshake. This means the local Schannel rejected the remote party's certificate or TLS parameters. Alert codes are the same as 36887 but now this is the local system doing the rejecting. Common cause: the remote server's certificate is not trusted by the local machine's certificate store.",why_it_happens:"Schannel sends fatal alerts when it cannot validate the remote party's certificate, when the certificate chain cannot be built, when the CRL is unavailable and hard-fail CRL checking is configured, or when the protocol or cipher suite is unacceptable.",what_good_looks_like:"No 36888 events. All servers this machine connects to have valid, trusted certificates.",common_mistakes:["Confusing 36887 (remote rejected us) with 36888 (we rejected remote) — the direction of the alert matters for troubleshooting","Not checking if the remote server's root CA is in the local Trusted Root store","Hard-fail CRL checking enabled but CRL is temporarily unreachable, causing false rejections"],causes:["Remote server's certificate is not trusted (unknown CA)","Remote server's certificate has expired","CRL checking required but CRL is offline","Remote server's certificate CN/SAN does not match hostname","Hard-fail CRL checking failing due to network issue"],steps:["Identify the remote server from application logs or network captures","Test certificate of remote server: certutil -verify -urlfetch <serverName>","Check if remote CA root is in Trusted Root store: certlm.msc → Trusted Root CAs","Check CRL reachability from this machine","If CRL is causing false failures: check WinHTTP proxy settings for CRL fetch"],symptoms:["this server rejecting remote certificate","TLS alert sent to remote","server not trusted by local","certificate chain not trusted","TLS rejected remote connection","Schannel generated fatal alert","application cannot connect TLS rejected"],tags:["tls","ssl","schannel","certificate","trust","crl"],powershell:`# Schannel Generated Alert Investigation
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Schannel'
    Id           = 36888
    StartTime    = (Get-Date).AddHours(-24)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Sort-Object TimeCreated -Descending | Format-List

# Check trusted root CAs (to verify if remote CA is trusted)
Get-ChildItem Cert:LocalMachineRoot |
    Select-Object Subject, NotAfter |
    Sort-Object Subject | Format-Table -AutoSize`,related_ids:[36871,36874,36887,36882],ms_docs:"https://learn.microsoft.com/en-us/windows-server/security/tls/schannel-security-support-provider-technical-reference"},{id:7022,source:"Service Control Manager",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Service Hung on Start",short_desc:"A service did not respond to the start request within the timeout period.",description:'Event 7022 is logged by the Service Control Manager when a service was sent the "start" command but did not transition to the Running state within the service start timeout (typically 30 seconds). The service is likely still starting or is stuck — it has not crashed, but it has not confirmed successful startup either. This is often seen with services that have long initialization (database validation, large cache warm-up) or are stuck on a dependency that is not available.',why_it_happens:"Services must notify SCM that they have started successfully within a timeout window. Services that perform heavy initialization work, wait for network resources, or have dependency deadlocks may exceed this window. The service may still eventually start, or it may be stuck indefinitely waiting for an external resource.",what_good_looks_like:"Services start and report running within 30 seconds. No 7022 events. System startup completes without hung services delaying boot.",common_mistakes:["Assuming the service is fully stopped when 7022 appears — it may still be running or in a starting state","Not checking the service's dependencies — a hung service often means a dependency (e.g., SQL Server, Active Directory) is unavailable","Not reviewing the Application log for errors from the service around the same time"],causes:["Service waiting for a dependent resource (database, network share, AD) that is offline","Service performing heavy initialization (large data import, cache warm-up)","Deadlock in service startup code","Service waiting on user input or interactive desktop","System under extreme resource pressure at startup"],steps:["Check the service status: Get-Service <ServiceName>","Check service dependencies: Get-Service <ServiceName> | Select-Object -ExpandProperty ServicesDependedOn","Review Application log for errors from the service","Check System log for 7001 (dependency failed) around the same time","If service is stuck: Stop-Service -Force <ServiceName>, investigate, restart","Increase service start timeout if initialization legitimately takes longer: HKLM\\SYSTEM\\CurrentControlSet\\Control → ServicesPipeTimeout (ms)"],symptoms:["service hung on start","service not starting","service stuck starting","service timeout on start","service not responding to start","service slow to start","service start hung"],tags:["service","startup","timeout","scm","hung","boot"],powershell:`# Service Start Hung Investigation
# Eventful

# Recent service hung events
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = @(7022, 7023)
    StartTime    = (Get-Date).AddDays(-1)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending | Format-List

# Check services currently not running (may still be hung)
Get-Service | Where-Object { $_.Status -ne 'Running' -and $_.StartType -eq 'Automatic' } |
    Select-Object Name, Status, StartType | Format-Table -AutoSize`,related_ids:[7001,7023,7034],ms_docs:"https://learn.microsoft.com/en-us/windows/win32/services/service-control-manager"},{id:7043,source:"Service Control Manager",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Service Did Not Respond to Control Request",short_desc:"SCM sent a stop/pause control to a service but the service did not respond in time.",description:"Event 7043 is logged when the Service Control Manager sends a stop, pause, or other control request to a service and the service does not acknowledge it within the control timeout. The service is not cooperating with the shutdown request. This causes system shutdown to hang waiting for the service, and may cause data corruption if the service was mid-transaction when the stop was requested.",why_it_happens:"Services that receive a STOP control must finish in-progress work and signal SCM within the timeout. Services stuck on long-running database commits, network operations, or those that have deadlocked will fail to respond. This is especially problematic during Windows Updates and system shutdowns.",what_good_looks_like:"Services stop cleanly within a few seconds of receiving a STOP control. No 7043 events during normal shutdowns.",common_mistakes:["Not investigating why the service was not stopping — it usually means it was stuck on an operation","Using -Force on Stop-Service without understanding why it's not stopping — this can cause data loss"],causes:["Service stuck waiting on a network operation at shutdown time","Deadlock in service code during cleanup","Service waiting on a database commit that cannot complete","Service not properly handling SERVICE_STOP control message"],steps:["Check Application log for errors from the service around the stop time","Use Task Manager or Process Monitor to see what the service process was doing","Check for associated database or file handle holds","Review service vendor documentation for known shutdown issues","If recurring: consider increasing the service shutdown timeout in registry"],symptoms:["service not stopping","service not responding to stop","shutdown taking too long","service hung on stop","service stop timeout","Windows update hanging on service","system shutdown slow service"],tags:["service","shutdown","timeout","scm","stop","hung"],powershell:`# Service Stop Timeout Investigation
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = 7043
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Sort-Object TimeCreated -Descending | Format-List`,related_ids:[7022,7034,7031],ms_docs:"https://learn.microsoft.com/en-us/windows/win32/services/service-control-manager"},{id:5719,source:"NETLOGON",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Netlogon — No Domain Controller Available for Secure Channel",short_desc:"The computer cannot establish a secure channel with any domain controller — domain connectivity broken.",description:"Event 5719 from NETLOGON is generated when a Windows computer cannot establish a secure channel with any domain controller. The secure channel is used for authentication pass-through, Group Policy, SYSVOL access, and password changes. When 5719 is present, the computer is effectively disconnected from the domain — users may log in with cached credentials, Group Policy will not apply, and domain-authenticated services may fail. This event is one of the most important indicators of domain connectivity problems.",why_it_happens:'Secure channel establishment fails when no DC can be reached (DNS failure, network issue), when the computer account password has become out of sync with Active Directory (machine account password issue), or when the secure channel is broken. Symptoms include: "trust relationship between this workstation and the primary domain failed" error, users unable to log in with domain credentials, and Group Policy not applying.',what_good_looks_like:"No 5719 events. The machine can reach a DC: nltest /dsgetdc:<domain> returns a DC. Net logon service running and secure channel verified: nltest /sc_verify:<domain>.",common_mistakes:["Assuming the machine account password is the problem without first checking DNS and network connectivity to DCs","Rejoining the domain as the first troubleshooting step — this is the nuclear option; try resetting the secure channel first","Not checking if DNS can resolve the domain name — 5719 is often caused by DNS failure, not account issues"],causes:["No domain controller reachable — DNS failure or network connectivity issue","Machine account password is out of sync with Active Directory","Computer object deleted or disabled in AD","Firewall blocking Kerberos/LDAP ports to domain controllers","Time skew > 5 minutes between workstation and DC (Kerberos requires time sync)"],steps:["Check DNS: Resolve-DnsName <domain.fqdn> — must return DC IPs","Check DC connectivity: Test-NetConnection -ComputerName <dcName> -Port 88 (Kerberos)","Verify time sync: w32tm /query /status — skew must be < 5 minutes","Test secure channel: nltest /sc_verify:<domain>","Reset secure channel if broken: Reset-ComputerMachinePassword -Credential (Get-Credential)","If computer object deleted: rejoin the domain (last resort)",'Check 5720 event — "computer account password changed" — to correlate with sync issues'],symptoms:["trust relationship failed","domain not available","cannot log in with domain account","this computer trust relationship has failed","domain controller not available","secure channel broken","domain disconnected","workstation domain trust failed","cannot contact domain controller","group policy not applying domain"],tags:["domain","netlogon","secure-channel","active-directory","trust","authentication"],powershell:`# Netlogon Secure Channel Diagnostics
# Eventful

# Recent Netlogon errors
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'NETLOGON'
    Id           = @(5719, 5805, 5722)
    StartTime    = (Get-Date).AddDays(-1)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending | Format-List

# Test secure channel (run on the affected machine)
nltest /sc_verify:$env:USERDNSDOMAIN

# Find nearest DC
nltest /dsgetdc:$env:USERDNSDOMAIN

# Check time sync
w32tm /query /status`,related_ids:[5720,7023,4625],ms_docs:"https://learn.microsoft.com/en-us/troubleshoot/windows-server/active-directory/how-domain-controllers-are-located"},{id:219,source:"Microsoft-Windows-Kernel-PnP",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"PnP Driver Installation Failed",short_desc:"Windows could not install or load a driver for a device — device may not function.",description:"Event 219 from Microsoft-Windows-Kernel-PnP is logged when Windows Plug and Play cannot find, install, or load an appropriate driver for a device. The device will appear in Device Manager with a yellow exclamation mark (error code 28 = no drivers, code 43 = driver error, code 10 = cannot start). This affects hardware devices including network adapters, storage controllers, USB devices, and printers — any of which can cause significant functionality problems if the correct driver is missing.",why_it_happens:"Driver installation failures occur when the correct driver is not present in the Windows driver store, when a driver update corrupts or replaces a working driver, when the device vendor's driver is not compatible with the OS version, or when Windows Update delivers an incompatible driver.",what_good_looks_like:"No Event 219 in System log. Device Manager shows all devices working properly (no yellow exclamation marks). All hardware detected and functional after OS install or update.",common_mistakes:["Using Device Manager without checking Event 219 for the specific error code that explains why the driver failed","Not checking Windows Update history — a driver delivered via Windows Update may have replaced a working vendor driver","Downloading drivers from unofficial sites instead of the vendor's website"],causes:["Driver not present in Windows driver store for this hardware","Wrong or incompatible driver installed","Windows Update delivered incorrect driver","Driver files corrupted","Hardware device failing — driver loads but device returns error to driver","32-bit driver on 64-bit OS or vice versa"],steps:["Open Device Manager (devmgmt.msc) — look for yellow exclamation marks","Right-click the device → Properties → note the error code","Check Event 219 in System log for the device instance path","Download the correct driver from the hardware vendor's website","If Windows Update installed a bad driver: Device Manager → driver → Update Driver → Browse My Computer → Let me pick → choose older version","Run: pnputil /add-driver <inffile> /install if installing manually","Run sfc /scannow if driver files may be corrupted"],symptoms:["device not working","driver installation failed","yellow exclamation in device manager","hardware not detected","network adapter not working after update","printer driver error","USB device not recognized driver","device driver error","cannot install driver","driver failed to load"],tags:["driver","pnp","hardware","device-manager","installation"],powershell:`# PnP Driver Failure Investigation
# Eventful

# Recent driver installation failures
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Kernel-PnP'
    Id           = 219
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Sort-Object TimeCreated -Descending | Format-List

# Devices with errors in Device Manager
Get-PnpDevice -PresentOnly |
    Where-Object { $_.Status -ne 'OK' } |
    Select-Object FriendlyName, Class, Status, ProblemCode | Format-Table -AutoSize`,related_ids:[7026,1001],ms_docs:"https://learn.microsoft.com/en-us/windows-hardware/drivers/install/device-manager-error-messages"}],r=[{id:1e3,source:"Application Error",channel:"Application",severity:"Error",skill_level:"Fundamental",title:"Application Crash (Faulting Application)",short_desc:"An application crashed — records the faulting process, module, and exception code.",description:'Event ID 1000 from "Application Error" is the primary event for application crashes. It records the name and version of the crashing application, the specific DLL or module that faulted, the exception code (e.g., 0xc0000005 = access violation, 0xe0434352 = .NET exception), and the offset within the module where the fault occurred. This event is generated for any crash in user-mode that Windows Error Reporting (WER) captures — not kernel crashes (those are Event 41/1001 in System log). It is your starting point for any "application keeps crashing" investigation.',why_it_happens:"When an application throws an unhandled exception — typically an access violation (reading/writing invalid memory), a null pointer dereference, a stack overflow, or an uncaught software exception — Windows Error Reporting intercepts the crash, creates a minidump, and logs Event 1000. The faulting module identifies which specific library caused the crash, which is often more useful than the application name since third-party DLLs frequently cause crashes in the host process.",what_good_looks_like:"Occasional crashes during extreme edge cases are normal for complex applications. Investigate: repeated crashes at the same offset in the same module (reliable reproduction means there is a deterministic bug), crashes beginning after an update (regression), crashes of critical business applications affecting productivity, crashes caused by known-malicious modules.",common_mistakes:["Only looking at the application name and ignoring the faulting module — the faulting module is usually the actual culprit","Not correlating with 1001 (WER bucket) or 1002 (hang) for related events","Trying to fix the application without checking for updates first — many crashes are fixed by vendor patches","Not checking if antivirus is injecting into the process (shell extensions and AV DLLs frequently cause crashes in other apps)","Ignoring that 0xe0434352 means a .NET exception — look for Event 1026 for more detail"],causes:["Bug in application code causing access violation or unhandled exception","Incompatible third-party DLL injected into process (shell extensions, AV)","Corrupt application installation","Missing or incompatible dependency (DLL, .NET runtime version)","Hardware memory fault causing random corruption","Application updated and introduced a regression","Incompatible Windows update affecting application APIs"],steps:["Filter Application log for Event 1000",'Note the "Faulting Application Name" and "Faulting Module Name"','Note the "Exception Code": 0xc0000005 = access violation, 0xe0434352 = .NET unhandled',"If faulting module is the app itself: check vendor for updates or known issues","If faulting module is a third-party DLL: investigate if it is an AV, shell extension, or inject","Check for pending Windows Update that may include a fix","If .NET exception (0xe0434352): check Event 1026 for more detail","Reproduce the crash and capture a full dump with ProcDump: procdump -e 1 <pid> C:\\crashes\\"],symptoms:["application crashed","program keeps crashing","app crashes","application keeps closing","program stopped working","this application has stopped working","application error popup","program crashes randomly","app closes itself","software keeps crashing"],tags:["crash","application","exception","faulting-module","wer","reliability"],powershell:`# Application Crash Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed
$appName   = ''  # Optional: filter by app name, e.g. 'outlook.exe'

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Application'
    Id        = 1000
    StartTime = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated      = $_.TimeCreated
        FaultingApp      = $data[0].'#text'
        AppVersion       = $data[1].'#text'
        FaultingModule   = $data[3].'#text'
        ModuleVersion    = $data[4].'#text'
        ExceptionCode    = $data[6].'#text'
        FaultOffset      = $data[7].'#text'
    }
} | Where-Object { -not $appName -or $_.FaultingApp -like "*$appName*" } |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[1001,1002,1026,7031],ms_docs:"https://learn.microsoft.com/en-us/windows/win32/wer/about-wer"},{id:1001,source:"Windows Error Reporting",channel:"Application",severity:"Info",skill_level:"Intermediate",title:"Windows Error Reporting: Fault Bucket",short_desc:"WER grouped a crash into a fault bucket for reporting — may include Watson report details.",description:"Event ID 1001 from Windows Error Reporting (note: this is Application log, different from the System log 1001 which is BugCheck) records a fault bucket assignment for a crash. WER groups similar crashes by fault signature (Bucket ID) so that crash patterns can be identified. The Bucket ID can be used to look up known issues in Microsoft's crash database. This event may appear alongside 1000 (crash) and provides additional context including whether a crash report was sent to Microsoft and whether a solution exists.",why_it_happens:"After WER captures a crash (triggering Event 1000), it analyses the crash data and assigns it to a fault bucket based on the crash signature. If crash reporting is enabled, WER may send the crash to Microsoft's crash analysis service. Event 1001 records this bucket assignment and any solutions found.",what_good_looks_like:"Look for the Bucket ID when searching Microsoft knowledge base or support forums — the ID can help find relevant patches or workarounds. If a solution is available, WER may display it in Action Center.",common_mistakes:["Ignoring this event because it looks informational — the Bucket ID is useful for research","Not checking Windows Action Center for WER-recommended solutions"],causes:["Generated automatically by WER alongside any crash event","Triggered for both application crashes and non-fatal faults"],steps:["Find Event 1001 that corresponds to a known crash (same time as 1000)","Note the Bucket ID","Search Microsoft support and knowledge base with the Bucket ID","Check Windows Action Center for any recommended solutions"],symptoms:["windows error reporting","fault bucket","crash report","wer report","watson report","error reporting crash"],tags:["wer","crash","fault-bucket","reporting","watson"],powershell:`# Windows Error Reporting Fault Bucket Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'Windows Error Reporting'
    Id           = 1001
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List`,related_ids:[1e3,1002,1026],ms_docs:"https://learn.microsoft.com/en-us/windows/win32/wer/about-wer"},{id:1002,source:"Application Hang",channel:"Application",severity:"Error",skill_level:"Fundamental",title:"Application Hang",short_desc:"An application stopped responding and Windows terminated it or it was killed by the user.",description:'Event ID 1002 from "Application Hang" is generated when a GUI application stops processing messages on its main thread for more than 5 seconds, causing the "(Not Responding)" state. Unlike 1000 (crash), the application has not crashed — it is alive but stuck. Windows may generate this event when the user clicks "Close" on a hung window and confirms termination, or after an extended period of unresponsiveness. The event records the application name, version, and the time it stopped responding.',why_it_happens:"Windows applications use a message loop on the main thread to handle user input and window updates. If the main thread is blocked — waiting on a slow file I/O, a network call, a database query, a mutex, or an infinite loop — it cannot process messages, and the window goes (Not Responding). Windows detects this by sending a WM_NULL message and waiting for a response. If no response comes within 5 seconds, the application is considered hung.",what_good_looks_like:"Occasional hangs during heavy operations are tolerable. Investigate: repeated hangs of the same application at the same operation, hangs affecting many users simultaneously, hangs that began after an update or configuration change, hangs during operations that involve file servers or databases (may indicate network/storage latency).",common_mistakes:["Treating hangs and crashes identically — hung processes often have different root causes (I/O blocking, deadlock) vs crashes (memory corruption)","Not checking if the hang correlates with disk or network latency events","Not capturing a dump of the hung process to see which thread is blocked and what it's waiting for","Ignoring that Outlook hangs often correlate with Exchange or network issues, not Outlook bugs"],causes:["Main thread blocked on slow I/O (disk, network, database)","Deadlock between two threads","Infinite loop in application code","Waiting on a COM object or shell extension that is hung","Slow antivirus scanning during file access","Memory pressure causing heavy paging","Remote file share latency causing synchronous operations to block"],steps:["Filter Application log for Event 1002","Note application name and time of hang","Check if hangs correlate with disk, network, or database events in System log","Check resource usage at time of hang: CPU, RAM, disk queue length","If recurring: capture hung process dump with Task Manager (Create dump file)","Analyse dump with WinDbg: !analyze -v, ~* k (all thread stacks)","Check if antivirus is scanning the directories the app accesses","Test network path latency if the app accesses file shares"],symptoms:["application not responding","app hangs","program freezes","not responding","application froze","outlook hangs","program stuck","app freezes","application becomes unresponsive","software stops responding"],tags:["hang","application","frozen","not-responding","reliability","performance"],powershell:`# Application Hang Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed
$appName   = ''  # Optional: filter by app name, e.g. 'outlook.exe'

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'Application Hang'
    Id           = 1002
    StartTime    = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $data = $xml.Event.EventData.Data
    [PSCustomObject]@{
        TimeCreated  = $_.TimeCreated
        Application  = $data[0].'#text'
        AppVersion   = $data[1].'#text'
        HangType     = $data[4].'#text'
    }
} | Where-Object { -not $appName -or $_.Application -like "*$appName*" } |
    Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[1e3,1001,1026],ms_docs:"https://learn.microsoft.com/en-us/windows/win32/wer/about-wer"},{id:1026,source:".NET Runtime",channel:"Application",severity:"Error",skill_level:"Intermediate",title:".NET Runtime Error",short_desc:"A .NET application threw an unhandled exception — includes full exception type and stack trace.",description:'Event ID 1026 from the ".NET Runtime" source is logged when a .NET application encounters an unhandled exception and crashes. Unlike Event 1000 (which gives low-level Windows crash data), Event 1026 provides the full .NET exception type (e.g., System.NullReferenceException, System.OutOfMemoryException), the full stack trace in managed code, and the thread that threw the exception. This is extremely valuable for .NET developers and MSPs dealing with business application crashes.',why_it_happens:"When a .NET application throws an exception that is not caught anywhere in the call stack, the CLR (Common Language Runtime) invokes the unhandled exception handler. If no application-level handler is registered, the CLR logs Event 1026 with the full exception information and then terminates the process. This also generates a companion Event 1000 (Application Error) with exception code 0xe0434352.",what_good_looks_like:"No Event 1026 events for production applications. In development or test environments, they are acceptable during debugging. For MSP-managed clients: 1026 for a business application (ERP, accounting, line-of-business) is a vendor support escalation item.",common_mistakes:["Looking only at Event 1000 for .NET crashes — 1000 gives exception code 0xe0434352 (useless) but 1026 gives the real exception type and stack","Not providing the stack trace to the software vendor — they need 1026 detail to diagnose the issue",'Missing that "System.OutOfMemoryException" needs memory investigation, not application reinstall'],causes:[".NET application bug — null reference, argument out of range, etc.","Out of memory (System.OutOfMemoryException)","Corrupt application installation or missing assembly","Incompatible .NET runtime version","Database connection failure causing exception in data access layer","File system permission error throwing UnauthorizedAccessException"],steps:["Filter Application log for Event 1026","Read the full event message — it contains the exception type and stack trace","Note the exception type: NullReferenceException, OutOfMemoryException, etc.","Note the innermost exception and the stack frame it occurred in","If OutOfMemoryException: investigate available memory and application memory usage","Provide the full exception and stack trace to the software vendor","Check for application updates that may have fixed the bug","Correlate with .NET version: [System.Runtime.InteropServices.RuntimeInformation]::FrameworkDescription"],symptoms:[".net error","dotnet crash",".net application crashed",".net runtime error","net framework error","application error 1026","managed code crash","c# application crash","asp.net error",".net exception"],tags:["dotnet","runtime","exception","application-crash","managed-code"],powershell:`# .NET Runtime Error Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddDays(-7)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = '.NET Runtime'
    Id           = 1026
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message |
    Sort-Object TimeCreated -Descending |
    Format-List`,related_ids:[1e3,1001,1002],ms_docs:"https://learn.microsoft.com/en-us/dotnet/core/tools/dotnet-run"},{id:1530,source:"Microsoft-Windows-User Profiles Service",channel:"Application",severity:"Warning",skill_level:"Beginner",title:"User Profile Registry Still In Use at Logoff",short_desc:"Windows could not cleanly unload a user's registry hive at logoff — can cause slow logins, profile corruption, and temporary profiles.",description:`Event ID 1530 from the User Profiles Service is generated when a user logs off but Windows cannot fully unload their registry hive (NTUSER.DAT) because one or more processes still have it open. Windows logs the offending processes in the event details. The immediate consequence is that the profile is not cleanly saved — on the next login, Windows may load a temporary profile instead of the user's real profile, causing the user to lose desktop settings, saved passwords, and application preferences. This event is responsible for the common support complaint: "all my settings are gone and my desktop is blank." Recurring Event 1530 means the offending process should be identified and addressed.`,why_it_happens:"When a user logs off, Windows attempts to unload the user registry hive. If any process (antivirus, backup agent, indexing service, or a misbehaving application) still has a handle to any key in HKEY_CURRENT_USER, the unload fails. Windows proceeds with logoff but cannot flush the hive cleanly. Subsequent logins may find the hive locked and load a fresh temporary profile instead. Common offenders: antivirus real-time scanning, Outlook holding its profile key, Windows Search indexing, and background sync agents.",what_good_looks_like:'Occasional single occurrences (e.g., during a forced logoff) are low priority. Investigate: recurring Event 1530 for the same user, users reporting blank desktops or missing settings after login, or Event 1530 immediately before a user reports a "temporary profile" login.',common_mistakes:["Rebuilding the user profile without first finding and fixing the root cause — Event 1530 will keep occurring","Not reading the event details — the offending process is listed in the event, which tells you exactly what to fix","Not restarting the offending service before attempting profile repair"],causes:["Antivirus scanning NTUSER.DAT at logoff","Outlook or Office holding profile registry keys open","Windows Search (SearchIndexer) indexing the profile","Backup agent with handles to user registry","Application crashed and left handles open","Remote desktop session not cleanly terminated"],steps:["Read the event details — it lists the process(es) holding the registry hive open","If it is antivirus: add NTUSER.DAT to the AV exclusion list, or configure the AV to release handles at logoff","If it is SearchIndexer: restart the Windows Search service or rebuild the index","If the user is already getting temporary profiles: copy their real profile data from C:\\Users\\<username>.bak","To force a clean profile copy: log in as admin, copy settings from the old profile to the new one","For recurring issue: use Process Monitor (Sysinternals) filtered to NTUSER.DAT to catch the offending process in real time"],symptoms:["blank desktop after login","all settings gone","temporary profile","desktop is empty","profile not loading","settings reset after reboot","user profile error","slow login","my documents missing","preferences lost"],tags:["profile","registry","logoff","login","settings","temporary-profile","corruption"],powershell:`# User Profile Registry Issue Investigation
# Eventful

# Recent profile unload failures
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'Microsoft-Windows-User Profiles Service'
    Id           = 1530
    StartTime    = (Get-Date).AddDays(-14)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending | Format-List

# Check for temporary profiles loaded currently
Get-WmiObject Win32_UserProfile |
    Where-Object { $_.Special -eq $false } |
    Select-Object LocalPath, Loaded, LastUseTime, Status |
    Format-Table -AutoSize`,related_ids:[1e3],ms_docs:"https://learn.microsoft.com/en-us/troubleshoot/windows-client/user-profiles-and-logon/fix-user-profile-corrupted"},{id:1008,source:"Microsoft-Windows-Perflib",channel:"Application",severity:"Warning",skill_level:"Beginner",title:"Performance Counter Provider Error (Usually Harmless)",short_desc:"A performance counter provider returned an error. Appears constantly in most Application logs — almost always harmless noise.",description:"Event ID 1008 from Perflib (Performance Library) is generated when a performance counter provider — a component that feeds data to Performance Monitor, Task Manager, or third-party monitoring tools — returns an error or fails to respond. Like Event 10016 (DCOM), this event appears in nearly every Windows Application log and is almost never the cause of user-reported problems. Windows has many built-in performance counter providers; some are buggy or register but provide no data, generating 1008 continuously. The event names the provider that failed. In the vast majority of IT support cases, 1008 is background noise. It only warrants investigation if a monitoring tool or application that specifically uses performance counters is broken.",why_it_happens:"Third-party software installs performance counter providers during installation and sometimes fails to cleanly remove them on uninstall, leaving broken registrations. Windows built-in providers can also fail if the underlying service they monitor is not running. The Perflib subsystem logs 1008 whenever it calls a provider and gets back an unexpected error or timeout.",what_good_looks_like:"Present in virtually every Windows Application log — this is normal. Only investigate 1008 if: Performance Monitor or a monitoring application that uses perf counters is broken, or the named provider matches a recently uninstalled application.",common_mistakes:["Assuming Event 1008 is causing the reported problem — it almost never is","Spending time rebuilding performance counters when the user's complaint is unrelated"],causes:["Broken performance counter registration left by uninstalled software (expected after uninstalls)","Built-in provider for a service that is stopped or disabled","Corrupted performance counter database"],steps:["Check if Event 1008 matches a recently uninstalled application — if so, ignore it","If Performance Monitor or monitoring tools are actually broken: rebuild perf counters","Rebuild performance counters: lodctr /r (run as admin from elevated command prompt)","If specific provider named: check if the associated service/application is installed and running","Otherwise: look elsewhere for the actual cause of the user's complaint"],symptoms:["performance counter error","perflib error","lots of warnings in application log","event log warnings","performance monitor not working","task manager shows 0"],tags:["performance","perflib","noise","harmless","counter","warning","common"],powershell:`# Performance Counter Error Summary
# Eventful — Usually harmless. Check which provider is named.

Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'Microsoft-Windows-Perflib'
    Id           = 1008
    StartTime    = (Get-Date).AddDays(-1)
} -ErrorAction SilentlyContinue |
    Group-Object { ($_ | Select-Object -ExpandProperty Message).Substring(0, 80) } |
    Select-Object Count, Name |
    Sort-Object Count -Descending | Format-Table -AutoSize

# Rebuild performance counters if actually needed
# Run as admin: lodctr /r`,related_ids:[1e3],ms_docs:null},{id:1033,source:"MsiInstaller",channel:"Application",severity:"Information",skill_level:"Beginner",title:"Software Installed or Removed",short_desc:'Windows Installer recorded a software installation, removal, or update — useful for correlating "started after installing X" complaints.',description:'Event ID 1033 (and related 1034, 11707) from MsiInstaller is written whenever a Windows Installer (MSI) package is installed, updated, or removed. It records the product name, version, manufacturer, and whether the operation succeeded. This event is invaluable for the common support scenario: a user says "my computer started crashing / running slowly / application stopped working — nothing changed" — checking MsiInstaller events often reveals a software install or update that happened just before the problems began. Related event 1034 covers uninstallation, and 11707/11708 cover installation success/failure in older MSI formats.',why_it_happens:"Written by the Windows Installer service (msiexec.exe) on completion of any MSI-based install or remove operation. Not all software uses MSI — modern installers (MSIX, Squirrel, Inno Setup, NSIS) may not produce this event — but most enterprise and traditional desktop software does.",what_good_looks_like:"Correlate timestamps with reported problem onset. Expected installs (Windows Update components, known software rollouts) are normal. Investigate: installs that coincide exactly with when a user says problems started, software installed outside business hours, unrecognised software, or multiple rapid installs from the same manufacturer.",causes:["User or admin installing software","Windows Update installing components via MSI","Software auto-updater running in background","IT management tool pushing a deployment","Malware installer (if software name is unrecognised)"],steps:["Filter Application log for Event 1033 and 1034 around the time problems started","Note the product name, version, and manufacturer","If a suspicious install coincides with problem onset: uninstall it and test","Cross-reference with Event 1000 (app crashes) timestamps — did crashes start after the install?","For Windows Update MSI components: check Windows Update history in Settings for the same timeframe","To see all recently installed software: Get-WmiObject Win32_Product | Sort-Object InstallDate -Descending"],symptoms:["started after installing something","problem after update","new software causing issues","software install timeline","what was installed recently","crashes after software update","application conflict"],tags:["installer","msi","software","installation","timeline","change-tracking"],powershell:`# Software Installation History
# Eventful

# MSI install/remove events (last 30 days)
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'MsiInstaller'
    Id           = @(1033, 1034, 11707, 11708)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue | ForEach-Object {
    $type = switch ($_.Id) {
        1033  { 'INSTALLED'   }
        1034  { 'REMOVED'     }
        11707 { 'SUCCESS'     }
        11708 { 'FAILED'      }
    }
    [PSCustomObject]@{
        Time    = $_.TimeCreated
        Type    = $type
        Message = $_.Message.Substring(0, [Math]::Min(120, $_.Message.Length))
    }
} | Sort-Object Time -Descending | Format-Table -AutoSize`,related_ids:[1e3,1002,11708],ms_docs:null},{id:11708,source:"MsiInstaller",channel:"Application",severity:"Error",skill_level:"Beginner",title:"Software Installation Failed",short_desc:"A Windows Installer package failed to install — records the product name and error code.",description:"Event ID 11708 from MsiInstaller is written when an MSI installation fails. It records the product name and the error code. This pairs with Event 1033 (which records successful installs) to give a complete picture of software change activity. Installation failures are relevant in IT support when: a user cannot install required software, a Windows Update component fails to install via MSI, or a deployment tool reports installation failure. The error code in the event maps to a specific failure reason. Common codes: 1603 = fatal error during installation (permissions, disk space, conflicting process), 1618 = another installation already in progress, 1619 = package file not found.",why_it_happens:"MSI installations fail for several reasons: insufficient permissions (the installer needs admin rights), the package requires a reboot from a previous install before it can proceed, disk space is insufficient, the package is corrupt, a prerequisite is missing, or antivirus is blocking the installer from writing files.",what_good_looks_like:"Absence is ideal. Occasional 11708 for background auto-updaters that retry successfully is low priority. Investigate: 11708 for software a user is actively trying to install, repeated 11708 for the same product (blocked by a persistent condition), or 11708 for a Windows component that is required for system functionality.",causes:["Installation run without administrator rights","Pending reboot blocking installation (error 1618)","Insufficient disk space on C:","Antivirus blocking installer file operations","Corrupt MSI package","Missing prerequisite (e.g., required .NET version not installed)","Conflicting software already installed"],steps:["Note the error code from the event","1603: Run installer as admin, check disk space, check AV exclusions, check for pending reboot","1618: Another installer is running — wait and retry, or reboot first","1619: Package file path is missing or inaccessible","Enable verbose MSI logging: msiexec /i package.msi /l*v install.log","Check the verbose log for the exact line that failed","Check free disk space: Get-PSDrive C | Select-Object Used, Free"],symptoms:["installation failed","software wont install","msi error","setup failed","install error 1603","cannot install application","deployment failed"],tags:["installer","msi","installation","failure","error","software"],powershell:`# Installation Failure Details
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'MsiInstaller'
    Id           = @(11708, 1033, 1034)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List

# Enable verbose MSI logging for next install attempt:
# msiexec /i "C:path	opackage.msi" /l*v "C:install-log.txt"`,related_ids:[1033,1e3],ms_docs:"https://learn.microsoft.com/en-us/windows/win32/msi/windows-installer-error-messages"},{id:11724,source:"MsiInstaller",channel:"Application",severity:"Info",skill_level:"Fundamental",title:"MSI Product Successfully Removed",short_desc:"Windows Installer completed an uninstall — records the product name and version.",description:"Event 11724 from MsiInstaller is logged when a software product is successfully uninstalled via Windows Installer. It records the product name, version, and optionally the user account that performed the uninstall. This event is useful for software audit trails, detecting unauthorized software removal, compliance tracking, and troubleshooting issues caused by accidental uninstallation.",why_it_happens:"Generated whenever Windows Installer completes a product removal, regardless of whether it was initiated by a user, an automated script, Group Policy software removal, or a silent uninstall via command line. Paired with Event 1034 (product removed in progress) and 1035 (reconfiguration).",what_good_looks_like:"Uninstall events during authorized change windows. Business-critical software not being removed outside of maintenance windows.",common_mistakes:["Not checking who performed the uninstall — the Subject field identifies the user account","Not correlating with SCCM/Intune deployment logs if automated removal was expected"],causes:["User uninstalled software from Control Panel","Automated deployment system removed software","Group Policy software removal applied","IT admin silently uninstalled via msiexec","Software conflicted with another product and was auto-removed"],steps:["Filter Application log for Event 11724","Note the product name and version — was this expected?","Check Windows Installer verbose log if available: %TEMP%\\MSI*.log","Correlate with SCCM, Intune, or PDQ Deploy deployment history","If unauthorized: check Event 4688 (process create) for msiexec around the same time"],symptoms:["software uninstalled","application removed","who uninstalled software","software gone missing","application disappeared","software audit uninstall","unauthorized software removal","program removed from PC"],tags:["msi","uninstall","software","audit","installer"],powershell:`# Software Uninstall History
# Eventful

# Recent software removals
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'MsiInstaller'
    Id           = @(11724, 1034)
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[11707,1033,1034],ms_docs:"https://learn.microsoft.com/en-us/windows/win32/msi/windows-installer-portal"},{id:455,source:"ESENT",channel:"Application",severity:"Error",skill_level:"Intermediate",title:"ESENT Database File Error",short_desc:"ESENT (Extensible Storage Engine) could not open or access a database file.",description:"Event 455 from ESENT is logged when the Extensible Storage Engine (used by Active Directory, Windows Update, Windows Search, Group Policy, and many other Windows components) encounters an error accessing its database file. The event includes the process name, database path, and an error code. Common errors: -1023 (file not found), -1811 (disk full), -510 (database is corrupted). ESENT database corruption can cause AD issues, Windows Update failures, and search index problems.",why_it_happens:"ESENT database errors occur when database files are corrupt (often after an unclean shutdown or disk failure), when the disk runs out of space preventing log file writes, when database files are moved or renamed while the service is running, or when access permissions on the database directory have changed.",what_good_looks_like:"No ESENT errors in Application log. Windows Update, AD replication, and Windows Search all functioning normally. Adequate disk space on system drive.",common_mistakes:["Ignoring ESENT errors because they seem low-level — they can indicate serious corruption in Windows Update or AD database","Not noting the process name in the event — svchost.exe hosting Windows Update has different implications than lsass.exe (AD)","Not checking disk space first — many ESENT errors are simply caused by full disk"],causes:["Database file corrupted by unclean shutdown or disk error","Disk full — ESENT cannot write transaction log files","Database files moved while service was running","Permissions changed on database directory","Hard disk failure causing I/O errors on database files"],steps:["Check disk space: Get-PSDrive C | Select-Object Used, Free","Identify the process from the event: svchost = likely Windows Update or Search; lsass = AD","For Windows Update ESENT errors: stop Windows Update, delete %WINDIR%\\SoftwareDistribution, restart service","For Windows Search: rebuild search index — Control Panel → Indexing Options → Advanced → Rebuild","For AD (ntds.dit): this is critical — follow AD database recovery procedures with ntdsutil","Run: chkdsk /f on the affected volume if disk errors are suspected"],symptoms:["ESENT database error","Windows Update database corrupt","Windows Search index error","Windows Update not working ESENT","search not working database error","ESENT error application log","database file error Windows"],tags:["esent","database","corruption","windows-update","search","active-directory"],powershell:`# ESENT Error Investigation
# Eventful

# Recent ESENT errors
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'ESENT'
    Id           = @(455, 508, 490)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending |
    Select-Object -First 10 | Format-List

# Check disk space
Get-PSDrive C | Select-Object Name, Used, Free, @{N='FreeGB'; E={[math]::Round($_.Free/1GB,1)}}`,related_ids:[508,1e3],ms_docs:"https://learn.microsoft.com/en-us/windows/win32/extensible-storage-engine/extensible-storage-engine-reference"},{id:8004,source:"Microsoft-Windows-AppLocker",channel:"Microsoft-Windows-AppLocker/EXE and DLL",severity:"Error",skill_level:"Intermediate",title:"AppLocker Blocked Executable",short_desc:"AppLocker blocked an executable (.exe or .dll) from running — policy enforcement.",description:'Event 8004 from AppLocker is generated when an executable (.exe) or DLL is blocked from running because it does not match any Allow rule in the active AppLocker policy. This event is logged in the "Microsoft-Windows-AppLocker/EXE and DLL" operational log. It captures the user, the blocked file path, the publisher information, and the file hash. AppLocker 8004 events indicate a policy enforcement action — software that should be allowed needs a new rule, or unauthorized software is being blocked as intended.',why_it_happens:"AppLocker enforces application whitelisting. Any executable that does not match a configured Allow rule is blocked with Event 8004. This occurs when a user tries to run unapproved software, when approved software is updated and the new path or hash no longer matches the rule, or when a new application is deployed without first creating an AppLocker rule for it.",what_good_looks_like:"AppLocker in Audit mode (8003 events) before enforcement. After enforcement: 8004 events only for genuinely unauthorized applications. No 8004 events for business-critical applications.",common_mistakes:["Deploying AppLocker in enforcement mode without first running in audit mode to identify what will be blocked","Not updating AppLocker rules when approved software is updated and path/hash changes","Forgetting that DLL rules (which can block core components) require careful testing before enforcement"],causes:["User attempting to run unauthorized or unapproved software","Approved software path changed after update and rule no longer matches","Application deployed without AppLocker rule being created first","Script or macro attempting to launch an executable not in the whitelist","Malware or attacker tool blocked by AppLocker"],steps:["Open Event Viewer → Applications and Services Logs → Microsoft → Windows → AppLocker → EXE and DLL","Filter for Event 8004","Note the File Path and Publisher — is this an authorized application that needs a rule?","If authorized: create an AppLocker rule — publisher rule preferred over path/hash","If unauthorized: confirm AppLocker is working as intended","Use Get-AppLockerPolicy -Effective to review current rules"],symptoms:["application blocked by AppLocker","program blocked from running","AppLocker blocking software","application whitelist blocking","exe blocked policy","cannot run application AppLocker","AppLocker rule missing","application blocked enterprise policy"],tags:["applocker","application-whitelisting","security","policy","exe","block"],powershell:`# AppLocker Block Events
# Eventful

# EXE/DLL blocks (8004) and audit (8003)
Get-WinEvent -LogName 'Microsoft-Windows-AppLocker/EXE and DLL' -ErrorAction SilentlyContinue |
    Where-Object { $_.Id -in @(8003, 8004) -and $_.TimeCreated -gt (Get-Date).AddDays(-7) } |
    Select-Object TimeCreated, Id,
        @{N='Result'; E={ if ($_.Id -eq 8004) {'BLOCKED'} else {'AUDIT-BLOCK'} }},
        Message |
    Sort-Object TimeCreated -Descending | Format-List

# Current effective AppLocker policy
Get-AppLockerPolicy -Effective | Test-AppLockerPolicy -Path "C:WindowsSystem32cmd.exe" -User Everyone`,related_ids:[8003,8006,8007],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/windows-defender-application-control/applocker/applocker-overview"},{id:8006,source:"Microsoft-Windows-AppLocker",channel:"Microsoft-Windows-AppLocker/Script",severity:"Error",skill_level:"Intermediate",title:"AppLocker Blocked Script",short_desc:"AppLocker blocked a script (.ps1, .vbs, .bat, .js) from running — policy enforcement.",description:"Event 8006 is generated when AppLocker blocks a script from executing because it does not match an Allow rule in the Script policy. Scripts covered include PowerShell (.ps1), VBScript (.vbs), batch (.bat/.cmd), and JavaScript (.js). Script blocking is a critical security control — most malware and attacker tooling uses scripts for execution. Event 8006 means the script enforcement layer is working, but may also indicate legitimate administration scripts need rules.",why_it_happens:"AppLocker Script rules apply the same publisher/path/hash logic as EXE rules but for scripting file types. Script blocking is particularly important for preventing PowerShell-based attacks and macro-driven malware. Blocked scripts generate 8006 in the Script log.",what_good_looks_like:"Only authorized scripts in approved locations (C:\\Windows\\System32, approved admin script paths). No unauthorized scripts blocked. IT scripts deployed to approved paths and covered by publisher or path rules.",common_mistakes:["Not enabling script enforcement because it breaks PowerShell remoting — test in audit mode first","Not including PowerShell script paths used by monitoring agents and RMM tools in AppLocker rules"],causes:["User attempting to run an unapproved script from Desktop or Downloads","Malware attempting to execute a downloaded PowerShell script","IT script deployed to a path not covered by AppLocker rules","Automation tool running scripts from a non-whitelisted location"],steps:["Open Event Viewer → Applications and Services Logs → Microsoft → Windows → AppLocker → Script","Filter for Event 8006","Note the Script Path — is this an authorized admin script that needs a path rule?","If unauthorized script attempt: AppLocker is working as intended","Create a path rule for approved script directories","Consider using publisher rules for signed scripts (e.g., Microsoft-signed PowerShell modules)"],symptoms:["script blocked by AppLocker","PowerShell script blocked","batch file blocked policy","VBScript blocked","script cannot run enterprise policy","AppLocker blocking scripts","admin script blocked AppLocker","automation script policy block"],tags:["applocker","script","powershell","security","policy","block"],powershell:`# AppLocker Script Block Events
# Eventful

# Script blocks (8006) and audits (8005)
Get-WinEvent -LogName 'Microsoft-Windows-AppLocker/Script' -ErrorAction SilentlyContinue |
    Where-Object { $_.Id -in @(8005, 8006) -and $_.TimeCreated -gt (Get-Date).AddDays(-7) } |
    Select-Object TimeCreated, Id,
        @{N='Result'; E={ if ($_.Id -eq 8006) {'BLOCKED'} else {'AUDIT-BLOCK'} }},
        Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[8004,8005,8007,4104],ms_docs:"https://learn.microsoft.com/en-us/windows/security/threat-protection/windows-defender-application-control/applocker/applocker-overview"},{id:1002,source:"Application Hang",channel:"Application",severity:"Warning",skill_level:"Fundamental",title:"Application Not Responding (Hang)",short_desc:"An application stopped responding to Windows for more than 5 seconds and was detected as hung.",description:'Event 1002 from "Application Hang" is generated by Windows Error Reporting when a foreground application stops responding to Windows messages for more than 5 seconds. This is distinct from a crash (Event 1000) — the application process is still running but has frozen. The event includes the application name, version, and a hang type code. Common causes include deadlocks, blocking UI thread operations, and single-threaded apps waiting on slow I/O.',why_it_happens:'Applications hang when their main UI thread gets blocked — waiting on a synchronous file read, a locked mutex, a blocking network call, or a deadlock in code. The Windows message pump stops processing, causing the "Not Responding" state. Users typically kill the app or Windows offers to close it, which generates this event.',what_good_looks_like:"No 1002 events for critical business applications. Interactive applications remain responsive. Occasional hang events for complex operations (large Excel calc, database query) are tolerable if infrequent.",common_mistakes:['Not checking if the hang correlates with slow network or disk I/O — "application hang" is often actually "application waiting for slow storage"',"Treating all 1002 events as application bugs — some hangs are caused by the environment (slow disk, network timeout)"],causes:["Application's main thread blocked on a synchronous network or disk operation","Deadlock between two threads","Anti-virus scanning files the application is trying to access","Application waiting for a COM server or external component","Large dataset being processed on the UI thread instead of a background thread"],steps:["Filter Application log for Event 1002","Note the application name and version","Correlate with disk or network slowness around the same time","If recurring: enable Process Monitor (Sysinternals) when hang occurs to capture wait chains","Check for known hang bugs in the vendor's release notes","Consider ProcDump with -h flag to capture a hang dump: procdump -h <pid> C:\\dumps\\"],symptoms:["application not responding","application freezing","program hangs","app freezes","Windows spinning wheel","application becomes unresponsive","program stops responding","application freeze","software hangs randomly"],tags:["hang","application","not-responding","freeze","wer","reliability"],powershell:`# Application Hang Investigation
# Eventful

$startTime = (Get-Date).AddDays(-7)

# Application hangs
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'Application Hang'
    Id           = 1002
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending | Select-Object -First 10 | Format-List`,related_ids:[1e3,1001,1026],ms_docs:"https://learn.microsoft.com/en-us/windows/win32/wer/wer-reference"}],o=[{id:21,source:"Microsoft-Windows-TerminalServices-LocalSessionManager",channel:"RDS",severity:"Info",skill_level:"Fundamental",title:"RDS: Session Logon Successful",short_desc:"A user successfully logged on to a Remote Desktop Services session.",description:"Event ID 21 from the TerminalServices-LocalSessionManager is generated on the RDS host when a user successfully establishes and logs on to a Remote Desktop session. It records the username, domain, session ID, and the source network address of the connecting client. This is the RDS-specific equivalent of Security Event 4624 Type 10, and provides more RDS-specific context. It is the primary event to check when verifying who has connected to an RDS server.",why_it_happens:"The Local Session Manager (LSM) on the RDS host manages all session lifecycle events. When a user completes authentication (handled by 1149 and Security events) and their session is created and initialized, the LSM logs Event 21 to record the successful logon. The session ID in this event can be used to correlate with other RDS events (22, 23, 24, 25, 40) for the full session lifecycle.",what_good_looks_like:"Expected: known users connecting during business hours from known IP ranges. Investigate: logons outside business hours for non-on-call staff, logons from unexpected IP addresses (not corporate VPN or known client locations), a user logging on to many sessions simultaneously, admin accounts logging on via RDS to servers they don't normally manage.",common_mistakes:["Only checking Security Event 4624 and missing the RDS-specific session detail in Event 21","Not using the Session ID to correlate the full session timeline (21 → 22 → 24/25/40 → 23)","Ignoring the Source Network Address field which tells you where the connection came from"],causes:["Authorised user connecting for work","Admin connecting for management","Automated process using RDP","Attacker using compromised credentials to connect","Session reconnection (look for Event 25 instead)"],steps:["Filter Microsoft-Windows-TerminalServices-LocalSessionManager log for Event 21",'Note "User", "Session ID", and "Source Network Address"',"Verify user is authorised and the source IP is expected","Track the session lifecycle using Session ID with Events 22, 24, 25, 40, 23","Correlate Source Network Address with your asset/user inventory","If suspicious: check concurrent sessions from same account or IP"],symptoms:["rdp login","remote desktop logon","rds session started","who connected via rdp","terminal services logon","remote desktop session started","rds user connected"],tags:["rdp","rds","remote-desktop","logon","terminal-services","session"],powershell:`# RDS Session Logon Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with RDS server hostname
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'Microsoft-Windows-TerminalServices-LocalSessionManager/Operational'
    Id           = 21
    StartTime    = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $ud   = $xml.Event.UserData.EventXML
    [PSCustomObject]@{
        TimeCreated    = $_.TimeCreated
        User           = $ud.User
        SessionID      = $ud.SessionID
        SourceIP       = $ud.Address
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[22,23,24,25,40,1149,4624],ms_docs:"https://learn.microsoft.com/en-us/windows-server/remote/remote-desktop-services/clients/remote-desktop-allow-outside-access"},{id:22,source:"Microsoft-Windows-TerminalServices-LocalSessionManager",channel:"RDS",severity:"Info",skill_level:"Fundamental",title:"RDS: Shell Start Notification",short_desc:"The user's shell (Explorer or application) started in their RDS session.",description:"Event ID 22 is generated when the user's shell or initial application starts within their Remote Desktop session. It typically follows Event 21 (logon) immediately and indicates the session is fully established and the user has access to their desktop environment. This event is useful for measuring session start time (time between Event 21 and 22 indicates logon processing duration) and for confirming the session progressed past simple authentication to full desktop access.",why_it_happens:"After the session is created and logon succeeds (Event 21), the Session Manager starts the user's shell process — typically explorer.exe or, in RemoteApp mode, the published application. Event 22 is logged when this process successfully initialises. A significant gap between 21 and 22 indicates slow logon processing (profile loading, Group Policy, logon scripts).",what_good_looks_like:"Normal: Event 22 appears within a few seconds of Event 21. Investigate: sessions where 22 never appears after 21 (session may be stuck in logon), large time gaps between 21 and 22 (slow logon — investigate GPO, profile, or slow profile path).",common_mistakes:["Not measuring the gap between Events 21 and 22 to diagnose slow logon times","Confusing slow shell start with slow authentication — if 21 is quick but 22 is delayed, the authentication is fine but logon is slow"],causes:["Normal session establishment following successful authentication","Slow logon scripts or Group Policy (causes delay between 21 and 22)","Large roaming profile loading slowly","Antivirus scanning profile on first load"],steps:["Filter TerminalServices-LocalSessionManager log for Event 22","Correlate with Event 21 using Session ID","Calculate time delta between 21 and 22 — more than 30 seconds warrants investigation","If slow: check GPO processing (Event 4001 in System log), profile size, and logon scripts","Use UE-V or FSLogix profiling if large profiles are causing delays"],symptoms:["rdp logon slow","remote desktop slow to load","rds desktop slow to appear","remote desktop login takes forever","terminal services slow logon","rdp session slow to start"],tags:["rdp","rds","shell","logon","performance","session"],powershell:`# RDS Shell Start Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with RDS server hostname
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'Microsoft-Windows-TerminalServices-LocalSessionManager/Operational'
    Id           = @(21, 22)
    StartTime    = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml = [xml]$_.ToXml()
    $ud  = $xml.Event.UserData.EventXML
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        EventID     = $_.Id
        EventType   = if ($_.Id -eq 21) { 'Session Logon' } else { 'Shell Started' }
        User        = $ud.User
        SessionID   = $ud.SessionID
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[21,23,24,25,40],ms_docs:null},{id:23,source:"Microsoft-Windows-TerminalServices-LocalSessionManager",channel:"RDS",severity:"Info",skill_level:"Fundamental",title:"RDS: Session Logoff",short_desc:"A user fully logged off from an RDS session — session resources released.",description:'Event ID 23 records a complete logoff from an RDS session — the user has signed out, all their session processes have terminated, and the session has been destroyed. This is distinct from a disconnect (Event 24) — a disconnect leaves the session alive. Event 23 is the definitive "session ended" event. Use it to calculate session duration (time between Event 21 and Event 23) and to confirm users are logging off rather than disconnecting (disconnected sessions consume memory on the RDS host).',why_it_happens:"When a user logs off via Start → Sign Out, closes all RemoteApp windows, or is logged off by a session policy, the LSM terminates all session processes, cleans up the session, and logs Event 23. Sessions can also be forcibly logged off by an administrator.",what_good_looks_like:"Expected: Event 23 should follow every Event 21 eventually. Users should log off, not just disconnect. Investigate: sessions with no Event 23 after disconnect (accumulating disconnected sessions), very short sessions (may indicate authentication or configuration problems), admin logging off other users' sessions.",common_mistakes:["Not distinguishing between logoff (23) and disconnect (24) — disconnected sessions linger and consume RAM","Not having session timeout policies to force logoff of long-disconnected sessions"],causes:["User manually logging off","Admin forcibly logging off a session","Session idle timeout policy logging off disconnected session","RemoteApp session closed when last published app closed"],steps:["Filter TerminalServices-LocalSessionManager log for Event 23","Correlate with Event 21 using Session ID to calculate session duration","Count Event 24 (disconnects) vs Event 23 (logoffs) — high disconnect ratio means users aren't logging off","Check session timeout policy: Get-RDSessionCollectionConfiguration -CollectionName <name>","Query active/disconnected sessions: qwinsta /server:<server>"],symptoms:["user logged off rdp","rdp session ended","remote desktop session closed","rds logoff","terminal session ended","rdp disconnected and logged off"],tags:["rdp","rds","logoff","session","lifecycle"],powershell:`# RDS Session Logoff Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with RDS server hostname
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'Microsoft-Windows-TerminalServices-LocalSessionManager/Operational'
    Id           = @(21, 23)
    StartTime    = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml = [xml]$_.ToXml()
    $ud  = $xml.Event.UserData.EventXML
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        EventID     = $_.Id
        EventType   = if ($_.Id -eq 21) { 'Logon' } else { 'Logoff' }
        User        = $ud.User
        SessionID   = $ud.SessionID
    }
} | Sort-Object SessionID, TimeCreated | Format-Table -AutoSize`,related_ids:[21,22,24,25,40],ms_docs:null},{id:24,source:"Microsoft-Windows-TerminalServices-LocalSessionManager",channel:"RDS",severity:"Info",skill_level:"Fundamental",title:"RDS: Session Disconnected",short_desc:"A user disconnected from their RDS session without logging off — session remains in memory.",description:"Event ID 24 records when a user disconnects from an RDS session without logging off. The session remains alive on the server — all their applications continue running, all their documents remain open. The user can reconnect later and pick up where they left off (Event 25). While convenient for users, accumulated disconnected sessions consume significant RAM on RDS servers. Event 24 should be monitored to detect users who are not logging off as required by policy.",why_it_happens:'A disconnect occurs when the RDP client closes without sending a logoff (closing the RDP window with the X button, network interruption, or locking the client machine). The RDS host detects the loss of the RDP channel and transitions the session to "Disconnected" state while keeping all session processes running. The Session ID is preserved for potential reconnection.',what_good_looks_like:"Some disconnects are normal (network blips, laptop lid close). Investigate: sessions that have been disconnected for days without reconnecting (likely abandoned), a pattern of users disconnecting but never logging off (consuming unnecessary resources), disconnects at unusual times that may indicate network issues.",common_mistakes:["Treating disconnect (24) as the same as logoff (23) — disconnected sessions keep running and consuming memory","Not having session timeout policies that log off long-disconnected sessions","Not querying qwinsta to find the current state of sessions on the RDS server"],causes:["User closed RDP window without logging off","Network interruption breaking the RDP connection","Client machine locked or suspended","RDP client crashed","Admin-initiated disconnect"],steps:["Filter TerminalServices-LocalSessionManager log for Event 24","Check if Event 25 (reconnect) follows — if not, the session is still disconnected","Query current session state: qwinsta /server:<server>","If sessions are accumulating as Disconnected: enforce logoff policy via GPO","Forcibly log off stale sessions: logoff <sessionid> /server:<server>","Set session policies: Computer Configuration → Policies → Admin Templates → Windows Components → Remote Desktop Services"],symptoms:["rdp disconnected","remote desktop session disconnected","rds session dropped","rdp keeps disconnecting","remote desktop connection dropped","rdp session disconnected unexpectedly","rds session drops randomly"],tags:["rdp","rds","disconnect","session","memory","lifecycle"],powershell:`# RDS Session Disconnect Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with RDS server hostname
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'Microsoft-Windows-TerminalServices-LocalSessionManager/Operational'
    Id           = 24
    StartTime    = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml = [xml]$_.ToXml()
    $ud  = $xml.Event.UserData.EventXML
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        User        = $ud.User
        SessionID   = $ud.SessionID
        SourceIP    = $ud.Address
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize

# Show currently disconnected sessions
Write-Host "\`n--- Currently Disconnected Sessions ---" -ForegroundColor Cyan
qwinsta /server:$computer 2>$null | Where-Object { $_ -match 'Disc' }`,related_ids:[21,23,25,40,4779],ms_docs:null},{id:25,source:"Microsoft-Windows-TerminalServices-LocalSessionManager",channel:"RDS",severity:"Info",skill_level:"Fundamental",title:"RDS: Session Reconnected",short_desc:"A user reconnected to a previously disconnected RDS session.",description:"Event ID 25 records when a user successfully reconnects to a previously disconnected RDS session. The existing session (with all its running applications) is resumed from the disconnected state. This is the expected follow-up to Event 24 (disconnect). The session ID remains the same as the original session. Event 25 is important for security because it can reveal when an attacker or other user reconnects to an existing session — especially if the connecting IP differs from the original session.",why_it_happens:"When an RDP client connects to an RDS server and the server finds an existing disconnected session for that user, it reconnects the client to the existing session rather than creating a new one. The LSM logs Event 25 and the session state changes from Disconnected back to Active.",what_good_looks_like:"Normal: Event 25 from the same IP as the original Event 21/24 logon. Investigate: Event 25 where the reconnecting IP differs from the original connection IP (possible session takeover), reconnections to sessions that were disconnected for an extended period, reconnections at unusual hours.",common_mistakes:["Not comparing the source IP in Event 25 with the source IP in the original Event 21 — a change may indicate session hijacking","Not correlating Event 25 with Security Event 4778 (both record RDP session reconnection)"],causes:["User reconnecting after network interruption","User reconnecting after laptop lid open","Admin reconnecting to their own disconnected session","Another user reconnecting to an abandoned session (if policies allow)"],steps:["Filter TerminalServices-LocalSessionManager log for Event 25","Match with preceding Event 24 using Session ID","Compare Source Network Address in Event 25 vs original Event 21","If IPs differ: investigate potential session hijacking","Calculate how long the session was disconnected before reconnection"],symptoms:["rdp reconnected","remote desktop reconnected","rds session resumed","reconnected to rdp","remote session came back","rdp reconnection"],tags:["rdp","rds","reconnect","session","lifecycle"],powershell:`# RDS Session Reconnect Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with RDS server hostname
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'Microsoft-Windows-TerminalServices-LocalSessionManager/Operational'
    Id           = 25
    StartTime    = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml = [xml]$_.ToXml()
    $ud  = $xml.Event.UserData.EventXML
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        User        = $ud.User
        SessionID   = $ud.SessionID
        SourceIP    = $ud.Address
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[21,23,24,40,4778],ms_docs:null},{id:40,source:"Microsoft-Windows-TerminalServices-LocalSessionManager",channel:"RDS",severity:"Warning",skill_level:"Intermediate",title:"RDS: Session Disconnected with Reason Code",short_desc:"An RDS session was disconnected — includes a reason code explaining why.",description:"Event ID 40 records an RDS session disconnection with a specific reason code that explains the cause. The reason code is the most valuable part — it distinguishes between user-initiated disconnects, network failures, timeout-based disconnects, and protocol errors. This event appears in the TerminalServices-LocalSessionManager log and the RemoteConnectionManager log. Key reason codes: 0 = no information, 5 = client disconnect (user closed window), 11 = client disconnected with admin disconnected, 12 = session timeout, 5 = RPC error, 9 = RDP protocol error.",why_it_happens:"When an RDS session disconnects, the terminal services stack determines why and records a reason code. This mechanism exists specifically to help administrators distinguish between expected disconnects (user closed window, timeout) and unexpected ones (network failure, protocol error, server resource issue).",what_good_looks_like:"Reason code 5 (client disconnect) is entirely normal — user closed window. Investigate: reason code 9 (RDP protocol error — possible network issue), reason code 2 (server out of resources — RAM or session limit), reason codes in the 256+ range (application-specific or custom RDP infrastructure codes), any reason code that repeats for many users simultaneously.",common_mistakes:["Treating all Event 40s as problems — reason code 5 (normal user disconnect) is not a problem","Not looking up the reason code — each code has a specific meaning that guides the investigation","Ignoring the user and session fields that identify which session disconnected"],causes:["User closed RDP window (reason 5)","Session idle timeout reached (reason 12)","Network interruption (reason 9 or protocol errors)","Server out of resources (reason 2)","Admin disconnected the session","RDP protocol negotiation failure"],steps:["Filter TerminalServices-LocalSessionManager log for Event 40","Note the Reason Code and look it up (common: 5=normal, 9=protocol error, 12=timeout)","If reason 9 or other error codes: check network connectivity and RDP port availability","If reason 2: check server RAM and session license count","Correlate with Event 24 for the same session to see the full disconnect record"],symptoms:["rdp disconnecting with reason","remote desktop dropped with error","rdp session ended reason code","why did rdp disconnect","rdp connection dropped reason","rds session disconnected with code"],tags:["rdp","rds","disconnect","reason-code","troubleshooting","session"],powershell:`# RDS Session Disconnect with Reason Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with RDS server hostname
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

# Reason code lookup
$reasonMap = @{
    0  = 'No information'
    1  = 'Server terminated'
    2  = 'Server out of memory'
    3  = 'Server out of memory 2'
    4  = 'Server idle'
    5  = 'Client disconnect (user closed window)'
    6  = 'Client logoff'
    7  = 'RDP log off'
    9  = 'Server RPC error / network issue'
    11 = 'Server admin disconnect'
    12 = 'Session timeout'
    263= 'Licensing error'
}

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'Microsoft-Windows-TerminalServices-LocalSessionManager/Operational'
    Id           = 40
    StartTime    = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml    = [xml]$_.ToXml()
    $ud     = $xml.Event.UserData.EventXML
    $code   = [int]$ud.Reason
    [PSCustomObject]@{
        TimeCreated  = $_.TimeCreated
        User         = $ud.User
        SessionID    = $ud.SessionID
        ReasonCode   = $code
        ReasonDesc   = $reasonMap[$code] ?? "Unknown ($code)"
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[21,23,24,25,41],ms_docs:null},{id:41,source:"Microsoft-Windows-TerminalServices-LocalSessionManager",channel:"RDS",severity:"Warning",skill_level:"Intermediate",title:"RDS: Session Connection Failed",short_desc:"A Remote Desktop session connection attempt failed before a session was created.",description:"Event ID 41 from TerminalServices-LocalSessionManager records a connection failure before a session was established. Note this is distinct from Event 41 in the System log (Kernel Power) — same ID, different source. In the RDS context, this event indicates a pre-session failure: the connection was refused or failed before the user was authenticated or a session was created. This can indicate authentication failures, licensing issues, or server resource problems.",why_it_happens:"The RDS connection broker or local session manager refuses or fails the connection attempt before creating a session. Common causes include NLA (Network Level Authentication) pre-authentication failures, the RDS server being at its session limit, the user not having Remote Desktop access rights, or a server-side issue.",what_good_looks_like:"Occasional failures are expected (wrong password, user not in RD Users group). Investigate: many failures from one IP (brute-force scan), failures for accounts that should have access (misconfiguration), failures after a change to RDS configuration.",common_mistakes:["Confusing this Event 41 (RDS connection failed) with System Event 41 (Kernel Power unexpected reboot)","Not checking if the user is a member of Remote Desktop Users group","Missing that NLA pre-auth failures here won't have a corresponding 4625 in Security log if NLA was fully rejected"],causes:["Authentication failure (NLA pre-auth)","User not in Remote Desktop Users group","RDS server at session or license limit","Firewall blocking RDP port 3389","Server resource exhaustion","RDP Restricted Admin mode misconfiguration"],steps:["Filter TerminalServices-LocalSessionManager log for Event 41","Note the error code and username (if available)","Check if user is in Remote Desktop Users or Administrators group","Check session limit: qwinsta /server:<server> (count sessions)",'Check RDP firewall rules: Get-NetFirewallRule -Name "RemoteDesktop*"',"Check RDS Licensing if in an RDS farm environment"],symptoms:["rdp connection refused","cannot connect rdp","remote desktop connection failed","rdp not accepting connections","remote desktop access denied","rds connection error","cant connect via remote desktop"],tags:["rdp","rds","connection-failed","authentication","access-denied","troubleshooting"],powershell:`# RDS Connection Failure Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with RDS server hostname
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'Microsoft-Windows-TerminalServices-LocalSessionManager/Operational'
    Id           = 41
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List

# Also check RemoteConnectionManager for more detail
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName   = 'Microsoft-Windows-TerminalServices-RemoteConnectionManager/Operational'
    StartTime = $startTime
} -ErrorAction SilentlyContinue |
    Where-Object { $_.LevelDisplayName -eq 'Error' -or $_.LevelDisplayName -eq 'Warning' } |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Select-Object -First 10 | Format-List`,related_ids:[21,1149,4625,4771],ms_docs:null},{id:1149,source:"Microsoft-Windows-TerminalServices-RemoteConnectionManager",channel:"RDS",severity:"Info",skill_level:"Intermediate",title:"RDS: User Authentication Succeeded",short_desc:"Network Level Authentication (NLA) pre-authentication succeeded for an RDP connection.",description:"Event ID 1149 from the TerminalServices-RemoteConnectionManager records that a user successfully passed Network Level Authentication (NLA) before their RDS session was created. NLA is the pre-authentication step that occurs before the full RDS session is established — it validates credentials at the network level to prevent unauthenticated users from reaching the login screen. Event 1149 means NLA passed; it does not guarantee a session was created (Event 21 confirms the session).",why_it_happens:"When an RDP client connects with NLA enabled (the default for modern Windows), it authenticates at the protocol level using CredSSP before the RDS session stack is initialized. The RemoteConnectionManager logs 1149 when NLA validation succeeds. This event appears even if the subsequent session creation fails for other reasons (license limit, session policy).",what_good_looks_like:"Expected: 1149 events from known users and IPs during business hours. Event 1149 without a corresponding Event 21 may indicate a session creation failure after successful NLA. Investigate: 1149 events from unexpected IP ranges, 1149 for accounts that should not have RDP access, 1149 at unusual hours for non-on-call accounts.",common_mistakes:["Confusing 1149 (NLA passed) with Event 21 (session created) — both should appear for a successful connection","Not checking the client IP in 1149 — this is where the connection physically came from before any load balancer or broker","Not having NLA enabled on RDS servers — without NLA, unauthenticated users reach the login screen and can exploit pre-auth vulnerabilities"],causes:["Legitimate user successfully pre-authenticated via NLA","Admin connecting to manage the server","Automated process with valid credentials connecting via RDP"],steps:["Filter Microsoft-Windows-TerminalServices-RemoteConnectionManager/Operational for Event 1149","Note the username and source IP","Correlate with Event 21 to confirm a session was created","If 1149 exists but no Event 21: session creation failed — investigate license or policy","If the source IP is unexpected: investigate who has those credentials"],symptoms:["rdp authentication succeeded","nla authentication","network level authentication passed","rdp pre auth succeeded","rds nla success"],tags:["rdp","rds","nla","authentication","network-level-authentication","security"],powershell:`# RDS NLA Authentication Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with RDS server hostname
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'Microsoft-Windows-TerminalServices-RemoteConnectionManager/Operational'
    Id           = 1149
    StartTime    = $startTime
} -ErrorAction SilentlyContinue | ForEach-Object {
    $xml  = [xml]$_.ToXml()
    $ud   = $xml.Event.UserData.EventXML
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        UserName    = $ud.Param1
        Domain      = $ud.Param2
        SourceIP    = $ud.Param3
    }
} | Sort-Object TimeCreated -Descending | Format-Table -AutoSize`,related_ids:[21,41,4624,4625],ms_docs:null},{id:1057,source:"Microsoft-Windows-TerminalServices-RemoteConnectionManager",channel:"System",severity:"Error",skill_level:"Intermediate",title:"RDS — Cannot Create or Bind TLS/SSL Certificate for RDP",short_desc:"Remote Desktop Services cannot create or use a TLS certificate — RDP connections will use weaker security or fail.",description:"Event 1057 is logged by the Remote Desktop Connection Manager when the RDS service cannot create a self-signed certificate, enroll for a certificate from the CA, or bind an existing certificate to the RDP listener. When this happens, RDP may fall back to a weaker security layer or users may receive certificate warnings when connecting. NLA (Network Level Authentication) requires a valid TLS certificate and may fail entirely if the certificate is missing.",why_it_happens:'Certificate issues arise when the Remote Desktop Services machine certificate (auto-enrolled via GPO) has expired, when the CA is unreachable for renewal, when the RDS server does not have the "Remote Desktop Services" certificate template permission, or when the certificate thumbprint in the RDS registry is outdated after a certificate renewal.',what_good_looks_like:"RDS has a valid, trusted machine certificate bound to the RDP listener. Users do not receive certificate warnings when connecting. NLA authentication works. Certificate thumbprint in HKLM:\\SYSTEM\\CurrentControlSet\\Control\\Terminal Server\\WinStations\\RDP-Tcp\\SSLCertificateSHA1Hash matches a valid cert in certlm.msc.",common_mistakes:["Manually configuring a certificate thumbprint in registry and then forgetting to update it when the certificate renews","Not deploying a CA-issued certificate for RDS — self-signed certs cause trust warnings for all users","Not granting the computer account Enroll permission on the Remote Desktop Services certificate template"],causes:["Machine certificate for RDS has expired and renewal failed","Certificate thumbprint in RDS registry points to a certificate that no longer exists","Auto-enrollment GPO not configured or CA unreachable","Computer account lacks Enroll permission on the certificate template","Certificate store corrupted or certificate accidentally deleted"],steps:["Check Event 1057 message for specific error code","Open certlm.msc → Personal → check for a valid RDS or Computer certificate","Run gpupdate /force to trigger auto-enrollment","Check auto-enrollment events: Application log → source Microsoft-Windows-CertificateServicesClient-AutoEnrollment","Manually bind a certificate via RDS MMC: Remote Desktop Session Host Configuration → Connections → RDP-Tcp → Properties → General","Check registry thumbprint: HKLM:\\SYSTEM\\CurrentControlSet\\Control\\Terminal Server\\WinStations\\RDP-Tcp\\SSLCertificateSHA1Hash"],symptoms:["RDP certificate error","RDP certificate warning","RDP cannot create certificate","Remote Desktop TLS certificate error","RDP certificate expired","NLA failing certificate","RDS certificate binding error","Remote Desktop certificate invalid"],tags:["rdp","certificate","tls","rds","nla","security"],powershell:`# RDS Certificate Investigation
# Eventful

# RDS certificate errors
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-TerminalServices-RemoteConnectionManager'
    Id           = @(1057, 1058)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending | Format-List

# Current RDS certificate binding (thumbprint in registry)
$thumbprint = (Get-ItemProperty "HKLM:SYSTEMCurrentControlSetControlTerminal ServerWinStationsRDP-Tcp" -ErrorAction SilentlyContinue).SSLCertificateSHA1Hash
if ($thumbprint) {
    $cert = Get-ChildItem Cert:LocalMachineMy | Where-Object { $_.Thumbprint -eq ($thumbprint | ForEach-Object { [System.BitConverter]::ToString([byte[]]$_) -replace '-','' }) }
    if ($cert) { $cert | Select-Object Subject, NotAfter } else { "Thumbprint in registry not found in cert store" }
} else { "No custom certificate configured — using auto-generated" }`,related_ids:[21,1149,4625,64],ms_docs:"https://learn.microsoft.com/en-us/windows-server/remote/remote-desktop-services/rds-tls-certificate-configuration"},{id:516,source:"TermServLicensing",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"RDS Licensing — Cannot Find License Server",short_desc:"RD Session Host cannot locate a Remote Desktop license server — grace period may be counting down.",description:"Event 516 from TermServLicensing is generated when the Remote Desktop Session Host cannot contact a configured Remote Desktop License Server. Without a license server, RDS runs in a grace period (120 days for Windows Server 2019+). After the grace period expires, users will be denied RDP connections. This is a critical infrastructure event for any RDS deployment serving more than 2 users.",why_it_happens:"The license server may be unreachable due to: it not being configured in Group Policy, the license server service being stopped, the RD Session Host pointing to the wrong license server name/IP, firewall blocking RPC traffic to the license server, or the license server not being activated.",what_good_looks_like:"No 516 events. RD Session Host can reach the license server. Licenses are available and being issued. Confirmed via: RD Licensing Diagnoser (lsdiag.msc) showing no errors.",common_mistakes:["Not noticing Event 516 until users start getting denied — the grace period countdown is silent","Not configuring the license server via GPO — many admins set it only in RDS Server Manager which can be lost after a reboot","Forgetting that RD Licensing requires RPC (TCP 135 + dynamic ports) through firewalls between session host and license server"],causes:["License server not configured in Group Policy or local RDS settings","License server offline or Remote Desktop Licensing service stopped","Firewall blocking RPC to license server","Wrong license server name or IP configured","License server not activated"],steps:["Run lsdiag.msc (RD Licensing Diagnoser) on the session host for a guided check","Verify license server configuration: GPO → Computer Configuration → Administrative Templates → Windows Components → Remote Desktop Services → Remote Desktop Session Host → Licensing","Check connectivity to license server: Test-NetConnection -ComputerName <licServer> -Port 135","Check license server service: Get-Service TermServLicensing -ComputerName <licServer>","Check remaining grace period: in Event Viewer on session host look for 1128 (licenses available) or 1129 (grace period remaining)"],symptoms:["RDS license server not found","Remote Desktop cannot find license server","RDS grace period","RDP license error","users denied remote desktop after 120 days","Remote Desktop licensing warning","TS licensing server not responding","RDS running in grace period"],tags:["rds","licensing","terminal-services","grace-period","rdp"],powershell:`# RDS Licensing Status
# Eventful

# Licensing events on session host
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'TermServLicensing'
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending |
    Select-Object -First 20 | Format-List

# Check configured license server (from registry)
$licMode  = (Get-ItemProperty "HKLM:SOFTWAREPoliciesMicrosoftWindows NTTerminal Services" -ErrorAction SilentlyContinue).LicensingMode
$licServer = (Get-ItemProperty "HKLM:SOFTWAREPoliciesMicrosoftWindows NTTerminal Services" -ErrorAction SilentlyContinue).LicenseServers
"License Mode: $licMode\`nLicense Server: $licServer"`,related_ids:[21,41,1057],ms_docs:"https://learn.microsoft.com/en-us/windows-server/remote/remote-desktop-services/rds-client-access-license"},{id:4005,source:"Microsoft-Windows-TerminalServices-RemoteConnectionManager",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"RDS — Maximum Connections Reached",short_desc:"Remote Desktop Session Host has reached the connection limit — new connections refused.",description:'Event 4005 is generated when the RD Session Host refuses a new connection because the maximum allowed number of sessions has been reached. Users attempting to connect will receive "Remote Desktop Services is currently busy" or "No more connections can be made to this remote computer" error messages. This is a capacity issue — either the server needs more capacity, zombie sessions need to be cleaned up, or users need to be redirected to another server.',why_it_happens:"RDS session limits are set by licensing (CAL count), by Session Limit Group Policy, or by Windows Server edition. Windows Server with Remote Desktop role supports unlimited users (with CALs), while Windows desktop OS supports only 1 concurrent RDP user. Zombie sessions (disconnected but not logged off) consume licenses and count against the limit.",what_good_looks_like:"Session count well below the licensed limit. Disconnected sessions cleaned up by idle timeout and disconnect timeout policies. Users prompted to log off rather than disconnect when done.",common_mistakes:["Not configuring session idle and disconnect timeouts, causing zombie sessions to accumulate","Forgetting that disconnected sessions still count against the license and session limits","Not monitoring RDS session count proactively — 4005 is the emergency alert"],causes:["Too many concurrent users for the server capacity","Disconnected zombie sessions consuming licenses","Session limit GPO set too low","License count insufficient for user count","Runaway processes creating multiple sessions per user"],steps:["Check current sessions: query session /server:<hostname>","Identify and clean up zombie sessions: logoff <sessionId> /server:<hostname>","Configure session timeout GPOs: Computer Config → Admin Templates → Windows Components → RDS → Session Host → Session Time Limits","Check license availability via lsdiag.msc","Review server resource usage — if CPU/RAM is maxed, capacity expansion is needed"],symptoms:["Remote Desktop connection refused","too many RDP connections","RDS maximum connections","cannot connect remote desktop busy","Remote Desktop full","RDP connection limit reached","no more remote desktop connections","RDS session limit exceeded"],tags:["rds","rdp","sessions","capacity","connections","limit"],powershell:`# RDS Session Overview
# Eventful

$rdsh = $env:COMPUTERNAME  # Replace with RDS server name

# Current sessions (all states)
query session /server:$rdsh

# Logoff a disconnected session by ID (uncomment and replace ID)
# logoff <sessionId> /server:$rdsh

# RDS connection refused events
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-TerminalServices-RemoteConnectionManager'
    Id           = 4005
    StartTime    = (Get-Date).AddHours(-24)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Sort-Object TimeCreated -Descending | Format-List`,related_ids:[21,40,41,516],ms_docs:"https://learn.microsoft.com/en-us/windows-server/remote/remote-desktop-services/rds-plan-and-deploy"},{id:1067,source:"Microsoft-Windows-TerminalServices-RemoteConnectionManager",channel:"System",severity:"Error",skill_level:"Advanced",title:"RDS Session Host Could Not Register with Connection Broker",short_desc:"RD Session Host failed to register with the Remote Desktop Connection Broker — HA farm broken.",description:'Event 1067 is logged when an RD Session Host server in an RDS farm cannot register with the Remote Desktop Connection Broker (RDCB). Without successful registration, the session host does not receive new sessions from the broker, effectively removing it from the farm. Users connecting to the farm URL may experience "no servers available" or be routed only to the remaining healthy session hosts.',why_it_happens:"Connection Broker registration fails when the broker is offline, when there is a network/firewall issue between the session host and the broker, when the session host's computer account in the RDS SQL database is invalid, or when the RDCB service itself is having issues.",what_good_looks_like:"All RD Session Hosts registered and showing as Available in Server Manager → Remote Desktop Services. No 1067 events. Load is balanced across all session hosts in the farm.",common_mistakes:["Not checking if the Connection Broker service (TSSDis) is running on the broker server","Firewall blocking TCP 3389/5504 between session hosts and the Connection Broker","Not re-adding the session host to the deployment after it was removed"],causes:["RD Connection Broker service stopped on broker server","Firewall blocking communication between session host and broker","SQL Server database backing the broker is offline","Session host computer object invalid in the broker database","Network connectivity issue between farm members"],steps:["Check RDCB service on broker: Get-Service TSSDis -ComputerName <brokerServer>","Test connectivity: Test-NetConnection -ComputerName <brokerServer> -Port 5504","Check SQL Server backing the RDCB is online","In Server Manager → Remote Desktop Services → check deployment health","Remove and re-add the session host to the deployment if registration is corrupt"],symptoms:["RDS farm not load balancing","session host not accepting new connections","RD Connection Broker registration failed","RDS server not available in farm","users not routed to session host","RDS broker disconnected","Remote Desktop farm broken"],tags:["rds","connection-broker","farm","ha","registration"],powershell:`# RDS Connection Broker Registration Events
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-TerminalServices-RemoteConnectionManager'
    Id           = @(1067, 1068, 1069)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending | Format-List`,related_ids:[21,41,1057],ms_docs:"https://learn.microsoft.com/en-us/windows-server/remote/remote-desktop-services/rds-roles"}],s=[{id:1014,source:"Microsoft-Windows-DNS-Client",channel:"Network",severity:"Warning",skill_level:"Fundamental",title:"DNS Name Resolution Timeout",short_desc:"A DNS query timed out — the DNS server did not respond within the timeout period.",description:`Event ID 1014 from the DNS-Client source is generated when a DNS name resolution attempt times out — the query was sent to a DNS server but no response was received within the configured timeout. This is distinct from a DNS NXDOMAIN response (the name exists but the record doesn't) — a timeout means the DNS infrastructure itself is unreachable or overloaded. Repeated 1014 events cause slow application startups, web browsing delays, and network connectivity issues that users describe as "the internet is slow" or "file shares are slow to open".`,why_it_happens:"The Windows DNS Client Resolver sends queries to the configured DNS server(s) and waits for a response. If the primary DNS server doesn't respond within the timeout period, it retries, then falls back to secondary DNS server(s). If all configured servers time out, Event 1014 is logged. Common causes include the DNS server being unreachable (network issue), the DNS server being overloaded, a firewall blocking UDP/TCP port 53, or the DNS server service itself having problems.",what_good_looks_like:"No Event 1014 events on a healthy machine. Occasional 1014 events during network changes are acceptable. Investigate: repeated 1014 events (the DNS server is consistently unreachable), 1014 events that correlate with user complaints about slow network, 1014 for internal names only (internal DNS server issue), 1014 for external names only (external DNS unreachable, possibly after DHCP change).",common_mistakes:["Concluding the DNS server is down without checking if the DNS server is reachable (ping, Test-NetConnection)","Fixing only the DNS setting and not finding why the DNS server became unreachable","Not checking whether internal vs external names are timing out — this isolates whether the internal DNS server or upstream forwarder is the issue","Ignoring 1014 during network speed investigations — DNS timeout adds latency to every hostname resolution"],causes:["DNS server IP is wrong (changed DHCP scope, manual misconfiguration)","DNS server service stopped or crashed","Network path to DNS server blocked by firewall or routing issue","DNS server overloaded or unresponsive","VPN connected and split tunneling routing DNS incorrectly","DHCP lease renewed with different DNS server IPs"],steps:["Find Event 1014 and note which hostname was failing and which DNS server was queried","Test DNS server reachability: Test-NetConnection -ComputerName <dns-ip> -Port 53","Test DNS resolution manually: Resolve-DnsName <hostname> -Server <dns-ip>","Check configured DNS servers: Get-DnsClientServerAddress","Check if the DNS server service is running on the DNS server itself","If VPN: check if DNS is being routed through VPN tunnel correctly","If DHCP: check if DNS server IPs in DHCP scope are correct"],symptoms:["internet is slow","websites slow to load","dns not working","cant resolve dns","dns lookup failing","name resolution failing","dns timeout","slow network","network drives slow to open","websites timing out"],tags:["dns","network","resolution","timeout","connectivity","fundamental"],powershell:`# DNS Resolution Timeout Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

# Find DNS timeout events
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-DNS-Client'
    Id           = 1014
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List

# Check current DNS configuration
Write-Host "
--- Current DNS Configuration ---" -ForegroundColor Cyan
Get-DnsClientServerAddress -ComputerName $computer -AddressFamily IPv4 |
    Where-Object { $_.ServerAddresses.Count -gt 0 } | Format-Table -AutoSize

# Test DNS resolution
Write-Host "
--- DNS Resolution Test ---" -ForegroundColor Cyan
Resolve-DnsName google.com -ErrorAction SilentlyContinue | Select-Object Name, Type, IPAddress | Format-Table -AutoSize`,related_ids:[1032,1030],ms_docs:"https://learn.microsoft.com/en-us/windows-server/networking/dns/dns-overview"},{id:1032,source:"Microsoft-Windows-DNS-Client",channel:"Network",severity:"Warning",skill_level:"Intermediate",title:"DNS Query Failed",short_desc:"A DNS query returned an error — the record does not exist or the server returned a failure.",description:"Event ID 1032 records a DNS query that failed with an error response from the DNS server — as opposed to a timeout (1014). A failure response means the DNS server responded, but the response indicated an error: NXDOMAIN (the name does not exist), SERVFAIL (the server had an internal error), REFUSED (the server declined to answer), or NOTIMP (not implemented). Understanding the specific error code helps diagnose whether the problem is a missing DNS record, a configuration issue, or a server-side problem.",why_it_happens:"The DNS server processes the query and returns a negative response code. NXDOMAIN means the queried name has no records anywhere in the DNS hierarchy. SERVFAIL typically means the DNS server couldn't complete recursive resolution (often a problem with its forwarders or internet connectivity). REFUSED means the server is not configured to answer queries from this client.",what_good_looks_like:"NXDOMAIN for external internet names is entirely normal — users and applications query names that simply don't exist. Investigate: NXDOMAIN for internal names that should exist (misconfigured internal DNS zone, missing record), SERVFAIL responses (the DNS server itself has a problem), repeated failures for names that should resolve (broken delegation or zone configuration).",common_mistakes:["Assuming every DNS failure is a network problem — NXDOMAIN just means the name doesn't exist and is normal for typos","Not distinguishing NXDOMAIN from SERVFAIL — they have completely different causes","Not checking the internal DNS zone for missing records when internal names fail to resolve"],causes:["NXDOMAIN: the queried DNS name does not exist","SERVFAIL: DNS server internal error, often forwarder unreachable","REFUSED: client not authorised to query this DNS server","Missing internal DNS record after a server rename or IP change","Split-brain DNS misconfiguration"],steps:["Find Event 1032 and note the queried hostname and error code","Test manually: Resolve-DnsName <hostname> -Type A","If internal name: check the DNS zone for the missing record","If SERVFAIL: check the DNS server's forwarder configuration","Add missing DNS records if appropriate: Add-DnsServerResourceRecordA","Flush DNS cache after fixing: Clear-DnsClientCache"],symptoms:["dns lookup failed","dns error","name not found dns","internal dns not resolving","server not found dns","nxdomain error","dns resolution error","cannot find server dns"],tags:["dns","network","resolution","nxdomain","servfail","connectivity"],powershell:`# DNS Query Failure Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-DNS-Client'
    Id           = 1032
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List`,related_ids:[1014,1030],ms_docs:null},{id:1020,source:"Microsoft-Windows-Dhcp-Client",channel:"Network",severity:"Warning",skill_level:"Intermediate",title:"DHCP: IP Address Conflict Detected",short_desc:"The DHCP client detected that another device is already using the assigned IP address.",description:"Event ID 1020 from the DHCP Client is generated when ARP (Address Resolution Protocol) probing detects that the IP address assigned by DHCP is already in use by another device. Windows declines the offered IP address and requests a new one. If all available IPs are in conflict or the DHCP server exhauster its pool, the machine will fall back to APIPA (169.254.x.x). This event indicates an IP conflict — two devices trying to use the same IP — which causes intermittent connectivity for both devices.",why_it_happens:"When a DHCP client receives an IP address offer, it sends ARP probes to verify the address is not in use. If another device responds to the ARP, the DHCP client detects the conflict and logs 1020. Common causes: a device was manually configured with a static IP that falls inside the DHCP scope, an expired DHCP lease was reassigned while the original device was offline and returns, or DHCP scope management is incorrect.",what_good_looks_like:"No Event 1020 events. Any IP conflict should be resolved immediately as it causes network disruption for the affected devices.",common_mistakes:["Not checking the DHCP server's active leases to find the conflicting device","Not creating DHCP exclusions for statically configured devices","Not using DHCP reservations for printers and servers instead of static IPs"],causes:["Device with static IP in the DHCP scope range","DHCP lease expired and address reassigned while original device was offline","DHCP scope not configured with proper exclusions for static devices","Rogue DHCP server assigning overlapping addresses","Duplicate MAC address cloning (VMs)"],steps:["Find Event 1020 and note the conflicting IP address","Check DHCP server active leases for that IP: Get-DhcpServerv4Lease -ScopeId <scope> | Where IPAddress -eq <ip>","Use ARP to find the conflicting device: arp -a | findstr <ip>","Identify the device with the static IP and either change it or create a DHCP exclusion","Add exclusions in DHCP for all statically configured devices","Consider using DHCP reservations for infrastructure devices instead of static IPs"],symptoms:["ip conflict","ip address conflict","duplicate ip","two devices same ip","address conflict dhcp","network conflict ip","cant connect network ip conflict","intermittent network ip"],tags:["dhcp","ip-conflict","network","arp","connectivity"],powershell:`# DHCP IP Conflict Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

# Find IP conflict events
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Dhcp-Client'
    Id           = 1020
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List

# Check current IP and ARP cache
Write-Host "
--- Current Network Configuration ---" -ForegroundColor Cyan
Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.IPAddress -ne '127.0.0.1' } | Format-Table -AutoSize

Write-Host "
--- ARP Cache (find conflicting device) ---" -ForegroundColor Cyan
Get-NetNeighbor -AddressFamily IPv4 | Where-Object { $_.State -ne 'Unreachable' } | Format-Table -AutoSize`,related_ids:[1030,1048,1063,1014],ms_docs:null},{id:1030,source:"Microsoft-Windows-Dhcp-Client",channel:"Network",severity:"Error",skill_level:"Fundamental",title:"DHCP: Unable to Obtain IP Address",short_desc:"The DHCP client could not get an IP address — may fall back to APIPA (169.254.x.x).",description:`Event ID 1030 is generated when the DHCP client fails to obtain an IP address from a DHCP server after multiple attempts. When this happens, the machine typically falls back to an APIPA address (169.254.x.x) which means it cannot communicate with other network devices. This is a critical network failure event. Users will report "no internet", "can't access file shares", or "limited connectivity".`,why_it_happens:"The DHCP client broadcasts DHCPDISCOVER packets and waits for a DHCPOFFER from a server. If no server responds after several retries (across multiple boot/retry cycles), 1030 is logged. Causes: DHCP server is down, the client is isolated from the DHCP server by a firewall or VLAN misconfiguration, the DHCP scope is exhausted (all IPs leased out), or the network cable/WiFi is not actually connected.",what_good_looks_like:"No Event 1030 events. Any Event 1030 is a network connectivity problem requiring immediate investigation.",common_mistakes:["Not checking the physical layer first — a disconnected cable or wrong VLAN causes 1030","Trying to diagnose DHCP server issues without first verifying the client can reach the DHCP server subnet","Not checking the DHCP scope for exhaustion: Get-DhcpServerv4ScopeStatistics"],causes:["Physical network disconnection (cable, port, WiFi)","DHCP server offline or service stopped","DHCP scope exhausted (all IPs leased)","Firewall or ACL blocking DHCP traffic (UDP 67/68)","VLAN or switch misconfiguration isolating the client","DHCP relay agent not configured for the client's VLAN"],steps:["Check physical connectivity: is the cable plugged in, is the link light on the switch port active?","Check if machine is in the correct VLAN: check switch port configuration","Check if DHCP server is reachable (once you have an IP): ping dhcp-server","Check DHCP scope statistics for exhaustion: Get-DhcpServerv4ScopeStatistics -ComputerName <dhcp-server>","Force a DHCP release and renew: ipconfig /release && ipconfig /renew","If APIPA address (169.254.x.x): the machine got no DHCP response at all","Check DHCP server event log for related errors"],symptoms:["cant get ip address","no ip address","169.254 ip address","limited connectivity","dhcp failed","no network","apipa address","cannot connect to network","no internet dhcp","network limited"],tags:["dhcp","ip-address","connectivity","apipa","network","fundamental"],powershell:`# DHCP IP Assignment Failure Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

# Find DHCP failure events
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Dhcp-Client'
    Id           = @(1030, 1020, 1048)
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List

# Check current IP state
Write-Host "
--- Current IP Configuration ---" -ForegroundColor Cyan
Get-NetIPConfiguration -ComputerName $computer -ErrorAction SilentlyContinue | Format-List`,related_ids:[1020,1048,1063,1014],ms_docs:null},{id:1048,source:"Microsoft-Windows-Dhcp-Client",channel:"Network",severity:"Warning",skill_level:"Intermediate",title:"DHCP: Lease Renewal Failed",short_desc:"The DHCP client failed to renew its existing IP address lease.",description:"Event ID 1048 is generated when the DHCP client has an existing lease but fails to renew it when the lease expires. At 50% of the lease duration, the client tries to renew with the original DHCP server. At 87.5%, it broadcasts to any DHCP server. If both fail, the lease expires and the client may lose its IP. This event indicates that the DHCP server has become unreachable since the lease was first obtained — a more subtle failure than 1030 which occurs on initial DHCP acquisition.",why_it_happens:"The DHCP client contacts the DHCP server (by unicast first, then broadcast) to renew its lease before expiry. If the server doesn't respond, the client retries until the lease expires. The failure could be because the DHCP server changed, moved, or crashed since the lease was first obtained, or because the network path to the DHCP server has changed.",what_good_looks_like:"No Event 1048 events. When 1048 appears, investigate whether the DHCP server is still reachable and whether its IP or configuration has changed.",common_mistakes:["Not realising the machine may still have network connectivity if the lease hasn't expired yet","Not checking if the DHCP server IP address changed (server moved or reIPed)"],causes:["DHCP server offline or IP changed","Network path to DHCP server changed","DHCP service stopped on server","Firewall change blocking DHCP renewal traffic"],steps:["Find Event 1048 and note the DHCP server address from the event","Check if that DHCP server is still reachable: Test-NetConnection -ComputerName <dhcp-ip> -Port 67","Force renewal: ipconfig /renew","If failure: check DHCP server status","Check DHCP server event log for service issues"],symptoms:["dhcp renewal failed","ip lease renewal failed","dhcp lease expired","network connectivity dropped after a while","ip address renewal failed"],tags:["dhcp","lease-renewal","network","connectivity"],powershell:`# DHCP Lease Renewal Failure Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Dhcp-Client'
    Id           = 1048
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List`,related_ids:[1030,1020,1063],ms_docs:null},{id:1063,source:"Microsoft-Windows-Dhcp-Client",channel:"Network",severity:"Warning",skill_level:"Intermediate",title:"DHCP: Lost Contact with Domain Controller",short_desc:"The DHCP client could not contact a domain controller — may affect domain authentication.",description:"Event ID 1063 indicates that the DHCP client could not reach a domain controller, which is logged as part of the domain join and DHCP registration process. When a domain-joined machine gets a DHCP lease, it attempts to register its DNS record with the domain-integrated DNS server, which requires DC connectivity. Failure to contact the DC may indicate DNS misconfiguration, network segmentation issues, or that the DC itself is unreachable.",why_it_happens:"During DHCP address acquisition or renewal on a domain-joined machine, the DHCP client service attempts to locate and contact a domain controller for DNS dynamic update authorisation. If the DC is unreachable (wrong DNS server, DC down, or network issue), Event 1063 is logged. This does not necessarily mean authentication will fail immediately (Kerberos tickets and credentials may be cached), but it indicates a domain connectivity problem.",what_good_looks_like:"No Event 1063 events on a healthy domain-joined machine. If 1063 appears: the machine cannot reach a DC. Domain authentication will work if credentials are cached but will fail after cache expiry.",common_mistakes:["Not realising 1063 can appear during transient boot-time network unavailability and not always indicating a persistent problem","Ignoring 1063 on laptops that frequently move between networks","Not correlating with 1014 (DNS timeout) which often precedes 1063"],causes:["DNS server not configured or unreachable (can't find DC)","Domain Controller offline","Network segmentation preventing DC communication","VPN not connected (for remote workers)","Firewall blocking DC communication ports"],steps:["Find Event 1063 and check if 1014 (DNS timeout) events also appear","Test DC connectivity: Test-ComputerSecureChannel","Find a DC: nltest /dsgetdc:<domain>","Test DC ports: Test-NetConnection -ComputerName <dc> -Port 389","Check DNS is configured correctly: Get-DnsClientServerAddress","If VPN dependent: confirm VPN is connected"],symptoms:["cant contact domain controller","domain controller unreachable","dhcp domain controller","lost contact with dc","domain connectivity issue"],tags:["dhcp","domain-controller","dns","network","domain-join"],powershell:`# DHCP Domain Controller Contact Loss Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-Dhcp-Client'
    Id           = 1063
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List

# Test domain controller connectivity
Write-Host "
--- DC Connectivity Test ---" -ForegroundColor Cyan
Test-ComputerSecureChannel -Verbose`,related_ids:[1014,1030,1048],ms_docs:null},{id:10317,source:"Microsoft-Windows-WLAN-AutoConfig",channel:"Network",severity:"Warning",skill_level:"Fundamental",title:"WLAN: Network Disconnection",short_desc:"The wireless adapter disconnected from a Wi-Fi network.",description:"Event ID 10317 from the WLAN-AutoConfig service records a wireless network disconnection. It includes the SSID of the network, the reason code for the disconnection, and the network adapter. This is the primary event for investigating intermittent WiFi connectivity issues. The disconnection reason code distinguishes between normal disconnects (user action) and unexpected ones (signal loss, authentication failure, deauthentication from AP, or driver issues).",why_it_happens:"WLAN disconnections occur when the association with the access point is lost. This can be caused by moving out of range, a 802.1X authentication failure (wrong certificate, expired password), the access point deauthenticating the client (roaming, load balancing, channel change), driver bugs, or Windows attempting to roam to a better AP and failing. The DisconnectReason field (or Reason Code) is crucial for root cause identification.",what_good_looks_like:"Occasional brief disconnections while roaming between APs are normal. Investigate: frequent disconnections in a fixed location (signal, interference, or driver issue), disconnections followed by long reconnect times, disconnections with reason codes indicating authentication failure (802.1X), many clients disconnecting from the same AP simultaneously.",common_mistakes:['Not checking the reason code — "disconnected" could mean user action or AP rejection',"Blaming the WiFi driver without first checking signal strength and channel utilisation","Not checking if multiple devices disconnect from the same AP simultaneously (AP issue) vs one device (client issue)","Not checking if the issue is with roaming between APs vs staying connected to one AP"],causes:["User or application disconnecting intentionally","Signal loss — too far from AP or interference","802.1X authentication failure (certificate, RADIUS server)","AP deauthentication for load balancing or channel change","WiFi driver bug","Power management putting NIC to sleep (aggressive power saving)","Roaming failure between APs"],steps:["Filter System log for Event 10317","Note the SSID, adapter name, and disconnect reason code","Check signal strength at the time of disconnect (may need AP or NMS)","Check WiFi driver version: Get-NetAdapter -Name Wi-Fi | Select-Object DriverVersion","Disable aggressive WiFi power management: Set-NetAdapterPowerManagement -Name Wi-Fi -AllowComputerToTurnOffDevice Disabled","Check if 802.1X authentication is used and if certificates are valid","Check channel utilisation on the AP if signal is adequate"],symptoms:["wifi drops","wireless disconnects","wifi keeps disconnecting","wireless connection drops","wifi unstable","wireless keeps dropping","wifi falls off","laptop wifi disconnecting","intermittent wifi","wifi drops every few minutes"],tags:["wifi","wireless","wlan","connectivity","disconnect","network"],powershell:`# WiFi Disconnection Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

# Find WiFi disconnect events
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-WLAN-AutoConfig'
    Id           = 10317
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List

# Check current WiFi adapter power management
Write-Host "
--- WiFi Power Management ---" -ForegroundColor Cyan
Get-NetAdapter | Where-Object { $_.MediaType -like '*802.11*' } | ForEach-Object {
    Get-NetAdapterPowerManagement -Name $_.Name | Select-Object Name, AllowComputerToTurnOffDevice
} | Format-Table -AutoSize

# Current WiFi state
Write-Host "
--- Current WiFi Connections ---" -ForegroundColor Cyan
netsh wlan show interfaces`,related_ids:[10400,1014,1030],ms_docs:null},{id:10400,source:"Microsoft-Windows-WLAN-AutoConfig",channel:"Network",severity:"Error",skill_level:"Intermediate",title:"WLAN: Association Failed",short_desc:"The wireless adapter could not associate with a Wi-Fi access point.",description:"Event ID 10400 records a failure to associate with a Wi-Fi access point. This is different from a disconnection (10317) — association failure occurs when the device is trying to connect but the 802.11 association exchange fails. This can happen because the SSID doesn't exist at that location, the authentication method is mismatched, the PSK (password) is wrong, or the AP is rejecting the client. After a successful association, authentication happens separately; if authentication then fails, that generates a different event.",why_it_happens:"Wi-Fi association is the 802.11 handshake where a client requests to join an AP's BSS. The AP can reject the association for several reasons: the authentication type isn't supported, the client is blocked (MAC filtering), the AP is at capacity (maximum associations reached), or there is a mismatch in security settings (WPA2 vs WPA3). After association, 802.1X or PSK authentication occurs.",what_good_looks_like:"No Event 10400 events. Any 10400 indicates the WiFi join process failed before authentication could even complete.",common_mistakes:["Not distinguishing between association failure (10400) and authentication failure (different event) — they require different fixes","Trying to fix PSK when the issue is an association-level rejection (MAC filter, capacity)","Not checking if the SSID is broadcast or hidden and whether the client has the correct SSID configured"],causes:["Wrong or missing PSK for WPA2-Personal networks","Security type mismatch (WPA2 vs WPA3)","AP at maximum association capacity","MAC address filtering on AP rejecting the client","SSID moved to a different frequency band client doesn't support","Driver not supporting the AP's 802.11 standard (e.g., WiFi 6E without WiFi 6 driver)"],steps:["Filter System log for Event 10400","Note the SSID and reason code in the event","Check if the SSID is visible in wireless networks: netsh wlan show networks","Verify the PSK is correct by forgetting and re-entering the network","Check for MAC filtering on the AP if PSK is definitely correct","Update WiFi driver: check device manager for updates","Check if other devices can connect to the same SSID from the same location"],symptoms:["wifi wont connect","cant connect to wifi","wifi association failed","wireless connection failed","cant join wifi network","wifi authentication failed","wrong wifi password even though correct","wifi refuses to connect"],tags:["wifi","wireless","wlan","association","connectivity","authentication"],powershell:`# WiFi Association Failure Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-WLAN-AutoConfig'
    Id           = 10400
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List

# List available WiFi networks
Write-Host "
--- Available WiFi Networks ---" -ForegroundColor Cyan
netsh wlan show networks mode=bssid`,related_ids:[10317,1014,1030],ms_docs:null},{id:4226,source:"Tcpip",channel:"Network",severity:"Warning",skill_level:"Advanced",title:"TCP Connection Limit Reached",short_desc:"Windows reached the limit for simultaneous incomplete outbound TCP connection attempts.",description:"Event ID 4226 from the Tcpip source records that the system reached the limit on simultaneous half-open TCP connection attempts. Windows limits incomplete outbound connections to throttle connection-rate-based malware (worms). On modern Windows 10/11 and Server, this limit was significantly increased (from 10 per second on XP to effectively unlimited on modern versions). If you see this event on a modern Windows system, it is unusual and may indicate aggressive scanning software, peer-to-peer applications, or malware performing port scans or rapid connection attempts.",why_it_happens:"TCP half-open connections (SYN sent, SYN-ACK not yet received) are counted by the TCP/IP stack. The limit exists to prevent a single compromised machine from participating in SYN-flood attacks or worm propagation by limiting how fast it can initiate outbound connections. On modern Windows, this limit only triggers for very aggressive connection rates, so seeing this event warrants investigation of what is generating so many simultaneous connections.",what_good_looks_like:"This event should essentially never appear on normal workstations or servers. If it does: immediately investigate what process is initiating so many connections. This is a strong indicator of malware, a misconfigured P2P application, or a security scanner.",common_mistakes:["On Windows XP: this event was commonly triggered by legitimate P2P software and the limit was a nuisance. On modern Windows, it should not appear in normal operation.","Assuming any process hitting this limit is malicious — security scanners and some backup tools can also trigger it"],causes:["Malware performing port scanning or worm propagation","P2P application (BitTorrent) with many simultaneous connections","Security scanner running on the endpoint","Backup or network monitoring software making many simultaneous connections","A browser loading a page with hundreds of parallel connections (rare)"],steps:["Filter System log for Event 4226","Note the time of the event","Check what process was generating connections around that time (Event 4688)","Use netstat to check current connections: netstat -anob","If malware suspected: isolate the machine and perform AV scan","Check for port scanning activity in firewall logs"],symptoms:["tcp connection limit","too many connections","network connection limit reached","tcp half open connections","connection rate limit","port scan detected"],tags:["tcp","network","connection-limit","malware","scanning","advanced"],powershell:`# TCP Connection Limit Investigation
# Eventful

$computer  = $env:COMPUTERNAME  # Replace with remote hostname if needed
$startTime = (Get-Date).AddHours(-24)  # Adjust time range as needed

# Find TCP limit events
Get-WinEvent -ComputerName $computer -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Tcpip'
    Id           = 4226
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, LevelDisplayName, Message | Format-List

# Check current TCP connections (run locally or via PSRemoting)
Write-Host "
--- Active TCP Connections (top talkers by process) ---" -ForegroundColor Cyan
Get-NetTCPConnection -State Established |
    Group-Object OwningProcess |
    Sort-Object Count -Descending |
    Select-Object -First 10 Count,
        @{N='Process'; E={ (Get-Process -Id $_.Name -ErrorAction SilentlyContinue).ProcessName }} |
    Format-Table -AutoSize`,related_ids:[4688,4624],ms_docs:null},{id:4201,source:"Microsoft-Windows-NDIS",channel:"System",severity:"Info",skill_level:"Fundamental",title:"Network Adapter Connected",short_desc:"A network adapter transitioned to a connected state — cable plugged in or wireless associated.",description:"Event 4201 from Microsoft-Windows-NDIS (Network Driver Interface Specification) is generated when a network adapter reports that its link state has changed to connected. This includes physical cable connections, Wi-Fi associations, and virtual NIC link changes. Pairing 4201 (connect) with 4202 (disconnect) gives a timeline of NIC link flaps, which are often the root cause of intermittent network problems, brief outages, or VPN drops.",why_it_happens:"Link state changes are physical events reported by the network adapter driver. A 4201 event happens when a cable is plugged in, when the switch port comes up, or when a wireless client successfully associates. Normal startup causes 4201; intermittent 4201/4202 pairs during normal operation indicate a physical layer problem.",what_good_looks_like:"Single 4201 event at system boot/startup. No further 4201/4202 pairs during normal operation. NIC drivers are current and NIC hardware is healthy.",common_mistakes:["Not correlating 4201/4202 pairs when investigating intermittent network outages — link flapping is often the root cause","Not checking the cable or switch port when 4201/4202 events show repeated link flaps"],causes:["Normal system startup — NIC initializing","Cable plugged in or switch port brought up","Wireless client associating with access point","Virtual switch or virtual NIC coming online","Recovery after a link flap — often paired with 4202 just before"],steps:["Check for paired 4201/4202 events — repeated pairs indicate link flapping","If flapping: inspect the cable, RJ45 connector, and switch port","Update NIC driver from vendor website","Check switch port statistics for errors on the port","Test with a known-good cable if physical connection is suspected"],symptoms:["network adapter connected","network cable plugged in","NIC link state changed","network link up","intermittent network drops","network connection flapping","NIC keeps connecting disconnecting"],tags:["network","ndis","nic","link-state","connectivity"],powershell:`# Network Adapter Link State History
# Eventful

# NIC connect/disconnect events
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-NDIS'
    Id           = @(4201, 4202)
    StartTime    = (Get-Date).AddHours(-24)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id,
        @{N='Event'; E={ if ($_.Id -eq 4201) {'CONNECTED'} else {'DISCONNECTED'} }},
        Message |
    Sort-Object TimeCreated | Format-Table -AutoSize`,related_ids:[4202,10317],ms_docs:null},{id:4202,source:"Microsoft-Windows-NDIS",channel:"System",severity:"Warning",skill_level:"Fundamental",title:"Network Adapter Disconnected",short_desc:"A network adapter lost its link — cable unplugged, switch port down, or wireless disassociated.",description:"Event 4202 is the disconnection counterpart to 4201. It is generated when a network adapter reports its link state has changed to disconnected. This includes physical cable disconnections, switch port going down, wireless disassociation, or driver/power management suspending the NIC. A single 4202 at system shutdown or sleep is normal; repeated 4202 events during operation indicate a physical or driver problem.",why_it_happens:"Link disconnection can be physical (cable fail, bad connector, switch port issue) or logical (driver crash, power management turning off NIC, wireless roaming event). When VPN or network-dependent services drop intermittently, 4202 events are the smoking gun showing the NIC itself dropped connection.",what_good_looks_like:"Only one 4202 event at system shutdown or sleep. No 4202 events during normal operation.",common_mistakes:['Not checking NIC power management settings — "Allow the computer to turn off this device to save power" is a common cause of random NIC disconnects',"Not checking the switch/access point side for errors — the NIC may be reporting disconnect because the switch port is flapping"],causes:["Cable disconnected or loose connector","Switch port going down or flapping","NIC driver crash causing virtual disconnect","Power management turning off NIC to save power","Wireless client disassociating or roaming to another AP","System suspending (sleep/hibernate)"],steps:["Check for repeated 4201/4202 pairs — repeated = link flapping",'Disable NIC power management: Device Manager → NIC → Properties → Power Management → uncheck "Allow computer to turn off this device to save power"',"Inspect cable and connectors; test with known-good cable","Check switch port counters for errors","Update NIC driver","If wireless: check signal strength and roaming aggressiveness settings"],symptoms:["network disconnected","NIC disconnected","network adapter link dropped","intermittent network loss","network drops out randomly","cable unplugged event","network keeps disconnecting","connection drops then reconnects","NIC link lost"],tags:["network","ndis","nic","link-state","disconnect","connectivity"],powershell:`# NIC Disconnect History and Power Management Check
# Eventful

# Link state events in the last 24 hours
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-NDIS'
    Id           = @(4201, 4202)
    StartTime    = (Get-Date).AddHours(-24)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id,
        @{N='Event'; E={ if ($_.Id -eq 4201) {'CONNECTED'} else {'DISCONNECTED'} }} |
    Sort-Object TimeCreated | Format-Table -AutoSize

# Check NIC power management setting (if PnP device = NIC)
Get-NetAdapter | ForEach-Object {
    $adapterName = $_.Name
    [PSCustomObject]@{
        Adapter      = $adapterName
        Status       = $_.Status
        LinkSpeed    = $_.LinkSpeed
    }
} | Format-Table -AutoSize`,related_ids:[4201,10317,10400],ms_docs:null},{id:4199,source:"Tcpip",channel:"System",severity:"Error",skill_level:"Fundamental",title:"IP Address Conflict Detected",short_desc:"Two devices on the network are using the same IP address — one or both will lose connectivity.",description:"Event 4199 (or similar Tcpip events for IP conflict) is generated when the Windows TCP/IP stack detects that another device on the network is using the same IP address as this computer. This causes intermittent connectivity loss as the network switches route traffic to one device or the other. The event identifies the conflicting MAC address, which can be used to track down the other device.",why_it_happens:"IP conflicts occur when: a static IP is assigned to a device that is already being used by another device, a DHCP lease is not properly expired when a device is decommissioned and the same IP is later assigned to a new device, or a DHCP server gives out overlapping addresses due to misconfiguration.",what_good_looks_like:"No IP conflict events. All static IPs documented and excluded from DHCP scopes. DHCP lease times appropriate so expired leases are reclaimed before reuse.",common_mistakes:["Not documenting static IP assignments, leading to overlaps with DHCP scope","Not adding static IPs as exclusions in the DHCP scope"],causes:["Static IP assigned to a device that is already in the DHCP pool and has been leased to another device","Two devices manually configured with the same static IP","DHCP server giving out an address that is already statically assigned","Decommissioned device's IP reused before the lease expired on another machine"],steps:["Note the conflicting MAC address from the event","Use arp -a to find the device currently holding that IP","Identify the conflicting device from its MAC address using the network switch MAC table or DHCP server","Resolve: either change the static IP, add an exclusion in DHCP, or reclaim the DHCP lease","ipconfig /release && ipconfig /renew to get a new IP on the affected machine"],symptoms:["IP address conflict","duplicate IP address","two computers same IP","network conflict IP","intermittent network due to IP conflict","address already in use","IP conflict detected","DHCP IP conflict"],tags:["network","ip","conflict","dhcp","tcp-ip"],powershell:`# IP Conflict Detection
# Eventful

# IP conflict events in System log
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Tcpip'
    Id           = @(4199, 4198)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending | Format-List

# Current ARP cache (find conflicting MAC)
arp -a`,related_ids:[1020,4201],ms_docs:null}],n=[{id:307,source:"Microsoft-Windows-PrintService",channel:"PrintService/Operational",severity:"Info",skill_level:"Fundamental",title:"Document Printed Successfully",short_desc:"A document completed printing — records who printed, which printer, page count, and document name.",description:"Event 307 from PrintService is written every time a document finishes printing successfully. It captures the submitting user, document name, printer name, port, byte size, and page count. This is the primary audit source for print activity and is invaluable when investigating who printed a sensitive document, building a print usage report, or confirming a job actually completed. The catch: the PrintService/Operational log is disabled by default and must be enabled before any events appear.",why_it_happens:"The Windows Print Spooler writes Event 307 to the PrintService/Operational log after a job completes all rendering and despooling. The log must be manually enabled — go to Event Viewer → Applications and Services Logs → Microsoft → Windows → PrintService → Operational → right-click → Enable Log.",what_good_looks_like:"Regular Event 307 entries matching expected business printing. Investigate: large jobs from unexpected users, printing outside business hours, jobs from accounts that should not access certain documents.",common_mistakes:["Not enabling the PrintService/Operational log first — it is off by default and generates zero events until enabled","Assuming no Event 307 means no printing occurred — the log may simply be disabled","Not checking the document name field, which often reveals what was printed"],causes:["User successfully printed a document from any application","A service or scheduled task generated a print job (reports, invoices, etc.)"],steps:["Enable the log first: Event Viewer → Apps and Services Logs → Microsoft → Windows → PrintService → Operational → Enable Log","Filter for Event ID 307 in PrintService/Operational","Check Document Name, User Name, and Printer Name fields in each event","To audit a specific user or printer: use PowerShell snippet below with a Where-Object filter","Cross-reference with Event 6161 if jobs were expected but did not complete"],symptoms:["who printed this document","print audit log","print job history","who sent job to printer","document print record","see all print jobs","print activity log","who used the printer","print history","audit what was printed","print job audit trail","track printing activity"],tags:["print","audit","document","printer","operational","history"],powershell:`# Print Job Audit — last 7 days
# Eventful
# Note: PrintService/Operational log must be enabled first

Get-WinEvent -FilterHashtable @{
    LogName   = 'Microsoft-Windows-PrintService/Operational'
    Id        = 307
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue | ForEach-Object {
    [PSCustomObject]@{
        TimeCreated = $_.TimeCreated
        User        = $_.UserId
        Message     = $_.Message
    }
} | Sort-Object TimeCreated -Descending | Format-List`,related_ids:[372,808,6161,1111],ms_docs:null},{id:372,source:"Microsoft-Windows-PrintService",channel:"PrintService/Admin",severity:"Error",skill_level:"Intermediate",title:"Print Driver Failed to Initialize",short_desc:"A printer driver module failed to load — commonly causes the printer to stop working or the spooler to crash.",description:"Event 372 means the print spooler failed to load or initialize a printer driver module (a plug-in DLL). This commonly follows a driver update, driver corruption, or adding a new printer with an incompatible driver. When a driver fails to initialize, print jobs through it fail silently or remain stuck in the queue. In persistent cases the Print Spooler service itself crashes (Event 7031) and restarts in a loop. The event message names the specific driver module, pointing you directly at which printer is the culprit.",why_it_happens:"Printer drivers consist of a UI DLL, rendering DLL, and configuration DLL. If any fails to load — due to a missing file, corrupt registry key, architecture mismatch (32-bit driver on 64-bit spooler), or a missing dependency — Event 372 fires. Windows Server print servers are especially vulnerable after cumulative updates that ship new spooler binaries incompatible with older third-party drivers.",what_good_looks_like:"No Event 372 in a healthy environment. Any occurrence warrants investigation — even if printing appears to work, a partially-initialized driver causes intermittent failures.",common_mistakes:["Restarting the print spooler without addressing the driver — it will fail again immediately on restart","Reinstalling the printer without removing the driver from the driver store — the corrupt driver gets reused","Not checking which specific driver is failing — the event names the module, telling you exactly which printer to target","Forgetting to also remove the driver from the driver store (pnputil), not just uninstall the printer"],causes:["Corrupt or incomplete printer driver installation","Windows Update modified spooler components incompatible with an existing third-party driver","Driver architecture mismatch — 32-bit driver on a 64-bit spooler or vice versa","Missing driver dependency DLL","Driver version conflict when two printers share the same driver","Antivirus quarantining a driver DLL"],steps:["Note the driver name and module from the Event 372 message",'Check if the spooler is crash-looping: look for Event 7031 with "Print Spooler" around the same time',"Open Print Management or Devices and Printers — identify which printer uses the failing driver","Remove the printer and driver: printui /s /t2, select the driver, click Remove","Remove from driver store: pnputil /enum-drivers — find the matching INF, then pnputil /delete-driver <inf> /uninstall","Restart the Print Spooler: Restart-Service Spooler","Reinstall the latest driver from the manufacturer website","On a print server: check Print Management → Drivers for orphaned or duplicate drivers"],symptoms:["print driver failed","printer stopped working","printer driver error","driver failed to load","print driver crash","printer not working after windows update","spooler crashing with driver error","printer driver initialization failed","printer broken after update","print driver plug-in error","printer stopped printing after update","driver load failure print"],tags:["print","driver","error","spooler","print-service","crash"],powershell:`# Check for print driver and spooler errors — last 7 days
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName   = 'Microsoft-Windows-PrintService/Admin'
    Id        = @(372, 808, 1111, 6161)
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List

# Also check if spooler has been crashing
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Service Control Manager'
    Id           = @(7031, 7034)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Where-Object { $_.Message -like '*Print Spooler*' } |
    Select-Object TimeCreated, Message | Format-List`,related_ids:[808,1111,6161,307,7031],ms_docs:null},{id:808,source:"Microsoft-Windows-PrintService",channel:"PrintService/Admin",severity:"Error",skill_level:"Intermediate",title:"Print Spooler Failed to Share Printer",short_desc:"The spooler could not publish a printer as a network share — other computers cannot connect to it.",description:"Event 808 means the Windows Print Spooler failed to share a printer on the network. When sharing is configured, the spooler tries to register the printer as a network share (and optionally publish it in Active Directory). Failure means clients cannot connect via UNC path (\\\\servername\\printername). The error code in the message is the key diagnostic — common codes point to the Server service being stopped, firewall blocking sharing ports, or insufficient permissions to publish in AD.",why_it_happens:"Printer sharing relies on the Server (LanmanServer) service and the RPC endpoint mapper. Failures occur when the Server service is stopped, when the spooler account lacks AD publish permissions, when firewall rules block ports TCP 139/445, or when the share name conflicts with an existing share.",what_good_looks_like:"No Event 808. If sharing is expected, confirm success by verifying the printer appears to remote clients with no 808 in the log.",common_mistakes:['Seeing "Shared" checked in printer properties and assuming sharing worked — that flag just means sharing is configured, not that it succeeded',"Not checking if the Server service (LanmanServer) is running","Forgetting that the spooler account needs specific AD rights to publish printers in the directory"],causes:["Server service (LanmanServer) stopped or disabled","Windows Firewall blocking File and Printer Sharing ports (TCP 139, 445)","Print spooler service account lacks permission to create the share or publish in AD","Share name conflicts with an existing network share","RPC service not responding","Group Policy blocking network sharing"],steps:["Check the error code in the Event 808 message body","Verify the Server service is running: Get-Service LanmanServer",'Check firewall allows File and Printer Sharing: netsh advfirewall firewall show rule name="File and Printer Sharing*"',"Try resharing: open Printer Properties → Sharing → uncheck Shared → OK → re-check → OK","Check System log for Service Control Manager errors at the same timestamp","If publishing to AD is required: verify the machine account has rights to create printerQueue objects in the OU"],symptoms:["printer not showing on network","cannot connect to shared printer","printer share failed","other computers cannot see printer","shared printer unavailable","network printer not accessible","printer share not working","printer disappeared from network","cannot map network printer","printer not published in active directory","shared printer not visible","printer share error"],tags:["print","sharing","network","spooler","print-service","server","active-directory"],powershell:`# Check printer sharing and spooler/server service status
# Eventful

# Service status
Get-Service Spooler, LanmanServer | Select-Object Name, Status, StartType

# Printer sharing errors — last 7 days
Get-WinEvent -FilterHashtable @{
    LogName   = 'Microsoft-Windows-PrintService/Admin'
    Id        = 808
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Format-List

# All shared printers and their current status
Get-Printer | Where-Object Shared |
    Select-Object Name, ShareName, PortName, DriverName, PrinterStatus`,related_ids:[372,1111,6161,307,7031],ms_docs:null},{id:1111,source:"Microsoft-Windows-PrintService",channel:"PrintService/Admin",severity:"Error",skill_level:"Intermediate",title:"Printer Driver Failed During Job Processing",short_desc:"The printer driver crashed or errored while rendering a print job — the job failed and the driver likely needs reinstalling.",description:"Event 1111 fires when a printer driver fails during the actual processing of a print job. Unlike Event 372 (driver fails to initialize at spooler start), Event 1111 fires mid-job: the spooler has loaded the driver, the job has started, and then the driver crashes or returns an unexpected error code. The driver name and error code in the event message identify which printer is affected. Repeated Event 1111 for the same driver almost always means a corrupt or incompatible driver that must be removed and reinstalled.",why_it_happens:"Print jobs are rendered inside the spooler process by the driver's rendering DLL. This DLL transforms the job data into the printer's page description language (PCL, PostScript, XPS, etc.). If the DLL has a bug, memory corruption, or an incompatibility with the document format or a recent Windows update, it returns an error and the spooler marks the job as failed.",what_good_looks_like:"No Event 1111. A single isolated occurrence after a malformed document may be benign. Repeated failures for the same driver are always a driver problem.",common_mistakes:["Retrying the same print job without fixing the driver — it will fail every time","Reinstalling the printer without purging the driver from the driver store — the corrupt driver is reused","Missing that Event 1111 on a shared print server affects every user of that shared printer"],causes:["Corrupt printer driver rendering DLL","Driver incompatibility with the current spooler version after a Windows update","Document format that the driver cannot render (specific font, oversized PDF, etc.)","Insufficient spool disk space preventing job staging","Antivirus blocking driver DLL execution in-process"],steps:["Note the driver name from the Event 1111 message","Clear the stuck queue: net stop Spooler → del /Q /F /S C:\\Windows\\System32\\spool\\PRINTERS\\* → net start Spooler","Remove and reinstall the driver: printui /s /t2 → select driver → Remove","Remove from driver store: pnputil /enum-drivers, find the INF, then pnputil /delete-driver <inf> /uninstall","Check disk space on the spool drive — full disk prevents spooling","Test with a simple one-page document to rule out document-specific corruption","On a print server: determine if multiple users are affected (server driver) vs just one user (may be client-side)"],symptoms:["print job failed","document wont print","print job disappears without printing","printer driver error mid job","printer keeps failing on same document","print job error","document stuck in print queue","printing fails every time","print job removed from queue automatically","printer driver crashed during printing","job deleted from queue without printing","printer driver failing repeatedly"],tags:["print","driver","error","job","spooler","print-service","rendering"],powershell:`# Driver failures and current print queue
# Eventful

# Recent driver job failures
Get-WinEvent -FilterHashtable @{
    LogName   = 'Microsoft-Windows-PrintService/Admin'
    Id        = @(1111, 372)
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List

# Current jobs in all queues
Get-PrintJob -PrinterName * |
    Select-Object PrinterName, DocumentName, JobStatus, UserName, SubmittedTime`,related_ids:[372,808,6161,307,7031],ms_docs:null},{id:6161,source:"Microsoft-Windows-PrintService",channel:"PrintService/Admin",severity:"Error",skill_level:"Fundamental",title:"Document Failed to Print",short_desc:"A specific print job failed — records who submitted it, which document, which printer, and why.",description:'Event 6161 is the most visible print failure event — it fires when a document queued for printing fails to complete. It records the document name, submitting user, printer, and an error description. This is the first event to check when a user reports "I tried to print but nothing came out." It can be caused by driver errors (correlate with 1111 or 372), the printer going offline, a port timeout, or the spooler crashing. Event 6161 is the symptom — Events 1111, 372, and 7031 nearby will tell you the cause.',why_it_happens:"A print job progresses through spooling (write to disk), despooling (send to driver), and rendering (convert to printer language). Failure at any stage produces Event 6161. Common triggers: printer offline mid-job, driver crash (see 1111), port timeout, spooler restart, or insufficient spool disk space.",what_good_looks_like:"No Event 6161. Even a single occurrence is worth investigating — the user sent a job that never printed.",common_mistakes:["Telling users to retry without first verifying the printer is actually online and ready","Not reading the error description in the event message body — it states the specific failure reason","Not correlating with Event 1111 (driver failure) or 7031 (spooler crash) — 6161 is the symptom, not the cause","Forgetting to check if the printer has paper, toner, and is not in error/offline state"],causes:["Printer offline, out of paper, out of toner, or showing an error state","Printer driver failed during rendering — check Event 1111","Print Spooler service crashed mid-job — check Event 7031","Printer port connection timeout or network printer unreachable","Insufficient disk space in the spool folder (C:\\Windows\\System32\\spool\\PRINTERS)","Document corrupted or in an unsupported format for the target driver"],steps:["Check the document name, user, and error description in Event 6161","Verify the printer is online and not showing a hardware error (paper, toner, error light)","Look for Event 1111 or 372 near the same timestamp — driver failure is the most common cause",'Look for Event 7031 with "Print Spooler" — spooler crash kills in-progress jobs',"Check spool disk space: Get-PSDrive C","Clear the queue and retry: net stop Spooler → delete spool files → net start Spooler","If a network printer: confirm it is reachable — ping or open its web UI in a browser"],symptoms:["document failed to print","print job disappeared","nothing came out of printer","print job not printing","sent to printer no output","document deleted from print queue","print job failed to complete","user cannot print","print jobs keep failing","printer not printing documents","job vanishes from queue","printing nothing comes out"],tags:["print","error","job","document","spooler","print-service","fundamental"],powershell:`# Recent print failures
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName   = 'Microsoft-Windows-PrintService/Admin'
    Id        = 6161
    StartTime = (Get-Date).AddDays(-3)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[1111,372,808,307,7031],ms_docs:null}],a=[{id:2,source:"DNS",channel:"DNS Server",severity:"Info",skill_level:"Fundamental",title:"DNS Server Started",short_desc:"The DNS Server service started — useful for pinpointing when DNS came back online after an outage.",description:'Event 2 in the DNS Server log records when the DNS service starts. On its own it is informational, but its timing is valuable in incident analysis: confirming when DNS came back after an outage, verifying DNS started after a server reboot, or identifying if DNS was stopped and restarted mid-session. The gap between Event 3 (stopped) and Event 2 (started) is the exact DNS downtime window. Note: DNS events are written to the "DNS Server" event log, not the System log.',why_it_happens:"Windows DNS Server writes this event to its own DNS Server log whenever the DNS service starts — whether from a system boot, a manual restart, or automatic recovery after a crash.",what_good_looks_like:"Event 2 appearing shortly after a system boot (matching Event 6005 in the System log). An Event 2 without a preceding Event 3 from the same session may indicate the service was restarted or recovered mid-session.",common_mistakes:['Looking for DNS start events in the System log — DNS writes to its own "DNS Server" log (Windows Logs → DNS Server in Event Viewer)',"Not correlating Event 2 timestamps with client DNS failure windows — the gap between Events 3 and 2 is the outage duration"],causes:["System boot sequence starting all services","Administrator manually restarted the DNS service","Automatic service recovery after a crash","DNS restarted after a configuration change"],steps:["Open Event Viewer → Windows Logs → DNS Server","Filter for Event IDs 2 (start) and 3 (stop) to build a DNS availability timeline","Correlate with System Events 6005/6008 to determine if DNS started with the OS or was restarted separately","If investigating a client DNS outage: the gap between Event 3 and the next Event 2 is the exact downtime window"],symptoms:["when did dns server start","dns server restarted","dns came back online","dns availability timeline","dns service restart time","when was dns restored","dns start time","dns service recovery"],tags:["dns","service","start","availability","server","timeline"],powershell:`# DNS Server start/stop history — last 30 days
# Eventful

Get-WinEvent -FilterHashtable @{
    LogName   = 'DNS Server'
    Id        = @(2, 3)
    StartTime = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id,
        @{N='Event'; E={ if ($_.Id -eq 2) {'STARTED'} else {'STOPPED'} }} |
    Sort-Object TimeCreated | Format-Table -AutoSize`,related_ids:[3,4e3,4007],ms_docs:null},{id:3,source:"DNS",channel:"DNS Server",severity:"Warning",skill_level:"Fundamental",title:"DNS Server Stopped",short_desc:"The DNS Server service stopped — clients using this server for name resolution will fail until it restarts.",description:"Event 3 in the DNS Server log records when the DNS service stops. This covers both clean stops (admin action, planned maintenance) and stops caused by a crash or service failure. If DNS stops and does not immediately restart, all clients using this server for name resolution begin failing. On a domain controller, DNS stopping also breaks Kerberos authentication and AD replication, which both rely on DNS. Event 3 without a following Event 2 means the service stayed down.",why_it_happens:"DNS Server service stops when: an administrator explicitly stops it, the server is shutting down, the service crashes and is not configured to auto-restart, or a configuration corruption causes start failure.",what_good_looks_like:"Event 3 should only appear during planned maintenance or server shutdown. Any Event 3 outside a maintenance window should be investigated immediately — check whether Event 2 follows quickly (auto-recovery) or the service stayed stopped.",common_mistakes:["Not checking if DNS auto-restarted — look for Event 2 immediately after the Event 3","Forgetting that DNS stopping on a DC causes Kerberos failures and domain authentication outages for all clients",'Not checking System log for Event 7034 or 7031 with "DNS Server" which would show why it crashed'],causes:["Administrator manually stopped the service for maintenance",'Service crash — check System log for Event 7034 naming "DNS Server"',"Server shutdown or restart in progress","DNS configuration corruption causing start failure on recovery attempt"],steps:["Note the timestamp and check if Event 2 follows shortly — if not, DNS was down for an extended period",'Check System log for Event 7034 or 7031 with "DNS Server" around the same time',"Restart DNS if stopped: Start-Service DNS or net start dns","Verify zones loaded after restart: Get-DnsServerZone — check no zones are in a failed state","If on a DC: check AD replication health with repadmin /replsummary after DNS is restored"],symptoms:["dns server stopped","dns service stopped","dns not responding","dns server down","dns outage","name resolution stopped working","dns service crashed","clients cannot resolve dns","dns went offline","no dns resolution","domain name lookup failing","dns server unavailable"],tags:["dns","service","stop","outage","server","availability","critical"],powershell:`# DNS service status and recent stop/start history
# Eventful

# Current DNS service status
Get-Service DNS | Select-Object Name, Status, StartType

# Stop and start history — last 7 days
Get-WinEvent -FilterHashtable @{
    LogName   = 'DNS Server'
    Id        = @(2, 3)
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id,
        @{N='Event'; E={ if ($_.Id -eq 2) {'STARTED'} else {'STOPPED'} }} |
    Sort-Object TimeCreated | Format-Table -AutoSize`,related_ids:[2,4e3,4007],ms_docs:null},{id:4e3,source:"DNS",channel:"DNS Server",severity:"Error",skill_level:"Intermediate",title:"DNS Server Cannot Access Active Directory",short_desc:"The DNS server cannot read zone data from Active Directory — all AD-integrated DNS zones will fail to load.",description:"Event 4000 means the DNS Server service could not open or connect to Active Directory to load DNS zone data. Windows DNS servers in an AD environment store zone records in AD (AD-integrated zones). If DNS cannot reach AD, it cannot load those zones, and resolution fails for any AD-integrated zone. On a domain controller, this almost always indicates an AD DS service problem rather than a DNS problem — DNS is the symptom, the AD DS service or database is the cause.",why_it_happens:"AD-integrated zone data is stored in the AD database (NTDS.DIT) and accessed by DNS via LDAP. If the AD DS service (ntds) is not running, if LDAP is unavailable, or if AD replication left the DC with corrupt partition data, DNS cannot read zone information and logs Event 4000.",what_good_looks_like:"Event 4000 should never appear in a healthy environment. Its presence always warrants investigation into AD DS health on that domain controller.",common_mistakes:["Restarting the DNS service without first confirming AD DS is running — DNS will fail again immediately","Treating DNS as the root cause when the actual problem is AD DS service failure","Not checking AD replication health alongside Event 4000 — they often occur together"],causes:["Active Directory Domain Services (NTDS) service not running","LDAP port unavailable on the local machine","AD database corruption","AD replication failure leaving partition data unavailable","DNS service starting before AD DS completes initialization on boot (usually self-resolves)"],steps:["Check if AD DS is running: Get-Service NTDS","If NTDS stopped: Start-Service NTDS, then check the Directory Service event log for errors","Run AD replication health: repadmin /replsummary","Test LDAP connectivity locally: Test-NetConnection 127.0.0.1 -Port 389","Once AD DS is confirmed healthy: Restart-Service DNS","Verify zones loaded: Get-DnsServerZone — check for failed zones","Run full DNS/AD diagnostic: dcdiag /test:dns /v"],symptoms:["dns cannot access active directory","ad integrated dns zone not loading","dns zones failed to load","dns server cannot read active directory","dns zones missing on domain controller","active directory dns failure","dns zone load error on dc","dns broken after ad problem","dns zones empty after reboot","all dns zones missing"],tags:["dns","active-directory","zone","error","domain-controller","ad-integrated"],powershell:`# DNS and Active Directory health check
# Eventful

# Service status
Get-Service NTDS, DNS | Select-Object Name, Status

# DNS zone status
Get-DnsServerZone |
    Select-Object ZoneName, ZoneType, IsDsIntegrated, ZoneFile, IsReverseLookupZone

# AD replication summary
repadmin /replsummary

# Recent DNS AD errors
Get-WinEvent -FilterHashtable @{
    LogName   = 'DNS Server'
    Id        = @(4000, 4007)
    StartTime = (Get-Date).AddDays(-1)
} -ErrorAction SilentlyContinue | Select-Object TimeCreated, Message | Format-List`,related_ids:[3,4007,2],ms_docs:"https://learn.microsoft.com/en-us/troubleshoot/windows-server/networking/dns-server-event-4000-4007"},{id:4007,source:"DNS",channel:"DNS Server",severity:"Error",skill_level:"Intermediate",title:"DNS Zone Not Loaded from Active Directory",short_desc:"A specific DNS zone failed to load from AD — records for that zone are unavailable until the zone loads.",description:"Event 4007 is zone-specific: DNS connected to Active Directory successfully but could not load data for a particular zone. The zone name is in the event message. This is more targeted than Event 4000 (which means AD is completely unreachable). With Event 4007, AD is accessible but the specific zone partition has a problem — common on newly promoted DCs that have not yet received zone data via replication, or when a zone was deleted on one DC and the deletion is replicating.",why_it_happens:"AD-integrated DNS zones are stored in specific AD partition objects (ForestDNSZones or DomainDNSZones). If the zone object is corrupt, missing from the local replica, or if replication has not yet delivered the zone data to this DC, Event 4007 fires.",what_good_looks_like:"No Event 4007. A newly promoted DC may briefly log 4007 for zones not yet replicated — this normally resolves after initial replication completes. Persistent 4007 for an active zone requires investigation.",common_mistakes:["Assuming all DNS is broken when only one specific zone failed — check which zone name appears in the message","Not checking AD replication to see whether zone data has actually arrived on this DC","Deleting and recreating a zone without checking whether other DCs have already replicated the original"],causes:["Zone data not yet replicated to this DC after zone creation or DC promotion","AD replication failure leaving zone partition out of sync","Zone object deleted on another DC with the deletion replicating here","Corrupt zone object in the local AD database","ForestDNSZones or DomainDNSZones partition not fully mounted"],steps:["Note the zone name from the Event 4007 message","Check replication: repadmin /showrepl to see if this DC is receiving updates","Verify the zone exists on another DC: Get-DnsServerZone -ComputerName <other-dc>","Force replication: repadmin /syncall /Ade","After sync, restart DNS: Restart-Service DNS — check if 4007 reoccurs","If zone is genuinely missing everywhere: recreate it as an AD-integrated zone","Run dcdiag /test:dns for full DNS/AD diagnostic"],symptoms:["dns zone not loading","specific dns zone failed","zone missing from dns","dns zone load failure ad","ad dns zone error","dns zone not replicating","dns zone unavailable","records not resolving for specific domain","dns zone failed after replication issue","new dc missing dns zones","zone empty after dc promotion"],tags:["dns","zone","active-directory","replication","error","domain-controller"],powershell:`# DNS zone errors and replication status
# Eventful

# Zone load errors — last 7 days
Get-WinEvent -FilterHashtable @{
    LogName   = 'DNS Server'
    Id        = 4007
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue | Select-Object TimeCreated, Message | Format-List

# All DNS zones and status
Get-DnsServerZone |
    Select-Object ZoneName, ZoneType, IsDsIntegrated, ZoneFile, IsReverseLookupZone

# Replication summary
repadmin /replsummary`,related_ids:[4e3,2,3],ms_docs:"https://learn.microsoft.com/en-us/troubleshoot/windows-server/networking/dns-server-event-4000-4007"},{id:6527,source:"DNS",channel:"DNS Server",severity:"Warning",skill_level:"Advanced",title:"DNS Zone Transfer Failed",short_desc:"A zone transfer from this DNS server to a secondary failed — the secondary may serve stale records.",description:"Event 6527 means an attempt to transfer a DNS zone to a secondary DNS server (AXFR full transfer or IXFR incremental) did not complete. Zone transfers keep secondary DNS servers in sync with the primary. Repeated transfer failures mean the secondary serves outdated records to clients. This is most relevant in environments with secondary DNS servers outside the primary AD environment (ISPs, DMZ secondaries, external resolvers). Note: AD-integrated zone sync between DCs uses AD replication, not zone transfers — this event does not apply to DC-to-DC DNS sync.",why_it_happens:"Zone transfers fail when: the requesting secondary's IP is not listed in the zone's transfer permission list, TCP port 53 is blocked between primary and secondary, the zone is very large causing a connection timeout, or the secondary server is decommissioned but still configured.",what_good_looks_like:"No Event 6527 for legitimate secondaries. Persistent 6527 from an IP you don't recognize may indicate an unauthorized transfer attempt.",common_mistakes:["Not checking if the secondary server IP is in the zone transfer permission list — this is the most common cause","Testing connectivity with ping — zone transfers require TCP 53, not ICMP","Confusing DC-to-DC AD replication with zone transfers — DCs do not use zone transfers to sync, they use AD replication"],causes:["Secondary server IP not listed in the zone's allowed transfer list","Firewall blocking TCP port 53 between primary and secondary","Secondary server decommissioned but still configured in zone notifications","Zone is too large causing the transfer connection to time out"],steps:["Note the secondary server IP from the Event 6527 message","Check zone transfer permissions: DNS Manager → Zone → Properties → Zone Transfers tab","Test TCP port 53 to the secondary: Test-NetConnection <secondary-IP> -Port 53","If the secondary is valid: add its IP to the zone transfer list","Force a test transfer: dnscmd /ZoneRefresh <zonename>","If the secondary is decommissioned: remove it from transfer and notify lists"],symptoms:["dns zone transfer failed","secondary dns not updating","dns zone not syncing to secondary","zone transfer error","secondary dns has stale records","secondary dns out of date","dns secondary server not syncing","zone transfer refused"],tags:["dns","zone-transfer","secondary","replication","warning","server"],powershell:`# DNS zone transfer configuration and errors
# Eventful

# Zone transfer settings
Get-DnsServerZone |
    Select-Object ZoneName, TransferServersSpecified, SecondaryServers, NotifyServers

# Zone transfer errors — last 7 days
Get-WinEvent -FilterHashtable @{
    LogName   = 'DNS Server'
    Id        = @(6527, 6702)
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Format-List`,related_ids:[2,3,4007],ms_docs:null}],c=[{id:1030,source:"Userenv",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Group Policy: Cannot Query GPO List",short_desc:"Windows could not retrieve the list of Group Policy Objects — network or domain connectivity problem.",description:"Event 1030 from source Userenv means Windows could not query Active Directory to get the list of Group Policy Objects that should apply to the user or computer. This is typically a domain controller connectivity problem: the machine cannot reach a DC over the network, DNS is not resolving the domain, or the Netlogon service is unavailable. As a result, Group Policy is not applied at all, and users may see stale cached policy or no policy enforcement. Event 1030 is often paired with Event 1058 (cannot access gpt.ini) from the same session.",why_it_happens:"Group Policy processing starts by querying Active Directory for the list of GPOs linked to the computer's OU, site, and domain. If the machine cannot reach a DC — because of a network issue, LDAP failure, or DNS not resolving the domain — it cannot build the GPO list and logs Event 1030.",what_good_looks_like:"No Event 1030. This event should never appear in a healthy, connected domain environment.",common_mistakes:["Treating this as a Group Policy problem when it is actually a network/DNS problem — fix connectivity first","Not checking if DNS resolves the domain name: Resolve-DnsName <domain> should return DC IPs","Forgetting to check if the machine has a valid IP and can reach a DC on the network","Missing that Event 1030 often clusters with 1058 from the same Group Policy processing cycle"],causes:["Machine cannot reach a domain controller (network issue, VLAN, firewall blocking LDAP port 389)","DNS not resolving the domain name to a valid DC IP","Netlogon service stopped or not running","Machine account expired or disabled in Active Directory","Time skew too large between client and DC — Kerberos requires time within 5 minutes"],steps:["Check DNS resolves the domain: Resolve-DnsName <domain.com>","Check DC connectivity: nltest /dsgetdc:<domain.com>","Check Netlogon service: Get-Service Netlogon","Check network path to DC: Test-NetConnection <DC-IP> -Port 389","Check time sync: w32tm /query /status — ensure time is within 5 minutes of DC","Review System log for any network or Netlogon errors at the same timestamp","Force a GP refresh after fixing connectivity: gpupdate /force"],symptoms:["group policy not applying","gpo not applying","group policy failed","cannot retrieve group policy objects","gpo list query failed","group policy not working","policy not applied at logon","group policy error on login","gpo not loading","user policy not applied","computer policy not applied","group policy connectivity error"],tags:["group-policy","gpo","error","domain","connectivity","netlogon","userenv"],powershell:`# Group Policy connectivity and error check
# Eventful

# Check DC is reachable and Netlogon is running
nltest /dsgetdc:$env:USERDNSDOMAIN
Get-Service Netlogon | Select-Object Name, Status

# Check DNS resolves the domain
Resolve-DnsName $env:USERDNSDOMAIN -ErrorAction SilentlyContinue | Select-Object Name, IPAddress

# Check time sync
w32tm /query /status

# Recent Group Policy errors in System log
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Userenv'
    Id           = @(1030, 1058)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[1058,1085,7017],ms_docs:"https://learn.microsoft.com/en-us/troubleshoot/windows-server/group-policy/event-id-1030-1058-group-policy"},{id:1058,source:"Userenv",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Group Policy: Cannot Access gpt.ini (SYSVOL Issue)",short_desc:"Windows cannot access the gpt.ini file for a GPO — typically a SYSVOL replication or network share problem.",description:"Event 1058 from Userenv means Windows found the list of GPOs that should apply but cannot access the gpt.ini file for one of them. The gpt.ini file lives in SYSVOL on the domain controller (\\\\domain\\SYSVOL\\domain\\Policies\\{GUID}\\gpt.ini). Failure to access this file means Group Policy cannot read the GPO version number and cannot apply the policy. The most common cause on domain controllers is a SYSVOL replication fault — one or more DCs have a SYSVOL folder that is not in sync. Event 1058 is almost always paired with Event 1030.",why_it_happens:"Group Policy reads GPO settings from SYSVOL — a shared folder on DCs that is synchronized by DFS-R (or the older FRS). If SYSVOL replication is broken, if SYSVOL is not shared (the share went missing), or if permissions on SYSVOL are incorrect, Windows cannot reach the gpt.ini files and logs Event 1058.",what_good_looks_like:"No Event 1058. Any occurrence should trigger an immediate SYSVOL health check on all DCs.",common_mistakes:["Running gpupdate /force on the client before fixing SYSVOL on the DC — the update will fail again","Only checking SYSVOL on one DC when the client may be authenticating to a different DC","Not checking whether the SYSVOL share itself exists on the DCs: net share SYSVOL","Confusing SYSVOL replication issues with network connectivity issues — the path in the error message tells you which DC is being contacted"],causes:["SYSVOL replication broken — DFS-R or FRS not syncing SYSVOL between DCs","SYSVOL share missing from a DC (net share SYSVOL returns not found)","Permissions on SYSVOL folder changed, blocking access","Network path to SYSVOL blocked by firewall","DC SYSVOL not yet initialized after promotion (usually self-resolves after initial sync)","AD replication failure causing GPO objects in AD to be out of sync with SYSVOL files"],steps:["Note which DC and GPO GUID appears in the Event 1058 message","Verify SYSVOL share exists on the contacted DC: net use \\\\<DC>\\SYSVOL","Check DFS-R SYSVOL replication health: dfsrdiag ReplicationState /member:<DC>","Check SYSVOL replication on all DCs: repadmin /replsummary","If SYSVOL is not shared, check DFS-R service on the DC: Get-Service DFSR","Check Event Viewer → DFS Replication log on the DC for replication errors","After fixing SYSVOL: force GP reapplication on clients: gpupdate /force"],symptoms:["group policy cannot access sysvol","gpt.ini not found","group policy sysvol error","sysvol not accessible","group policy gpo file missing","gpo version check failed","group policy not applying sysvol","dfsr sysvol not syncing","group policy sysvol replication issue","cannot read gpo settings","sysvol share missing","group policy broken after dc demotion"],tags:["group-policy","gpo","sysvol","dfsr","replication","error","userenv"],powershell:`# SYSVOL and Group Policy health check
# Eventful

# Test SYSVOL accessibility from this machine
Test-Path "\\\\$env:USERDNSDOMAIN\\SYSVOL"

# Check SYSVOL share on all DCs (run on a DC)
# net share SYSVOL

# DFS-R replication state
dfsrdiag ReplicationState /member:$env:COMPUTERNAME

# Recent gpt.ini errors
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Userenv'
    Id           = 1058
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[1030,1085,7017],ms_docs:"https://learn.microsoft.com/en-us/troubleshoot/windows-server/group-policy/event-id-1030-1058-group-policy"},{id:1085,source:"Microsoft-Windows-GroupPolicy",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"Group Policy: Failed to Apply a Setting",short_desc:"Windows successfully retrieved a GPO but failed to apply a specific setting within it — the policy name and error are recorded.",description:'Event 1085 is more specific than 1030 or 1058: Group Policy connected to AD and retrieved the GPO, but when trying to apply a particular extension (Security, Software Installation, Scripts, Folder Redirection, etc.), the application failed. The event message names the extension that failed (e.g. "Security" means security settings within the GPO failed to apply). This means most of the policy may have applied, but a specific category did not. The error code in the message points to the specific cause.',why_it_happens:"Group Policy applies different setting types through separate CSEs (Client-Side Extensions). If a CSE encounters an error — missing folder path for Folder Redirection, incorrect permissions for Software Installation, a corrupt policy file — it fails independently and logs Event 1085 for that extension.",what_good_looks_like:"No Event 1085. A specific GPO failing repeatedly for a specific extension means there is a misconfiguration in that GPO or a dependency issue on the client.",common_mistakes:["Trying to fix the computer without reading which extension failed — the extension name in the event tells you exactly which part of the GPO to check","Not running gpresult /h to see which specific setting within the failed extension was the problem",'Forgetting that 1085 for "Software Installation" commonly means the distribution point share is unreachable'],causes:["Folder Redirection target path missing or permission denied","Software Installation distribution point share unavailable","Security template referencing a missing or invalid account","Scripts CSE: logon/logoff script path unreachable","Drive Map CSE: target path or credentials invalid","Missing prerequisite (e.g. software deployment requires .NET Framework not installed)"],steps:["Read the extension name in Event 1085 — this tells you which category of policy failed","Run gpresult /h C:\\gpresult.html to get the full GP result with failure details","Open gpresult and find the failed GPO under the named extension","For Folder Redirection failures: verify the target path is accessible from the client","For Software Installation failures: verify the distribution share is reachable (net use \\\\server\\share)","For Security failures: check for references to deleted accounts or invalid SIDs in the GPO","Edit the GPO in GPMC to correct the misconfiguration, then gpupdate /force"],symptoms:["group policy failed to apply setting","gpo setting not applying","group policy partial failure","folder redirection not working","software deployment gpo failing","drive map not applying","logon script not running from gpo","security policy not applied","gpo extension failed","group policy applied with errors","gpo not fully applying","specific gpo setting not working"],tags:["group-policy","gpo","error","cse","extension","folder-redirection","software"],powershell:`# Group Policy result and extension failure report
# Eventful

# Generate HTML GP result report
gpresult /h C:\\Temp\\gpresult.html /f
Write-Host "Report saved to C:\\Temp\\gpresult.html — open in a browser"

# GP result summary in console
gpresult /r

# Recent Group Policy apply failures
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-GroupPolicy'
    Id           = 1085
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[1030,1058,7017,5312],ms_docs:null},{id:7017,source:"Microsoft-Windows-GroupPolicy",channel:"Microsoft-Windows-GroupPolicy/Operational",severity:"Warning",skill_level:"Intermediate",title:"Group Policy Processing Time Exceeded",short_desc:"Group Policy took longer than expected to process — typically causing slow login or slow startup for users.",description:"Event 7017 in the Group Policy Operational log means GP processing exceeded the expected time threshold. This is the primary diagnostic event when users complain about slow logins or slow computer startup. The event logs how long processing actually took and which components were slow. The most common culprits are software deployment (MSI packages over slow links), folder redirection to slow/distant file servers, or simply too many GPOs linked at too many levels. On laptops connecting over VPN, slow GP is extremely common.",why_it_happens:"Group Policy applies synchronously at startup (computer policy) and at logon (user policy) before the desktop appears. If any CSE — especially Software Installation, Folder Redirection, or Scripts — is slow to complete because of network latency, large MSI packages, or a large number of GPOs, total processing time increases and Event 7017 is generated.",what_good_looks_like:"No Event 7017. Logon times under 30 seconds are generally acceptable. Investigate if users regularly report logon times over 60 seconds.",common_mistakes:["Applying the fix to only one GPO when the problem is the accumulated total of many GPOs","Not running gpresult /h to see which specific CSE is consuming the most time",'Enabling "Always wait for network" Group Policy without understanding it forces synchronous processing and increases logon time'],causes:["Software Installation GPO deploying large MSI packages at logon","Folder Redirection to a file server over a slow link","Too many GPOs linked at domain, site, and OU level — total processing time adds up","Slow domain controller LDAP response","VPN or WAN latency between client and SYSVOL/DC"],steps:["Run gpresult /h C:\\Temp\\gpresult.html — the HTML report shows per-CSE processing times","Check the Operational log for which CSE had the longest duration: Event Viewer → Microsoft → Windows → GroupPolicy → Operational","For Software Installation delays: consider moving large deployments to a software deployment tool (Intune, SCCM)","For Folder Redirection delays: consider caching options or moving the file server closer","Run gpresult /z for a verbose text report showing all applied policies","Consider enabling loopback processing or reducing GPO links to flatten the processing chain"],symptoms:["slow login","logon takes too long","slow startup from group policy","login slow windows","group policy processing slow","taking ages to log in","long logon time","desktop takes minutes to appear","slow user login domain","applying group policy slow","login screen stuck on applying settings","slow to get to desktop after login"],tags:["group-policy","gpo","performance","slow-logon","warning","latency"],powershell:`# Group Policy processing time and slow CSE report
# Eventful

# Generate full GP result with timing
gpresult /h C:\\Temp\\gpresult.html /f
Write-Host "Open C:\\Temp\\gpresult.html in a browser to view per-CSE timings"

# GP processing time events — last 7 days
Get-WinEvent -FilterHashtable @{
    LogName      = 'Microsoft-Windows-GroupPolicy/Operational'
    Id           = @(7017, 8004)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[1030,1058,1085,5312],ms_docs:null},{id:5312,source:"Microsoft-Windows-GroupPolicy",channel:"Microsoft-Windows-GroupPolicy/Operational",severity:"Info",skill_level:"Fundamental",title:"Group Policy: List of Applied GPOs",short_desc:"Records the complete list of GPOs that were applied during this GP processing cycle — the definitive record of what policy was active.",description:"Event 5312 in the Group Policy Operational log is generated at the end of each GP processing cycle and lists every GPO that was successfully applied to the user or computer. This is the authoritative record of what Group Policy was in effect at a given time. It is the first event to check when a tech needs to confirm whether a specific GPO actually applied, when troubleshooting policy not behaving as expected, or when auditing which policies were active at a particular date and time.",why_it_happens:"Group Policy writes Event 5312 at the end of every successful processing cycle — at computer startup and at user logon — listing all GPOs that were applied. If GP processing failed entirely, this event may not appear (check for 1030 or 1058 instead).",what_good_looks_like:"Event 5312 at every logon and startup, listing the expected set of GPOs. Missing GPOs in the list require investigation — check if the GPO is linked to the correct OU and that the security filtering/WMI filter is correct.",common_mistakes:["Checking GPMC to see which GPOs are linked without checking Event 5312 to see which actually applied — linked does not mean applied",'Not checking the security filtering on a GPO — a GPO may be linked but the computer or user account may not be in the "Apply Group Policy" security group',"Confusing computer policy GPO list (at startup) with user policy GPO list (at logon) — they are separate events"],causes:["Normal Group Policy processing cycle at computer startup or user logon","gpupdate /force manually triggered","GP refresh interval elapsed (default 90 minutes)"],steps:["Filter Event Viewer for Event 5312 in the Group Policy Operational log","Find the event that matches the logon/startup time you are investigating","Read the list of GPO names — any GPO you expected to see but is missing needs investigation","For a missing GPO: check GPMC to confirm it is linked to the correct OU","Check security filtering on the GPO: is the computer or user account in the allowed group?","Check WMI filter if one is applied — the filter may be excluding this machine","Run gpresult /r for a quick on-screen summary of applied policies"],symptoms:["which gpos applied","what group policies are active","list of applied group policies","gpo not showing as applied","check which policies applied","confirm gpo applied","group policy audit","which policies were active at logon","gpo applied list","group policy result","what gpos are applied to this computer","policy audit log"],tags:["group-policy","gpo","audit","info","applied","list","compliance"],powershell:`# Applied GPO list and GP result
# Eventful

# Quick GP result summary
gpresult /r

# Applied GPO events — last 7 days
Get-WinEvent -FilterHashtable @{
    LogName      = 'Microsoft-Windows-GroupPolicy/Operational'
    Id           = 5312
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending |
    Select-Object -First 5 | Format-List`,related_ids:[1030,1058,1085,7017],ms_docs:null}],l=[{id:8193,source:"VSS",channel:"Application",severity:"Error",skill_level:"Intermediate",title:"VSS: Unexpected Error (General VSS Failure)",short_desc:"The Volume Shadow Copy Service encountered an unexpected error — the most common VSS error, usually blocking backups.",description:"Event 8193 from the VSS source is the most frequently encountered VSS error. It indicates the Volume Shadow Copy Service hit an unexpected error during an operation — typically while a backup application (Windows Server Backup, Veeam, Backup Exec, Azure Backup, etc.) was trying to create a shadow copy. The error code (hr = 0x...) in the message identifies the specific failure. When a backup or shadow copy operation fails, this event almost always appears in the Application log. The hr = 0x80070005 code means Access Denied; 0x8004230f means the shadow copy provider is not registered.",why_it_happens:"VSS acts as a coordinator between the requesting application (backup software), the VSS writers (applications like SQL, Exchange, AD that freeze their data), and the VSS provider (the component that creates the actual snapshot). Any failure in this chain produces Event 8193. Common triggers: VSS writers in a failed state, VSS provider not registered, COM+ application catalog corruption, or the background copy service having insufficient permissions.",what_good_looks_like:"No Event 8193. Backups completing without errors. VSS errors are not normal and indicate something needs attention — they will cause backup failures every time until resolved.",common_mistakes:["Restarting the VSS service without resetting failed VSS writers — the next backup will fail again","Not checking the hr error code, which is the most important field in the message",'Ignoring VSS errors because "the backup seemed to finish" — some backups report success even with VSS errors, resulting in inconsistent restore points',"Not checking if VSS writers are in a failed state before and after a backup attempt"],causes:["VSS writer in a failed/error state (run vssadmin list writers to check)","COM+ application catalog corrupt, preventing VSS component registration","VSS provider not registered in the system (hr = 0x8004230f)","Insufficient disk space on the shadow copy storage volume","Access denied to VSS service account (hr = 0x80070005)","Antivirus or backup agent interfering with the VSS snapshot process","Pending Windows updates or a required reboot blocking VSS operations"],steps:["Run vssadmin list writers — check for writers in a Retryable Error or No Stable Data state","Check the hr error code in the Event 8193 message for the specific failure reason","Restart failed writers: net stop <servicename> && net start <servicename> for each failed writer service","Check VSS provider: vssadmin list providers — verify Microsoft Software Shadow Copy Provider is listed","Check COM+ application catalog: run services.msc → COM+ System Application → restart it","Run the PowerShell snippet below to reset all common VSS-related services","Check Application log for Event 8194 and 8196 immediately after 8193 — they often appear together"],symptoms:["backup failed vss error","volume shadow copy error","vss error","shadow copy failed","backup failed unexpected error","vss unexpected error","veeam backup vss error","windows backup failed vss","azure backup vss error","backup exec vss failure","shadow copy service error","backup failing consistently","vss writer error backup","cannot create shadow copy"],tags:["vss","shadow-copy","backup","error","writers","application"],powershell:`# VSS health check and writer reset
# Eventful

# List all VSS writers and their current state
vssadmin list writers

# List VSS providers
vssadmin list providers

# Check for VSS errors in Application log — last 7 days
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'VSS'
    Id           = @(8193, 8194, 8196, 8224)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List

# Reset VSS-related services (run as admin — stop first, then start)
# $vssServices = @('VSS', 'swprv', 'sdrsvc', 'vds', 'COMSysApp')
# $vssServices | ForEach-Object { net stop $_ /y }
# Start-Sleep 5
# $vssServices | ForEach-Object { net start $_ }`,related_ids:[8194,8196,8224],ms_docs:null},{id:8194,source:"VSS",channel:"Application",severity:"Error",skill_level:"Intermediate",title:"VSS: Writer Callback Interface Error",short_desc:"A VSS writer failed to respond during the snapshot process — the backup or shadow copy cannot proceed.",description:'Event 8194 from VSS means the Volume Shadow Copy Service tried to query the IVssWriterCallback interface for a VSS writer and got an unexpected error. VSS writers are components in applications like SQL Server, Exchange, Active Directory, and Hyper-V that guarantee data consistency during snapshots — they "freeze" writes temporarily while the shadow copy is taken. If a writer fails during this handshake, VSS cannot safely snapshot the data and the backup fails. Event 8194 almost always appears alongside Event 8193.',why_it_happens:"VSS writer communication uses COM/RPC channels. If the writer's host service has crashed, if RPC is blocked between components, or if the writer is in a failed state from a previous incomplete backup, the callback fails. Exchange, SQL, and AD writers are the most frequent sources of this error — they are complex writers that can enter a failed state if the host application is under stress or has recently crashed.",what_good_looks_like:"No Event 8194. This error should never appear in a healthy backup environment.",common_mistakes:["Not running vssadmin list writers to identify which writer is in a failed state","Restarting VSS without restarting the writer's parent service — the writer state is tied to its service","Not correlating Event 8194 with the backup software log — the backup tool usually names the problematic writer"],causes:["VSS writer in a failed or unstable state from a prior interrupted backup","Host application service (SQL Server, Exchange, IIS, etc.) crashed or restarting","RPC communication failure between VSS coordinator and writer","Writer registered but its host process has exited","System under heavy I/O load preventing the writer from responding in time"],steps:['Run vssadmin list writers — identify writers in "Retryable Error" or "No Stable Data" state','Note the writer name — correlate with its host service (e.g. "SqlServerWriter" → SQL Server service)',"Restart the host service of the failed writer: Restart-Service <ServiceName>",'After restarting: re-run vssadmin list writers — all writers should show "Stable"',"Retry the backup — if 8194 recurs, check the backup software log for which writer is failing","If all writers are stable and errors persist: run the VSS service reset script from the 8193 steps"],symptoms:["vss writer callback failed","vss writer error","backup failed writer error","sql vss writer error","exchange vss writer failed","shadow copy writer not responding","vss writer in failed state","backup inconsistent vss writer","vss writer callback error","shadow copy failed writer"],tags:["vss","shadow-copy","backup","error","writers","sql","exchange"],powershell:`# VSS writer state check
# Eventful

# List all writers and their state
vssadmin list writers

# Get VSS errors from Application log — last 7 days
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'VSS'
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Where-Object { $_.LevelDisplayName -eq 'Error' } |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[8193,8196,8224],ms_docs:null},{id:8196,source:"VSS",channel:"Application",severity:"Error",skill_level:"Intermediate",title:"VSS: Failed to Resolve Service Account",short_desc:"VSS could not resolve an account name required to run a VSS writer — typically a permissions or service account problem.",description:"Event 8196 from VSS means the Volume Shadow Copy Service failed to resolve (look up) an account name during its operation. The account named in the error message is a service account that VSS or one of its writers needs to function — typically the account under which a VSS writer service runs. This is most commonly seen after: a service account password change where the service was not updated, after a domain rename, or after migrating a server to a new domain where service account SIDs are no longer valid.",why_it_happens:"VSS writers run as specific service accounts (LocalSystem, NetworkService, or a custom domain account). During VSS initialization, it attempts to look up and validate these accounts. If the account is deleted, renamed, disabled, locked out, or the domain suffix changed, the lookup fails and Event 8196 is logged.",what_good_looks_like:"No Event 8196. This error is a clear indicator of a service account or permissions problem that needs direct investigation.",common_mistakes:["Not checking whether the service account named in the event actually exists and is enabled in AD","Forgetting that scheduled password changes can affect the VSS writer service if the password was changed in AD but not updated in the service properties","Missing that this error often appears after a domain or server migration"],causes:["Service account for a VSS writer was deleted or disabled in Active Directory","Service account password changed in AD but not updated in Services (services.msc)","Service account locked out due to failed authentication attempts","Server migrated to a new domain — old domain SIDs no longer valid","Domain rename or restructure invalidating account references"],steps:["Note the account name in the Event 8196 message","Check if the account exists and is enabled in AD: Get-ADUser <accountname> -Properties Enabled, LockedOut","Check if the account is locked out: Get-ADUser <accountname> -Properties LockedOut",'Open services.msc → find the VSS writer service → check the "Log On As" account matches and the password is current',"If account was deleted: recreate it or reassign the service to run under a valid account","After correcting: restart the VSS writer service and retest the backup"],symptoms:["vss account error","vss service account failed","vss permissions error","shadow copy account resolution failed","vss failed to resolve account","backup vss account error","vss writer account problem","vss error after password change","vss error after migration","shadow copy permissions denied"],tags:["vss","shadow-copy","backup","error","account","permissions","service-account"],powershell:`# VSS service account and writer check
# Eventful

# List all VSS writers and their state
vssadmin list writers

# Check VSS errors related to accounts
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'VSS'
    Id           = @(8196, 8193)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Format-List

# List service logon accounts for VSS-related services
Get-WmiObject Win32_Service |
    Where-Object { $_.Name -in @('VSS','swprv','SQLWriter','MSExchangeIS') } |
    Select-Object Name, State, StartName`,related_ids:[8193,8194,8224],ms_docs:null},{id:8224,source:"VSS",channel:"Application",severity:"Info",skill_level:"Intermediate",title:"VSS: Service Shutting Down Due to Idle Timeout",short_desc:"The VSS service stopped because it has been idle — this is normal, but repeated entries around backup time indicate VSS is not staying active during a backup.",description:"Event 8224 from VSS means the Volume Shadow Copy Service shut itself down due to inactivity. VSS runs on demand — it starts when a snapshot is requested and shuts down after an idle period to conserve resources. Event 8224 on its own is completely normal and requires no action. However, when this event appears during a backup window or immediately after a failed backup, it can indicate that the backup software triggered VSS, VSS started, but then the backup software failed to properly request the snapshot before the idle timeout expired — leaving a failed backup with no shadow copy created.",why_it_happens:"VSS is implemented as a demand-start service. After completing its work, or when no snapshot request has been received within the idle timeout period, it shuts down cleanly and logs Event 8224.",what_good_looks_like:"Event 8224 appearing at random intervals with no correlation to backup failure = normal. Event 8224 appearing immediately after a backup attempt with backup failure events = investigate the backup software's VSS coordination.",common_mistakes:["Treating every Event 8224 as an error — it is informational and normal in most contexts","Not correlating the timestamp with backup job times — 8224 during a backup window is the suspicious pattern"],causes:["Normal idle timeout — VSS shuts down when not in use","Backup software failed to initiate a snapshot request fast enough after starting the VSS service","Backup job cancelled or timed out before VSS completed the snapshot"],steps:["Check the timestamp — if it aligns with a backup window, cross-reference the backup software log","If occurring during backup failures: check whether the backup software has a VSS timeout setting that needs increasing","If occurring at random (no backup correlation): no action needed — this is normal VSS behavior",'Check backup software logs for "VSS timeout" or "failed to request snapshot" messages near the 8224 timestamp'],symptoms:["vss shutting down","volume shadow copy stopped","vss idle timeout","vss service stopped during backup","backup vss timeout","shadow copy service shut down","vss stopped unexpectedly during backup"],tags:["vss","shadow-copy","backup","info","idle","timeout"],powershell:`# VSS service state and idle events
# Eventful

# Current VSS service state
Get-Service VSS | Select-Object Name, Status, StartType

# VSS shutdown events — last 7 days
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'VSS'
    Id           = 8224
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[8193,8194,8196],ms_docs:null}],d=[{id:18590,source:"Microsoft-Windows-Hyper-V-VMMS",channel:"Microsoft-Windows-Hyper-V-VMMS/Admin",severity:"Critical",skill_level:"Intermediate",title:"Hyper-V: VM Paused Due to Storage I/O Issue",short_desc:"A virtual machine was automatically paused because Hyper-V detected a storage problem — the VM is frozen until storage is restored.",description:"Event 18590 means Hyper-V paused a virtual machine because it detected a critical storage I/O error on the volume hosting the VM's VHD/VHDX files. Hyper-V pauses the VM (rather than crashing it) to protect data integrity — a paused VM holds all its state in memory and will resume cleanly once storage is restored. Users see the VM as completely unresponsive. The root cause is almost always: the storage volume is full, the LUN is offline, the SMB share hosting the VHD is unreachable, or a storage controller fault.",why_it_happens:"Hyper-V monitors every write to the VHD/VHDX files. If a write fails and cannot be completed — because the disk is full, the LUN went offline, the network path to an SMB share dropped, or a RAID/SAN array reported an error — Hyper-V pauses the VM to prevent a split-brain state where the VM thinks data was written but it was not.",what_good_looks_like:"No Event 18590. VMs running continuously with no pauses. Monitor free space on VHD storage volumes and LUN/share health proactively.",common_mistakes:["Trying to resume the VM without first fixing the storage issue — it will pause again immediately","Not checking free space on the host volume hosting the VHD files","Forgetting that SMB share storage (VHDs on file servers) can lose connectivity mid-operation, causing this event","Not checking the Hyper-V VMMS/Admin log for the specific storage path mentioned in the event message"],causes:["Host storage volume full — VHD file cannot expand (especially dynamic VHDs)","LUN or disk offline on the storage array","SMB/CIFS share hosting VHD files became unreachable (network issue)","Storage controller hardware failure","iSCSI target disconnected","CSV (Cluster Shared Volume) failure in a clustered environment"],steps:["Do NOT try to resume the VM yet — fix storage first","Check free space on the host volume: Get-PSDrive | Where-Object { $_.Free -lt 5GB }","Check if the storage volume or LUN is online: Get-Disk | Select-Object Number, OperationalStatus, HealthStatus","Check if SMB storage paths are accessible: Test-Path \\\\<server>\\<share>","Free up space or bring storage online","Once storage is healthy: right-click the VM in Hyper-V Manager → Resume (or Start-VM <VMName>)","Verify the VM resumes and applications inside are stable after resume"],symptoms:["hyper-v vm paused","virtual machine paused","vm frozen hyper-v","hyper-v vm not responding","vm paused storage error","virtual machine automatically paused","hyper-v vm went offline","vm paused disk full","hyper-v storage error vm","virtual machine paused unexpectedly","vm paused in hyper-v manager","hyper-v vm critical pause"],tags:["hyper-v","vm","storage","paused","critical","vhd","vmms"],powershell:`# Hyper-V VM status and storage check
# Eventful

# List all VMs and their state
Get-VM | Select-Object Name, State, Status, Uptime

# Check host storage free space (flag volumes under 10GB free)
Get-PSDrive -PSProvider FileSystem |
    Select-Object Name, @{N='FreeGB'; E={[math]::Round($_.Free/1GB,1)}}, @{N='UsedGB'; E={[math]::Round($_.Used/1GB,1)}} |
    Where-Object { $_.FreeGB -lt 10 }

# Check VM storage file paths
Get-VM | Get-VMHardDiskDrive |
    Select-Object VMName, Path, @{N='SizeGB'; E={[math]::Round((Get-Item $_.Path -ErrorAction SilentlyContinue).Length/1GB,1)}}

# Recent Hyper-V critical events
Get-WinEvent -FilterHashtable @{
    LogName   = 'Microsoft-Windows-Hyper-V-VMMS/Admin'
    StartTime = (Get-Date).AddDays(-7)
    Level     = @(1, 2)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List`,related_ids:[3040,32022,13003],ms_docs:null},{id:3040,source:"Microsoft-Windows-Hyper-V-VMMS",channel:"Microsoft-Windows-Hyper-V-VMMS/Admin",severity:"Warning",skill_level:"Fundamental",title:"Hyper-V: Integration Services Outdated",short_desc:"The integration services (tools) inside a guest VM are outdated — performance, backup, and time sync may be impaired.",description:"Event 3040 means the Hyper-V Integration Services components inside the guest virtual machine are older than the version expected by the host. Integration Services (also called Hyper-V Guest Services) provide the communication channel between the VM and the Hyper-V host — enabling heartbeat monitoring, time synchronization, live backup (VSS), graceful shutdown, and data exchange. Outdated integration services typically means the guest OS was installed with an older Windows ISO and has not been updated, or the VM was migrated from an older host and the guest updates have not been applied.",why_it_happens:"Integration Services components are built into Windows (since Windows Server 2012 / Windows 8, they are updated via Windows Update). On older guests or guests without Windows Update, the version bundled with the original OS installation remains installed while the host gets newer Hyper-V features that require a newer guest-side component.",what_good_looks_like:"All running VMs showing current integration services versions. Check via Hyper-V Manager → select VM → check Integration Services version, or Get-VM | Select-Object Name, IntegrationServicesVersion.",common_mistakes:["Not updating integration services on migrated VMs — migration does not update the guest components","Trying to install integration services from the old ISO (.iso) method on modern Windows guests — Windows Update handles this automatically since Windows 8/2012","Ignoring this warning and then being surprised when live backup (VSS) or heartbeat fails"],causes:["Guest OS installed from an older Windows image and not updated via Windows Update","VM migrated from an older Hyper-V host version","Windows Update disabled or blocked inside the guest","Guest is running a Linux distribution with outdated linux-azure or hyper-v-daemons package"],steps:["On Windows guests: ensure Windows Update is running and apply all pending updates — Integration Services are delivered via Windows Update on Server 2012+ / Windows 8+","On older Windows guests (2008 R2, 7): In Hyper-V Manager, right-click VM → Insert Integration Services Setup Disk, then run setup inside the VM","Verify version after update: Get-VM <VMName> | Select-Object IntegrationServicesVersion","For Linux guests: install or update the linux-azure or hyperv-daemons package via the distro package manager","After updating: test heartbeat, graceful shutdown, and live backup (VSS) within the VM"],symptoms:["hyper-v integration services outdated","vm integration components old","hyper-v tools need update","vm heartbeat not working","hyper-v backup failing integration services","vm time sync not working","graceful shutdown not working vm","hyper-v integration services version mismatch","vm vss backup failing hyper-v","hyper-v guest tools outdated"],tags:["hyper-v","vm","integration-services","warning","backup","vss","heartbeat"],powershell:`# Check integration services version across all VMs
# Eventful

Get-VM | Select-Object Name, State, IntegrationServicesVersion,
    @{N='ISState'; E={ ($_ | Get-VMIntegrationService | Where-Object Enabled | Select-Object -ExpandProperty OperationalStatus | Sort-Object -Unique) -join ',' }} |
    Format-Table -AutoSize

# Check which integration services components are enabled per VM
Get-VM | ForEach-Object {
    $vm = $_
    Get-VMIntegrationService -VM $vm | Select-Object @{N='VM';E={$vm.Name}}, Name, Enabled, OperationalStatus
} | Format-Table -AutoSize`,related_ids:[18590,32022,13003],ms_docs:"https://learn.microsoft.com/en-us/windows-server/virtualization/hyper-v/manage/manage-hyper-v-integration-services"},{id:13003,source:"Microsoft-Windows-Hyper-V-VMMS",channel:"Microsoft-Windows-Hyper-V-VMMS/Admin",severity:"Error",skill_level:"Intermediate",title:"Hyper-V: Lost Communication with Virtual Machine",short_desc:"Hyper-V lost the management communication channel with a running VM — heartbeat failed or worker process crashed.",description:"Event 13003 means Hyper-V VMMS lost its management communication channel with a virtual machine. This is separate from the VM itself being unresponsive to users — the VM may still be running and serving users, but Hyper-V cannot manage it (heartbeat fails, graceful shutdown is unavailable, live backup via VSS is broken). The worker process (vmwp.exe) responsible for that VM may have crashed. This event often precedes a VM entering a saved state or being forced off.",why_it_happens:"Each VM has a dedicated worker process (vmwp.exe). If this process crashes — due to a bug, memory corruption, or Hyper-V host memory pressure — VMMS loses its communication channel with the VM. The VM may keep running at the hardware level but cannot be managed gracefully.",what_good_looks_like:"No Event 13003. Heartbeat checks for all VMs returning healthy status.",common_mistakes:["Assuming the VM has crashed when users report it is working normally — the VM may be running fine with just the management channel broken","Force-turning off the VM immediately without first trying graceful shutdown","Not checking vmwp.exe process on the host to see if the worker is running"],causes:["VM worker process (vmwp.exe) crashed — check host System log","Integration Services heartbeat timeout (outdated or disabled)","Hyper-V host memory pressure killing worker processes","Host hardware error affecting worker process execution"],steps:["Check if the VM is still serving users — ping it, test application access","Check host task manager or: Get-Process vmwp | Measure-Object — count should match running VMs","Try graceful shutdown first: Stop-VM <VMName> -Force -TurnOff if graceful fails","Check System log on the host for vmwp.exe crash events at the same time","After restart, check integration services are current (see Event 3040)","Monitor for recurrence — repeated 13003 may indicate hardware instability on the host"],symptoms:["hyper-v lost communication with vm","vm heartbeat failing","hyper-v cannot manage vm","vm worker process crashed","hyper-v vm unmanageable","vm heartbeat timeout hyper-v","hyper-v vmms communication error","vm graceful shutdown not working","hyper-v management channel lost"],tags:["hyper-v","vm","heartbeat","error","worker-process","vmms"],powershell:`# VM heartbeat and worker process health
# Eventful

# Check VM heartbeat status
Get-VM | ForEach-Object {
    $hb = Get-VMIntegrationService -VM $_ -Name 'Heartbeat'
    [PSCustomObject]@{
        VMName    = $_.Name
        State     = $_.State
        Heartbeat = $hb.PrimaryOperationalStatus
        LastContact = $hb.LastCommunicationTimestamp
    }
} | Format-Table -AutoSize

# Check worker process count vs running VMs
$runningVMs = (Get-VM | Where-Object State -eq 'Running').Count
$workers    = (Get-Process vmwp -ErrorAction SilentlyContinue).Count
Write-Host "Running VMs: $runningVMs  |  Worker processes: $workers"`,related_ids:[18590,3040,32022],ms_docs:null},{id:32022,source:"Microsoft-Windows-Hyper-V-VMMS",channel:"Microsoft-Windows-Hyper-V-VMMS/Admin",severity:"Error",skill_level:"Intermediate",title:"Hyper-V: VM Configuration File Not Found or Inaccessible",short_desc:"The VM configuration file (.vmcx or .xml) is missing or cannot be opened — the VM cannot start.",description:"Event 32022 means Hyper-V cannot find or open the configuration file for a virtual machine. In modern Hyper-V (Server 2016+), this is the .vmcx binary file. In older versions it is an XML file. Without the configuration file, the VM cannot start — Hyper-V does not know what hardware the VM has, where its VHD files are, or how much memory it has. This typically happens after storage migration without moving the config, accidental deletion, or corruption of the VM config file.",why_it_happens:"VM configuration files are stored in the VM's configuration store path (default: C:\\ProgramData\\Microsoft\\Windows\\Hyper-V\\). If the storage path was changed, if the VM was moved and the config was not moved with it, or if the file was deleted, Event 32022 fires.",what_good_looks_like:"All VMs starting without 32022. No orphaned VM entries in Hyper-V Manager.",common_mistakes:["Moving VHD files without moving the configuration file — the VM config is stored separately from the VHD","Trying to start the VM before locating and restoring the config file","Not using Export VM when moving VMs between hosts — Export packages config and VHD together"],causes:["VM files manually moved or copied without using Hyper-V export","VM configuration file accidentally deleted","Storage path changed and config files not relocated","Incomplete VM import — config file not created","Ransomware or accidental deletion of VM files"],steps:["Locate the VM's storage path: Get-VM <VMName> | Select-Object ConfigurationLocation","Check if the .vmcx or .xml file exists at that path","If the file is missing: check backups, previous snapshot locations, or other storage paths","If you have the VHD but not the config: use New-VM to create a new VM and attach the existing VHD","For future moves: always use Export-VM / Import-VM to keep config and VHD together"],symptoms:["hyper-v vm configuration not found","vm config file missing","virtual machine cannot start config error","hyper-v vm file not found","vm disappeared after moving files","hyper-v vm import failed config","vmcx file missing","vm configuration inaccessible","hyper-v vm not starting after move"],tags:["hyper-v","vm","configuration","error","vmcx","storage","import"],powershell:`# VM configuration paths and file existence check
# Eventful

Get-VM | ForEach-Object {
    $configPath = $_.ConfigurationLocation
    $configFile = Get-ChildItem -Path $configPath -Filter '*.vmcx' -ErrorAction SilentlyContinue |
                  Select-Object -First 1
    [PSCustomObject]@{
        VMName        = $_.Name
        State         = $_.State
        ConfigPath    = $configPath
        ConfigExists  = if ($configFile) { 'YES' } else { 'MISSING' }
    }
} | Format-Table -AutoSize`,related_ids:[18590,3040,13003],ms_docs:null},{id:20415,source:"Microsoft-Windows-Hyper-V-VMMS",channel:"Microsoft-Windows-Hyper-V-VMMS/Admin",severity:"Warning",skill_level:"Intermediate",title:"Hyper-V: Saved State Could Not Be Restored",short_desc:"A VM's saved state file could not be opened or is corrupt — the VM must be started fresh, losing the previous state.",description:"Event 20415 means Hyper-V could not restore a virtual machine from its saved state file. Saved state is similar to hibernate — the VM's entire memory is written to disk so it can be resumed later. If the saved state file (.vsv) is missing, corrupt, or on unavailable storage, the VM cannot resume from where it left off. The VM must either be started fresh (losing any in-flight work) or the saved state must be deleted. This commonly occurs after: host storage issues during a save operation, host failure during a save, or migration without transferring the saved state file.",why_it_happens:"Saved state files are written during: a live save operation, a VM pause, or a Hyper-V host shutdown with default action set to save. If the host crashes or loses storage during the save, the file may be incomplete. If the VM is migrated without the saved state file, or if the file is on storage that is no longer available, restoration fails.",what_good_looks_like:"VMs resuming cleanly from saved state with no 20415 events.",common_mistakes:["Not understanding that deleting the saved state and starting fresh means any unsaved application work is lost","Not backing up saved state files before attempting recovery operations","Migrating a VM while it is in saved state without transferring the .vsv file"],causes:["Host crashed or lost power while writing the saved state file (incomplete file)","Saved state file stored on storage that is now offline or removed","VM migrated to a new host without transferring the .vsv saved state file","Saved state file accidentally deleted","File system corruption affecting the saved state file"],steps:["In Hyper-V Manager: right-click the VM → Delete Saved State — this discards the state and allows a fresh start","Or via PowerShell: Remove-VMSavedState <VMName>","Start the VM fresh: Start-VM <VMName>","Notify users that any in-progress work from the last session is lost","Investigate why the save failed — check storage and host health at the time of failure"],symptoms:["hyper-v vm saved state error","vm cannot resume from saved state","hyper-v saved state corrupt","virtual machine saved state failed","vm stuck in saved state","hyper-v vm will not resume","delete saved state hyper-v","vm saved state file missing","hyper-v resume failed"],tags:["hyper-v","vm","saved-state","warning","resume","storage"],powershell:`# Check for VMs in saved state
# Eventful

# List VMs in saved state
Get-VM | Where-Object State -eq 'Saved' |
    Select-Object Name, State, ConfigurationLocation, Path

# Remove saved state and start fresh (only if confirmed OK to lose state)
# Remove-VMSavedState <VMName>
# Start-VM <VMName>`,related_ids:[18590,32022,3040],ms_docs:null},{id:14024,source:"Microsoft-Windows-Hyper-V-VMMS",channel:"Microsoft-Windows-Hyper-V-VMMS/Admin",severity:"Error",skill_level:"Intermediate",title:"Hyper-V: Failed to Start Virtual Machine",short_desc:"Hyper-V could not start a virtual machine — the specific reason is in the event message.",description:'Event 14024 from Hyper-V VMMS means a virtual machine failed to start or transition state. The error message contains the specific reason: could be insufficient host memory, missing VHD file, configuration error, authorization failure, or a Hyper-V component error. This is the primary event to investigate when a VM shows as "Off" but will not start, or when a start attempt fails immediately. The hex error code in the message maps to a specific failure category.',why_it_happens:"VM start failures occur when: the host lacks sufficient free memory to allocate to the VM, the VHD/VHDX files are missing or corrupt, the VM's virtual switch no longer exists on the host, the VM config is corrupt, or the user account doesn't have permission to start VMs.",what_good_looks_like:"VMs starting cleanly with no Event 14024. Pre-flight checks (memory, storage, networking) completed before starting.",common_mistakes:["Not reading the error message text carefully — the specific reason is always stated","Not checking host free memory before starting large VMs","Not verifying the virtual switch the VM is attached to still exists (common after host config changes)"],causes:["Insufficient host RAM — the VM's startup RAM exceeds available free memory","VHD/VHDX file missing, locked, or on offline storage","Virtual switch the VM is connected to was deleted or renamed","VM configuration is corrupt","Hyper-V authorization failure — account lacks permissions","Required Hyper-V components not running on the host"],steps:["Read the error message in Event 14024 — note the specific reason",'Check host free memory: Get-VMMemory <VMName> | Select-Object Startup — compare to: (Get-Counter "\\Memory\\Available MBytes").CounterSamples.CookedValue',"Verify VHD files exist: Get-VM <VMName> | Get-VMHardDiskDrive | Select-Object Path","Check virtual switch: Get-VMNetworkAdapter <VMName> | Select-Object SwitchName — then Get-VMSwitch","If memory is the issue: shut down other VMs or increase host RAM","If switch is missing: create a new switch or disconnect the adapter before starting"],symptoms:["hyper-v vm failed to start","virtual machine wont turn on","vm start failed hyper-v","hyper-v cannot start vm","virtual machine failed to power on","vm start error","hyper-v vm not starting","failed to start virtual machine error","hyper-v insufficient memory vm","vm switch not found hyper-v"],tags:["hyper-v","vm","start","error","memory","vmms","configuration"],powershell:`# Hyper-V VM start failure diagnostics
# Eventful

# VM states
Get-VM | Select-Object Name, State, Status, DynamicMemoryEnabled,
    @{N='MemoryStartupGB'; E={[math]::Round($_.MemoryStartup/1GB,1)}} | Format-Table -AutoSize

# Host available memory
$hostMem = [math]::Round((Get-CimInstance Win32_OperatingSystem).FreePhysicalMemory / 1MB, 1)
Write-Host "Host free RAM: $hostMem GB"

# Virtual switches
Get-VMSwitch | Select-Object Name, SwitchType, NetAdapterInterfaceDescription

# Recent Hyper-V errors
Get-WinEvent -FilterHashtable @{
    LogName   = 'Microsoft-Windows-Hyper-V-VMMS/Admin'
    Id        = 14024
    StartTime = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Format-List`,related_ids:[18590,3040,32022,13003],ms_docs:null}],h=[{id:1002,source:"Microsoft-Windows-CertificationAuthority",channel:"Application",severity:"Error",skill_level:"Advanced",title:"Certificate Services — Failed to Revoke Certificate",short_desc:"Certificate Services failed to revoke a certificate — CRL or database error.",description:"Event 1002 from Microsoft-Windows-CertificationAuthority indicates that the CA failed to process a revocation request. This may mean the certificate was already revoked, the CA database is inaccessible, or the serial number was not found. Clients relying on up-to-date CRL publication will be affected if revocation is blocked.",why_it_happens:"Revocation failures occur when the CA database is locked or corrupt, when the certificate serial number cannot be found (already expired or wrong CA), or when the CA service lacks permissions to write to the revocation store. High-volume environments can also hit database contention.",what_good_looks_like:"No 1002 errors in the Application log. CRL publications succeed on schedule. CDP extension URLs in issued certs are reachable and return a valid, non-expired CRL.",common_mistakes:["Attempting to revoke a certificate from the wrong CA — serial numbers are unique per CA","Not checking if the certificate was already expired; expired certs often cannot be revoked through the normal UI","Ignoring CA database fragmentation — run certutil -databaselocations and check free space","Forgetting to restart Certificate Services after database maintenance"],causes:["CA database locked or running out of space","Certificate serial number not found — wrong CA or already deleted","Certificate already revoked","CA service account lacks write permission to the database directory","CA database corruption requiring defragmentation"],steps:["Open Event Viewer → Windows Logs → Application, filter for source Microsoft-Windows-CertificationAuthority","Note the serial number and reason code from the event message",'Verify the certificate exists: certutil -view -out "SerialNumber" -restrict "SerialNumber=<serial>"',"Check CA database free space: certutil -databaselocations","Check CA service account permissions on the CA database directory","If database is corrupt: stop CerSvc, run certutil -f -databaserecover, restart service","Review CA Application log for events 100–200 indicating service issues"],symptoms:["cannot revoke certificate","certificate revocation failed","CRL not updating","certificate services error","revoke cert error","CA database error","certificate authority failed","revocation request failed"],tags:["certificate","pki","ca","revocation","crl","adcs"],powershell:`# Certificate Authority — check for revocation errors and CRL status
# Eventful

# Recent CA errors in Application log
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'Microsoft-Windows-CertificationAuthority'
    Level        = @(1, 2)   # Critical + Error
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List

# CRL publication status (run on CA server)
certutil -crl

# Check CA database size and locations
certutil -databaselocations`,related_ids:[1003,4870,4896],ms_docs:"https://learn.microsoft.com/en-us/windows-server/identity/ad-cs/active-directory-certificate-services-overview"},{id:1003,source:"Microsoft-Windows-CertificationAuthority",channel:"Application",severity:"Warning",skill_level:"Advanced",title:"Certificate Services — CRL Distribution Point Not Reachable",short_desc:"CA cannot publish the CRL to one or more distribution points.",description:"Event 1003 indicates that the CA attempted to publish a new Certificate Revocation List (CRL) to a configured distribution point (CDP) and failed. Clients that cannot download a valid CRL will fail certificate validation and may be denied access to resources or HTTPS sites that perform CRL checking.",why_it_happens:"CRL publication failures are usually caused by a network share being unavailable, an LDAP write permission being denied, or an HTTP/HTTPS publication target being unreachable. The CA publishes CRLs on a configured schedule and on demand when certificates are revoked.",what_good_looks_like:"CRL published successfully to all CDPs on every scheduled interval. The CRL validity period should exceed the publication interval by at least 50% to allow for missed publications. Clients can retrieve the CRL without errors.",common_mistakes:["Setting the CRL validity period too short — it should always be longer than the publication interval","Not testing CRL retrieval from client machines, not just from the CA server","Forgetting that LDAP CDPs require the CA computer account to have write access to the CDP object in AD","Not monitoring CRL expiry — an expired CRL causes ALL certificates from that CA to fail validation"],causes:["Network share for delta/base CRL is offline or permissions changed","LDAP write permission denied to CA computer account","HTTP/HTTPS CDN or web server hosting CRL is unreachable","CA certificate expired, blocking CRL signing","DNS resolution failure for CDP hostname"],steps:["Identify which CDP is failing from the event message","For file/share CDPs: verify the share is online and CA computer account has write access","For LDAP CDPs: run certutil -dspublish to re-publish, check AD permissions","For HTTP CDPs: verify the web server is online and the upload/copy mechanism is working","Manually publish CRL: certutil -crl","Check CRL validity: certutil -url <cdpUrl> or PKIView snap-in","Review CDP config in CA Properties → Extensions tab"],symptoms:["CRL not published","certificate validation failing","clients getting certificate errors","CRL distribution point unreachable","certificate revocation check failed","https certificate error all users","PKI CRL expired","certificate services CRL error"],tags:["certificate","pki","crl","cdp","adcs","ca-publish"],powershell:`# Check CRL publication status and reachability
# Eventful

# View current CRL details from CA
certutil -crl

# Test a specific CDP URL
# certutil -url "http://crl.contoso.com/CertEnroll/ContosoCA.crl"

# Republish CRL manually (run on CA server as admin)
# certutil -dspublish -f "C:WindowsSystem32CertSrvCertEnroll*.crl"

# Check CRL validity period and next update
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'Microsoft-Windows-CertificationAuthority'
    Id           = 1003
    StartTime    = (Get-Date).AddDays(-30)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Sort-Object TimeCreated -Descending | Format-List`,related_ids:[1002,13,64,65],ms_docs:"https://learn.microsoft.com/en-us/windows-server/identity/ad-cs/active-directory-certificate-services-overview"},{id:64,source:"Microsoft-Windows-CertificateServicesClient-AutoEnrollment",channel:"Application",severity:"Error",skill_level:"Intermediate",title:"Certificate Auto-Enrollment Failed",short_desc:"Automatic certificate enrollment failed — client could not enroll for or renew a certificate.",description:"Event 64 from Microsoft-Windows-CertificateServicesClient-AutoEnrollment is logged when the auto-enrollment process fails to enroll for or renew a certificate. This commonly breaks machine authentication, 802.1X Wi-Fi/LAN access, email signing, or HTTPS services that rely on automatically issued certificates. The event message includes the template name and a descriptive error code.",why_it_happens:"Auto-enrollment runs at user logon and on Group Policy refresh. It contacts the CA to request certificates defined by auto-enrollment Group Policy. Failures occur when the CA is unreachable, the machine lacks Enroll permission on the template, the template version is incompatible with the CA, or the SAN in the request cannot be fulfilled.",what_good_looks_like:"Event 65 (successful enrollment) accompanies each issued certificate. Computers should hold a valid Machine certificate, and users a valid User certificate where templates require them. Certificate snap-in (certmgr.msc / certlm.msc) shows unexpired, valid certificates from your CA.",common_mistakes:["Forgetting to publish the certificate template to the CA — a template can exist in AD without being published","Not granting the Authenticated Users or Domain Computers group Enroll permission on the template","Template requiring EKU or SAN fields the CA cannot populate automatically","Not checking if the CA is reachable from the affected machine: certutil -ping <caName>"],causes:["Machine lacks Enroll or AutoEnroll permission on the certificate template","Certificate template not published to the CA","CA is offline or unreachable from client","Template schema version incompatible with CA version","Machine name longer than 15 characters causing CN truncation in certificate","Certificate template requires manual approval (CA manager approval flag set)"],steps:["Open Event Viewer → Application log, filter for source Microsoft-Windows-CertificateServicesClient-AutoEnrollment, Event 64","Note the template name and error message in the event","Verify the template is published to the CA: certsrv.msc → Certificate Templates folder","Check Enroll and AutoEnroll permissions: Certificate Templates console → template Properties → Security tab","Run gpupdate /force on the client, then check for Event 65","Test CA connectivity from client: certutil -ping <CAComputerName>","Review GPO auto-enrollment setting: Computer/User Config → Windows Settings → Security Settings → Public Key Policies → Certificate Services Client – Auto-Enrollment"],symptoms:["certificate auto enrollment failed","computers not getting certificates automatically","machine cert missing","802.1x wifi certificate error","domain computer certificate expired","auto enroll certificate error","certificate template not enrolling","GPO cert not deploying","PKI enrollment failed","certificate request failed"],tags:["certificate","pki","auto-enrollment","adcs","group-policy","802.1x"],powershell:`# Certificate Auto-Enrollment Diagnostics
# Eventful

# Check auto-enrollment events (64=fail, 65=success)
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'Microsoft-Windows-CertificateServicesClient-AutoEnrollment'
    Id           = @(64, 65)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id,
        @{N='Status'; E={ if ($_.Id -eq 65) {'SUCCESS'} else {'FAILED'} }},
        Message |
    Sort-Object TimeCreated -Descending | Format-List

# List machine certificates and expiry
Get-ChildItem Cert:LocalMachineMy |
    Select-Object Subject, Issuer, NotAfter,
        @{N='DaysLeft'; E={ [int](($_.NotAfter - (Get-Date)).TotalDays) }} |
    Sort-Object DaysLeft | Format-Table -AutoSize

# Test CA connectivity
# certutil -ping <CAName>`,related_ids:[65,1003,6066],ms_docs:"https://learn.microsoft.com/en-us/windows-server/identity/ad-cs/certificate-autoenrollment"},{id:20,source:"Microsoft-Windows-CertificateServicesClient-AutoEnrollment",channel:"Application",severity:"Warning",skill_level:"Intermediate",title:"Certificate Near Expiry — Auto-Renewal Pending",short_desc:"Auto-enrollment detected a certificate approaching expiry and queued renewal.",description:"Event 20 from the auto-enrollment client logs when a certificate reaches the renewal threshold (by default 80% of its lifetime elapsed) and renewal has been queued. If the renewal then fails, Event 64 will follow. This event itself is informational but is the first warning that a certificate chain of events is underway that could result in a service outage if not confirmed successful.",why_it_happens:"Windows Certificate Services auto-enrollment monitors certificate lifetimes and initiates renewal when the renewal percentage threshold is crossed. The renewal triggers on Group Policy refresh or logon. If the CA or template is unreachable, the renewal attempt logs Event 64.",what_good_looks_like:"Event 20 followed within minutes by Event 65 (enrollment success) and a new certificate in certlm.msc/certmgr.msc with extended validity. No Event 64 errors.",common_mistakes:["Seeing Event 20 and assuming renewal was successful — always verify with Event 65 or certlm.msc","Not monitoring certificate expiry proactively; Event 20 is a late warning","Letting certificates expire because the CA was offline during the renewal window"],causes:["Certificate has reached 80% of its validity period","Template configured with short validity (e.g. 1 year)","Certificates issued with fixed end dates instead of relative lifetimes"],steps:["Verify renewal succeeded: check certlm.msc for a new certificate with updated NotAfter date","If renewal failed, look for Event 64 in Application log","Run gpupdate /force to trigger immediate renewal attempt","Check CA health: certutil -ping <CAName>","Monitor: Get-ChildItem Cert:\\LocalMachine\\My sorted by expiry"],symptoms:["certificate about to expire","certificate renewal pending","certificate expiring soon","auto renewal certificate","cert renewal queued","certificate near expiry warning","pki certificate expiry"],tags:["certificate","pki","expiry","renewal","auto-enrollment","adcs"],powershell:`# Check certificate expiry across machine store
# Eventful

# All machine certs sorted by expiry
Get-ChildItem Cert:LocalMachineMy |
    Select-Object Subject, Issuer, NotAfter,
        @{N='DaysLeft'; E={ [int](($_.NotAfter - (Get-Date)).TotalDays) }},
        Thumbprint |
    Sort-Object DaysLeft | Format-Table -AutoSize

# Certs expiring within 30 days
Get-ChildItem Cert:LocalMachineMy |
    Where-Object { $_.NotAfter -lt (Get-Date).AddDays(30) } |
    Select-Object Subject, Issuer, NotAfter, Thumbprint | Format-List`,related_ids:[64,65,1003],ms_docs:"https://learn.microsoft.com/en-us/windows-server/identity/ad-cs/certificate-autoenrollment"},{id:36882,source:"Schannel",channel:"System",severity:"Error",skill_level:"Intermediate",title:"Schannel — Certificate Chain Could Not Be Built",short_desc:"TLS/SSL handshake failed because the certificate chain could not be verified.",description:'Event 36882 from Schannel (the Windows TLS/SSL implementation) means the system could not build a valid certificate chain from the presented certificate to a trusted root. This causes TLS connections to fail and generates errors in applications as "certificate chain error", "untrusted certificate", or connection refused. This event is logged on the client side of the TLS connection.',why_it_happens:"Certificate chain building fails when an intermediate CA certificate is missing from the local store or the AIA (Authority Information Access) extension download fails, when the root CA certificate is not in the Trusted Root Certification Authorities store, when the certificate has been revoked and CRL checking fails, or when the clock is skewed causing validity period mismatches.",what_good_looks_like:"No Schannel errors in the System log. Applications connect to HTTPS/LDAPS/SMTPS without certificate errors. certutil -verify <certfile> returns without errors.",common_mistakes:["Not checking if intermediate CA certificates are present in the Intermediate Certification Authorities store","Forgetting to distribute the root CA certificate via GPO to all domain machines","Not checking if the server certificate AIA URLs are reachable for online chain building","Ignoring time skew — a clock more than 5 minutes off can cause valid certificates to appear invalid"],causes:["Intermediate CA certificate missing from local store","Root CA certificate not trusted on the client","AIA URL unreachable — cannot download intermediate certificate online","Certificate presented by server is expired or not yet valid","System clock skewed beyond acceptable tolerance","Certificate revoked and client configured to enforce CRL checking"],steps:["Identify the service/connection failing from application logs","Run certutil -verify -urlfetch <certFile> to test chain building with URL fetching","Check Intermediate Certification Authorities store: certlm.msc → Intermediate CAs","Check Trusted Root Certification Authorities store for the root CA","Verify system clock: w32tm /query /status","Check AIA URL reachability from the client machine","Deploy missing root or intermediate CA via GPO: Computer Configuration → Policies → Windows Settings → Security Settings → Public Key Policies"],symptoms:["certificate chain error","untrusted certificate","TLS handshake failed","SSL certificate error","cannot verify certificate","certificate not trusted","https connection failing","certificate chain could not be built","intermediate certificate missing","root CA not trusted"],tags:["certificate","tls","ssl","schannel","chain","pki","trust"],powershell:`# Schannel and Certificate Chain Diagnostics
# Eventful

# Recent Schannel errors in System log
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Schannel'
    Level        = @(1, 2)   # Critical + Error
    StartTime    = (Get-Date).AddDays(-1)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Format-List

# Verify a certificate chain (replace path with actual cert file)
# certutil -verify -urlfetch "C:path	ocertificate.cer"

# Check time sync status (clock skew can break TLS)
w32tm /query /status

# List trusted root CAs
Get-ChildItem Cert:LocalMachineRoot |
    Select-Object Subject, NotAfter,
        @{N='DaysLeft'; E={ [int](($_.NotAfter - (Get-Date)).TotalDays) }} |
    Sort-Object DaysLeft | Format-Table -AutoSize`,related_ids:[36871,36874,36887,36888,64],ms_docs:"https://learn.microsoft.com/en-us/windows-server/security/tls/schannel-security-support-provider-technical-reference"},{id:13,source:"Microsoft-Windows-CertificateServicesClient-AutoEnrollment",channel:"Application",severity:"Warning",skill_level:"Intermediate",title:"Certificate Template Not Found or Access Denied",short_desc:"Auto-enrollment could not access a required certificate template.",description:"Event 13 from the certificate auto-enrollment client indicates that the client found a reference to a certificate template (via Group Policy or the user/machine configuration) but either the template does not exist in Active Directory or the account does not have Read permission on the template object. Enrollment silently fails for that template until the issue is resolved.",why_it_happens:"This event fires during the GPO-driven auto-enrollment refresh. It means the auto-enrollment GPO is pointing to a template name that is either misspelled, not yet created in the Certificate Templates container in AD, or has had its Read permission removed from the requesting account.",what_good_looks_like:"No Event 13 errors. Auto-enrollment GPO references only published, accessible templates. All machines/users successfully receive the certificates they are configured to auto-enroll for.",common_mistakes:["Creating a GPO referencing a template before the template is published to the CA and replicated in AD","Renaming a template without updating the GPO auto-enrollment reference","Accidentally removing Read permission from Authenticated Users on the template object"],causes:["Certificate template name in GPO does not match the template Common Name in AD","Template exists but Read permission removed from requesting group","Template not yet replicated to all AD domain controllers","Template was deleted but GPO still references it"],steps:["Identify the template name from the event message","Verify the template exists in AD: Certificate Templates console (certtmpl.msc)","Check template Read permission: right-click template → Properties → Security tab → Authenticated Users should have Read","Verify the template is published on the CA: certsrv.msc → Certificate Templates","Force AD replication if recently created: repadmin /syncall /AdeP","Run gpupdate /force on client after fixing permissions"],symptoms:["certificate template not found","auto enrollment template missing","certificate template access denied","GPO cert template not working","certificate enrollment template error","cannot find certificate template","template read permission error"],tags:["certificate","pki","auto-enrollment","template","adcs","group-policy"],powershell:`# List certificate templates and their enrollment permissions
# Eventful

# Auto-enrollment errors (Event 13 and 64)
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'Microsoft-Windows-CertificateServicesClient-AutoEnrollment'
    Id           = @(13, 64)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending | Format-List

# List templates published to local CA (run on CA server)
# certutil -catemplates

# Check current machine certs
Get-ChildItem Cert:LocalMachineMy |
    Select-Object Subject, Issuer, NotAfter | Format-Table -AutoSize`,related_ids:[64,65,1003],ms_docs:"https://learn.microsoft.com/en-us/windows-server/identity/ad-cs/certificate-autoenrollment"}],u=[{id:5002,source:"Microsoft-Windows-WAS",channel:"System",severity:"Error",skill_level:"Intermediate",title:"IIS Application Pool Stopped — Worker Process Failure",short_desc:"Windows Process Activation Service (WAS) stopped an app pool after repeated worker process failures.",description:"Event 5002 from Microsoft-Windows-WAS (Windows Activation Service) is logged when an IIS application pool is taken offline after a series of rapid worker process (w3wp.exe) crashes or failures to start. IIS has a rapid fail protection threshold (default: 5 failures in 5 minutes) and will disable the pool automatically to prevent system instability. All sites in that app pool begin returning 503 Service Unavailable.",why_it_happens:"App pools enter rapid-fail protection when the worker process crashes repeatedly without recovering. Common causes include unhandled .NET exceptions on startup, missing DLLs or dependencies, incorrect service account credentials, or a misconfigured web.config. WAS disables the pool to prevent a crash loop from consuming all server resources.",what_good_looks_like:'App pool in "Started" state. No 5002/5011 events in System log. Worker process w3wp.exe running stably without restarts. Application Event log has no related 1000/1026 crash events.',common_mistakes:["Restarting the app pool without investigating why it crashed — it will crash again","Not correlating with Application log Event 1000 (crash) or 1026 (.NET exception) which will explain the actual failure","Forgetting that the app pool identity (service account) credentials may have expired or been changed","Not checking the IIS Worker Process Identity — it needs access to the application folder and its dependencies"],causes:["Worker process crashing due to unhandled .NET or native exception","App pool identity account password expired or changed","Missing or inaccessible application dependencies on startup","web.config syntax error or misconfiguration","Rapid memory pressure causing repeated worker process recycling","Corrupt IIS application or module failing to load"],steps:["Open IIS Manager → Application Pools — note which pool is stopped","Check System log for Event 5002 and note the app pool name","Check Application log for Event 1000 (crash) or 1026 (.NET) around the same time","Review IIS log files in C:\\inetpub\\logs\\LogFiles for 503 or startup error details","Verify the app pool identity account and password: IIS Manager → Application Pools → Advanced Settings → Identity","Check folder permissions for the web application root — pool identity needs Read (and Modify if writes needed)","Temporarily enable Failed Request Tracing in IIS for detailed error capture"],symptoms:["IIS website returning 503","IIS app pool stopped","website not working 503 error","IIS application pool crashed","web app not responding","IIS worker process crashed","app pool disabled by rapid fail","website down IIS error","w3wp.exe crash","application pool keeps stopping"],tags:["iis","app-pool","was","503","w3wp","web-server"],powershell:`# IIS App Pool Status and Crash Investigation
# Eventful

# List all app pools and their current state
Import-Module WebAdministration -ErrorAction SilentlyContinue
Get-WebConfiguration system.applicationHost/applicationPools/add |
    Select-Object name, state, autoStart, @{N='Identity'; E={$_.processModel.userName}} |
    Format-Table -AutoSize

# Recent WAS events (5002, 5011, 5059 = stop/failure events)
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-WAS'
    Id           = @(5002, 5011, 5059)
    StartTime    = (Get-Date).AddDays(-1)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending | Format-List

# Co-occurring app crashes
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'Application Error'
    Id           = 1000
    StartTime    = (Get-Date).AddDays(-1)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Sort-Object TimeCreated -Descending |
    Select-Object -First 5 | Format-List`,related_ids:[5011,5059,1e3,1026],ms_docs:"https://learn.microsoft.com/en-us/iis/manage/managing-your-configuration-settings/an-overview-of-feature-delegation-in-iis"},{id:5011,source:"Microsoft-Windows-WAS",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"IIS Application Pool Identity Could Not Log On",short_desc:"IIS app pool failed to start because its identity account could not authenticate.",description:"Event 5011 from WAS indicates the application pool failed to start because the configured identity account could not be authenticated. The app pool will not start, and all sites assigned to it will return 503. This is a credential or account problem — most commonly an expired password, a locked account, or a mistyped username.",why_it_happens:'When IIS starts an app pool, WAS uses the configured identity to create the worker process token. If the password is wrong, the account is disabled, or the account no longer has the "Log On As a Service" or "Log On As a Batch Job" right, WAS cannot create the process and logs Event 5011.',what_good_looks_like:"App pool starts cleanly with Event 5019 (worker process started successfully). No 5011 errors. Service account password managed by GMSA (group managed service account) to eliminate password rotation issues.",common_mistakes:["Rotating a service account password without updating it in IIS app pool settings","Using a regular user account instead of a managed service account (gMSA) for app pools","Locking the service account through too many failed logon attempts (which may be the old password still configured in IIS)"],causes:["App pool identity account password has expired or been changed","Account disabled in Active Directory","Account locked due to repeated authentication failures",'"Log On As a Service" right removed from the account',"Typo in account name or domain in IIS app pool identity settings"],steps:["Open IIS Manager → Application Pools → select the failing pool → Advanced Settings → Process Model → Identity","Verify the account name and password are correct","Check the account status in AD: Get-ADUser <username> | Select Enabled, LockedOut, PasswordExpired","If using a service account, reset the password and update it in IIS","Consider migrating to ApplicationPoolIdentity (built-in virtual account) or a gMSA",'After fixing credentials, restart the app pool: Restart-WebAppPool -Name "<poolname>"',"Check Event 4625 in Security log for the logon failure details"],symptoms:["IIS app pool cannot start","app pool identity logon failed","IIS service account wrong password","website 503 after password change","IIS app pool account locked","application pool identity error","IIS not starting account problem"],tags:["iis","app-pool","was","service-account","503","credentials"],powershell:`# Check IIS App Pool Identity Account Status
# Eventful

Import-Module WebAdministration -ErrorAction SilentlyContinue

# Get all app pools and their identities
Get-WebConfiguration system.applicationHost/applicationPools/add |
    Select-Object name, @{N='IdentityType'; E={$_.processModel.identityType}},
        @{N='UserName'; E={$_.processModel.userName}} |
    Format-Table -AutoSize

# Check if a specific service account is locked or expired
# $user = Get-ADUser "svc-iispool" -Properties LockedOut, Enabled, PasswordExpired
# $user | Select-Object Name, Enabled, LockedOut, PasswordExpired | Format-List

# Restart a specific app pool
# Restart-WebAppPool -Name "DefaultAppPool"`,related_ids:[5002,5059,4625],ms_docs:"https://learn.microsoft.com/en-us/iis/manage/configuring-security/application-pool-identities"},{id:5059,source:"Microsoft-Windows-WAS",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"IIS — Worker Process Startup Failed",short_desc:"WAS could not start a worker process for an application pool.",description:"Event 5059 is logged by WAS when it attempts to start a worker process (w3wp.exe) for an application pool and the process fails to start, start within the timeout, or register with WAS. This is a broader failure than 5011 (which is specifically a credentials issue) and can indicate missing executables, permissions errors, or a broken IIS installation.",why_it_happens:"Worker process startup can fail for many reasons: the w3wp.exe executable is missing or corrupt, IIS ISAPI filters are broken, .NET Framework installation is corrupt, or the system is under extreme resource pressure. WAS logs 5059 and eventually 5002 (pool disabled) if startup consistently fails.",what_good_looks_like:"App pools start cleanly. System log shows 5019 (worker started) not 5059. IIS site responds within startup timeout.",common_mistakes:['Not running "iisreset" and checking if IIS itself starts cleanly after a failed reboot',"Forgetting to repair .NET Framework after a failed update or upgrade","Not checking if disk space is available for IIS logs and temp files"],causes:["w3wp.exe or required IIS binaries missing or corrupt",".NET Framework registration with IIS broken","Insufficient disk space for IIS temporary files","IIS module configuration error preventing startup","System resource exhaustion (out of memory, handles)"],steps:["Run: iisreset /noforce — if IIS does not start, investigate further","Check IIS installation integrity: dism /online /enable-feature /featurename:IIS-WebServer","Re-register .NET with IIS: %windir%\\Microsoft.NET\\Framework64\\<version>\\aspnet_regiis.exe -i","Check disk space on the system drive and IIS log drive","Review IIS Setup log: C:\\Windows\\System32\\inetsrv\\setupapi.log","Try restarting WAS service: Restart-Service WAS"],symptoms:["IIS worker process failed to start","IIS not starting after reboot","all IIS websites down","w3wp failed to launch","IIS 503 all sites","WAS service error IIS"],tags:["iis","was","w3wp","app-pool","startup","web-server"],powershell:`# IIS Service and Worker Process Status
# Eventful

# IIS service status
Get-Service W3SVC, WAS, WMSVC -ErrorAction SilentlyContinue |
    Select-Object Name, Status, StartType | Format-Table -AutoSize

# Running w3wp processes
Get-Process w3wp -ErrorAction SilentlyContinue |
    Select-Object Id, WorkingSet, CPU, StartTime | Format-Table -AutoSize

# WAS errors
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-WAS'
    Level        = @(1, 2, 3)
    StartTime    = (Get-Date).AddHours(-2)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending | Format-List`,related_ids:[5002,5011,1e3],ms_docs:"https://learn.microsoft.com/en-us/iis/get-started/whats-new-in-iis-8/iis-80-using-aspnet-35-and-aspnet-45"},{id:1309,source:"ASP.NET 4.0.30319.0",channel:"Application",severity:"Warning",skill_level:"Intermediate",title:"ASP.NET Unhandled Exception in Request",short_desc:"An ASP.NET web application threw an unhandled exception while processing a request.",description:"Event 1309 from ASP.NET is logged to the Application event log when an unhandled exception occurs during web request processing. The event includes the exception type, message, and a partial stack trace. While the application typically returns a 500 error to the browser, the root cause details are in this event. Frequent 1309 events indicate an application code bug, a missing dependency, or a misconfiguration.",why_it_happens:"ASP.NET logs this event when an exception propagates up to the HttpApplication level without being caught by application code. Common causes: database connection failures, missing configuration keys in web.config, null reference exceptions in request handlers, and file/resource access errors.",what_good_looks_like:"No 1309 events. Application returns 200 responses. If occasional 1309 events are expected (user errors), they should not include system-level exceptions like SqlException or FileNotFoundException.",common_mistakes:["Not looking at the stack trace in the event — it usually points directly to the failing method","Fixing the symptom (500 error page) instead of the root cause in the application","Not enabling detailed errors or custom error logging in the application to capture full stack traces"],causes:["Database connection failure (wrong connection string, DB offline)","Missing required key in web.config or appSettings","Null reference exception in application code","File or resource the application depends on is missing","Unhandled exception in async code path","Third-party library throwing unexpected exception"],steps:['Open Application log, filter for source "ASP.NET*" and Event 1309',"Read the exception type and message — SqlException = database issue, FileNotFoundException = missing file","Read the stack trace to identify the failing method and line number",'Enable detailed error logging in web.config: <customErrors mode="Off"/> temporarily',"Check web application logs if the app has its own logging (e.g., NLog, Serilog)","Test the failing operation manually if reproducible","Check the application's database connection string and database availability"],symptoms:["website returning 500 error","ASP.NET error 500","web application throwing exceptions","website internal server error","ASP.NET application crash","web app error after update","IIS 500 internal error","ASP.NET unhandled exception"],tags:["iis","asp.net","web-application","500","exception","request"],powershell:`# ASP.NET Application Errors
# Eventful

# Recent ASP.NET exceptions (1309 = unhandled, 1310 = error handled)
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'ASP.NET 4.0.30319.0'
    Id           = @(1309, 1310)
    StartTime    = (Get-Date).AddHours(-24)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message |
    Sort-Object TimeCreated -Descending | Select-Object -First 10 | Format-List

# Also check for .NET CLR exceptions (Event 1026)
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = '.NET Runtime'
    Id           = 1026
    StartTime    = (Get-Date).AddHours(-24)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message | Sort-Object TimeCreated -Descending |
    Select-Object -First 5 | Format-List`,related_ids:[5002,1e3,1026],ms_docs:"https://learn.microsoft.com/en-us/aspnet/web-forms/overview/getting-started/"},{id:1026,source:".NET Runtime",channel:"Application",severity:"Error",skill_level:"Intermediate",title:".NET Runtime — Unhandled Exception (Application Crash)",short_desc:"A .NET application crashed due to an unhandled exception — full exception and stack trace logged.",description:'Event 1026 from ".NET Runtime" is logged when a .NET application crashes due to an unhandled exception that terminates the process. Unlike Event 1000 (which only gives module info), Event 1026 contains the full exception type, message, and stack trace. This is the most useful event for diagnosing .NET application crashes and should always be checked alongside Event 1000.',why_it_happens:"When a .NET application throws an exception that is not caught anywhere in the call stack, the .NET runtime terminates the process and logs the full exception details to the Application event log. This applies to WinForms, WPF, console apps, Windows services, and IIS-hosted apps (which also generate Event 1309).",what_good_looks_like:"No 1026 events. Applications handle expected exceptions and log them internally. Only truly unexpected conditions cause process termination.",common_mistakes:["Reading only Event 1000 and missing the detailed stack trace in Event 1026","Not searching for the exception type in the vendor's known issues database","Not checking if a newer version of the application or .NET runtime fixes the issue","Ignoring OutOfMemoryException — this is not just a code bug, it may indicate a memory leak or undersized server"],causes:["Unhandled exception in application code (null reference, argument, index out of range)","Unhandled exception in async Task that was not awaited","OutOfMemoryException due to memory leak or insufficient RAM","StackOverflowException from infinite recursion","Application crashed on startup due to missing .NET dependency"],steps:['Open Application log, filter for source ".NET Runtime" and Event 1026',"Read the exception type: NullReferenceException, OutOfMemoryException, etc.","Read the stack trace to identify the exact method and line number that threw","Check if vendor has released a fix for this exception type in recent updates","If OutOfMemoryException: check memory usage with Task Manager or Process Monitor","Enable .NET crash dump collection: procdump -e 1 -p <pid> C:\\dumps\\ for next occurrence","Cross-reference with Event 1000 for the faulting module information"],symptoms:[".NET application crash","application crashed with exception","unhandled exception dotnet",".net runtime error","application stopped working dotnet","null reference exception crash","dotnet app crash log","managed application crash","c# application crash event log"],tags:["dotnet","application","crash","exception","runtime","managed-code"],powershell:`# .NET Application Crash Investigation
# Eventful

$startTime = (Get-Date).AddDays(-7)

# Full .NET crash details (1026)
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = '.NET Runtime'
    Id           = 1026
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending | Select-Object -First 5 | Format-List

# Corresponding WER crash record (1000)
Get-WinEvent -FilterHashtable @{
    LogName      = 'Application'
    ProviderName = 'Application Error'
    Id           = 1000
    StartTime    = $startTime
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Message |
    Sort-Object TimeCreated -Descending | Select-Object -First 5 | Format-List`,related_ids:[1e3,1002,1309,5002],ms_docs:"https://learn.microsoft.com/en-us/dotnet/standard/exceptions/best-practices-for-exceptions"}],m=[{id:1046,source:"Microsoft-Windows-DHCP-Server",channel:"System",severity:"Error",skill_level:"Intermediate",title:"DHCP Server Not Authorized in Active Directory",short_desc:"DHCP server is not authorized in AD and has shut down to prevent IP address conflicts.",description:"Event 1046 from the DHCP Server service indicates the server has determined it is not authorized in Active Directory and has stopped serving IP addresses to prevent rogue DHCP from disrupting the network. This is a built-in AD protection that stops unauthorized DHCP servers from handing out incorrect IP configurations. Any clients that arrive at the unauthorized server will fail to obtain a lease.",why_it_happens:"Windows DHCP servers check Active Directory to verify they are authorized to serve addresses on the domain. A server fails authorization if: it was never authorized by a Domain Admin using the DHCP MMC console or netsh, the server's IP address changed after authorization, or the server cannot reach a domain controller to verify its authorization status.",what_good_looks_like:"No 1046 events. DHCP service running in Authorized state. Clients obtain leases normally. DHCP MMC shows the server with a green icon (authorized).",common_mistakes:["Adding a new DHCP server and forgetting to authorize it before going live","Cloning a DHCP server VM without re-authorizing the new IP address in AD","Not checking that the DHCP server can reach a domain controller — if AD is unreachable, it may also stop serving addresses"],causes:["DHCP server was never authorized in Active Directory by a Domain Admin","Server IP address changed after authorization","Server cannot reach a domain controller to verify authorization (network issue)","Server was deauthorized accidentally or intentionally","Rogue DHCP server detection triggered on a legitimate server in a workgroup"],steps:["Log on to the DHCP server with a Domain Admin account","Open DHCP MMC console (dhcpmgmt.msc)","Right-click the server name → Authorize","Wait 30–60 seconds, then right-click → Refresh — the server should show as Authorized (green icon)","If the icon remains red: verify DNS resolves the server name and it can reach a DC: nltest /dsgetdc:<domain>","Alternatively via command line: netsh dhcp add server <serverFQDN> <serverIP>","After authorization, verify clients can obtain leases: ipconfig /release && ipconfig /renew on a test client"],symptoms:["DHCP server not giving out IPs","clients cannot get IP address from DHCP","DHCP server unauthorized","computers getting APIPA 169.254 addresses","DHCP stopped working","no DHCP leases being issued","DHCP server shut itself down","clients not getting IP after server added","DHCP not authorized AD"],tags:["dhcp","ip-addressing","active-directory","authorization","network"],powershell:`# DHCP Server Authorization Status
# Eventful

# Check DHCP service status
Get-Service DHCPServer -ErrorAction SilentlyContinue | Select-Object Name, Status, StartType

# Recent DHCP Server errors (1046, 1047 = unauthorized events)
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-DHCP-Server'
    Id           = @(1046, 1047, 1048)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending | Format-List

# Check authorized DHCP servers in AD (requires DHCP or Domain Admin rights)
# netsh dhcp show server

# Check DC reachability for DHCP authorization
# nltest /dsgetdc:<yourdomain.com>`,related_ids:[1047,1048,1063],ms_docs:"https://learn.microsoft.com/en-us/windows-server/networking/technologies/dhcp/dhcp-deploy-wps"},{id:1020,source:"Microsoft-Windows-DHCP-Server",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"DHCP Scope Address Pool Exhausted",short_desc:"DHCP scope has run out of available IP addresses — new clients will fail to get a lease.",description:"Event 1020 indicates that a DHCP scope has exhausted its available IP address pool. New clients requesting an address will receive no offer from the server and will fall back to APIPA (169.254.x.x) or fail to connect to the network. Existing leases are unaffected until they expire. This event is critical for any environment where devices regularly join the network.",why_it_happens:"Scope exhaustion occurs when the number of concurrent clients exceeds the number of addresses in the scope, or when stale leases from decommissioned devices have not expired. Short lease times can cause exhaustion on busy networks (conference rooms, wireless), and malicious DHCP starvation attacks can deliberately exhaust a scope.",what_good_looks_like:"Scope utilization below 80%. Lease duration appropriate for the environment (1 day for stable clients, shorter for guest Wi-Fi). Regular lease cleanup for stale/inactive entries.",common_mistakes:["Setting lease duration too long (e.g., 8 days) — stale leases from decommissioned PCs fill the pool","Not expanding the scope or adding a superscope when the network grows","Not monitoring scope utilization; 1020 is the emergency alert, not a proactive warning"],causes:["Network has grown beyond the scope's address range","Stale leases from decommissioned devices holding addresses","Lease duration too long relative to client turnover rate","DHCP starvation attack (malicious client requesting all available leases)","Scope exclusions and reservations consuming too much of the range"],steps:["Open DHCP MMC → expand the scope → Address Pool — check how many addresses remain","View active leases: DHCP MMC → Leases — identify inactive or stale entries","Short-term: delete stale leases manually for decommissioned devices","Medium-term: expand the scope address range if subnet permits","Consider reducing lease duration for dynamic clients to reclaim addresses faster","Add a secondary scope or expand the subnet if the network has genuinely grown","Monitor scope utilization: Get-DhcpServerv4ScopeStatistics -ComputerName <server>"],symptoms:["DHCP scope full","clients not getting IP address","computers getting 169.254 address","DHCP ran out of addresses","new devices cannot join network","DHCP pool exhausted","IP address pool full","APIPA address all computers","no DHCP lease available"],tags:["dhcp","ip-addressing","scope","exhaustion","network","leases"],powershell:`# DHCP Scope Utilization and Stale Leases
# Eventful

$dhcpServer = $env:COMPUTERNAME  # Replace with DHCP server name if remote

# Scope utilization summary
Get-DhcpServerv4ScopeStatistics -ComputerName $dhcpServer |
    Select-Object ScopeId, AddressesFree, AddressesInUse,
        @{N='UtilPct'; E={ [math]::Round($_.AddressesInUse / ($_.AddressesFree + $_.AddressesInUse) * 100, 1) }} |
    Sort-Object UtilPct -Descending | Format-Table -AutoSize

# Leases not seen for more than 30 days (potential stale)
Get-DhcpServerv4Lease -ComputerName $dhcpServer -ScopeId <scopeId> -ErrorAction SilentlyContinue |
    Where-Object { $_.LeaseExpiryTime -lt (Get-Date).AddDays(-30) } |
    Select-Object IPAddress, HostName, ClientId, LeaseExpiryTime |
    Sort-Object LeaseExpiryTime | Format-Table -AutoSize`,related_ids:[1063,1046],ms_docs:"https://learn.microsoft.com/en-us/windows-server/networking/technologies/dhcp/dhcp-deploy-wps"},{id:1063,source:"Microsoft-Windows-DHCP-Server",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"DHCP DNS Update Failed",short_desc:"DHCP server could not dynamically update DNS records for a client lease.",description:"Event 1063 is logged when the DHCP server attempts to register or update a DNS A record on behalf of a client (DDNS — Dynamic DNS) and the DNS server refuses or is unreachable. Clients whose DNS records are not updated will not be resolvable by hostname on the network, causing failures in any application or script that resolves names. This is common in environments with misconfigured DNS zones or after DHCP service account changes.",why_it_happens:"DHCP dynamic DNS updates require the DHCP server to have permission to write DNS records on behalf of clients. For Active Directory-integrated zones, the DHCP Administrators group or the DnsUpdateProxy group must be configured. If the DHCP server computer account lacks permission to create/modify records in the DNS zone, all DDNS updates fail.",what_good_looks_like:"DHCP server registers A (and PTR) records for clients on lease issue/renewal. Clients resolvable by hostname from DNS. No 1063 events in System log.",common_mistakes:["Forgetting to add the DHCP server to the DnsUpdateProxy group for secure DNS updates","Using the DHCP server computer account for DNS updates instead of a dedicated credential",'Not checking if the DNS zone is configured for "Secure and nonsecure" or "Secure only" updates',"Not updating the DHCP DNS credentials after a service account password change"],causes:["DHCP server lacks permission to write to AD-integrated DNS zone","DNS server is offline or unreachable from DHCP server","DNS zone configured for secure-only updates and DHCP uses wrong credentials","DHCP service account credentials used for DNS updates have expired","DNS zone does not allow dynamic updates (set to None)"],steps:["Open DHCP MMC → right-click server → Properties → DNS tab — review DDNS settings","Check the credentials used for DNS updates: DHCP MMC → server Properties → Advanced → Credentials","Verify the DNS zone allows dynamic updates: DNS MMC → zone Properties → General → Dynamic updates","Add DHCP server to DnsUpdateProxy group in AD (for multi-DHCP environments)","Test DNS update manually: ipconfig /registerdns on a test client","Check DNS server reachability from DHCP server: Resolve-DnsName <clienthostname>"],symptoms:["DHCP DNS update failed","computers not registering in DNS","hostname not resolving on network","DDNS not working","DNS A record not created by DHCP","clients not showing in DNS","dynamic DNS update error","computers missing from DNS"],tags:["dhcp","dns","ddns","dynamic-update","network","name-resolution"],powershell:`# DHCP DNS Update Failure Investigation
# Eventful

# Recent DHCP DNS update errors
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-DHCP-Server'
    Id           = @(1063, 1064, 1065)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending |
    Select-Object -First 20 | Format-List

# Test DNS resolution for a specific client
# Resolve-DnsName "workstation01.contoso.com" -ErrorAction SilentlyContinue`,related_ids:[1020,1046,4001],ms_docs:"https://learn.microsoft.com/en-us/windows-server/networking/technologies/dhcp/dhcp-dns-integration"},{id:20080,source:"Microsoft-Windows-DHCP-Server",channel:"System",severity:"Error",skill_level:"Intermediate",title:"DHCP Failover Partner Unreachable",short_desc:"DHCP failover partner server is not responding — failover relationship at risk.",description:"Event 20080 from the DHCP Server service is logged when a Windows DHCP failover partner becomes unreachable. DHCP failover allows two DHCP servers to serve the same scope for redundancy. When the partners lose communication, the active server enters a communication-interrupted state and may not be able to issue new leases from the partner's address pool. If the partner remains unreachable, the server may eventually enter PARTNER-DOWN state and take over the full scope.",why_it_happens:"DHCP failover uses a TCP connection between partners for health checks and lease synchronization. If the partner server is offline, the network path is broken, or a firewall blocks the DHCP failover port (TCP 647), the partners lose communication and 20080 is logged. During maintenance windows, this is expected; in production without a planned maintenance, it indicates a server or network problem.",what_good_looks_like:'No 20080 events outside of planned maintenance. Both DHCP servers in "Normal" failover state. DHCP MMC shows the partner as reachable. Leases are being synchronized between partners.',common_mistakes:["Not setting the DHCP server to PARTNER-DOWN mode before planned maintenance — if the partner is offline for longer than the MCLT (Maximum Client Lead Time), the server will stop issuing addresses from the partner's pool","Forgetting that firewall rules must allow TCP port 647 between the two DHCP servers","Not monitoring DHCP failover state — a failed partner in COMMUNICATION-INTERRUPTED state for hours means clients may not renew leases correctly"],causes:["DHCP partner server is offline or the DHCP service has stopped on it","Firewall blocking TCP port 647 between DHCP servers","Network connectivity failure between DHCP servers","DHCP service on partner crashed or was restarted","Partner server is under maintenance but not set to PARTNER-DOWN mode"],steps:["Check DHCP service on the partner: Get-Service DHCPServer -ComputerName <partnerName>","Test TCP 647 connectivity: Test-NetConnection -ComputerName <partnerIP> -Port 647","Open DHCP MMC → server Properties → Failover tab — check current state","If partner is down for planned maintenance: set to PARTNER-DOWN mode in DHCP MMC","If partner is unexpectedly offline: restore the partner service/server","After restoration, force synchronization: DHCP MMC → scope → right-click → Replicate Scope"],symptoms:["DHCP failover partner offline","DHCP server unreachable","DHCP high availability broken","DHCP partner not responding","DHCP redundancy failed","DHCP partner communication interrupted","DHCP failover error","secondary DHCP server not syncing"],tags:["dhcp","failover","high-availability","network","redundancy"],powershell:`# DHCP Failover Status
# Eventful

$dhcpServer = $env:COMPUTERNAME  # Replace with DHCP server name if remote

# DHCP failover relationship status
Get-DhcpServerv4Failover -ComputerName $dhcpServer -ErrorAction SilentlyContinue |
    Select-Object Name, PartnerServer, Mode, State, MaxClientLeadTime |
    Format-Table -AutoSize

# Recent failover communication events
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-DHCP-Server'
    Id           = @(20080, 20081, 20082)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending | Format-List

# Test connectivity to partner
# Test-NetConnection -ComputerName <partnerIP> -Port 647`,related_ids:[1046,1020,1048],ms_docs:"https://learn.microsoft.com/en-us/windows-server/networking/technologies/dhcp/dhcp-failover"},{id:1048,source:"Microsoft-Windows-DHCP-Server",channel:"System",severity:"Warning",skill_level:"Intermediate",title:"DHCP Server Detected Another DHCP Server on Network",short_desc:"DHCP server detected a conflicting DHCP server — rogue or unauthorized server active.",description:"Event 1048 is logged when the Windows DHCP server detects another DHCP server responding on the same network segment. In an AD environment, this means a server that is either unauthorized (rogue) or a failover partner is sending DHCP offers. Rogue DHCP servers can hand out incorrect IP configurations (wrong gateway, DNS servers) causing network disruption for any clients that accept their offers.",why_it_happens:"DHCP servers send DHCPINFORM broadcasts to detect other servers on the network. A second server responding could be an unauthorized personal router with DHCP enabled, a misconfigured VM, a network appliance, or a legitimate failover partner that has lost synchronization.",what_good_looks_like:"Only authorized DHCP servers visible on the network. All DHCP offers originate from expected servers. Event 1048 not appearing in logs.",common_mistakes:["Forgetting that consumer routers and home network devices broadcast DHCP offers by default — any device plugged into the corporate switch can become a rogue DHCP server","Not checking if a new server or VM was stood up with DHCP enabled before authorization"],causes:["Unauthorized router or device plugged into the network with DHCP enabled","A new DHCP server stood up without authorization","DHCP failover partner misconfigured","VM cloned from DHCP server without disabling DHCP service","Network appliance (ISP modem, wireless AP) with built-in DHCP server active"],steps:["Identify the rogue server IP from the event message","Trace the MAC address: arp -a <rogueIP> to get MAC, then trace via switch port","Use Get-DhcpServerv4Lease to find the device: look for the rogue IP in any DHCP scope","Once identified, disable DHCP on the rogue device or isolate the switch port","Check authorized DHCP servers in AD: netsh dhcp show server","After remediation, monitor for further 1048 events"],symptoms:["rogue DHCP server detected","computers getting wrong IP address","computers getting wrong default gateway","two DHCP servers on network","unauthorized DHCP server","clients getting wrong DNS from DHCP","DHCP conflict on network","duplicate DHCP server"],tags:["dhcp","rogue-dhcp","network-security","ip-addressing","network"],powershell:`# Detect DHCP Server Conflict
# Eventful

# Recent DHCP rogue server detection events
Get-WinEvent -FilterHashtable @{
    LogName      = 'System'
    ProviderName = 'Microsoft-Windows-DHCP-Server'
    Id           = @(1048, 1049)
    StartTime    = (Get-Date).AddDays(-7)
} -ErrorAction SilentlyContinue |
    Select-Object TimeCreated, Id, Message | Sort-Object TimeCreated -Descending | Format-List

# List authorized DHCP servers in AD
# netsh dhcp show server

# Check ARP table to find rogue server MAC
# arp -a <rogueServerIP>`,related_ids:[1046,1047,1020],ms_docs:"https://learn.microsoft.com/en-us/windows-server/networking/technologies/dhcp/dhcp-deploy-wps"}],p=[...t,...i,...r,...o,...s,...n,...a,...c,...l,...d,...h,...u,...m];function g(e){return e?String(e).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#039;"):""}function f(e){return e.toLocaleTimeString([],{hour:"2-digit",minute:"2-digit",second:"2-digit"})}export{p as a,g as e,f};
